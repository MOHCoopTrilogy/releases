<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# Multiplayer & networking

[<- back to all fixes](../BUGFIXES.md)

**55 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-2412` | Harness clients connect, are force-joined to allies, ride the Higgins boat - and never actually enter the game. coop_isActive stays -1. | MEASURED, cause not fully identified. With developer 1 on the server, PRB player.snap shows both clients as ent=0/ent=1, tn=player, tm=american, hp=100, at org=352,-7440,-463 - which is the Higgins boat start position, i.e. the map DID load | NOT SOLVED. Three routes tried and all failed: (1) `rcon map m3l1a` and `rcon restart` - neither reloads a running server (bug-2411); (2) launching the client with `+exec autospawn.cfg` holding waits + popmenu + a synthetic +attack/-attack  |
| `bug-2410` | launch_dedicated_2player.ps1 boots the server WITHOUT developer 1, so every script marker is silently gated off. | ScriptThread::Println is developer-gated (scriptthread.cpp:2869). The harness seeds sv_maxclients, rconpassword and the map but never `developer`, so a harness run produces a server log with ZERO coop markers - OBSTACLES, BEACHDEAD, SWEEPOW | NOT PATCHED IN THE HARNESS YET - worked around live with `rcon developer 1`, after which SWEEPOWN appeared immediately. The harness should seed `developer 1` next to `logfile 2` in its dedicated_start.cfg block. Recorded here rather than ed |
| `bug-2366` | user console: 22x '^~^~^ Script Error : You must specify an explicit classname for misc object tik models', all from coopified.scr:8043. | coop_higginsSinkBurn used the bare `spawn "models/fx/water_trail_bubble.tik"` form. That TIKI declares no classname - its emitter lives in init{client{}} and there is nothing else in the file - so the bare form fails. 22 errors, one per ite | `spawn script_model model "..."`, matching the working call site. |
| `bug-2310` | user: 'some of the crate carrying guys are now shooting instead of moving the crates, the crates are stuck in their hands with their rifle' | MY REGRESSION from bug-2305's partial fix. coop_vigSpawn threads coop_beachBehave as its LAST act and returns the actor; the caller's very next line threads the real job. But `thread` runs the new thread IMMEDIATELY, up to its first wait -  | One waitframe at the top of coop_beachBehave, before it touches the actor. That inverts the order for every caller at once: this thread yields, the caller's job thread runs and sets the flag, and on the next frame the guard ends it without  |
| `bug-2305` | user: 'the guys that are shooting should be susceptible to getting killed too' | coop_vigSpawn gives every actor it makes `nodamage` (coopified.scr:3250) - the right default for a man holding a pose, wrong for a firing line the player watches. SECOND DEFECT FOUND WHILE FIXING IT: coop_vigSpawn threads coop_beachBehave a | (1) `takedamage` at the firing-line call site undoes the nodamage for that caller only; they stay NOTSOLID deliberately, since fifty solid bodies in the obstacle belt is how players get shoved into tank traps. (2) New coop_firingLineCutDown |
| `bug-2215` | Script Error : You must specify an explicit classname for misc object tik models -> Cannot cast 'none' to listener -> command 'notsolid'/'anim'/'remove' applied to NIL. 710 of each in one session, fro | spawn "<tikpath>" only works when the TIKI declares a classname in its init block. models/fx/dudimpact.tik and models/animate/fx_water_spray.tik do not (the log says 'Classname (null) used, but TIKI had no Initialization commands'), so the  | Use the vanilla idiom for a classname-less model: spawn script_model model "<path>" (18 uses across the retail scripts). 3 call sites. |
| `bug-2188` | Script Error : command 'anim'/'say'/'takedamage' and Field 'isinsequence' applied to NULL listener (maps/m3l1a.scr lines 4357-4369, nine per playthrough); visible symptom: an allied soldier stuck in p | sequence_5_medic runs TWICE concurrently, once for the left pair and once for the right. Whichever thread reaches the 'delete the other sequence' block (:4241-4265) first REMOVES the other side's medic and patient - and it can do that while | Added the same liveness re-check retail uses, immediately after the waitexec returns and before the goto: 'if( !isalive local.medic \|\| !isalive local.patient ){ goto sequence_5_medic_done }'. |
| `bug-2182` | omohaaded.exe segfaults during Common Initialization when launched from G:\mohaa-gl2, but the SAME binary starts normally from the GOG root | NOT a code regression - established by running the identical freshly-built binary from both directories: from G:\mohaa-gl2 it segfaults right after the version banner (exit 139), from the GOG root it reaches 'Dedicated server idle' and stay | No code change. Recorded so the next session does not mistake it for a fresh regression and bisect the engine for it. Verification for this session was done with openmohaa.exe +set dedicated 1, which loaded m4l0 cleanly (5197 lines, coop fr |
| `bug-2171` | coop_coverLean defaulted to 0 in cg_predict.c but 1 in player.cpp - wall-cover lean was never predicted client-side | The 2026-08-23 SHIPS ON flip (user: I am good with coop lean cover on) was applied to the SERVER half only. The block comment on both halves states they MUST stay paired or the predictor and server compute different fLeanAngle. The cvar is  | cg_predict.c: default 0 -> 1 so both halves agree. Same class as the fossil the original flip was fixing - a feature switched on in one place that reached nobody. Rebuilt cgame.dll; game.dll unchanged since player.cpp was already correct. |
| `bug-2124` | Adversarial review workflow (23 agents) over the same-day Stage A supine implementation: 5 confirmed defects, 0 refuted findings survived, caught BEFORE any playtest. | (F1, HIGH) the supine ENTER trigger had no guard for an armed crouch-press exit: TickCoopProne cleared supine in ClientThink, CoopProneBodyYaw re-latched it the same frame from PlayerAngles (ADS held, aim ~180 off the belly), and the exit b | F1: enter trigger yields while m_fCoopProneExitAt armed or the flip window runs. F2: ExitAt cleared in forced-leave and at prone entry. F3+F4 one move: the latch moved into Player::Killed() - m_bCoopDiedSupine = (m_bCoopProne && m_bCoopSupi |
| `bug-2123` | Deep-vet findings batch (Stage A): DBNO-while-prone resurrected bug-2112; inverted bone frames on the flipped body; head yaw flapping +/-27; flip/yaw timing mismatch; supine WASD statue-slide; unflipp | Specification-grade audit (two agent passes, ~575k tokens) of the supine system. Key mechanisms: (1) bMustLeave omitted m_bCoopDbno, so going down while prone kept PMF_VIEW_PRONE/viewheight-16 - the exact bug-2112 camera-under-map path via  | All implemented same-day as spec items A1-A9 + P1: dbno term in bMustLeave; supine bone-frame negation package + aim-lead re-base (coop_supineSpine); hysteresis re-based on the eased target; coop_supineFlipRate 200 during per-direction flip |
| `bug-2112` | 'when DBNO the cam kinda dips under the map, it should be separate from prone cam' | MY REGRESSION from the prone work. PMF_VIEW_PRONE was derived from hull height alone (maxs.z == 20), and a DOWNED player has that same hull - so DBNO got PRONE_VIEWHEIGHT 16. The DBNO camera then applies cg_dbnoCamVert -30 from the view ori | The derivation now requires m_bCoopProne AND the hull, restoring DBNO exactly while leaving real prone untouched. |
| `bug-2044` | spawnlocations.scr writes level.flags["coop_spawn5angels"] .. "coop_spawn8angels" - ANGELS, not ANGLES. Every reader uses ...angles, so players 5-8 silently get default facing instead of the authored  | Typo at 4 adjacent lines (2693, 2695, 2697, 2699). The matching ...origin writes on the surrounding lines are spelled correctly, so positions work and only the ANGLES are lost - which is why it went unnoticed: players 5-8 spawn in the right | NOT YET FIXED - found during a dead-guard sweep, logged for a pass when the m1l1 work settles. Needs the 4 writes renamed to ...angles and a check that no reader was written against the typo. |
| `bug-2043` | The probe did not self-start on a DEDICATED server, only on a listen server. Booting headless with nothing set produced mask=0. | The default was seeded only in coop_defaults.cfg, which is exec'd by the CLIENT before its saved config. A dedicated server has no client, so the file never runs and coop_probe stays unset - getcvar then returns "" and int() makes it 0. Lis | prb_init seeds the value itself when getcvar returns "", so it holds on every host since main.scr::main runs on all of them. Only an EMPTY value is replaced - `coop_probe 0` stays 0 and any number the user chose stays theirs. |
| `bug-2031` | [player report] 'mission m1l1 stuck in spawn truck. can not begin story. npc spawn camping with rocket spam' | OPEN - not root-caused. Evidence from the player's report zip: they are on v1.4.1 with today's binaries, and qconsole.log shows m1l1 loaded TWICE inside four minutes (01:21:33 and 01:25:11), consistent with restarting because they could not | NOT FIXED. Needs a repro on m1l1 from a campaign launch with coop_dev 1 to read viewpos at the stuck position, plus the spawnlocation set for m1l1. |
| `bug-1801` | t2l1: 20 x Script Error : Failed execution of command 'set_respawn' / 'set_respawn_time' for class 'ProjectileGenerator_Heavy' Targetname 'nebelwerfer1-4' | NOT pre-existing as first assumed - it was OUR code. coop_mod/main.scr launcher-respawn sweep matched world entities by MODEL ALONE. A ProjectileGenerator can carry a launcher model as its visual, so on t2l1 the sweep swept up all four nebe | Skip any entity whose classname contains ProjectileGenerator before arming it, and extended the LAUNCHER-RESPAWN println to carry class and targetname so a future false match is visible in one line. Verified: t2l1 now boots with 0 script er |
| `bug-1785` | one of the two level 2 guards is still floating | The pair was spawned at ( -798 -1735 ) and ( -799 -1747 ) - TWELVE units apart, well inside an actor's ~32-unit bounding box, so the two were placed inside each other. The engine resolves an overlap by pushing an occupant out, and out is fr | Second checker relocated to its own post at ( -391 -1850 ), 423 units clear. CONFIRMED BY MEASUREMENT: it now settles at z -119.875, a FRACTIONAL value, meaning droptofloor resolved against a real surface - the same height the known-good le |
| `bug-1784` | the level 2 guards that ask for your level 2 papers have just never spawned in the right spots | Every checker origin came from `viewpos`, which reports the EYE position rather than the feet - roughly 82 units high. This project has documented that trap twice (main.scr:724 bug-940 'the hand-authored coords carry an eye-height Z (viewpo | droptofloor after placement on all three checkers, the same call aihandler.scr:272 uses on spawned replica actors, plus a coop_aggroDebug probe printing the settled position. MEASURED: the level-1 checker moved -37 -> -119.875, an 83-unit d |
| `bug-1681` | rcon appeared not to reach the dedicated test server - every command timed out silently, including a plain getstatus. CAUSE WAS MY OWN CLIENT, not the server. | MY RCON CLIENT OMITTED THE CONNECTIONLESS DIRECTION BYTE. This is bug-1143, already recorded: current OpenMOHAA builds require the packet to be \xff\xff\xff\xff\x02 + 'rcon <pw> <cmd>'. Without the \x02 the server parses the command as 'con | Added the \x02 byte to the scratchpad rcon client, with the bug-1143 reference in a comment at the site so a future rewrite cannot lose it again. VERIFIED IMMEDIATELY: 'rcon hzmdev coop_stealthStart' now returns "coop_stealthStart" is:"1",  |
| `bug-1660` | team revive never completes - the prompt freezes at 'Reviving teammate... 2%' and the downed player eventually self-revives | dbno_teamrevive_monitor tracked the current reviver as an ENTITY and tested `local.best != local.reviver` to detect a change of medic. That comparison reads as 'different' on every iteration, so the reset branch ran every 0.1s tick, zeroing | track the reviver by entnum (local.reviverNum) and compare local.best.entnum against it; clear it wherever the reviver is dropped. PENDING PLAYTEST |
| `bug-1612` | ^~^~^ Script Error : [] applied to invalid type 'listener' at (global/ambience.scr, 455) - fired on EVERY map load, both m2l2a and m2l2b. | Variable-name collision between two subsystems. coop_mod/ambience.scr:42 already owns level.coop_ambEnt as a SINGLE script_model (the levelwide ambient bed emitter, 6 sites). The telephone-gag work added level.coop_ambEnt[alias] = emitter a | Renamed the dictionary to level.coop_ambByAlias at all three sites (global/ambience.scr write, m1l2b.scr:980 read, m1l2b.scr:957 comment), leaving level.coop_ambEnt as the single entity. Verified no coop_ambEnt reference is indexed any more |
| `bug-1482` | e2l3: paratrooper leader never fast-travels to the final house - Field 'jumpToFinalHouse' on NULL listener | Doubled targetname typo: $ParatrooperLeaderParatrooperLeader instead of $ParatrooperLeader. Every other reference in the same label is correct (lines 138/146/147/155), so this is a slip, not a design change. | FIXED 2026-08-06, deployed. maps/e2l3/FinalHouse.scr:150 - removed the doubled targetname; now $ParatrooperLeader.jumpToFinalHouse = 1. |
| `bug-1474` | maps/e1l3/Conquest.scr:1240 - 236x Field 'origin' applied to NULL listener plus Cannot cast 'none' to vector | ATTRIBUTION CORRECTED - originally logged against maps/e1l3.scr. MyFriendly's inner while-condition dereferences local.dest.origin, and the NULL-dest recovery that would repair local.dest sits INSIDE that while's body, so it is reachable on | FIXED 2026-08-06, deployed. Hoisted the NULL-dest recovery from inside the inner while's body to BEFORE the inner while's own condition (which dereferences local.dest.origin), plus a belt-and-braces 'local.dest &&' short-circuit guard in th |
| `bug-1450` | User: 'it looks like it's stuck on one map currently' - MAPTEST_HOLD on m1l2b repeated for 270s+, rotation frozen | TWO causes. (1) The walkers were being SHOT by live AI mid-walk and going DBNO (client logs: 'BLEEDING OUT: 47s'), so the alive-check failed and the clean-cycle gate could never be satisfied. (2) A client had also dropped to 3/4, and the ro | Walkers are now untouchable for the whole cycle: level.coop_dbno_disabled=1, nodamage re-applied on EVERY teleport hop (a respawn or map load clears it), health topped to 1000. Gates are bounded: 2min roster wait then run DEGRADED (map grad |
| `bug-1436` | Ride audit: e3l3 AB41 - dead riders walked the convoy on foot (no re-seat, all callbacks empty) | No ride-active flag existed and coop_playerJustRespawned was a stub, so respawners during the AB41 convoy were never re-seated. | level.coop_ab41Ride raised at the cannon mount and cleared in StartJumpThread; respawn handler glues the respawner to a fresh HULL seat (the mount block's own [209] recipe) - never a turret slot, to avoid ejecting a live gunner. |
| `bug-1385` | user: 'this will run like our lobby system where we basically have a version of the map exclusively meant for this game mode' - arena and campaign were sharing one map name. | Design, not a defect. e3l4 served as both a campaign mission and the arena test bed, so every arena save was keyed to 'e3l4' and only a coop_buildmap cvar gate kept them apart (bug-1384). A gate is a promise that has to be re-made on every  | Dedicated arena map modelled on co_lobby1..8: maps/ho_battaglia.scr with its own name and its own save files, and the geometry copied from e3l4's BSP as LOOSE files (maintt/maps/ho_battaglia.bsp + _sml.bsp + .pth, 32 MB) rather than through |
| `bug-1339` | root of the launcher soft-lock: DBNO revive lost a picked-up PIAT | coop_key_heavyModels listed only panzerschreck + bazooka - the PIAT (the launcher on Breakthrough e-maps) was not a 'heavy', so coop_key_onDown never dropped it and DBNO's takeall destroyed it | Uk_W_Piat.tik added to the heavy list - a downed player's PIAT now drops at their feet as a persistent glowing walk-over pickup like the other two. Belt-and-braces with bug-1337's respawning placed pickups |
| `bug-1290` | FALSE POSITIVE: multi-agent audit reported 'the injury vignette is permanently maxed after any DBNO revive' - reported to the user as a confirmed live bug before it was verified | The claim rested on one unchecked premise: that dbno.scr:49's `healthonly 9999` actually puts 9999 into health, poisoning the cgame peak-health tracker (which only resets at h <= 0). Entity::EventSetHealthOnly (fgame/entity.cpp) CLAMPS: `he | Reverted the 'fix' before it shipped. It would have been a real regression in the opposite direction: re-seeding the peak on the revive edge makes a player revived at 100/750 real HP (STAT_HEALTH 13, genuinely near death) report frac 1.0 an |
| `bug-1286` | t3l1: 'I can still move' during the opening scripted scene - the intro freeze does not hold | A one-shot 'freezecontrols 1' does not hold in coop. A probe settled it in one run: coop_introFreeze reported players=1 froze=1, so the freeze WAS being applied to the player - and the player could still walk. The engine's Player::m_bFrozen | Copied the recipe this mod already uses TWICE for the same engine behaviour rather than inventing one - coop_mod/lobby.scr::lobbyLockWatch and the xp.scr debrief loop both re-assert the lock every frame, and both carry a comment saying m_bF |
| `bug-1282` | t3l1: 'the allies didnt get killed at the truck' - the two riflemen survive the scripted mortar strike | Decided by a couple of hit points of rounding, hence intermittent - it killed them on one run and not the next. A deliberate probe answered it exactly: pre-blast both sat on 100.000 hp, so the soviet-ally buffer exemption IS working (they a | Finish the two scripted actors deterministically one frame after the blast: 'if (ent != NULL && ent.health > 0){ ent kill }' for $startingsolidier6 and $startingsolidier7. Targeted at those two only - the radiusdamage still does the real wo |
| `bug-1270` | t3l1: mortar fires and hits the Opel truck but there is no explosion | A race between santa's rocket and the gate that permits the truck to explode. JeepExplosion loops `while (level.bJeepCanExplode==0) { self waittill damage ; self health 3000 }`, i.e. while the gate is shut every hit is absorbed and the truc | Open the gate on the same line as the shot instead of one second afterwards, removing the race. The gate's stated purpose ('don't let anyone but santa blow us up') is unaffected, since the player cannot meaningfully damage the truck between |
| `bug-1250` | in DBNO you can see under the water, with no underwater treatment at all | cg_dbnoEyeDrop (default 50) lowers the eye for the downed view. The underwater detection is CG_PointContents(cg.refdef.vieworg) inside CG_CalcFov, which runs at the END of CG_CalcViewValues - frame line 54 - while the drop is applied in CG_ | Trace the drop instead of applying it blind: CG_Trace from the eye to the dropped position with MASK_CAMERASOLID (which includes MASK_WATER) and scale the drop by the hit fraction, minus 2u so the eye never sits coplanar with the plane. The |
| `bug-1246` | user: I started the map WITH a helmet, went dbno, revived, and it's gone | Helmet index 2 is the list entry 'No Helmet', and helmet_apply's `tik == none` early-exit sits AFTER the surface-hide block - so index 2 does not merely skip attaching a prop, it actively strips the helmet BAKED INTO the player model (surfa | NOT A REVIVE BUG - the two in-place-revive re-hat hooks added under bug-1236 were solving a problem that did not exist (harmless, idempotent, retained). USER ACTION: select tile 1 (Standard Issue, re-shows the baked helmet) or tile 3+. DESI |
| `bug-1241` | blood trails barely appear while crawling in DBNO | TryDropBloodTrail's gates were flat: a 0.45s interval and a 56-unit distance gate regardless of how wounded. A DBNO player is pinned at healthonly 100 vs a 750 max (13% health) but crawls slowly, so the distance gate rarely cleared - the tr | Gates now scale with wound severity (0 at the wound threshold, 1 at death's door): interval to 30%, distance to 25%, chance rising to 1.0. Deliberately NOT a DBNO special case - fgame has no per-player DBNO flag at all (it is pure script st |
| `bug-1238` | cg_dbnoEyeDrop does nothing (reported 3x across 3 different placements) | Placed wrong TWICE. (1) In CG_CalcViewValues after `vieworg[2] += viewheight` - discarded, because CG_OffsetFirstPersonView later does VectorCopy(pCent->lerpOrigin, origin) and rebuilds the eye from the model's 'eyes bone' tag. NOT inert th | Applied at the very END of CG_OffsetFirstPersonView, after every other write to origin[2], immediately before VectorCopy(origin, cg.playerHeadPos). The view model (pREnt) is offset by the same amount: this call chain runs BACKWARDS - cg_mod |
| `bug-1236` | i lost my helmet after i revived from dbno | helmet_apply is only hooked into the SPAWN path (player.scr manageAliveSpawning). dbno_corpse_revive calls `respawn` so it inherits that hook, but the two IN-PLACE revives - dbno_ai_revive (AI medic) and medkit.scr self-revive - stand the p | Threaded coop_mod/helmet.scr::helmet_apply at the end of both in-place revive paths. helmet_apply deletes any existing helmet ent before re-attaching, so it is idempotent, and it self-exits for players who never touched the switcher. |
| `bug-1220` | ^~^~^ Script Error : command 'origin'/'hide'/'notsolid'/'nottriggerable'/'triggereffect'/'set_respawn'/'notdroppable' applied to NULL listener (12x on e2l2) | Script threads on e2l2 call entity commands on an entity reference that is NIL/NULL - the thread dies at that line, so whatever it was setting up never completes. | NOT FIXED - detected 2026-07-29 by _research/regression harness against %APPDATA%\openmohaa\maintt\qconsole.log. Classified S0 script.nulllistener. Guard the entity refs with both NIL and NULL checks (see mohaa_script_notes). |
| `bug-1067` | Agent-authored probe coop_selftest_dbno.scr line 90: Couldn't compile / 'Script was not properly loaded' - probe emitted zero markers | The build-agent invented a player getter getcurrentdmweapontype (used as a command: local.x = local.p getcurrentdmweapontype). It exists nowhere in the engine/mod - an unknown EV_GETTER-style token parse-kills the whole file. depthscan2 (br | Replaced both getcurrentdmweapontype reads with the literal "na" (weapon-type was diagnostic only; the RESPAWNKIT test measures ammo-reserve persistence). Re-scan BALANCED. Lesson: depth-scan is necessary but NOT sufficient for agent-author |
| `bug-1065` | Script Error : Cannot cast 'none' to listener (coop_mod/xp.scr:327 if( local.player.classname != "Player" )) | level.coop_smoke_player (the support-smoke owner) is never INITIALIZED at map start - it is only ASSIGNED when someone throws a support smoke (objective_drop.scr:134, officer.scr:3452) and set NULL on recovery. So before the first smoke it  | Defense in depth: (1) xp.scr:326 xp_award now guards `== NULL \|\| == NIL` before the .classname deref (protects ALL xp_award callers); (2) xp.scr:482 xp_ai_killed hardened the same way (same latent NIL gap); (3) variables.scr inits level.c |
| `bug-1028` | LIVE-BOOT AUDIT: 5 non-HZM maps (t1l2/t1l3/t2l3/t2l4/t3l2) print harmless 'label <map> does not exist in coop_mod/spawnlocations.scr' each boot - no per-map spawn label. | main: threads a map-named label; these maps have none. COSMETIC - default spawns work (all 5 verified boot to coop-ready + spawn fine). NOT a broken-spawn bug (corrected an earlier overstatement). | FIXED 07-22: added 5 empty labels mirroring t2l2's silencing pattern. No behavior change. |
| `bug-939` | Duplicate gear (rifle + mine detector copy) floats in the air above the spawn point after a spectate->respawn (screenshot-verified) | spawnInventory hands out the respawn kit as script-spawned PICKUP entities (targetname playerNInventory) which giveInventory triggereffects at the player. Since the reissue-first ordering (bug-897/bug-924 backfill + mission-item reissue via | spawnInventory now tracks every pickup entity it spawns in per-invocation locals (local.spawnedPickups) and threads coop_cleanupSpawnedPickups, which waits 2s (well past triggereffect ~1ms / backfill / 0.5s ammo restore / 0.8s late assert)  |
| `bug-909` | Minesweeper + 2nd primary (Thompson) permanently lost on DBNO self-revive; REISSUE called n=1 but no 'REISSUE give' line; at spawn n=-1 | Empty MOHAA array reports .size == -1, so append idiom array[array.size + 1] stores the FIRST element at index [0]; every consumer loop is 1-indexed (mi=1..size) so element [0] is invisible. Hit coop_missionItems (detector registered at [0] | Clamp first index to 1 at both append sites (addMissionItemToEveryone local.n<1 -> 1; addToInventory weaponIndex<1 -> 1). Same idiom exists in loadout.scr/eventsystem.scr but those arrays are seeded 1-indexed first - left alone. |
| `bug-874` | e1l2 coop: all players respawn pinned to the parked minesweeper tank for the whole hideout+escape; level.coop_disableSpawnWarper never reset (respawn-where-you-died never activates) | coop_updateSpawn6 (e1l2.scr) was defined but never threaded, so e1l2_update6 (which sets level.coop_disableSpawnWarper=game.false) never ran. e1l2_update5 loop condition while(\$coop_tank2PlayerSpawn!=NULL && \$tank2!=NULL) never became fal | spawnlocations.scr e1l2_update5: added "&& level.crabdead == 0" to the while loop so it exits when the convoy stops (crabdead set 1 in Crab.scr::ObjectiveGuardCrab). e1l2.scr coop_updateSpawn5: after waitthread e1l2_update5 returns, added " |
| `bug-597` | ROOT CAUSE of the dead lobby LOADOUT button + pick carry-over + coop detection: server->client stufftext 'exec' and 'vstr' silently FILTERED by the OpenMOHAA anti-malicious-server whitelist | cg_servercmds_filter.cpp whitelists server-stuffed commands (Reborn-exploit protection). 'seta coop_*' and 'pushmenu' are allowed (why objectives + admin menu work) but 'exec' and 'vstr' are NOT - silently eating: coop_mod/cfg/detect.cfg (t | Scoped exemptions in CG_IsStatementAllowed: 'exec' allowed only for mod-namespaced paths (ui/loadout/, ui/coop_*, coop_mod/); 'vstr' allowed only for coop_* or CVAR_USER_CREATED cvars. Hostile-server protection intact. cgame.dll - REMOTE CL |
| `bug-431` | DBNO crawl animation plays opposite to movement direction (W crawls backward) | DBNO_CRAWL legs state (player_legs.st) plays ONE anim (coop_dbno_crawl = weapon_rifle/major_pain/riflep_crawl.skc) for ALL movement directions; that clip's crawl motion runs opposite the player's forward (W) input. | Swapped coop_dbno_crawl alias to riflep_floorcrawl.skc (the alternate rifle belly-crawl) as a best-guess flip. UNVERIFIED - anim direction can't be confirmed from files; needs an in-game check. If still reversed/odd, fallbacks: try another  |
| `bug-367` | co_lobby1 lobby: the frozen player can still JUMP (and drift), which also drops the pose. | freezecontrols (PMF_FROZEN) DOES block jump/move server-side (bg_pmove.cpp:1426 returns before jump; player.cpp:4049 sets PMF_FROZEN when m_bFrozen). But the coop spawn churn (BACKUP SPAWN spam, coop_isActive oscillation) respawns the playe | Added lobbyFreezeWatch: a per-player per-frame (waitframe) watchdog that re-asserts 'self freezecontrols 1' and re-pins the slot origin while coop_lobbyActive. Threaded once per player (guard flag coop_lobbyFreezeWatch) from lobbyOnSpawn. T |
| `bug-330` | dedicated server segfaults loading bare DM maps (obj/obj_team1) - discovered while testing NAT, NOT rendezvous-related | UNRESOLVED - our game.dll crashes on non-coop maps under the dedicated server (baseline reproduces with zero rendezvous cvars). Coop maps load fine. Likely a coop hook assuming coop init. | none yet - logged as known separate issue; coop maps (the only supported content) unaffected. Also noted: omohaaded.exe has headless env quirks (stalls with fs_homepath, dies without it); the CLIENT exe with +set dedicated 1 from the GOG di |
| `bug-328` | free cam: after reloading, camera stuck at an upward angle - mouse cannot pan up or down | The freecam orbit pitch clamp computed its +/-85 window from cl.viewangles alone. Server-side pitch shifts (reload/respawn/script view nudges) land in ps.delta_angles while cl.viewangles stays frozen under capture - the window drifted off t | [237] clamp uses the EFFECTIVE pitch: cl.viewangles[PITCH] + SHORT2ANGLE(cl.snap.ps.delta_angles[PITCH]) when the snap is valid. Same family as bug-321/327 (clamps computed from half the truth). |
| `bug-326` | crouch cover: camera still sometimes snaps on its own to a completely different direction | Two flicker hazards: (1) the cover-entry orbit seed (+/-55 toward the opening) re-fired on ANY cover re-detect - a one-frame server pose flicker or 1P/3P toggle mid-fight yanked camera+aim toward the 'opening'; (2) when cover ends, cg_freec | [228] (1) seed only on FRESH entries (>500ms since last covered frame, iLastCoverMs debounce); (2) fold-grace: remember the fold flag for 600ms (iLastFoldMs) and still fold a capture-drop within that window. exe+cgame PAIR. |
| `bug-325` | cover: peek enter/exit snaps view to stale angles; low-cover blindfire ignores the camera aim | While covered the free-look camera direction exists ONLY client-side (camera_offset orbit); cl.viewangles stay frozen. Peek start (capture drop) reverted to the frozen angles = snap; peek end re-applied a stale orbit = snap back; the server | [226] cg publishes cg_freecamFold=1 while PMF_COOP_COVER. Exe: (1) CL_FinishMove sends COMPOSITED angles (viewangles+orbit) in the usercmd while covered+captured - server viewangles track the camera (low-cover blindfire + torso twist follow |
| `bug-297` | Freecam v1: camera orbits great but WASD stays character-relative - can't steer with the camera like modern games | v1 froze cl.viewangles entirely while orbiting; movement basis in pmove is viewangles, so W always ran at the frozen facing | FREECAM v2 in CL_CreateCmd: with capture active + movement input, ease cl.viewangles[YAW] toward cameraYaw+inputAngle at cl_freecamTurnRate (480 deg/s default), counter-rotate camera_offset[YAW] so the camera holds still, and re-express for |
| `bug-287` | Wrong condition in logic | Condition was: if (in_strafe.active) | Changed to: if ( !cl_freecamCapture ) |
| `bug-166` | DBNO (Down-But-Not-Out) stopped functioning at some point (players die instantly instead of going down) | The map-test cfgs disabled DBNO with `seta coop_dbno 0`. `seta` ARCHIVES the cvar, so the first maptest run wrote `seta coop_dbno "0"` into the user's omconfig.cfg (line 111) permanently. dbno.scr defaults DBNO ON when the cvar is unset/emp | (a) Changed all 5 coop_mod/cfg/maptest_*.cfg from `seta coop_dbno 0` to `set coop_dbno 0` (session-only, no longer archives). (b) Added `seta coop_dbno 1` to autoexec.cfg, which runs after omconfig at launch and force-re-enables DBNO every  |
| `bug-164` | Alt walk key doesn't slow the player (sprint feature): holding Alt still runs at full speed | ClientMove computed altWalk (BUTTON_COOPWALK) only inside TickSprint to GATE sprint, never set walk speed. The run/walk branch keys off BUTTON_RUN (Shift), which stays SET when only Alt is pressed, so the player kept running. The Alt->+butt | Added to ClientMove ground branch (after sprint boost, before crouch mult): if (BUTTON_COOPWALK held && !m_bCoopSprinting && coop_sprint enabled) client->ps.speed = sv_runspeed*sv_walkspeedmult. Crouch mult still stacks; gated on coop_sprin |
| `bug-dbno-medkit-letgo-same-key` | DBNO: 'letting go / medkit share the same key' - holding USE to self-revive kills the player instead | dbno_letgo_monitor (hold USE 60 frames = let go/die) and medkit.scr::dbno_selfrevive (hold USE 200 frames = revive) BOTH polled self.useheld concurrently; let-go fired first at 60 frames so a self-revive hold could never complete. | Made the two terminal actions mutually exclusive by medkit count on the SAME key: hold USE = self-revive if coop_medkits>=1 (medkit.scr gated on '&& mk>=1'), = give-up if coop_medkits<1 (dbno.scr let-go gated on 'have_kit<1'); tap USE = med |
