<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# Other

[<- back to all fixes](../BUGFIXES.md)

**100 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-1705` | you get +5 XP twice for destroying searchlights | spotdamage is threaded from two places (:129 setup loop and :459), so one light ran two copies of the loop and both awarded. The adjacent spotnervous increment has always been guarded by checkdeath for exactly this reason; the XP hook added | Moved the XP award inside the existing if (self.checkdeath != 1) guard so both effects share one latch. |
| `bug-1665` | blueprint pickup never fires - 'BP OWNER-MISS' 90/90 samples, 'BP OWNER ok' never once printed (9th attempt) | MEASURED at last, after eight attempts that reasoned instead of instrumenting. The resolver probe sampled 90 consecutive failures and every single one said the same thing: `waitthread coop_mod/replace.scr::player_closestTo local.ent` return | Resolve the owner INLINE in collectible.scr with the exact loop shape the probe proved works, and drop 'closest player' as the semantic entirely - each player has their own private copy, so the only player who may take copy N is its owner.  |
| `bug-1642` | no AI can ring the alarm again for the rest of the map after the alerted stack is externally zeroed | ai_alarm_alerted_bumpstack:464 decrements level.ai_alerted_index with no floor; a negative index wedges ai_gofor_alarm in a permanent wait-3 loop | floor the decrement inside the label (planned in Step 22a) - NOT YET IMPLEMENTED (found by master-plan vetting round 3, not by a playtest) |
| `bug-1635` | v1.2.4 shipped the Auto Take-Cover settings toggle but the engine feature is absent from the shipped game.dll | auto-cover was built into game.dll AFTER the 08-08 15:41 gl2 binaries were synced for release packaging, and the post-release deploy went to the GOG root only (bug-1634) - so neither the user nor the release ever ran it. The menu writes coo | OPEN: next release (1.2.5) must ship a game.dll built from current source; verify with grep -qa coop_coverAuto on the packaged binary before publishing. Local play gets the correct dll from the next build.ps1 run |
| `bug-1632` | blueprints still not collectable after bug-1624: 'BP think started' logs, then never INRANGE/PICKUP | bug-1624 moved the PLAYER to an entnum param but kept passing the blueprint MODEL entity as thread param 1 - the same entity-param binding failure it documented. local.ent arrived NIL, the guard on line 2 ended the thread one frame after th | PENDING VERIFY A6: resolver replaced with the ESTABLISHED recipe replace.scr::player_closestTo (health/dmteam/coop_isActive filter, never NULL-tests $player[i]; aisquad/officer precedent) + ownership by string entnum after |
| `bug-1550` | Console flooded during play: '^~^~^ COV TRIG/COVC CMD/COV SND' many times per second, plus 'music set to normal with fallback normal' ~2/sec | Leftovers from the 08-05 coverage sweep and the 07-19 debug session. coop_covtrace was still 1 in the running session, and autoexec.cfg still ships `set developer 1` (its own comment says 'REVERT by deleting this line once debugging is done | Turned coop_covtrace / developer / coop_helmetDebug off live over rcon (console went silent). Permanent removal of the autoexec seeds still pending a deploy. |
| `bug-1545` | User: stop hiding baked headgear at runtime - the hand-maintained nodraw whitelist in helmet_apply has been wrong or incomplete four times (bug-1359, bug-1526, bug-1533) and can never cover a skin nob | Hiding a surface at runtime is the wrong layer for two independent reasons. (1) It is an open-ended enumeration problem: the live roster is 416 models/player/*.tik across 44 mounted paks, the head-covering surface names are author-chosen, a | Moved the removal to the SOURCE. Generated models/player/<skin>_nohat.tik for all 135 armory-roster skins (scratchpad/nohat_build.py): the file is byte-identical to the winning pak copy minus the head-covering skelmodel line and its surface |
| `bug-1542` | Fog editor SAVE silently wrote nothing - user confirmed in FOG MODE pressing the correct key, no file anywhere on disk | coop_fog_mapName was a bespoke parse of level.script, and it is the ONLY save subsystem in the mod that does not use level.coop_mapname. main.scr:100 shows level.script can legitimately be NIL (the framework back-fills it FROM coop_mapname) | coop_fog_mapName now returns level.coop_mapname (canonical, always set, lowercase) with an 'unknown' fallback, matching every other save subsystem. Fixes save, fixes the case mismatch on load, and makes the shipped coop_mod/fog/m1l3c.dat re |
| `bug-1534` | User: wants standout headgear found during the pak audit (gas masks, goggles, distinctive caps) turned into new unlockable helmet-switcher pieces usable by any character, not just the skin it happens  | N/A - feasibility investigation + prototype, not a bug fix. Every existing coop_helmets/*.tik uses a bespoke EXACT-FIT skelmodel: geometry lifted from a WORN gear skd that is single-weighted (weight 1.0) to one bone in the source skeleton,  | Rebuilt the transplant as a fresh Python script (parsing the real on-disk SKD v5 layout from openmohaa-hzm/code/tools/md5_2_skX/skx_format.h, cross-checked against tiki_skel.cpp's actual loader) and ran it on german_Wehrmacht_GasTrooper.tik |
| `bug-1526` | Whole-roster audit (user request): some armory skins carry a baked helmet/hat/mask that overlaps a custom helmet chosen via the switcher | helmet_apply's nodraw list (bug-1359) was built incrementally as specific bugs surfaced, not from a full scan. 4 skins used surface names never added: allied_Tanker.tik (lambert8), allied_infantry.tik (outside/inside, its own 2-piece helmet | Added lambert8/britparamask as blanket nodraw entries (unique names, safe). outside/inside/hat are NOT safe as a blanket toggle - 6 other skins (claus, allied_1st_manon, allied_Manon, allied_SAS, allied_Commanding_Officer, allied_british_ge |
| `bug-1520` | User: Searchlight Disabled +5 XP (one of two seed ideas for new XP sources, MG Gunner Killed being the other, already done) | Neither of the two independent searchlight-destroyed trigger systems (spotlight.scr::spotdamage, searchlight.scr::InitSearchlightDamageTrigger) attributed the damaging entity anywhere - spotdamage checked parm.other only against world, Init | Both now check parm.other.classname == "Player" (same attribution pattern already trusted in global/turret.scr and global/ai.scr) and thread coop_mod/xp.scr::xp_searchlightDisabled, awarding +5. |
| `bug-1496` | MEASUREMENT ARTIFACT - reported as '659 of 747 map voice lines never played', with e3l4/e3l2/t2l4/e2l3 claimed 100% silent. The claim overstates missing content badly. | The coverage probe exists ONLY on the SERVER sound path (Entity::Sound). A large share of map VO - dominant for Spearhead/Breakthrough - is delivered through the CLIENT TIKI frame-command path, which never reaches that function and is there | Recorded. The 49 runtime-confirmed DEAD aliases (COV SNDMISS - fired and resolved to nothing) remain valid; the 'never requested' list does not and must not be treated as missing content. To measure VO properly, add an equivalent probe on t |
| `bug-1487` | t2l1/t2l2: unknown animation '21G700_Jumpout' in models/human/german_winter_type1.tik | The animation EXISTS - mainta/pak1.pk3 and maintt/pak1.pk3 models/human/animation/scripted/level_t2l1.tik:154 defines it. The mod's new_generic_human.tik does not include that scripted-anim file, so the model never learns it. Same class as  | RE-INVESTIGATED 2026-08-06, NO CODE DEFECT FOUND - static analysis says this should already work. hzm-mohaa-coop-mod/models/human/new_generic_human.tik UNCONDITIONALLY includes models/human/animation/scripted/level_t2l1.tik (verified at lin |
| `bug-1476` | 6 distinct runtime animation errors: 'stop' missing in german_afrika_private.tik (x10 on m1l1), 3 unarmed_* missing in german_shepherd.tik, 21G700_Jumpout + bazooka_crate_standtocrouch missing in germ | Scripts request animations the TIKI does not define; the actor falls back to nothing, so the intended pose/motion never plays. | DUPLICATE - rollup of bug-1485 (stop/m1l2a), bug-1490 (dog unarmed_*), bug-1487 (21G700_Jumpout), bug-1489 (bazooka_crate_standtocrouch). See those four for individual fix status; no separate action needed here. |
| `bug-1475` | Static VO orphan scan: 0 of 1801 map-bound voice aliases unreferenced; 659 never requested at runtime; 49 fired into a missing sound | No cut per-map dialogue exists in the trilogy - every line is wired by some script. Silence therefore always has a runtime cause: dead thread, never-fired trigger, or dead alias. This narrows any future 'missing VO' report to those three. | Recorded as a sweep finding. The 49 dead aliases and the fully-silent maps (e3l4 104/104, e3l2 38/38, t2l4 23/23, e2l3 19/19) are the actionable output. |
| `bug-1468` | Sweep monitor: 'm6l1b cov=0/?' - and the audit showed 21 of 54 rotation maps had NO expectation manifest, including the ENTIRE Spearhead (t-series) and Breakthrough (e-series) catalogs | The manifest generator rejected any BSP whose ident was not '2015'. Allied Assault maps use ident 2015; Spearhead and Breakthrough maps use ident 'EALA' (version 21). All 20 e/t maps were therefore silently skipped from the very first gener | Generator accepts both idents. The EALA layout is identical for our purposes (header base 12, lump 13 = models, lump 14 = entities), verified by locating the entity text by content and matching it to the lump table. 20 manifests generated:  |
| `bug-1455` | User (repeatedly): 'I'm telling you right now m1l2a has one of the players at least still stuck on team select screen' - while the sweep log claimed all 4 embodied on allies | The readiness gate trusted dmteam. A client on the TEAM-SELECT SCREEN already holds a server-side team assignment (dmteam=='allies') but has never spawned - isSpectator stays TRUE. So the gate passed a client that was not in the world, and  | Readiness now requires isSpectator == 0 (plus alive and a real team), re-running skipTeamAndWeaponSelect + popmenu until it clears; the EMBODY proof line now prints spec= so the contradiction can never hide again. Lesson: when a user's eyes |
| `bug-1451` | User: 'some of these maps have scripted sequences... m1l1 can't test due to the intro sequence alone' | The walker teleported players regardless of map state. During a scripted intro (m1l1 truck ride, e3l4 convoy, m1l3a/b jeeps) the player is glued / physics_off / turret-bound, so a teleport is either UNDONE by the sequence or BREAKS it - and | Empirical agency probe: nudge player 1 by 24 units and check whether the move sticks (vector_length > 8). Retry every 3s for 2 minutes, logging COVWALK SEQUENCE. On success log COVWALK AGENCY ok after Ns and walk; on failure log COVWALK NOW |
| `bug-1449` | Client-side covtrace (COVC DROP): every client silently dropped the server command 'vstr g_m1l3' - 5 drops per client per sweep | The dev auth-token re-apply is gated on getCvar("coop_devKeys") != NIL, but an UNSET cvar reads as an empty string, not NIL - so the branch runs for every player on every server, sending 'vstr g_m1l3' for a cvar that was never set. cg_serve | PENDING (not deployed - sweep in progress): change the guard to a non-empty test, e.g. getCvar("coop_devKeys") != "" && getCvar("coop_devKeys") != NIL, so the auth-token flow only runs when dev keys are actually configured. Detector note: t |
| `bug-1446` | Sweep monitor: 'm1l3a: coverage=27/?' - three rotation maps had no expectation manifest and only stub walk lists | The manifest generator globbed retail BSPs with a lowercase 'pak*.pk3' pattern. GOG's main/ ships Pak0-Pak6 with a CAPITAL P, so every map that lives only in main/Pak5.pk3 (m1l3a, m3l3, m5l2a) was silently skipped - no manifest, stub walk l | Generator glob widened to '[Pp]ak*.pk3' (17 paks now scanned); the three maps manifested (22 + 90 + 52 triggers) with real walk and force lists. Takes effect next pass - the pk3 is not mutated mid-run (bug-1428). |
| `bug-1445` | Compile census first run reported 2 FAILs: ui/BIND.SCR 'was not properly loaded' and _notes/scr_makearray.scr 'Can't find' | FALSE POSITIVES - the census manifest was generated from every *.scr in the tree. ui/*.scr are UI DEFINITION files (bind-menu layout: header/bind_width/align keywords, no labels or braces) parsed by the UI system, not the Morpheus script co | Census manifest now excludes ui/, _notes/ and _research/ (741 real game scripts). Detector-honesty lesson: a new detector's first findings must be verified by hand before they are reported as defects - the mining report's rank-2 rule (never |
| `bug-1442` | User: 'how confident are you that jumping around in spectator mode is actually triggering anything at all' - measurement showed ~1 unique trigger fired per map (m3l1a: 1 of 183) | coop_mod/main.scr::startThread is ASYNCHRONOUS - it spawns a trigger_once with a setthread and returns immediately - so 'waitthread startThread' did NOT wait for the walk list. COVWALK START and END printed in the same second; the map then  | Generated walk lists now raise level.covwalk_done at their end (and print a PROGRESS ping every 25 triggers); covwalk::run blocks on that flag with a 300s cap and prints COVWALK TIMEOUT if it is not reached. Per-visit dwell tightened 0.30 - |
| `bug-1430` | Ride audit: every map's coop_playerJustLeft disconnect handler is dead code | The dispatch at the player-count-change site compared prevPlayerCount < playerCount - i.e. fired the 'player LEFT' callback on a JOIN. Inverted since [206]; m1l3b's .30cal disconnect recovery and every other JustLeft handler never ran. | Comparison flipped to > with an explanatory comment. All handlers scan the live $player array, so firing them on genuine leaves is what they were written for. |
| `bug-1397` | User: 'we should go ahead and ghost the barrier items around the arena now so we dont see the small tiny boxes anymore' - HOME toggle appeared to do nothing on first press | wall_spawn_one spawned each barrier's ghost crate VISIBLE without consulting level.coop_wall_ghostOn, which wall_init sets to 0. State and screen disagreed: the first HOME press flipped the flag 0->1 and called 'show' on already-visible cra | Honour the flag at spawn time: 'if( level.coop_wall_ghostOn != 1 ){ local.g hide }' right after the alpha call. Ghosts now default hidden (the live preview crate already shows where the NEXT barrier lands) and HOME is truthful from the firs |
| `bug-1372` | 115x per level: "^~^~^ Script Error : binary '>=' applied to incompatible types 'int' and 'string'" at global/parade.scr - 'if (self.spawncount >= self.totalguys)'. | #totalguys and #activeguys are MAP ENTITY KEYVALUES, and keyvalues arrive as STRINGS. Comparing them to an int throws and aborts the parade spawn thread at that line while the caller still reports success - the same string-typed-value class | int()-coerce self.totalguys and self.activeguys once in RunParade before the spawn loop, and coerce local.spawnpoint.totalguys at the two SpawnParadeGuy comparison sites (a different entity's keyvalue). Guarded against NIL/empty so the cast |
| `bug-1360` | user: 'many skins still have default helmets and hats and caps still appearing under the helmet or hat or cap you select' - reported AGAIN after the surface-nodraw list was already extended | Two separate defects wearing one symptom. (1) 'surface head' only ever hits the FIRST surface of that name, and head1.skd carries two called 'head' - the prefix form 'surface "head*"' is required. (2) A fresh 'model' command RESETS every su | Re-thread helmet_apply after BOTH 'local.player model (...)' sites in player.scr, and re-call it after a skin change in helmet.scr; extend the nodraw set to the 9 extra baked helmet surfaces at all 4 toggle sites. |
| `bug-1359` | user: some of the new skins have helmets that appear underneath the helmet selection option | the headgear system nodraws only THREE hardcoded surface names (us_helmet, us_helmet_inside, bob_helmet_camo). The 33 community skins carry their own baked helmets under nine other surface names, so the chosen headgear prop rendered ON TOP  | extended the nodraw list with helmet_mk2a, britberet, brit_beret, officer_hat, creasecap, AL_Brit_CMDHAT, BritTankHat, woolcap, offcap2 at ALL FOUR toggle sites (player show/hide + mannequin show/hide). Unknown surface names are a harmless  |
| `bug-1352` | user: 'tons of script errors about fog in the console' - Script Error : binary '>' applied to incompatible types 'string' and 'int', every 2s; AND the e2l2 fog profile never actually looked applied | coop_fog_loadProfile assigned the SPLITTER'S RAW STRINGS to level.coop_fog*. coop_fog_apply's first statement is 'if( level.coop_fogDist > 0 )' - string>int errors and ABORTS the thread right there, so $world farplane was never set even tho | float() the 6 numeric fields and int() the cull flag at load (float() is the codebase idiom - blueprint.scr, ambience.scr, aivoice.scr) |
| `bug-1349` | user: 'my fog still isn't right on e2l2' after the load + weather fixes | e2l2's INTRO CINEMATIC re-fogs the world per camera scene (7 setFog calls in cinematic.scr), all landing after the 10s re-assert window | the profile keeper is now persistent: 2s poll for the whole map, re-applying only when $world.farplane has actually drifted from the profile |
| `bug-1345` | user: 'it was raining at the beginning of the level, did that impact the fog settings I set in build mode?' - yes | global/weather.scr snapshots level.farplane/farplane_color at ITS init and re-asserts the snapshot after every lightning flash (unflash:) and at its restore point (:120) - on rain maps the fog profile gets stomped repeatedly with stale pre- | coop_fog_apply now syncs the weather system's snapshot vars (level.farplane / level.farplane_color) to the profile's values on every apply - lightning flash/restore cycles then re-assert the PROFILE instead of fighting it |
| `bug-1343` | user: e2l2 night fog profile not applied despite shipping in the pk3 | coop_fog_loadProfile applies during coop init, then the MAP's own fog init (e2l2.scr:121 setFog 2000) runs later and stomps it | coop_fog_lateAsserts: re-apply the profile at +2/+5/+10s after load; only runs on maps that HAVE a profile so all other maps' scripted fog untouched |
| `bug-1327` | pilot face entry wound invisible / wrong side ('needs to be on the right side of his face') | wound was painted at U0.47 = ON the nose-bridge midline; UV orientation was unverified. Parsed head1.skd (bone-space eye/nose/chin regions vs UV) - dot(imageRightDir, characterRight) = -1.000: the texture is photo-like, his RIGHT cheek is t | erased the midline wound (feathered restore from retail base), repainted at U0.345 V0.365 = his right temple, with a cheek trickle + spatter; same wound composited into blood3 at its native 512px. bloods stay 256px (painted from retail base |
| `bug-1284` | ^~^~^ Script Error : binary '+' applied to incompatible types 'string' and 'none' (coop_mod/helmet.scr, 149) | A debug println concatenated local.player.flags['coop_helmetTouched'] into a string. That flag is unset the FIRST time any player touches a helmet - precisely the event the trace exists to watch - so the concatenation hit NIL and threw, abo | Split into separate single-value printlns with a NIL guard on each maybe-unset term. Standing rule, now demonstrated three times: never concatenate a possibly-NIL value into a println - print the values separately. |
| `bug-1267` | Idle-spectate returns you with a default kit instead of your own state; e3l4 full of $player array casts | Player::Spectator() destroys the state before any script sees the transition - it sets health = max_health and calls FreeInventory(). Nothing snapshotted it, and a spectate return is not flagged coop_respawning (resetSpawn clears it), so ev | Extended coop_ammoSnapshotLoop to also record health, medkits and cover placements alongside the ammo pools; set coop_specReturn in manageSpectator BEFORE the state is torn down; OR'd it into local.wasRespawn so the exact-ammo path runs; an |
| `bug-1263` | At full health you cannot pick up a 2nd medkit | Three pickup paths exist. At full health the engine returns from Health item pickup before ItemPickup() (fgame/health.cpp), so the pack is never consumed, no heal delta occurs and the pack entity never vanishes - which is exactly what paths | Raised path C's gate to < 2 (the max carry used everywhere else in the file), made it CONSUME the pack (hide + notsolid) since the engine will not, and unified it onto the same coop_medkit_refill_cooldown / coop_medkit_in_use flags the othe |
| `bug-1247` | seamless ocean upscale reintroduced a tiling seam (ocean1b 14x worse) and altered a capped alpha range | Tiling was preserved during the resize (3x3 tile, resize, crop centre) but UnsharpMask ran on the CROPPED tile. A convolution clamps at the image border, inventing edge pixels and reintroducing the very seam the 3x3 tiling prevented. Separa | Sharpen INSIDE the tiled space before cropping; clamp each output channel back to the source's original range. Verified by measuring edge-wrap error of output vs original per file - every tile now seams tighter than retail. ocean1b's residu |
| `bug-1228` | m1l1 allied PRIVATES render mangled/upside-down - 'limbs stretched into spikes, faces flattened'; earlier framing 'only feet visible'. The 2nd-ranger CAPTAIN on the same map renders perfectly. Symptom | The .tik overflowed the TIKI setup op-buffer by 35 bits (65,571 emitted vs a 65,536 cap; TIKI_InitSetup gives MSG_Init 8192 bytes via the literal at tiki/tiki_parse.cpp:727). The op straddling the cliff was a REDUNDANT SECOND 'scale 0.52' n | Removed the redundant second 'scale 0.52' from the setup block. The scale at the top of the block already sets the same value, so nothing is lost; 'radius 20' (previously never written at all) now emits again. Comments cost zero buffer bits |
| `bug-1222` | selftest: multi-segment run reported m1l1 STALL 'silent for 3547s' | On a new log segment (= a new game launch) the parser kept the previous segment's map visit as 'current', so the next launch's boot lines were attributed to the map that was running when the previous process exited - producing a bogus multi | Reset current = boot at the start of every segment, so pre-map lines of a relaunch land in the boot bucket. |
| `bug-1221` | capture watch created 6 segments for a 2-launch simulated run ('log rotated' fired repeatedly) | Rotation detection hashed the first 512 bytes of the log every poll. While the file was still SHORTER than 512 bytes and growing, the hash changed on every append, so a still-growing file looked like a replaced file. | Hash a fixed 256-byte head, return None when the file is shorter than that, and only compare once we have already consumed >= 256 bytes. Truncation (size < offset) remains the primary signal. |
| `bug-1203` | User: 'US should not use british dialogue, so whatever dialogue they are using should be accurate to their character model.' | Two separate cross-nationality leaks. (1) flchatter picks an allied speaker by TEAM only, and MOHAA's team flag is just american/german - British paratroopers (models/human/allied_uk_*, sc_al_brit_*) and Italians both report as the wrong si | flchatter now drops any allied speaker whose model contains _uk_/brit/sas_/commando before playing a US pool. New coop_mod/aivoice.scr resolves nationality from the MODEL (de/it/us/uk) and British actors only ever draw the verified DFRUK re |
| `bug-1200` | User: 'I think its the smoke from the gun firing too.. way too much, tune it down.' | Not the weapon TIKs (our shipped m1_garand.tik/mp40.tik fx lines are identical to retail) and not garandsmoke.tik (stock, count 1 scale 0.02). It is the HZM smoke-whip feature in CG_ParseCGMessage: coop_smokeWhipMuzzle defaulted 0.35 and co | Lowered defaults to coop_smokeWhipMuzzle 0.15 and coop_barrelSmoke 0.25. All of these are CVAR_ARCHIVE and re-read every volley, so they tune live from the console with no rebuild - but an already-archived value wins over a changed default, |
| `bug-1182` | Font swap (bug-font-swap work) targeted the WRONG FILES and was silently inert - the engine never loaded the atlases that were replaced. | renderergl2/tr_font.cpp R_LoadFont_sgl implements an HZM hi-DPI feature: it builds hiresName = "<name>@3x" and, if fonts/<name>@3x.RitualFont exists, loads THAT and resolves the glyph sheet as gfx/fonts/<name>@3x.tga. The mod ships the @3x  | Restored all three plain .tga files to their originals (the @3x files were never touched). Regenerated correct @3x-metric Bahnschrift atlases and A/B'd them against the real in-game font - confirmed the difference is marginal - then held th |
| `bug-1176` | Bullet-impact smoke puffs rendering 'ginormous' - user: 'the smoke when bullets impacts hit the ground is insane' | Two overlapping sfx originspawn sprite puffs, each explicitly tuned to grow substantially over their 1.3-1.6s life (scalerate 2.5/3.0 from a 0.4/0.3 starting scale) per the file's own comment ('slow, fat, semi-opaque puffs'). Intentional tu | Roughly halved both starting scale (0.4->0.2, 0.3->0.15) and growth rate (scalerate 2.5->1.3, 3.0->1.5). NOT YET VISUALLY VERIFIED. |
| `bug-1023` | COOP-CONVERSION AUDIT: unconverted gag scripts use raw single-entity $player, throw with 2+ players. CONFIRMED: t2l3_medic.scr:475/504/448, t1l3_flak_cannon.scr:81, t1l3_misc.scr:296 (fades all screen | $player is a multi-entity array in coop; single-listener use (vector_length/turnto/viewangles) throws. | PROPOSED: replace raw $player single-use with coop_mod/replace.scr::player_closestTo <ref>. Full list in report. |
| `bug-1016` | User: 'none of the textures look right at all, they wrap around... doesn't look like the actual map'. Cover-pack pieces show wrong texel density + per-piece pattern restarts. | Measured: our meshes bake 128 units/repeat at scale 1 (proportional per face - no smear bug) while retail faces use 256 u/r (m3l1b drawverts). Since UVs are baked, world density scales with piece scale: slab s=6 = 768 u/r (3x coarse), s=0.7 | In progress (agent): measure per-texture retail density from BSP drawverts (median over faces), bake density-variant meshes d1/d2/d3/d4/d6 (UV span = face_units*dN/256), fitter picks variant minimizing world-density error (accept <=33%), pr |
| `bug-1003` | bp:depot builds as 'giant slabs floating in the air over scattered tiny cubes' (user screenshot shot0241.tga, m2l1). No crash - engine placed all 128 records faithfully. | bsp2bp fitter defect: gross-overhang hard-reject drives wall scales down to 0.44-0.5 (28-32u crumbs) so walls fragment into gappy confetti (43 pieces of crumbs at z 0-97) while the roof eats 71 pieces at z 160-242 -> floating roof over noth | Agent rewriting fitter: scale from dominant dims (wall scale = height/64, one course; slab scale = thickness/8), hard scale floor (0.75 wall / 1.0 slab), overlap-generous tiling flush at run ends, budget by dropping detail never structure,  |
| `bug-958` | user fell out of the map at one spot after a killwall session (5 kills: 5759, 5035, 5036, 5034, 5027) | All 5 killed brushes were plateau FLOOR SHEETS (dirtclip pads up to 585x506x32-80 with walkable tops) - the plateau's actual walking surface over raw terrain. The 'invisible wall' feel was their 30-60u STEP EDGES between overlapping sheets. | All 5 restored (commented #RESTORED-FLOOR in the loose homepath cmpatch, applies on map restart). cm_killbrush now REFUSES floor-sheet-shaped brushes (h<=96, footprint>160x160) unless 'force' is appended - message explains why. Void guardia |
| `bug-952` | cmpatch v1 regression: players clip through VISIBLE rock walls (13 killed clip brushes WERE the collision for nonsolid visual rock meshes); other invisible walls remain (not among the 74) | The 'collision falls back to visible geometry' assumption is false where visual rock is nonsolid dressing and the clip shell IS the wall. Radius-matching probe coords to brushes is imprecise for selection. | v2: draw-vertex cross-check (verts inside brush volume +-8) auto-classified the 74: 13 rock shells RESTORED, 61 free-standing fences remain killed. NEW PRECISION LOOP: wall probe now prints 'WALLPROBE <kind> BRUSH <n>' using gi.PointBrushnu |
| `bug-943` | tracescan.scr region cvars: '-4600' + 0 string-concatenated to -46000; '* 1' on cvar string = compile-time type error | morfuscript getcvar() returns string; '+' with a number CONCATENATES strings, '*' throws binary-op type error. Neither coerces. | int( getcvar("name") ) - the canonical cast used project-wide (global/weather.scr etc). |
| `bug-937b` | Feature (user-approved): invisible walls on e1l2 gun-emplacement rocks = baked retail PLAYERCLIP fences (BSP worldspawn brushes, script-unremovable; verified no entity-based walls near the area via BS | Retail SP maps paint CONTENTS_PLAYERCLIP over rocky terrain to fence the intended path; coop players explore where SP never went. | coop_noPlayerClip cvar (engine default 0, autoexec seta 1): player pmove tracemask drops CONTENTS_PLAYERCLIP only - real geometry/floors/walls (CONTENTS_SOLID) untouched, AI masks untouched. Caveat documented: fences sometimes guard unfinis |
| `bug-911` | ALL script println output AND script compile/runtime errors invisible in qconsole.log - debugged blind across multiple sessions | ScriptThread::Println returns early when developer->integer == 0 (scriptthread.cpp:2869), and script Error/compile diagnostics are likewise developer-gated. autoexec forced developer 0 (console-drop fix), so every script-side debug print an | Dev sessions run with 'set developer 1' (temp line in autoexec, non-archived). Engine ^~^~^ lines are C-side Com_Printf and always show - which is why the bus looked alive while script prints were dead. |
| `bug-899` | Coop (e1l2): stepping on a minefield landmine does nothing to players; in SP it hurts/kills. Live-confirmed. | The detonation paths applied damage ONLY through the engine builtin `landminedamage self N 150` (scriptthread.cpp EventLandmineDamage -> RadiusDamage MOD_LANDMINE). That engine radius-damage does not reach coop players under gametype 2 (unl | Added coop-safe helper coop_damageNearbyPlayers(origin,baseDamage,radius) in global/landmine_sp.scr mirroring ChargeDamage: iterates $player[1..size], guards health>0 / dmteam!=spectator / coop_isActive==1 / within radius, applies distance- |
| `bug-806` | Wrong reference: 795 should be 804 | Used "795" instead of "804" | Changed 795 â†’ 804 |
| `bug-797` | User: 'blood still affects the actual player... blood appearing on my skin from being shot' + 'blood pooling is happening to me from being injured - I thought we were making blood pool around dead bod | Players passed the gore paths via the Player::Postthink lazy blood_model + explicit IsSubclassOfPlayer bypasses: tier-1 blood skins painted the player model (log: GORESKIN ent=0 tier=1), and the living-wounded drip emitter attached to playe | No body gore on players, no player pooling (final spec): IsSubclassOfPlayer early-outs added to CoopGoreUpdateSkinTier (tier skins), CoopGoreTryDripAttach (living AND corpse drip) and DropBloodPool (pool + grow chain; players never pool, al |
| `bug-733` | Missing error handling in unknown | Code path had no error handling â€” exceptions would propagate uncaught | Added try/catch block |
| `bug-726` | Wrong condition in logic | Condition was: if ( local.speaker != NULL ) | Changed to: if ( randomfloat( 1 ) |
| `bug-702` | Wrong reference: this should be pAttacker | Used "this" instead of "pAttacker" | Changed this â†’ pAttacker |
| `bug-615` | Popped helmets fly with only one lateral velocity component (toss looks flat/axis-aligned) | EventPopHelmet: VectorScale(obj->velocity, fRandom, oWorldTag.axis[0]) scales the ZERO velocity INTO the axis vector instead of adding an axis[0] impulse to the velocity - the first lateral component is discarded (transcribed vanilla bug). | Changed to VectorMA(obj->velocity, fRandom, oWorldTag.axis[0], obj->velocity), mirroring the correct axis[1] line below it. |
| `bug-614` | Helmet fly-off (retail sethelmet/pophelmet) invisible in coop: popped helmets vanish in 5s, land silently, never settle, and one lateral toss component is lost | HelmetObject::HelmetTouch was unreachable dead code: class response table was {NULL,NULL} and G_Impact skips SOLID_NOT movers, so no EV_Touch ever arrives; the landing signal for SOLID_NOT MOVETYPE_TOSS entities is EV_Stop (g_phys.cpp ~1162 | Wired {&EV_Stop, &HelmetObject::HelmetTouch} so helmets settle flat on landing; added landing clank Sound("grenade_bounce_metal") (alias exists in AA/SH/BT ubersounds); lifetime now cvar g_helmetlife (default 30s). game.dll rebuilt + deploy |
| `bug-536` | Deployed sandbag not recognized as crouch cover after height raised to 64u | engine TickCoopCover low-cover check needs solid at chest 36u AND CLEAR at head 58u; 64u blocked the 58u head trace -> read as a wall. | Set collision to 54u: < 58 (cover function recognizes it) AND covers a crouched body (protected while in cover). Pop up to shoot = exposed by design. |
| `bug-535` | Attached helmets (helmet switcher) land on the SIDE of the head | attachmodel to Bip01 Head forces use_angles=true -> helmet inherits the head bone's tilted local axes -> dome points sideways. | Use the engine `attach` event with use_angles=0 (world-upright, follows head POSITION only) via a spawned script_model + entity lifecycle mgmt. World-space Z offset onto the crown. |
| `bug-533` | parse error: syntax error, unexpected TOKEN_IDENTIFIER (helmet.scr failed to compile; helmet switcher + sandbag deploy + all name-append coop binds silently dead, zero ^~^~^ NAMECMD in qconsole.log) | GameScript `surface us_helmet -nodraw` written UNQUOTED - the .scr compiler read `-nodraw` as MINUS the identifier `nodraw`, failing the whole file's compile. helmet.scr is threaded from the per-spawn hook (player.scr::manageAliveSpawning), | Quote the surface flag: `surface us_helmet "-nodraw"` / `"+nodraw"` (matches working precedents T1L1_PlaneRide.scr:71, itemhandler.scr:1737, m3l1a.scr). Also hardened the offset vector to the proven local.v[0]= idiom. TOKEN_IDENTIFIER in a  |
| `bug-528` | Missing error handling in unknown | Code path had no error handling â€” exceptions would propagate uncaught | Added try/catch block |
| `bug-425` | error: pathspec 'Full' did not match any file(s) known to git | PowerShell 5.1 arg-splits a multi-line string passed to a native exe when it contains embedded double-quotes/special chars. `git commit -m $msg` where $msg had a quoted phrase (Begin Full Campaign in quotes) sent the fragments to git as ext | For multi-line commit messages use `git commit -F <file>` (write the message with the Write tool first). For gh, avoid --jq with spaces/quotes in PS 5.1 - pipe the JSON to ConvertFrom-Json and filter with Where-Object instead. |
| `bug-381` | ^~^~^ Warning: Can't find player animation 'coop_emote_atease' (and coop_lobbyidle_*); lobby idle poses never played, rotation looked frozen on a fallback idle. | anims_shared.txt declares '$path models/human/animation' (line 5), so every alias path is RELATIVE to that dir (e.g. misc/salute.skc = models/human/animation/misc/salute.skc). The lobby aliases were written with the FULL path 'models/human/ | Use paths relative to $path: idle/atease.skc, idle/standidle01.skc, idle/chatter01.skc, idle/smoking03.skc. Also removed EMOTE_LOBBY_CHATTER from the lobby.scr rotation pool per user (talking-to-no-one looked off). |
| `bug-370` | co_lobby1: lobby soldier is CROUCHED holding the rifle (glitched), pose not applying. | Two regressions from the previous pose attempt: (1) freezeplayer in lobbyMapMain sets level.playerfrozen BEFORE the player deploys, pinning him in the spawn CROUCH so the at-ease pose can never stand him up (freezing before posing). (2) lob | Removed freezeplayer. lobbyPoseAndFreeze now: holster (rifle slings, stays in inventory so no -HAS_WEAPON bounce) -> wait -> forcetorsostate STAND then forcelegsstate EMOTE_ATEASE WHILE UNFROZEN (so pmove stands him out of the crouch) -> wa |
| `bug-368` | co_lobby1: lobby soldier holds his rifle at the ready (then an INVISIBLE rifle after takeall) instead of the parade-rest/hands-on-hips at-ease pose. | forcelegsstate EMOTE_ATEASE only drives the LEGS. The TORSO keeps its weapon-raise anim (weight 1) over the at-ease legs unless forced to an 'action none' state (which drops torso frame-slot weight to 0 so the legs full-body anim shows; pla | Removed self takeall. After forcelegsstate EMOTE_ATEASE, added forcetorsostate STAND (action none -> weight 0). Added lobbyPoseMaintain loop re-forcing legs+torso every 1s to self-heal the spawn-loadout settle. (Engine-verified by Phase B p |
| `bug-363` | co_lobby1 lobby: player is correctly warped to the slot, frozen and posed, but the shared cuecamera shows the empty tree range instead of the squad ('glitched view'). | LOBBYDBG proved the camera entity sat at origin 0 0 0. spawnLobbyCamera positioned it with 'level.coop_lobbyCam.origin = (...)', but a Camera entity IGNORES .origin= (and .angles=) - same class of no-op as moveto on a script_model. So cueca | Position the Camera with 'movetopos <vec>' and aim it with 'watch <marker>' at a script_origin look-at marker placed at the squad centre - the proven t2l2 recipe. Made camera pos + look-at cvar-driven (coop_lobbyCamX/Y/Z, coop_lobbyLookX/Y/ |
| `bug-361` | Incorrect value in code | Had "COOPDEBUG: manageAliveSpawning 5 for: " | Changed to "LOBBYDBG: manageAliveSpawning REACHED active=1 (s |
| `bug-360` | Wrong condition in logic | Condition was: if (local.player.dmteam == "spectator") | Changed to: if (level.coop_lobbyMap) |
| `bug-356` | co_lobby1 lobby: stand-in soldier can't wear the player's own MP skin, and the earlier real-player attempt rotated with the mouse + showed a crosshair. Also 'unknown animation rifle_stand_nervous_c in | Architecture was wrong. A stand-in Actor can never wear the player's chosen skin (player skins are models/player/*.tik, which can't be actors - bug-355). The prior real-player attempt failed because it pinned viewangles in a script loop (lo | Rebuilt the lobby around the REAL player using the mod's own proven map-intro/cutscene recipe (t2l2.scr:1063-1079, t1l2.scr:181-199): (1) cuecamera level.coop_lobbyCam - one shared static squad shot for every client; (2) player freezecontro |
| `bug-332` | 3P scoped guns STILL show back-of-head after bug-329 fix | TWO third-person deciders must stay in lockstep (own comment, bug-296 lesson): cg_view.c camera force-1P got the STAT_INZOOM term (twice, incl. the [237] final re-assert) but cg_modelanim.c bThirdPerson (BODY DRAW) never did - camera went f | [241] mirrored the zoom term on the body side: bThirdPerson = qfalse when STAT_INZOOM && !PMF_TURRET, placed after the \|= chain. LESSON reaffirmed: ANY new view-force term must be added to BOTH deciders in the same commit. |
| `bug-329` | 3P scoped guns: scope overlay renders while the camera is still behind the player (looking into the back of the head) | Ordering: the INZOOM force-first-person ran BEFORE the cover force-third-person, so scoping while covered (or peek-scoping) left renderingThirdPerson true with the scope overlay drawn. | [237] re-assert the INZOOM force AFTER every 3P force (native zoom is FINAL, turrets exempt as before). Scoped RMB now reads exactly like first person from any 3P mode. |
| `bug-327` | crouched in cover: sometimes stuck looking UP, mouse-down cannot recover | Since [226] the usercmd carries the composited (viewangles+orbit) aim, so the SERVER view already contains the orbit offset - but the cover camera still APPLIED the orbit on top when rendering = pitch double-application. Rendered pitch coul | [232] Cover no longer captures the mouse into the orbit AT ALL (CG_FreecamEligible returns false under PMF_COOP_COVER - inverted from the bug-303 era force): aim is live, camera chases it, engine's native +/-85 is the only pitch limit. Remo |
| `bug-324` | .30cal 3P fire: muzzle burst appears to the LEFT of the player (at his head), not at the gun barrel (shot0020) | The 1P viewmodel is draw-skipped in 3P but its frame commands still run (kept for fire SOUND, bug-315) - including tagdlight + tagspawnlinked muzzle flash, which spawned at the hidden eyes-bone gun. Visible once the seat setback separated p | cg_bCoopMuteVisualCmds global set around the hidden entity's command dispatch (cg_modelanim, cleared after); CG_ProcessEntityCommands whitelists sound/stopsound/loopsound/stopaliaschannel while set. Also added coop_vehTurretDown (ARCHIVE, d |
| `bug-321` | camera sometimes locks looking up or down while in cover | Exe-side orbit pitch clamp (CL_MouseMove) sums camera_offset against the CLIENT's frozen viewangles, but the SERVER re-pins the view during cover (entry auto-turn, peek return). The two drift; summed pitch wedges past the pole and the mouse | cg_view.c cover block re-clamps the orbit pitch against the SERVER (predicted ps) pitch every covered frame, to +/-82 (inside the exe's 85) so a wedged offset actively recovers. |
| `bug-316` | FOV slider doesn't apply to first person view (only worked while zoomed / felt like it reset) | Upstream OPM code only trusted server ps.fov while STAT_INZOOM and pinned every other frame to cg_fov (hardcoded 80). Server side already honored the fov userinfo (80-160, G_ClientUserinfoChanged -> EV_Player_Fov, MP 80-lock already comment | cg_predict.c both camera_fov sites: non-zoom branch uses ps.fov when > 0 (predicted + interpolated paths); cg_fov remains only as a 0-fallback. |
| `bug-311` | RMB peek from cover is 'VERY janky - you dont actually pop out from the door opening' (aims from inside the wall pose; shots clip the doorframe) | peek v1 only releases the torso to the aim chain + swings the camera; the BODY never physically steps toward the corner, so the muzzle stays behind the wall edge | QUEUED (design): physical step-out - during peek, slide origin ~24-32u toward the detected open side (traced, collision-safe), slide back on release; keep velocity zero; optionally bias the shoulder cam to the same side. This is the real po |
| `bug-306` | Turret manning pose never played (still unarmed_idle); log: 'TIKI_LoadTIKIfile: unknown section coop_turret_stand_idle ... skipping line' for every player TIK | blind EOF append landed AFTER the file's final closing brace: the cover agent's frame-command alias blocks are nested inside the trailing structure and the file ENDS with a closing brace - anything appended after it is OUTSIDE the animation | relocated the 3 aliases INSIDE: inserted before the final closing brace, tab-indented to match the agent's entries. RULE: anims_shared.txt now ends with a closing brace - new aliases must be INSERTED BEFORE it, never appended at EOF; verify |
| `bug-303` | Wall cover in 1P: moving the mouse to peek a doorway exits cover (back to first person) | mouse-look turns cl.viewangles; the server's wall-sustain trace runs along the CURRENT view yaw, so looking away breaks the geometric check after the 0.5s grace -> cover drops -> PMF_COOP_COVER clears -> auto-3P reverts | CG_FreecamEligible now returns eligible whenever PMF_COOP_COVER is set (even with cg_freecam 0 / cg_3rd_person 0): in cover the mouse drives the free ORBIT (viewangles frozen -> sustain trace stays valid) so you can look around/peek freely; |
| `bug-302` | XP popup appeared once then vanished; console: 'binary * applied to incompatible types string and int' at xp.scr:28 + cascading int/none errors in every award | level.coop_xp_scale = local.sc * 1 - MOHAA script does NOT coerce string*int (runtime type error), and a runtime error KILLS the running thread: xp_init died at line 28, so rank tables/teamkills/paraN never initialized; every award then cas | float(local.sc) + self-healing init (xp_identify/xp_award waitthread xp_init when not initialized) + NIL guard before the <0 compare on restored totals + teamkills NIL guard |
| `bug-295` | Blender MD5 anim import fails 'selected armature does not match the skeleton' (no_arm_match) despite mesh/anim joint lists being byte-identical | bone_tree_md5() assumed exactly ONE root joint ([a for a in lst if a[2]==-1][0]); MOHAA rigs have THREE roots (Bip01 + both feet as root-level IK goals), so the md5anim tree silently dropped the feet while bone_tree_blender listed all roots | bone_tree_md5 now collects ALL parent==-1 roots sorted by name (mirrors btb child ordering) and returns one tree entry per root |
| `bug-294` | Blender kit imported as floating arms only - USarmyplyr.skd is NOT the 3P body | models/player/us_army/USarmyplyr.skd is the FIRST-PERSON VIEWMODEL (surfaces lefthand/triggerhand/viewsleeves; 'plyr' suffix misleading). The third-person body rig is the AI-style body skd (models/human/allied_army_soldier/usarmy.skd, 42 bo | Kit regenerated around usarmy_ai.skd with the fixed one-line-per-joint writer; README updated. RULE: player 3P bodies live under models/human/<faction>/ or as the tik's body skd - 'plyr' files under models/player/*/ are viewmodel hands. |
| `bug-293` | Blender io_scene_md5 anim import: IndexError pop from empty list in pad_frames (line 335) on md5_2_skX-generated md5anim | writeMD5Anim wrapped frame floats at a fixed 6-per-line with no regard for joint boundaries. The md5anim de-facto convention (id exporters) = ONE LINE PER ANIMATED JOINT, and Blender importers parse line-by-line popping one line's floats pe | Frame writer now iterates boneData, emitting one line per flagged joint containing exactly that joint's flagged component values (bit-ordered). Verified: 49 flagged joints = 49 lines, per-line count == popcount(flags) for every line. ALSO p |
| `bug-288` | Wrong condition in logic | Condition was: if (s_adsShoulderEnv > 0.001f) | Changed to: if (s_freecamEnv > 0.001f) |
| `bug-271` | Wrong condition in logic | Condition was: if (ps->pm_flags & PMF_CAMERA_VIEW) | Changed to: if ((ps->pm_flags & PMF_CAMERA_VIEW) |
| `bug-269` | Incorrect value in code | Had `coop_xpClass` | Changed to `level.coop_officer_busy` |
| `bug-260` | Salute emote: 'Can't find player animation americanselectionidle2' persisted even with a byte-clean anims_shared override (whose OTHER aliases loaded fine - engine-probed) | TIKI 'weight' flag POOLS numbered aliases under the digit-stripped base name for random selection: americanselectionidle1..7 (all carrying weight 0.25) register as SEVEN entries named 'americanselectionidle' - the numbered names are NOT add | Added weight-free DIRECT aliases in the anims_shared override tail (coop_emote_salute/atease/stretch -> the same .skc files) and pointed the EMOTE_* legs states at them. Engine-probed: all resolve (234/233/235). Also built the permanent 'ti |
| `bug-259` | Salute emote: state engaged ('You salute' shown) but no animation; engine warned 'Can't find player animation americanselectionidle2' - ALL anims_shared.txt aliases were missing from the player TIKI | CRLF DOUBLING: the override was built by decoding the pak's bytes (string kept literal \r\n) then writing via python io.open TEXT mode, which translated each \n to os.linesep -> every vanilla line became \r\r\n (434 of them). The TIKI token | Rewrote the override in BINARY mode: vanilla pak bytes verbatim (byte-identical prefix asserted) + additions with clean \r\n. RULE: any file whose CONTENT comes from zip bytes must be written back in binary mode - never text mode. |
| `bug-221` | Wrong condition in logic | Condition was: if (m_iMovePosFlags & MPF_POSITION_CROUCHING) | Changed to: if ((last_ucmd.buttons & BUTTON_COOPADS) |
| `bug-165` | Reload camera movement not visible even after bumping cg_reloadCamDip to 6 | UNRESOLVED - signal chain source-verified (coop player_Torso.st 'viewmodelanim reload' x10 -> player.cpp:11750 'reload'->VM_ANIM_RELOAD -> ps.iViewModelAnim set+networked, same field the working ADS off-hand-hide reads at cg_modelanim.c:175 | PENDING runtime data. Added TEMP diagnostic gated by cvar cg_reloadCamDebug 1 that prints every iViewModelAnim transition + whether it falls in [VM_ANIM_RELOAD..RELOAD_END]. User to set cg_reloadCamDebug 1, reload, report console to determi |
| `bug-097` | Wrong reference: end should be else | Used "end" instead of "else" | Changed end â†’ else |
| `bug-094` | Wrong operator: && should be >= | Used "&&" instead of ">=" | Changed operator && â†’ >= |
| `bug-027` | Wrong operator: \|\| should be >= | Used "\|\|" instead of ">=" | Changed operator \|\| â†’ >= |
| `bug-026` | Wrong condition in logic | Condition was: if (cg.rain.density > 0.0f) | Changed to: if (cg.rain.density > 0.0f && cg.rain.speed > 800.0f) |
| `bug-021` | md5_2_skX.exe segfault on -decompile | Invoked -decompile with -outdir but no .tik/base name; DecompileSKX takes the modelDataDir branch and builds the output path from an EMPTY outTIKI, producing a bad path. Buffered stdout made the failure look like a crash with no banner. | Run -decompile WITHOUT -outdir (writes md5 next to inputs), or pass a .tik base. Confirmed full round-trip works (mp44_clip skd v5/skc v13 -> md5 -> skd/skc/tik). |
| `bug-018` | Incorrect value in code | Had "smoke in my face when i fire" | Changed to "per volley" |
| `bug-007` | Wrong reference: NULL should be vBarrel | Used "NULL" instead of "vBarrel" | Changed NULL ÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢ vBarrel |
| `bug-t2l2-f5bind` | F5 doesn't put me anywhere (seat cycle debug) | coop_seatCycleDebug poller was added to maps/t2l2.scr but the F5 key was NEVER bound to 'set coop_seatcycle 1'. No bind existed anywhere. (Also gated behind waitForPlayer, so also dead until player spawns.) | Added 'bind F5 "set coop_seatcycle 1"' to autoexec.cfg near the ADS debug binds. |
| `bug-buglog-dataloss` | buglog.json DESTROYED: an external auto-detect hook rewrote the whole file at 2026-07-27T05:58:49Z with a fresh {version:1,bugs:[bug-001]} object (bug-001 = auto-description of a tr_shader.c '>'->'>=' | The hook replaces the file with its own schema instead of appending to the existing array. File is untracked in git, no backups existed. | PARTIAL: file rebuilt as a valid array with this session's entries only. Clobbered evidence preserved at scratchpad/buglog_clobbered_evidence.json. RECOVERY LEAD: the 8 session transcripts in C:/Users/curry/.claude/projects/C--mohaa-coop-de |
