<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# Stealth, disguise & contain

[<- back to all fixes](../BUGFIXES.md)

**85 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-1937` | one trailing space in a player's name silently killed their entire name bus | Deep-trace F13: containsText consumed the failed char after a partial match without re-testing it - a double space (engine append joins with a space) could never match a space-prefixed key. | The mismatch branch re-tests the failed char as a new match start (single-char re-test is sufficient for all bus keys; strictly more matches for every other caller). |
| `bug-1872` | feature request - XP for stealth play: containing a situation, radio distraction, level 2 papers, blueprints, papers accepted by a guard | FEATURE. All five events were already DETECTED and none paid anything. The user also flagged that the mod is already complicated, which shaped the design. | ONE dispatcher, xp_stealth_act local.player local.kind, with amount and bucket chosen by kind - not five labels. Adding a sixth stealth act later is one else-if plus one call. Kinds and sites: contain 25 valor (bust.scr::coop_bustContained) |
| `bug-1810` | User: 'The issue was when I contained an enemy someone jumped out of the closet and it caused me to get caught.' A contain performed cleanly was busted by a guard that spawned during the takedown. | coop_bustWitnessed walks level.coop_actorArray['german'] and asks `cansee` with no regard for WHEN each actor arrived. On m6l2a the scene3 spawner materialises three german_winter_type1 at a time a short distance from the player on a 1s loo | Stamped self.coop_regT = level.time at BOTH actor-array registration sites (actorHandler and coop_ensureInArray) so every actor carries the moment it entered the world - map actors register during init so theirs is effectively zero. coop_bu |
| `bug-1794` | These random guards really do not see to care to investigate bodies after they have been contained, they still just walk by them / it might be the duplicative guards | UNCONFIRMED PENDING PROBE. Every who-can-see-this sweep (bust.scr::coop_bustBodyWatch above all) walks level.coop_actorArray, not the world. coop_spawnReplica builds count-scaling clones with a bare spawn <model>; if a clone never lands in  | Made registration exactly-once via self.flags[coop_inActorArray] (guards addActorToArray against double entry too), added coop_ensureInArray as an idempotent late-registration path, and called it on every replica from coop_spawnReplica. Add |
| `bug-1793` | I walked thru the level 2 area and it still did not trigger. He did not react at all to me going through. | coop_m6l2aBypassWatch ended with break after the first trip, making the checkpoint a ONE-SHOT per map load. The log proved it: bypass TOUCH + TRIPPED fired once at 20:57:59 during the two-player boot, so the watch thread had already exited  | Removed the terminal break and replaced it with a 3s settle so a single crossing cannot re-task the checker every frame. The while condition (level.coop_m6HasPapers2 != 1) is now the only thing that retires the watch. Also escalates via bus |
| `bug-1792` | Body being investigated text doesnt seem to leave if I kill the guy investigating the body by containg him too | In coop_bustBodyWatch the HUD DRAW was gated only on distance, while local.near (and therefore local.shown) was gated on local.seen. A player inside the radius whose examiner could not see him got the warning drawn with local.shown stuck at | Gated the draw on local.seen==1 so draw and near answer the same question, added an else that clears slot 254 when not seen, and made the stand-down clear unconditional (only the println stays latched). |
| `bug-1786` | bypassing the level-2 checkers without level 2 papers didn't trigger the alert | NOT the guards - the trigger volume was in the air. coop_m6bypass spawned at origin ( -745 -1606 21 ) with setsize ( -64 -64 -40 ) ( 64 64 72 ), occupying z -19..+93. The floor on that walkway is -119.875, so a standing player occupies roug | Origin z 21 -> -80, so the box spans -120..-8 and contains the player's full standing height with headroom. X/Y untouched - they were already correctly positioned past the checker. The trip now also prints who set it off, so it can be confi |
| `bug-1765` | add a second level-2 papers checker to accompany the one guarding the steps | design request - one checker can be waited out | coop_m6l2aPapers2b spawned at the user's viewpos ( -799 -1747 -37 ), yaw 234 (reading was -126), same type_disguise sentry / disguise_level 2 as the first so the pair reads as a posted two-man check. |
| `bug-1750` | design: the escape after planting was open-ended, so a stealth run had no time pressure at all | coop_m6l2aQuietEnding waited indefinitely for a player to reach the exit, then detonated. Correct for 'a clean infiltration ends clean' but it made the last stretch of the mission free. | The escape is a race: coop_m6EscapeFuse seconds (default 120) on the map's own stopwatch widget. Reach the exit in time and the ending beat plays and the mission ends clean. Miss it and the charges keep their own schedule - the ending beat  |
| `bug-1748` | 4 of the 6 charge sites are inside hangars with garrisons, so finishing the mission forced a firefight and defeated the stealth layer | Retail design: cp1_bombplant1/2 and cp2_bombplant1/2 sit inside the two hangars. Nothing wrong with them, but they are incompatible with the stealth route built onto this map. | Four exterior charges on three user-placed objects (radio mast x2, generator, power box) take over slots cp1_bomb1/cp1_bomb2/cp2_bomb1/cp2_bomb2. Count stays 6 so the objective text, [n remaining] counter and completion test are untouched,  |
| `bug-1743` | only the FIRST corpse on the map ever gets a kneeling investigator; every later body gets starers and nobody who investigates | level.coop_bustBodyMedic is the 'someone is already kneeling on a body' guard, tested at bust.scr:500 and set at :501 - and it was cleared NOWHERE in the entire codebase. It is also a LEVEL flag rather than a per-body one, so the first corp | Give the flag a real lifecycle. Converted the two bare `end`s in coop_bustBodyExamine to `break` so every exit reaches the teardown, and clear level.coop_bustBodyMedic there. Added a second clear at the single exit of coop_bustBodyWatch as  |
| `bug-1739` | bashed the guard and he ran instead of being jolted - again, after bug-1737 was fixed and verified | The stun's RE-ASSERTION has never worked, so the whole mechanic rode on the single initial pain animation and held only by luck. coop_bustStaggerRecover re-hits the guard with `hurt 3` whenever he has left the pain think state, gated on `cu | Track whether the guard was actually dropped to the vulnerable value (local.wasDropped) and, when he was, use an ABSOLUTE re-hit floor of 2 instead of the percentage. A dropped guard dies to the player's next round either way, so holding hi |
| `bug-1738` | cover blown after a clean contain - the player had just silenced the guard who was investigating the body, and the body-loiter escalation fired anyway 6 seconds later | coop_bustBodyWatch used ONE latch (local.found) for two different jobs. Keeping the WATCH thread alive so the corpse stays rediscoverable is correct. But the same latch also forced `local.seers = 1` on every tick once anyone had ever seen t | Make local.seers a LIVE per-tick count and delete the latch line. local.found still gates the outer while, so the body remains rediscoverable for the rest of the map and a new actor spotting it re-heats the area. The existing else branch -  |
| `bug-1737` | heard the bash noise but he started running towards the alarm, he didn't get jolted like he normally does - and it alerted the piano officer | ORDERING, exposed by bug-1735. coop_bustBash does `local.guard hurt local.dmg` and then, IN THE SAME FRAME, calls coop_bustArm. The engine does not turn that damage into a think state until the actor's next think (Actor::EventPain runs the  | Insert a bounded condition-wait between the hurt and coop_bustArm: poll local.guard.thinkstate until it reads 'pain', max 12 frames, breaking early if the guard is gone or dead. Condition-based rather than a fixed delay, and hard-bounded so |
| `bug-1736` | and I also cant use my papers now | coop_busted was cleared ONLY on the success path (coop_bustDisarm, reached when the contain resolves). A guard who SURVIVES the stun exits through coop_bustStaggerRecover, which restored his health, his pain handler and his victim flag but  | Call coop_bustDisarm from the end of coop_bustStaggerRecover, BEFORE the flags are touched (it early-exits once coop_busted is no longer 1). It does all four jobs in the right order: clears coop_busted and coop_bustArmed, takes the pistol b |
| `bug-1735` | second time I tried to contain someone my pistol didnt come out | coop_bustArm was DEAD CODE. Its idempotence guard was `if( coop_busted == 1 ){ end }`, written when coop_bustArm was the only setter of that flag. The same-day change at bust.scr:919 ('FLAG THE PLAYER NOW, not in coop_bustArm', so aggro is  | Split the arming latch onto its own flag, coop_bustArmed. coop_bustArm guards and sets that; coop_busted is still set for the aggro/papers gating. Latch released everywhere coop_busted is released: coop_bustDisarm (contained), coop_bustStag |
| `bug-1734` | latent: fixing bug-1733 would have regressed bug-1731 - the contained guard would survive the silenced round that used to kill him | coop_bustStaggerRecover made the stunned guard 'vulnerable' by writing coop_bustVulnHealth into local.guard.health. That is the ENGINE health, which while the coop pain handler is attached is only the coop_aiBuffer pile - aihandler.scr::han | Pick the governing field once (local.useReal = handler still attached AND coop_actorActualHealth valid) and use it consistently for the vulnerability drop, the stun floor, the per-tick re-hit test and the survival restore. Falls back to eng |
| `bug-1732` | silenced pistol drawn during a contain, trigger pressed, never fires - clip never decrements | HOLDING FIRE IS WHAT TOOK THE GUN AWAY. enableClickablePapers' papers-on-held-fire branch (itemhandler.scr:3029) equips level.coop_itemPapers when primaryfireheld AND coop_activeWeapon is NULL/NIL. During a bust coop_activeWeapon is NULL fo | Add coop_busted != 1 to the guard at itemhandler.scr:3029 so the papers-on-held-fire mechanic is suppressed for the containment window, where the player has been deliberately handed a weapon. |
| `bug-1731` | contained guard took 3 shots to kill; time to be seen, time to raise the alarm | A stunned guard kept full health and his helmet, which ate the first hit (g_helmetlife). Three shots is three chances for a death cry and three seconds of standing over him. | Drop health to coop_bustVulnHealth (25) for the stun window so one silenced round finishes him, and restore the saved value if he survives. |
| `bug-1730` | accidentally contained a guard while trying to open a door | coop_bustUseRange is 112u and USE is the same key as every door. A map running coop_bustAnyone offers the contain on every german, so it has far more chances to catch one by accident than an officer-only map. | Per-map level.coop_bustUseRangeMap; m6l2a set to 64. m2l2a and m6l1c keep their signed-off 112. The PROMPT still appears at 400u so the option stays discoverable. |
| `bug-1729` | loadout swap on blown cover cycled between papers and guns, papers rendering attached to the rifle | coop_armOnBlown cleared coop_forcePapersEquip but never removed the papers ITEM. It stayed in inventory, so coop_papersAnytime and the engine's weapon selection could still raise it and fight spawnWeaponAssert for the hands. | remove_item papers_level1 and papers_level2 on blown, and NIL level.coop_itemPapers. Cover is blown so papers are worthless; remove_item also zeroes level.papers, the engine disguise-level gate. |
| `bug-1728` | a distant guard who was not facing the player insta-aggroed after a contain kill | The stuck-coop_busted self-heal treated 'guard is dead' as 'no bust running', but a dead guard is exactly what a contain still resolving looks like - coop_bustMonitor runs its whole witness window over the corpse. It cleared coop_busted sec | Heal only when the guard ENTITY is gone, not merely dead. Also set coop_busted at bash time rather than arm time, closing the window between the punch and the pistol. |
| `bug-1727` | contain never offered on a walking guard | The watcher skipped any actor with an enemy. On a disguise map m_Enemy is how the system works - PassesTransitionConditions_Disguise (actor.cpp:9057) REQUIRES an enemy before an actor will salute or challenge - so every german who had notic | Skip only actors whose thinkstate is "attack" (EV_GETTER, actor.cpp:1831). |
| `bug-1726` | silenced pistol drawn but would not fire; papers stuck inside it; guard recovered and raised the alarm | coop_bustAnyoneWatch threaded a fresh enableClickablePaperseach time the nearest target changed. Each sets coop_clickablePapersEnabled true on entry; coop_bustArm clears it ONCE as the handshake that ends the papers loop when a bash starts. | Clear the flag and yield a frame before threading a new offer, and re-check coop_busted after the yield, so only one papers loop per player can ever exist. Rule merged into TRAPS T20. |
| `bug-1723` | set-2 radio distraction isolated a sentry who could not then be contained | enableClickablePapers sets canContain only for type_disguise "officer". m6l2a's set-2 radio pulls alarm2_officer_patroller, who must be a SENTRY because officers can never accept papers and he guards the route to the train schedule. The dis | Per-map opt-in level.coop_bustAllowSentry; m2l2a and m6l1c keep the officer-only rule. |
| `bug-1722` | a failed contain on m6l2a silently did nothing - no alarm, no reinforcements | coop_bustGoLoud escalates by firing $waittrigger_alarm_master, an entity of global/alarm_system.scr. m6l2a runs the OTHER alarm system (global/alarmer.scr) and has ZERO such entities, so escalation fell through to takeAllDisguises only: no  | Fallback branch: when $waittrigger_alarm_master is absent, thread global/alarmer.scr::turn_alarm_on on one of the map's own $alarm entities (it needs a real speaker for playsound alarm_switch). |
| `bug-1721` | the two 'silent containable' patrol men could not be contained at all | disguise_range 1 was used to silence them. actor.cpp:9075 requires 32 < dist < range, so range 1 is an empty interval and the disguise think is unreachable - but the contain prompt is hosted INSIDE that think (anim/disguise_papers.scr:16 is | Kept disguise_range 1 and raised the prompt directly from a proximity watcher (coop_m6l2aSilentContainWatch), the same way coop_m6l2aRadioGo does for a distracted man who likewise never challenges. Per-player latch, never re-armed (bug-1709 |
| `bug-1720` | 4642 'Path not found in Actor::MoveToPatrolCurrentNode' in one m6l2a session; AI abandon their patrols | coop_isProtectedActor's saluter clause was `type_disguise == "salute" && level.coop_enableDisguises`. On an OPT-IN stealth map that flag is FALSE when coop_apply_personality runs (~1s after actor spawn, bug-1648) - it is not set until a pla | Added level.coop_stealthCapable, set at MAP LOAD by opt-in stealth maps, and widened the clause to (coop_enableDisguises \|\| coop_stealthCapable). m2l2a/m2l2b/e1l4 unaffected - they set coop_enableDisguises at load so the left operand was  |
| `bug-1719` | level-2 papers checker would have denied forever | The level-2 pickup set a private flag but never called add_item "papers_level2". State_Disguise_Papers compares level.m_iPapersLevel (script name: level.papers, an EV_GETTER/SETTER on Level) against the actor's disguise_level, and ONLY glob | remove_item "papers_level1" then add_item "papers_level2", m2l2a's order (:449-450); checker keeps disguise_level 2. |
| `bug-1714` | m6l2a stealth: AI react to a silenced kill nobody witnessed | Our silencedpistol.tik carried only `secondary quiet` (the melee). Retail carries `quiet` on the PRIMARY fire too. Without it every shot ran weapon.cpp:2164 BroadcastAIEvent(AI_EVENT_WEAPON_FIRE, ...), and in MULTIPLAYER that radius is clam | Restored `quiet` on the primary fire block. No git or buglog record of the removal being deliberate; likely lost in the sp/dm/realism damage restructure. |
| `bug-1712` | 'when I click fire to spawn I also almost always fire a shot, that has been an issue for a long time' | Respawn is edge-triggered off BUTTON_ATTACKLEFT (player.cpp:5605). The player is necessarily still holding that button on the frame he spawns, and MOHAA weapons fire from the button being HELD rather than only from a fresh press - so the cl | Added Player::m_bFireLockUntilRelease, armed in Player::Spawned() and enforced immediately above the button-edge computation: while set, ATTACKLEFT/ATTACKRIGHT are masked out of current_ucmd->buttons, and one genuine release clears it perma |
| `bug-1709` | m6l1c radio distraction: 'I hit him, and pulled out my silenced pistol, which then my papers appeared inside of and I couldn't shoot him. We've seen this before' | coop_radioContainWindow re-armed enableClickablePapers every 0.5s for any player whose coop_clickablePapersEnabled read false. But coop_bustArm (itemhandler.scr:3503) CLEARS that exact flag as part of starting the bash - it is a handshake,  | Replaced the re-arm test with a private per-errand latch, flags[coop_radioWindowOpen]: the window opens once per player and is cleared when the officer returns to post. Late joiners still get a window; nothing is ever re-armed underneath th |
| `bug-1704` | m6l1c: guards at the front gate permanently hostile to a disguised player after an accidental shot | setEnemyAttackStates ended with a bare "local.enemy attackplayer" = Actor::ForceAttackPlayer, setting m_bForceAttackPlayer, cleared only in the Actor constructor (actor.cpp:3092). EnemyIsDisguised() returns false unconditionally while set.  | Restore now finds the nearest live non-spectator player and calls the script attackPlayer helper, which takes the advisory attackentity branch. Bare attackplayer kept only when no live player exists. |
| `bug-1703` | m6l1c: AI and player viewmodel/HUD stutter after picking up the uniform; 13-14 setEnemyAttackStates restores per second, sustained | managePlayerInventory gated a disguise re-grant on !group.player.is_disguised. is_disguised is the ENGINE flag, recomputed per frame, and player.cpp:5541 forces it FALSE while any actor holds the player as enemy, is attacking, and carries r | Added && !group.player.has_disguise. has_disguise is the mod-owned fact the grant sets and does not move when a rifle points at you. The model-refresh branch below already covers respawn. |
| `bug-1701` | m6l1c: 'when I pick up the uniform I am constantly switching between my silenced pistol and holstered' + 'the ai are being all twitchy too' + 'when they hit the alarm they stopped being twitchy' | giveDisguiseToAll is a self-retry loop that defeats its own success test. It deactivates weapons, grants has_disguise, then calls activateAllPlayerWeapons to RE-RAISE the weapon, and only THEN evaluates anyPlayerNotDisguised - which reads t | REVERTED the m6l1c opt-in rather than patching the loop under a live playtest: removed level.coop_enableDisguises / coop_stealthMap / coop_silencedOnSpawn from maps/m6l1c.scr, restoring the shipped DORMANT behaviour. Left a comment block at |
| `bug-1701b` | stutter persisted after bug-1703: 13-14 restores/sec unchanged; player own viewmodel and HUD stuttering with movement | giveDisguiseToAll retry test used anyPlayerNotDisguised (engine is_disguised). Same two-states-one-flag error as bug-1703 but in the retry itself: the failure branch clears coop_enableDisguises (its own re-entry guard), wipes coop_hasDisgui | Retry now calls new coop_anyDisguiseGrantFailed (tests has_disguise, skips dead/spectator). Retry bounded to 3 attempts with a wait, counter reset on success. |
| `bug-1700` | user (m6l1c): 'I killed everyone in sight before a disguise was picked up... went downstairs holstered and they were already aggro'd' - the disguise is ignored by actors that engaged you earlier | THE ONE-WAY attackplayer LATCH, and it makes loud->quiet missions structurally impossible. attackPlayer's legacy branch runs `local.enemy attackplayer` when handed a NIL target. That is Actor::ForceAttackPlayer (actor.cpp:9390-9392), which  | sentientIsSeen now falls back to local.sentient - the entity that was just SEEN, i.e. the offender by definition - when the caller's target is NIL/NULL, and only when it is a Player. attackPlayer then takes its `attackentity` branch, which  |
| `bug-1698` | user: 'lets make sure the coop contain situation ships on next time not off' - the m2l2a stealth route + Phase C contain shipped disabled in 1.2.6 | coop_stealthStart was seeded 0 in BOTH autoexec.cfg:1178 and coop_defaults.cfg:207 by the 2026-08-09 baseline revert, so every player of 1.2.5/1.2.6 has it archived at 0 and none of Phase C is reachable by them. | Set to 1 in BOTH files. THE AUTOEXEC LINE IS THE ONE THAT MATTERS and changing only coop_defaults.cfg would have looked correct and done nothing: coop_defaults execs BEFORE the saved player config (so it is only a default a saved value over |
| `bug-1696` | user: 'They walked right past the body this time' then 'They both walked past officers dead body' - body detection stopped after the first finder | MY OWN OPTIMISATION, introduced the same session. When the investigation was made to last the whole mission I latched `local.found` and used it to skip the ENTIRE cansee sweep once a first finder was located, reasoning that the sweep was th | The sweep always runs again, so newcomers still react; `local.found` now only keeps the AREA hot (forcing seers >= 1) so the loiter timer survives the finder wandering off or being killed. The cost is accepted deliberately - correctness ove |
| `bug-1695` | user: 'the guard got glitched checking the body, he kept walking and got stuck between crouching to check while he was walking constantly' | I REPEATED MY OWN bug-1682 MISTAKE, IN THE SAME SESSION, IN THE SAME FILE. coop_bustBodyExamine opened with `exec global/disable_ai.scr` as though that stops an actor - it is only `self.enableEnemy = 0` and does not touch his think, so his  | Use `anim`, which is the think-OWNING call - Actor::PlayAnimation does SetThinkIdle(THINK_ANIM) (actor.cpp:10819) - and issue it ONCE, since re-issuing restarts the animation every tick and is its own stutter. The loop now only watches: if  |
| `bug-1694` | Script Error : Field 'model' applied to NULL listener (coop_mod/weaponstate.scr) - and it was silently broadcasting SILENCED shots as explosions | NOT COSMETIC, which is why it is logged rather than swept up. weaponstate.scr's fired branch reads the CACHED flags["coop_activeWeapon"] and does `switch (local.item.model)`. During a Phase C bust the real weapon churns - the loadout arrive | Fall back to the ENGINE when the cache is empty (`waitthread returnActiveWeapon`) instead of trusting a stale flag, and default local.silentBullet to true so that a genuinely empty hand makes NO noise - inventing an explosion from a weapon  |
| `bug-1693` | user: 'other players are not required to flash their papers to the paper guards that ask for it if another player has already done it. They just get a free pass at that point' - the squad-wide papers  | coop_paperPassAll (itemhandler.scr:1176) sets `local.guard.coop_paperPass = 1` when a checker accepts, and Phase A correctly hoisted that call out from under an always-true branch in anim/disguise_accept.scr so it does fire ('^~^~^ PAPERS g | enableClickablePapers now consumes it: when a guard whose coop_paperPass is 1 challenges another player, that player's papers are presented automatically (`local.player use level.coop_itemPapers`). Deliberately NOT done by shortcutting the  |
| `bug-1692` | user: 'when it happened i only got m1 garand with one mag, not my full loadout' - Situation Escalated hands over a partial kit | TWO independent causes on the same path. (1) MISSING SIDEARM: a player who was mid-bust when the situation escalated is still holding the silenced pistol and still carries flags["coop_busted"]. silencedpistol.tik is ammotype "pistol", so th | coop_armOnBlown now clears flags["coop_busted"] and takes models/weapons/silencedpistol.tik back before issuing the kit - the bust is over the moment it goes loud, since escalation IS its resolution - and calls loadout_ammoTopup directly af |
| `bug-1691` | Phase C polish round: containment resequenced, Naxos prompts reworked, and four HUD strings specified by the user | Design refinement from live play rather than defects, recorded because the sequencing and the slot choices are load-bearing and would otherwise be re-derived. (a) The disarm sat at the END of the 10s witness window, so the player stood over | ["CONTAINMENT IS NOW TWO BEATS: papers back the INSTANT he drops plus 'Leave the Area Immediately.', then at +10s unseen 'Situation Contained. Proceed with Caution.' for the killer and '<name> Contained the Situation.' for everyone else. Th |
| `bug-1688` | LATENT SEVERE: coop_stealthArmOnHurt - the watchdog that arms an unarmed player who is being shot - has NO CALLER ANYWHERE and has never run | Found by accident: I hosted the new first-contact officer advisory inside coop_stealthArmOnHurt, and it never appeared. A grep for the label across the whole mod returns exactly ONE line - its own definition at itemhandler.scr:1423. Nothing | NOT ENABLED - deliberately. The advisory was moved to coop_papersAnytime, which is provably live (it is the loop that was eating the trigger in bug-1685). Threading coop_stealthArmOnHurt would switch on damage-triggered global arming that h |
| `bug-1687` | user: 'two guys walking right past the officers dead body' (x2), then 'So is there no way to make them investigate the body?' | A FILTER THAT IS CORRECT FOR ONE QUESTION AND WRONG FOR THE NEIGHBOURING ONE. The new body-investigation watcher reused coop_isProtectedActor to decide who could notice the corpse. That label answers 'should the AI-dynamics layer leave this | Split the one question into the two it was really doing. DETECTION now counts any live german who can see the corpse, with no protection filter at all - that is what the user asked for ('anyone that visibly spots their body'), and it does n |
| `bug-1686` | user: 'I feel like the second I shoot my silenced gun or he gets alerted or something it causes others to just instantly react. No one could have seen his body' | THE MECHANIC WAS UNWINNABLE BY CONSTRUCTION. The engine recomputes m_bIsDisguised every frame in coop (player.cpp:5519-5545, the bug-1638 parity block) and a held Weapon that is not an InventoryItem sets it FALSE. So the instant coop_bustAr | coop_stealthBlocksAggro now keeps deferring for a player with flags["coop_busted"] == 1, while level.coop_bustLoud != 1 and !level.alarm - i.e. for exactly the duration of an unresolved bust. Nothing is weakened: coop_bustWitnessed remains  |
| `bug-1685` | user: 'I got the gun with the papers stuck insdie and it would not shoot at all' / 'that seems to be a big issue... he just doesn't shoot' | A THIRD PAPERS PATH, and the only one still ungated. coop_papersAnytime is a PERSISTENT per-player loop (not the challenge-time enableClickablePapers that coop_bustArm already disables) whose only condition is 'primary fire pressed while ho | Added `&& local.player.flags["coop_busted"] != 1` to coop_papersAnytime's condition. A busted player is never presenting papers - they are in a gunfight. LESSON RECORDED: when a feature has more than one entry point, gating one is not gatin |
| `bug-1684` | user: 'it takes my handgun before hes killed' - the silenced pistol is raised correctly and then replaced mid-bust | NOT A STRIPPER - A RAISE RACE, and it was measured rather than reasoned about. A new ^~^~^ BUSTWEAP probe sampled the active weapon every 0.25s through a bust and printed the sequence directly: t=0 gun=silencedpistol.tik, t=6 gun=(none), t= | New coop_bustPistolHold: for the stagger window + 1.5s (the last pickups land after the stun ends), re-assert the silenced pistol whenever the active weapon is not it. Releases immediately once the officer is dead OR the player is disarmed  |
| `bug-1683` | user: 'got my handgun, couldn't shoot it' - the bust arms the player with a silenced pistol that will not fire | coop_bustArm hands the weapon over with a bare `give`, which grants the WEAPON and leaves the rounds to the map's ammo table. A stealth map has no reason to stock pistol ammo. silencedpistol.tik is ammotype "pistol", clipsize 8, startammo 8 | coop_bustArm now grants the pistol pool explicitly right after the give: `local.player ammo "pistol" 40`. 40 matches what the armory hands a COLT M1911 (loadoutroster.scr case "48"). Arming a player with a gun they cannot fire is worse than |
| `bug-1682` | Phase C first playable run: 'I heard a bash noise, he started just walking away from me... and he took off running for alarm' - the bashed officer was never stunned at all | THE STAGGER DID NOTHING BECAUSE disable_ai DOES ALMOST NOTHING. `exec global/disable_ai.scr` is, in its entirety, `self.enableEnemy = 0` - read straight out of the retail pak, not assumed. It only stops an actor ACQUIRING enemies. It does n | Rebuilt the stagger on the mechanism the ENGINE actually uses, verified in source rather than inferred: Actor::EventPain (actor.cpp:5310-5318) runs the pain script and then SetThinkState(THINKSTATE_PAIN, THINKLEVEL_PAIN). THINKLEVEL_PAIN ou |
| `bug-1677` | user: 'i did get my equipment though but it was all holstered' - the loud cover-blown re-arm hands the loadout over holstered | coop_armOnBlown ended with `local.p thread activatePlayerWeapon`. That label is a REACTIVATOR, not a raiser: itemhandler.scr:1121 opens with `if (... \|\| local.player.flags["coop_lastActiveWeapon"] == NULL){ end }`, because it exists to re | coop_armOnBlown now calls `thread spawnWeaponAssert local.p local.desired` instead. spawnWeaponAssert (itemhandler.scr:1606) re-asserts the desired weapon on frame 1, corrects a wrong engine auto-pick over the next 5 frames, and has a late  |
| `bug-1676` | PHASE C REDESIGN (user): the reactive bust could never win its race - 'he fires a shot everytime before I even get my equipment'. Replaced with a PLAYER-INITIATED contain. | bug-1674 established the shape: coop_stealthArmOnHurt arms on the officer's FIRST SHOT, which is also what trips the global cover-blown (takeAllDisguises -> coop_armOnBlown), so the containable state could not exist. The reactive trigger co | DELETED coop_bustFromChecker and its call in anim/disguise_deny.scr:43. New entry point coop_bustBash in bust.scr, called from itemhandler.scr::enableClickablePapers - the loop that ALREADY tracks 'an officer has stopped this player' (threa |
| `bug-1674` | Phase C first live run: cover went straight to LOUD without the bust firing, and the issued loadout stayed HOLSTERED | TWO SEPARATE OPEN ITEMS, both observed on the same run; neither diagnosed yet. (1) THE BUST DID NOT FIRE. The log shows only '^~^~^ STEALTH cover blown, loadouts issued' (itemhandler.scr::coop_armOnBlown) and no '^~^~^ BUST start', so cover | OPEN, but the shape is now clear. A bust in progress must SUPPRESS the automatic global cover-blown for that player - being shot at by the officer IS the bust, and it is meant to be recoverable. Next step: find what escalates on the player  |
| `bug-1671` | m2l2a: shooting the Naxos prototype does nothing - the RETAIL way to complete the objective had been silently disabled by our own stealth workaround | $naxos is a trigger_multiple with spawnflags 128 = TRIGGER_DAMAGE (fgame/trigger.h:211; flag order in the QUAKED spec at trigger.cpp:214). At spawn the engine sets takedamage = DAMAGE_YES, contents CONTENTS_CLAYPIDGEON and solidtype SOLID_B | Stopped disabling the trigger. Both routes now work: the quiet hold-USE sabotage (unchanged, still sets level.coop_naxosQuiet to suppress the scripted bust) and simply shooting it, which runs the full loud retail path. Safe for the stealth  |
| `bug-1662` | late joiner is locked out of the disguise: after one player takes the uniform/papers, a player who spawns in later cannot show papers and checkers do not challenge them | giveUniformToAll / giveDisguiseToAll / givePapersFlagToAll run ONCE, at the moment of pickup, over the players connected at that instant. Nothing re-issues them to a body that arrives afterwards, so a late joiner (or any player whose first  | NOT A NEW DEFECT - this is exactly what master-plan expectation 1 covers ('unarmed... and a new player joins') and what Phase C Step 10 implements via the per-spawn re-give inside managePlayerInventory, gated on the level flag trio. It is d |
| `bug-1657` | CONFIRMED IN A 2-PLAYER TEST: non-host clients are never challenged by papers checkers - Player2 walked past the downstairs guard and every officer with total impunity | PassesTransitionConditions_Disguise evaluates EVERY condition against m_Enemy - EnemyIsDisguised, IsEnemyConfirmed, the height check, the distance band - and then ran its final G_SightTrace against G_GetEntity(0), the HOST. So a checker cou | trace against m_Enemy (the enemy actually being evaluated) instead of entity 0. m_Enemy is NULL-checked at the top of the function, so this also SUPERSEDES the B0 guard at this site - entity 0 is no longer dereferenced here at all. Solo is  |
| `bug-1655` | unguarded NULL dereference of G_GetEntity(0) in the disguise transition sight trace - server crash whenever client 0 is absent | PassesTransitionConditions_Disguise assigns Entity *player = G_GetEntity(0) (:9039) and dereferences player->centroid (:9045) with no NULL test. Entity 0 is NULL between ClientDisconnect and the next connect, and in the window before Client | Phase B0, shipped as its own fix independent of the stealth feature: early `return false` when entity 0 is NULL at the sight trace, and a `pClient0 &&` guard at both grenade sites. Provably behaviour-identical whenever entity 0 exists, so S |
| `bug-1654` | the talking scientist freezes mid-conversation and never returns to his table after a quiet Naxos sabotage | our own coop_naxosSabotage feature colliding with a retail scene. objective2 sets level.scienceinterupt = 2 ('the prototype blew up, scientists stop your routine') when $naxos triggers - and the quiet sabotage triggers $naxos deliberately t | gate the assignment on the loud path only - `if( level.coop_naxosQuiet != 1 ){ level.scienceinterupt = 2 }`. A sabotage nobody noticed leaves their routine untouched, which is the point of the feature |
| `bug-1651` | user report: the level-2 papers checker near the second U-boat opened fire from a long distance instead of challenging | EXPECTED ENGINE BEHAVIOUR, not a mod defect - and exactly the mechanism vetting round 3 flagged when it re-derived T5. State_Disguise_Papers transitions to ACTOR_STATE_DISGUISE_HALT purely on the enemy being more than 256 units away (:96-11 | RESOLVED AS VANILLA 2026-08-10 - no grace window needed, and the open question is closed. The engine gives three outcomes from ACTOR_STATE_DISGUISE_PAPERS (actor_disguise_common.cpp:59-83): (a) papers shown but level.m_iPapersLevel < m_iDis |
| `bug-1646` | all five coop disguise-anim gates were ALWAYS TRUE, and four statements below them were unreachable | the coop preamble in disguise_{wait,deny,halt,accept,salute}.scr gated its attack branch on `&& thread coop_mod/aihandler.scr::canseeUndisguisedPlayers`. `thread` starts a label asynchronously and evaluates to a HANDLE, not to the return va | master plan v2 Step 2 / commit A1: all six now test the engine's per-target `is_disguised` (read into a local after the classname test, not relying on && short-circuit), and all four stranded statements are hoisted above the branch. Blast r |
| `bug-1640` | m2l2a: scripted-scene actors (card table scientists, norway pair) abandon their animation and stand idle; guards go permanently 'suspicious' with no script or alarm involvement | COLLISION LATCH (found by research fleet R2, verified in source at actor.cpp:3363-3376 ANIM_MODE_NORMAL and :3388-3404 ANIM_MODE_PATH). When a moving actor hits a temp obstacle that is a player and !IsTeamMate(player), the engine calls Beco | PARTIALLY CLOSED by bug-1638: with per-frame parity live, a disguised player is now IsTeamMate=true in coop, so bumps no longer latch while disguised. STILL OPEN: replace G_GetEntity(0) with the actual colliding player (or test 'any player  |
| `bug-1639` | parity test: holstered player still aggro'd - log shows AGGRO_SRC sentientIsSeen + ALARMTRIP firing at a disguised target | the mod's script aggro pipeline (sentientIsSeen -> attackplayer, aisquad, alarm hook) never consulted ANY disguise flag; with the experimental whole-window gates reverted (baseline) it attacked disguised players freely. attackplayer also se | engine-parity gating (pairs with bug-1638): coop_stealthBlocksAggro now defers PER TARGET to the live engine is_disguised flag (no cvar window; hoisted getter reads); canseeUndisguisedPlayers and the alarm_system guard switched from the sta |
| `bug-1638` | HZM baseline: guards immediately aggro a disguised player in coop (the ORIGINAL m2l2a complaint, reproduced on the reverted baseline) | THE SAGA ROOT: Player::Think's disguise maintenance (m_bIsDisguised recompute: holster=disguised, draw=blown, per frame) ran ONLY in GT_SINGLE_PLAYER. In MP the flag froze at whatever the last changeGameType window computed - disguise grant | run the maintenance block in every gametype (g_coopDisgParity 1 default, 0 reverts live); PathSearch::PlayerCover + UpdateEnemies deliberately kept SP-only. Pairs with the bug-1631 zero-threat veto fix. Retail stealth semantics now native i |
| `bug-1636` | card player stood up mid-hand again despite the aisquad search gate (census: idle thinkstates, zero DISG/attack) | the re-arming sit loop's escape hatch: 'neither settled nor committed (e.g. stuck in curious) - treat as real' broke him out permanently whenever a non-idle episode outlasted the 1s settle window. Engine CURIOUS episodes (footsteps/sight of | only alarm==1 / attack / pain count as real; any other non-idle episode is waited out entirely (0.1s poll, 30s guard) and then he re-holsters and sits back down. aisquad stamp-below-gate + SS1 search stealth-gate (same day) remain as the ot |
| `bug-1631` | papers-checker guard keeps getting interrupted mid-challenge, flips to idle repeatedly, never says 'my mistake' (regressed with unarmed-from-spawn route) | ROOT FOUND 2026-08-09 19:0x: player.cpp:5488-5494 disguise maintenance (runs during every changeGameType-0 window) vetoes m_bIsDisguised if ANY actor has m_Enemy==player && IsAttacking(). In coop, actorenemy retention parks harmless ZERO-TH | FREEZE ARM FIXED 2026-08-10 (master plan v2 Phase A / commit A1): DELETED the `local.guard type_disguise "none"` line from itemhandler.scr::coop_paperPassAll. Deletion not deferral - State_Disguise_Accept already demotes an accepting sentry |
| `bug-1630` | officer sees through level-1 papers and attacks, but players stay unarmed - cover is blown with no guns | coop_armOnBlown only fired from naxos-caught and alarm paths; the engine-side legit bust (type_disguise none / level-2 officers, EnemyIsDisguised false) had no script hook | coop_blownOnDamage watcher: 0.25s poll, player hp drop while a german in attack thinkstate is within 1600u -> setcvar coop_stealthNoAggro 0 + coop_armOnBlown (same recipe as coop_naxosCaught). Polling not waittill damage - player damage rou |
| `bug-1624` | User: 'We do still spawn with guns, and can switch around to guns when we pick up the papers' - stealth route armed at uniform pickup instead of from first spawn (master plan D1). | The stealth flags (coop_noWeapon/coop_startUnarmed/coop_forcePapersEquip) armed only inside likeynorway at the uniform pickup, so the whole walk to the papers was played armed and loadout grants could race the strip (the bug-1604/1606/1607  | m2l2a.scr: cvar-gated (coop_stealthStart == 1) flag arming moved to the main coop branch (gametype != 0) at map load, before any player can spawn; likeynorway keeps only a defensive coop_stealthStripWeapons sweep (still force-sets disguise  |
| `bug-1623` | Feature: quiet Naxos sabotage (m2l2a). User intent: the papers-only route should never require blowing cover - 'hold the use key to sabotage it, model changes from clean to destroyed'. | Retail $naxos is a trigger_multiple - WALKING INTO the volume detonates the prototype and fires ohnothenaxos1 (the scripted bust), making the objective an unavoidable cover-blow. The destruction swap (naxosnotsmashed -> naxossmashed + anten | Coop disarms the touch trigger and replaces it with a hold-USE interaction (coop_naxosHold, default 2.5s; useheld mechanic proven by the blueprint pickups): completing the hold sets coop_naxosQuiet and fires the retail trigger machinery - o |
| `bug-1619` | stealth_autotest.ps1 returned SETUP-FAIL on six consecutive smoke runs ('never reached disguise handout'). | Four separate harness defects, fixed in order: (1) booted with a bare ui_startdmmap instead of exec coop_mod/start_server.cfg, so no spawnable coop player; (2) launched the window minimized, which the SDL spawn-click cannot target; (3) dele | PARTIAL. The harness now launches, spawns the player and captures the right log slice (confirmed: loadout handed out at 01:41:49). The pickup step is not yet reliable, so it was NOT left running overnight - a harness that always reports SET |
| `bug-1618` | With both disguise flags reading 1 and no alarm, actors in attack state still climbed 0 -> 7 -> 9 -> 15. User: 'the first two guards dont immediately try to shoot me but the others barge in and do'. | THREE more raw `attackplayer` call sites that never pass through the guarded attackPlayer wrapper. The dominant one is coop_spawnReplica: the enemy count-scaling clones are created with `forceactivate` + `attackplayer` hardcoded, so every d | Added one shared predicate aihandler.scr::coop_stealthBlocksAggro(target) and gated all three sites on it (replica spawns without a target, squad go-loud continues, morale berserk skips). NOT yet confirmed live - deployed at 01:20 but the u |
| `bug-1617` | Disguise silently cleared ~18s after pickup with no cover-blown line anywhere; enemies then correctly attacked an 'undisguised' player. | weaponstate.scr decided disguised == (coop_activeWeapon == NULL), i.e. ANYTHING in hand un-disguises you - including the PAPERS, which the stealth route deliberately forces into the hands via coop_forcePapersEquip. So carrying the papers br | Both sites now test ( activeWeapon == NULL \|\| activeWeapon.classname != "Weapon" ), adopting the engine's rule rather than inventing one. Drawing a real weapon still breaks cover, which is vanilla and is what the in-game hint 'Keep your w |
| `bug-1616` | Cover kept breaking on m2l2a even with every forced attack blocked - ai_alarm rang the alarm on a disguised player. | A SECOND, independent aggro path. The engine sets self.alarmthread on $ai_alarm actors and calls it when the actor is alerted, so it never passes through aihandler::attackPlayer and the guard there structurally cannot see it. ai_alarm_alert | Probe first (ALARMTRIP), then a guard scoped identically to bug-1615: only while disguises are enabled AND no alarm is up AND the alerting enemy is a still-disguised player. An alarm after a genuinely blown cover is unaffected - the user wa |
| `bug-1615` | m2l2a papers-only stealth route: enemies aggro within seconds of the disguise, with every disguise flag reading correct. | aihandler.scr::attackPlayer ends in the raw engine command `attackplayer` and its own comment says it 'forces an enemy actor to attack the player, even when they are disguised'. Four callers reach it and only ONE (disguiseHandler:1013) chec | Added coop_aggroDebug tracing at the funnel plus an AGGRO_SRC tag per caller, then a guard in attackPlayer (coop_stealthNoAggro, default ON) that refuses when the target is a disguised player, or - when the call carries no target, which pro |
| `bug-1606` | User on m2l2a with coop_stealthStart 1: 'i still get spotted even with my papers now, they immediately catch onto me' - guards attack instantly despite uniform, papers and no weapon. | flags[coop_isDisguised] is the ONLY flag AI detection reads (aihandler.scr::canseeUndisguisedPlayers tests !coop_isDisguised). It has exactly one write site, inside setIsDisguised, and at runtime that is driven by WEAPON STATE TRANSITIONS i | coop_stealthStripWeapons now calls setIsDisguised <player> true true after stripping. The third argument (forceState) is required because setIsDisguised is otherwise gated on coop_hasDisguise, which is not necessarily set at pickup time. Ru |
| `bug-1604` | User on m2l2a: 'they immediately catch onto me when I pick up the papers/uniform', 'i cant actually show my papers', 'when I am spotted I dont get my inventory so i cant fight back'. Also: the papers  | The coop_startUnarmed stealth start was implemented as `group.player takeall` in the spawn path. takeall strips ITEMS as well as weapons, so it wiped the uniform that handlePlayerSpawn granted ~40 lines earlier. That single defect produced  | Rewritten on e1l3's proven recipe. level.coop_noWeapon makes managePlayerInventory early-exit at four separate gates (itemhandler.scr:711/1379/1414/1540) so no weapon is ever GRANTED - nothing is taken away, so the uniform survives. level.c |
| `bug-1603` | User: 'prior level i pick up the papers/uniform and I cannot actually use the papers'. Papers picked up on m2l2a but PRIMARY FIRE never showed them. | REGRESSION from the same day's coop_startUnarmed stealth start. flags[coop_activeWeapon] is initialised at itemhandler.scr:1297, INSIDE giveWeaponLoadout. The stealth-start branch ends before that call, so on a map spawning players unarmed  | Two layers. (1) Root cause: the stealth-start branch now sets flags[coop_activeWeapon] = NULL explicitly - unarmed IS 'no active weapon' and must say so. (2) Defence in depth: enableClickablePapers now accepts NULL OR NIL, since 'no weapon  |
| `bug-1539` | User: Grillo had no bullet hole in his chest/back when killed on m2l1, despite the wound code shipping earlier today | Two compounding errors, both from assuming rather than checking which model Grillo actually wears. (1) The death handler painted the wound onto surfaces 'shirt'/'cullshirt' - those belong to allied_oss_man_m1.tik (the brown resistance outfi | Added hzm-mohaa-coop-mod/models/human/allied_oss_man_disguised.tik declaring officer_tunic_blood1/2 and officer_tunic_c_blood1/2 tiers (those shaders already existed in scripts/coop_gore.shader; verified all 4 backing .jpg textures are pres |
| `bug-1533` | User: prior helmet-switcher nodraw audits (bug-1359, tonight's beret/goggles fix) only ever scanned this repo's own hzm-mohaa-coop-mod/models/player/ folder - the LIVE roster also pulls from the base  | A Python zipfile sweep of every mounted pak (43 paks across main/mainta/maintt + all maintt zzzz* addons) found 416 distinct models/player/*.tik files (225 excl. _fps arms-only variants) - about 3x what git tracks. Reading every surface's s | Added the 12 new surfaces as blanket nodraws (verified either unique in the full corpus or, where reused by several skins, headgear on every single use - no torso/lining collision found anywhere) in both branches of helmet_apply. Promoted ' |
| `bug-1498` | m2l2a (and by extension every coop map with simultaneous joins): 4 clients connect, get kits, then all sit T:spectator H:100 forever; walker prints 'WAITING roster 4/4' then silence; qconsole.run.1126 | A SILENT ENGINE RESTART RACE between the coop gametype hack and queued respawn events. (1) main.scr::changeGameType force-sets g_gametype to 0 via setcvar/gi.cvar_set, which BYPASSES CVAR_LATCH - the live integer really is 0 during each win | FIXED 2026-08-06, deployed (game.dll, hash-verified to gl2 + GOG). Player::Respawn now trusts the SERVER SHAPE, not the live cvar: 'if (g_gametype->integer != GT_SINGLE_PLAYER \|\| game.maxclients > 1)' takes the MP respawn path, with a '^~ |
| `bug-955` | killwall 'not doing anything': 7 presses all identified brush 6299 (already dead), engine refused each ('already non-solid'), real wall stood; user feared bad kills - audit: ZERO kills persisted (no l | CM_PointBrushNum: (a) returned neutralized (contents==0) brushes - dead geometry shadows the live blocker at the same spot; (b) reconstruction bug: returned on the FIRST side the point was OUTSIDE of = effectively first brush in leaf list,  | CM_PointBrushNum: skip contents==0 brushes + true all-sides containment (safe: every caller is the HZM wall-probe family). exe rebuilt; deploy watcher replaced with full batch (exe + game.dll + build.ps1) on game close. |
| `bug-880` | e1l3 escape disguise: coop players carried a silenced pistol that could be drawn and blow the disguise; papers were not force-held and DBNO/medkit revive re-armed combat weapons mid-disguise | getUniform gave weapons/silencedpistol.tik to all players during the disguise instead of a papers-only loadout; managePlayerInventory's coop_noWeapon early-exit was never engaged so respawns re-armed; the two revive re-give chains (dbno_ai_ | PAPERS-ONLY: removed the silenced-pistol give; set level.coop_noWeapon=game.true (managePlayerInventory early-exits -> no weapon on any spawn) + level.coop_forcePapersEquip=game.true; snapshot level.coop_weaponLoadout into level.coop_e1l3Re |
| `bug-767` | 4th user report: in-game ESC menu draws overlapping garbled text - stock dm_main stack (MAIN MENU/SELECT TEAM/...) meshed with coop_dm_main stack (Main Menu/LOADOUT/...) on one board, right after usin | ROOT: CL_ShutdownUI cleared hudList with 'for (i = 0; i < NumObjects(); i++) RemoveObjectAt(i)' - Container is 1-BASED (RemoveObjectAt(0) no-ops) and forward removal shrinks the list under the index, so with 2+ entries (coop always ui_addhu | Three engine fixes, openmohaa.exe rebuild ONLY (client+uilib link into the exe; cgame.dll/game.dll untouched). (A) ROOT: CL_ShutdownUI now does hudList.FreeObjectList() - full clear, no dangling entries (also cures the HUD-fade loop ~1878 w |
| `bug-625` | Mission-critical carried items audit (follow-up to bug-620): physical papers/binoculars (level.coop_playerItem1..4 via replace.scr::item) were lost on EVERY DBNO revive path and on the armory regive ( | Registered map items are only re-given by main.scr::itemGetAll in the SPAWN pipeline (player.scr:936-937); no DBNO revive chain and not the armory regive (loadoutpick.scr::loadout_regiveWatch) ever re-gave them after their takeall. medkit.s | keyitems.scr::coop_key_guardian now one-shots main.scr::itemGetAll 1s after every observed down->up transition (covers ai/team/self revives; corpse revive already respawns); medkit.scr self-revive chain got the picks tier + mission-items ap |
| `bug-253` | HUD fade 'wasnt doing anything at all' - health/ammo panels never dimmed | Menu::GetContainerWidget() returns m_itemlist.ObjectAt(1) - the FIRST widget in the menu's flat item list, NOT a parent container of all widgets. A MOHAA Menu is a flat list of sibling widgets; setting m_alpha on 'the container' faded only  | UI_SetMenuWidgetsAlpha iterates pMenu->m_itemlist (public) and calls SetDirectAlpha on EVERY item; each item's children inherit via Display() parent_alpha chain. |
