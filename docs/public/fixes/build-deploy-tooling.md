<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# Build, deploy & tooling

[<- back to all fixes](../BUGFIXES.md)

**21 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-2411` | The canonical rcon client named across ~15 records lived in a scratchpad/ path that no longer exists, taking the 'drive the console yourself' workflow with it. | SOURCE_OF_TRUTH records scratchpad/rcon.py as the entry point for console-driven testing, and that directory resolved to a session-scoped temp path. Every later session that needed console access had to hand the user cvar homework instead. | Rebuilt as docs/tools/rcon.py, in a directory that persists. The one detail that makes it work is the 0x02 connectionless DIRECTION byte after the four 0xFF bytes - omitting it is what produced the earlier 'rcon is broken' misdiagnosis (bug |
| `bug-2396` | The retracted bug-2336 claim ('the install is missing most of its speech, verify/repair your GOG install') is STILL reproducible by both of the scans that produced it. | G:\mohaa-gl2\{main,mainta,maintt} are NTFS JUNCTIONS into the GOG install. A pak-only scan misses the loose half entirely (the game ships most speech loose), and Git-Bash `find` does not follow junctions without -L, so it also reports the f | Recorded as a row in TRAPS.md T6 beside the other load-order rules: any 'is this asset present?' check must cover paks AND the walked junction (Python os.walk follows them). Re-ran correctly here - 5,110 distinct wavs from 6,860 pak entries |
| `bug-2229` | While fixing bug-2225 I ran gen_gore_skins.py over a 104-TIK subset and it silently replaced a 229-block coop_gore_gen.shader with 30 blocks. The output looked perfectly healthy afterwards. | The tool REPLACES coop_gore_gen.shader from the blocks produced by the current run; it does not merge into the existing file. So any run over a subset of the targets deletes every block the subset does not cover, and nothing about the resul | _assert_not_shrinking(): before writing, count the blocks in the existing file and refuse if this run would produce fewer, naming both counts and saying the tool replaces rather than merges. GORE_ALLOW_SHRINK=1 overrides it for a genuinely  |
| `bug-1889` | user: 'go ahead and fix the breakthrough ending and helmet' - both were already fixed; the records said otherwise | Two stale records pointed at work that was already done. bug-1027 (e3l4 outro fails to compile) was fixed incidentally by an earlier e3l4 round and never closed. The decapitation helmet pop was implemented and shipped in v1.3.1 hours earlie | Verified both against the code and the running engine rather than the record, closed bug-1027 with its verification method, and removed the OPEN.md entry. Two of my own intermediate claims were also wrong and are corrected here so they do n |
| `bug-1860` | docgen check: FAILED - docs/generated is stale (CHRONOLOGY.md, manifest.json) immediately after a successful docgen build | CHRONOLOGY embeds `git diff --shortstat`, computed WITHOUT the SELF_EXCLUDE pathspec that git_porcelain() already applies to the dirty-file list. The generated files live in the repo, so `build` writing them changed the insertion/deletion c | docgen.py:1139 now runs `git diff --shortstat -- . :(exclude)docs/generated`, matching git_porcelain()'s existing exclusion. `docgen check` passes and is stable across repeated runs. |
| `bug-1805` | A new player following the README could not install: it said to open /releases/latest and download MOHCoopTrilogy-Setup-<version>.exe, but the latest release (v1.2.9) carries no installer at all. The  | The full installer is ~6.8 GB (an exe stub plus four ~2 GB .bin payload parts) and is packaged by a SEPARATE manual step, installer/package_installer.ps1 - publish_release.ps1 never builds or attaches it. So it ships only periodically, whil | README now names the five installer files explicitly with PINNED per-file URLs to v1.2.8 (the current full package), states the totals, and explains that an installer version older than the release version is expected and correct because th |
| `bug-1804` | v1.2.9 release notes published with a UTF-8 BOM as a stray leading glyph and every em-dash rendered as mojibake, in both the GitHub release body and the Discord announcement. | Two defects, neither in the send paths. (1) The caller read the notes file with `Get-Content -Raw` and no -Encoding; PowerShell 5.1 defaults that to ANSI, so the $Notes string was already mojibake in memory and both GitHub and Discord trans | Added a -NotesFile parameter that passes the path straight to `gh release create --notes-file` with no PowerShell string round-trip, and decodes explicitly via System.IO.File.ReadAllText(UTF8) for the Discord body. The -Notes path now write |
| `bug-1800` | resume_publish.ps1 could not resume an interrupted publish - three separate failures in a row | Never exercised on a real interruption. (1) It read the manifest only from manifests/, but publish_release.ps1 writes it to %TEMP% and copies it there only AFTER undrafting - so on an interrupted run it is never where resume looks. (2) Its  | Manifest lookup falls back to the %TEMP% staging copy; draft lookup uses ConvertFrom-Json with per_page=100 instead of a piped --jq; source map mirrored to publish_release.ps1 exactly, including the GOG-root binary policy, with a note that  |
| `bug-1725` | linecheck.py passed a file that could not compile (prose without //) | None of the three scanners asked whether a line was a statement at all - braces, quotes and operators all balance happily in English prose. bug-1715 shipped once and was nearly reintroduced the same night. | Added a narrow rule: a line directly under a comment, with no argument-shaped token, whose verb is not a known statement word and whose args are not all numeric. Also added /* */ block tracking, which removed a pre-existing false positive i |
| `bug-1600` | docs/TRAPS.md silently converted from CRLF to LF across all 808 lines by a doc-pruning script. | Textbook T2, committed inside the file that documents T2. The script read with io.open(encoding='utf-8'), which performs universal-newline translation so CRLF arrives as LF, then wrote back with newline='\n', which stamps LF on the whole fi | Re-normalised to CRLF in binary mode (replace CRLF->LF then LF->CRLF, idempotent). Rule for any script that edits a repo file in place: read AND write in binary, detect the newline from the bytes, and never pass newline= to a text-mode writ |
| `bug-1578` | Session handoff: the helmet unlock gate refuses SELECTING a locked helmet but nothing re-checks what is already equipped; and the refusal message fires on every automated re-send, so a preview parked  | Two halves of one asymmetry. (1) armory_helmet_set's gate can only DECLINE a pick - it never removes or re-checks a value already sitting in flags[coop_helmetIdx], and helmet_apply (the single choke point for every spawn, revive and skin ch | helmet_apply now range-checks the stored index and re-runs cosmetic_isUnlocked on it before wearing it, falling back to a DETERMINISTIC index 1 (Standard Issue) - not nearest-unlocked, which would drift a player's appearance on every map lo |
| `bug-1564` | reported 'zero .pth files in any pak' - the scan had opened no archives at all | Scanned pak directories with MSYS-style paths (/g/mohaa-gl2/maintt) from native Windows Python, where os.path.isdir returns False and glob matches nothing. An empty result was read as a finding and stated to the user as fact. Rerun with G:\ | Corrected to the user in the same turn. Rule: a search that returns nothing must prove it searched something - print the count of files/archives actually scanned before trusting a zero result. Use Windows paths for native Python, MSYS paths |
| `bug-1559` | My ad-hoc brace-depth checker reported 'depth 2' on a file that is actually well-formed | The inline python depth scanner I had been pasting all session strips // comments and string literals but NOT /* */ block comments. maps/m3l1b.scr has unbalanced braces inside a commented-out block at line 410, so every scan of that file wa | Stop using the ad-hoc scanner. docs/tools/depthscan2.py exists precisely for this and handles comment/string braces correctly (TRAPS already says raw brace counts are invalid). It reports OK on the same file. |
| `bug-1456` | User: 'm1l1 intro forces spectator until the truck sequence ends... there will probably be more maps that do that' | The bug-1455 fix force-joined ANY client reading isSpectator. But several maps deliberately hold the squad in spectator for the duration of a scripted intro (m1l1's truck ride is the archetype) - force-joining there yanks players out of the | The gate now distinguishes the two identical-looking states by the freeze shim: level.coop_physicsOff == 1 means a scripted sequence owns the squad, so WAIT it out (logging COVWALK INTRO); otherwise a spectator is genuinely stuck at team se |
| `bug-1227` | UnicodeDecodeError: 'charmap' codec can't decode byte 0x9d in position 85738 - .wolf/buglog.json became unreadable to any session opening it with Python's default (cp1252) encoding on Windows. | Appending to buglog.json means re-serialising the WHOLE file. Writing it back with json.dump(..., ensure_ascii=False) converted ~1500 pre-existing \uXXXX escapes (6 ASCII bytes each) into raw UTF-8 byte sequences (2-3 bytes each), leaving 3 | Re-emitted the file with io.open(..., encoding='ascii') and json.dump(..., ensure_ascii=True), restoring 0 non-ASCII bytes, then asserted the reloaded data compares EQUAL when read as cp1252 and as utf-8. RULE for future sessions: when appe |
| `bug-1213` | m1l1 allied 2nd-ranger_private actors render MANGLED (limbs stretched into spikes, faces flattened) both in and out of the truck; 2nd-ranger_captain in the same truck renders correctly. Six investigat | STILL OPEN, but the MOD DATA IS NOW EXONERATED with proof. Independently re-derived: (1) full TIKI setup + skeletor merge simulation of both models with the REAL session keyvalues (log 9242-9245: private=m1 garand/head4/srg, captain=thompso | NO GUESS SHIPPED. Delivered instead: (a) two real engine defects found and fixed in passing (see bug-1189), (b) a decisive gated diagnostic (^~^~^ POSECHK in tiki/tiki_tag.cpp TIKI_GetFrameInternal, cvar tiki_posecheck 1) that splits the re |
| `bug-1009` | bp:depot v2 in-game = 'a bunch of scattered blocks' (user screenshots shot0242/0243): healthy full-size pieces but isolated clusters across a yard-sized area, floating beam, roof on one column. | TWO: (1) extraction region 800x800 captured the target building PLUS surrounding yard clutter -> template = scattered diorama; source 'building' is also a partly open canopy shed (sparse supports authentic). (2) Audit gap: ortho ELEVATION r | Agent hardening bsp2bp: mandatory connectivity gate (flood-fill piece adjacency, >=90% in largest component) + isometric 3D render in the review set; re-extracting with tight per-structure regions (center +/- dim/2 + 24u). Demo target swapp |
| `bug-424` | release v1.1.42 already exists - never overwrite a published tag; bump the version | An earlier publish_release.ps1 run was interrupted mid-upload (user stopped it). gh release create had already made a DRAFT v1.1.42 and uploaded all 8 changed assets (5 binaries + snd/tex/code pk3, ~855MB), but never reached undraft or the  | Resumed instead of restarting (avoids re-uploading 855MB). resume_publish.ps1's draft-lookup uses a gh --jq expr that PS 5.1 mangles, so ran the resume logic inline: copied the surviving TEMP manifest-1.1.42.json into manifests/ (also copie |
| `bug-402` | parse error (coop_mod/lobby.scr, 233): Couldn't parse 'coop_mod/lobby.scr' -> 'Script was not properly loaded' -> co_lobby1 lobby loaded as a BARE MAP (no props/camera/mannequin, player 'spawned norma | lobbyCamSway composed a function call (vector_scale(...)) AND a vector literal (( 0 0 local.vs )) inside ONE vector-arithmetic expression: `base + vector_scale(...) + ( 0 0 vs )`. Morpheus can't parse a function-call operand mixed with a ve | Split into standalone single-op statements: roff=vector_scale(...); voff=( 0 0 vs ); newlook=base+roff; newlook=newlook+voff; origin=newlook. Also defensively split other function-call-in-arithmetic lines (randomfloat/int in slot pick, int  |
| `bug-348` | co_lobby1 lobby didn't work: spawned normally with gun, no props/camera; log: 'parse error (coop_mod/lobby.scr,105)' + 'Couldn't parse lobby.scr' | Function call inside a vector LITERAL is a MOHAA parse killer: '( 0 0 int(local.drop) )' (line105) and '( int(local.p) int(local.y) 0 )' (line58). Whole file failed to compile -> lobbyMapMain never ran -> no props/camera/lock; player got no | Pull the int() results into locals first, then use the plain variables in the vector: local.pi=int(local.p); ...=( local.pi local.yi 0 ). Same for feet-drop. Added co_lobby1: label to spawnlocations.scr with the 4 anchor origins+angles. Var |
| `bug-331` | '/' view-mode cycle dead in-game: Script Error 'coop_mod/thirdperson.scr was not properly loaded' on every press | The [237] rewrite was emitted through a bash-heredoc python generator: the intended two-character \n escape inside the stufftext string literals collapsed into LITERAL NEWLINES mid-string (4 sites) - unterminated string = whole-file compile | Rewrote the 4 strings as single lines with real \n escapes (Edit tool + chr(92) python, no heredoc). LESSONS: (1) NEVER emit script files through bash-heredoc python when strings contain escapes - write the generator to a file first (the st |
