<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# Audio

[<- back to all fixes](../BUGFIXES.md)

**64 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-2416` | RAMPAUD fired correctly but printed EMPTY values on a dedicated server: 'RAMPAUD cut sfxduck= voxCut= voxFloor= hold=0.550'. | s_sfxduck, coop_voxCut and s_sfxduckVoxFloor are CLIENT cvars, registered by the client sound system. A dedicated server has no sound system, so they do not exist there and the script's getcvar returns "". The probe itself is fine - it fire | NO CHANGE. Recorded as a known limit of the harness rather than patched: the probe is aimed at the user's listen-server playthrough and works there. Reading the duck state headlessly would need a client-side print, which is a larger change  |
| `bug-2408` | user: 'what can we do about the welding torch?' - measured at 8,074 plays of weldingtorch.wav in a 7.5-minute session, roughly 18 per second. | NOT the script, and the obvious suspect was eliminated first. coop_welderWork does re-issue `welding_start` inside its loop, and welding_start carries `0 attachedmodelanim tag_weapon_left idleon` - but that loop period is 19-34 s, which acc | Coop-pak override of models/miscobj/welding_torch.tik, copied VERBATIM from main/Pak0.pk3 with exactly two verbs changed: `first sound welding_torch` -> `first loopsound welding_torch` in idleon, and `first stopaliaschannel welding_torch` - |
| `bug-2406` | user: 'I still hear the dialogue over the music and it sounds like it is at full volume ... Pain noises are also full volume whole time.' | NOT DIAGNOSED - and one false negative was caught before it was reported as a finding. The user queried s_sfxduck / coop_voxCut / s_sfxduckVoxFloor from the console and all three read 1, which looked like proof the duck never fires. It is n | PROBE ONLY, no behaviour change. coop_rampAudioCut now threads coop_rampAudioProbe, which reads back s_sfxduck / coop_voxCut / s_sfxduckVoxFloor / the hold value at cut+0.5s, again after the 5s release, and again at t+37s. Threaded rather t |
| `bug-2401` | The shipped ubersound.scr generated-block header asserts 'alias resolution is LAST-DECLARATION-WINS ... Appending is the fix.' The engine is FIRST-WINS. | Alias_ListAdd returns qtrue WITHOUT replacing when a node of that name already exists (qcommon/alias.c:313-352), printing 'DUPLICATE ALIASES' - the live log carries 1,764 of those. RegisterAliasAndCache only calls GlobalAlias_Add when bLoad | NOT YET APPLIED - record correction only, no behaviour change, and deliberately not touched during a vetting pass. The appended snd_step_wade block is inert on m3l1a (retail's own declaration at :182 registers first) and snd_ping's appended |
| `bug-2394` | ERROR PlaySound: snd_step_grill needs an alias (x4 in the live m3l1a log) | The metal-footstep family (snd_step_grill1-4, snd_landing_grill1) carries a `maps` field listing m1l2b..m3l1b..m6l3d but NOT m3l1a, and the field is a hard PREFIX load filter, so m3l1b does not cover m3l1a and the alias simply did not exist | Added m3l1a to the maps field of all five members of that family. The change is right - the log errors are real and a footstep is self-rate-limiting, so unlike the AI callout pools (bug-2307) widening it cannot cause over-firing. Checked ra |
| `bug-2390` | user, three separate times: 'the voice/dialogue is still way louder than the music. STILL.' - the REAL cause, found by the audit sweep. | THE TAKEOVER'S DUCK WAS BEING ERASED BY THE CINEMATIC'S OWN CLEANUP. coop_m3l1a_beachAudio sets coop_voxCut 0.55 for a 95-second music takeover, but coop_rampHandoff lands only ~20.5s into that window (2.6s arc + 17.95s underwater) and thre | The takeover publishes level.coop_voxCutHold and clears it when it ends; both restores honour that hold when set and otherwise return to 1. The blast cut still works, the takeover cut now survives the handoff, and nothing is left ducked onc |
| `bug-2387` | REVERTED. Six m3l1 aliases were re-pointed on the false premise from bug-2386. | Built on bug-2386's error. The 'missing' targets - dfr_m3l1_030h2.WAV, _032h2, _049h2, _021h1, _082h, medic2.wav - are ALL present as loose files on disk. Those aliases were never broken; I swapped six working lines for older takes of the s | All six reverted to their original targets and the commented-out lines restored. The one observation that survives is the mechanism: duplicate alias names ARE discarded first-wins (alias.c:322-327), so appending an override to uberdialog.sc |
| `bug-2378` | user: 'I CANT hear the captain again wtf... he was working FINE' immediately after the vox cut shipped. | I shipped coop_voxCut 0.18 against an explicit adversarial warning not to: the review said the T+20..T+61 window carries the whole retail beach chain and a low vox cut regresses bug-2298, the defect that took three wrong diagnoses to fix. I | voxCut 0.55 (clearly quieter than the 1.0 it sat at, still legible) and the MUSIC raised instead (tmvolume 1.5 -> 1.9), which is the end I should have reached for first. coop_cueCut stays 0.30 because injury/pain noises are not speech and n |
| `bug-2371` | user, third report: 'the voice/dialogue is still way louder than the music. STILL.' | MY FIX NEVER RAN, ONCE. I put the coop_voxCut / coop_cueCut lines inside `if(level.coop_rampAudioCut != 1)` - the block that only executes when the ramp cinematic did NOT fire. On the normal coop path the cinematic always fires and sets tha | Moved outside the branch, onto the path both halves reach. The branch only decides WHO performs the duck, not whether the music takeover happens. |
| `bug-2369` | user, second report on the same thing: 'the voice dialogue lines and injury sounds all play way over the music still. Nothing has changed there.' | He was right that nothing had changed, and s_sfxduck was never what carried these. TWO separate exemptions, and my earlier fix (bug-2354, un-crushing the music) addressed neither. (1) DIALOGUE is exempt from the cinematic duck ENTIRELY: bug | New cvar coop_cueCut scaling tier-1 cue gain, mirroring the existing coop_voxCut for speech. FLAGS 0 ON PURPOSE - only CVAR_ARCHIVE cvars are written by Cvar_WriteVariables, so it physically cannot latch into a config and permanently mute e |
| `bug-2356` | user: 'I also think we should extend how long the music plays with everything else ducked... the sound effects should slowly rise back to where they need to be over time (preferably as the music start | n/a - tuning. | Music-only window 60s -> 95s. The world's return fades over 34s instead of 12s and OVERLAPS the music release rather than following it, so the battlefield seeps back rather than switching on. coop_cvarFade runs 24 steps over its duration, s |
| `bug-2354` | user: 'the dialogue that the allied actors play still plays way louder than the music.. I like that we can hear it, but it needs to be WAYYYY lower until the duck ends' | THE DIALOGUE WAS NOT TOO LOUD - THE MUSIC WAS BEING CRUSHED, and this file's own header said why. coop_rampAudioCut's comment states the rule outright: 's_volume - NEVER. It is clamped 0..1 and squared into the listener gain, so it would ta | The ramp blast now plays coop_tinnitus_ring and coop_shellshock DIRECTLY instead of routing through coop_tinnitus_triggerBlast, so no s_volume is stuffed. Both live under sound/coop_tinnitus/, which S_HZM_CueTier gives tier 1, so they ride  |
| `bug-2353` | Found by audit: the Omaha shellshock cue was a LOOP that nothing ever stopped, running under everything for the rest of the mission. | `playlocalsound coop_shellshock 1` - the trailing 1 is the loop flag. tinnitus.scr:68 and :111 both play the same alias as a one-shot; this was the odd one out and nothing ever issued a matching stoplocalsound. | Trailing 1 removed. |
| `bug-2344` | Self-caught while auditing my own work: the 15 new 'voices from the past' would have played at FIVE PERCENT and the beat would not have existed. | The memory voices play DURING the ramp cinematic, and coop_rampAudioCut has already pulled s_sfxduck to 0.05 by then. S_HZM_CueTier returns 1 for anything under `coop_tinnitus/` - which is why the new shellshock and underwater beds were fin | Added `coop_memory/` to S_HZM_DuckExempt, NOT to S_HZM_CueTier - and the distinction is the whole point. A cue tier ALSO arms a sidechain that ducks music and ambience under the cue (hold 350ms at cueTier > 0), so tier 1 would have had each |
| `bug-2342` | user: 'this is our new shellshock sound effect... the underwater SFX file should also play atop the shellshock file... I also think we should be at the bottom of the ocean for a few seconds' and 'voic | n/a - new content. NOTE the two uploads were 48kHz/24-bit stereo, which this engine will not take; both were converted to mono 22050/16-bit to match the existing shellshock01.wav, and trimmed (78s -> 10s to match the 10s re-trigger gate har | coop_rampUnderwater is now four beats - plunge 0.4s, sink to the bottom 1.8s (ease-OUT, decelerating into the sand), 12s drifting on the seabed, swim up 3.2s (ease-IN) - 17.4s submerged, then the unchanged 0.55s break, which it feeds at exa |
| `bug-2338` | user feature request: 'add explosions out in the sea (needs to be close enough to be visible) that flashes consistently to coincide with the bombs that end up hitting the seawall and bunker areas. tho | n/a - new content. | coop_navalGunfire: a flash out on the water (fx_mortar_higgins, an effectentity with NO radiusdamage - see bug-2244 for why that check matters) then the report 0.5-1.0s later, which is both correct for the distance and the thing that sells  |
| `bug-2313` | user: 'the sound effects of the welders honestly dont need to be so loud either' + 'the fires dont need sound either If they have it' + 'for anyone carrying bodies it makes more sense to carry them ou | THREE separate causes. (1) welding_torch was re-declared for the trilogy with retail's own soundparms, vol 1.0 / minDist 400 / maxDist 5000 - tuned for m2's enclosed sub pen where the geometry stops it, but on an open tidal flat with three  | (1) welding_torch 0.55 / 120 / 1400 - full volume only at the man's shoulder, gone by the next tank trap. minDist was the more important half: at 400 it never faded within the area you fight in. (2) All five fire loopsounds removed - visual |
| `bug-2309` | user: 'I still dont hear the captain telling me to get the bangalores... maybe its that all the rest of the audio is too loud, i dont know, what do you think it is' | THE BEACH RAN OUT OF SOUND CHANNELS, and the user's own log said so: ~6,000 'OpenAL: Couldn't play ND sound' lines plus 175 'Not playing sound m1garandfire1a'. That message is printed when S_OPENAL_PickChannel3D returns -1, which happens on | MAX_SOUNDSYSTEM_CHANNELS_3D 32 -> 96, taking total OpenAL sources from 101 to 165 against OpenAL Soft's 256 default ceiling. Raised rather than rationed, consistent with the entity pool and the effects pool. Noted in the header that source  |
| `bug-2307` | user: 'they are constnatly spamming hes hit, its ridiculous, nonstop' / 'I am stuck with basically the music and dialogue spam which ruins the atmosphere' | MY OWN REGRESSION, introduced by bug-2297. I saw 179 'needs an alias' errors for dfr_attack_* on m3l1a, found 3,654 declarations all gated to maps excluding this one, found 37 generic attack/cover lines shipping unreachable, and declared th | Reverted the 48 attack/cover declarations; kept only the two genuine gaps (10 wading footsteps on a beach-landing map, and snd_ping's missing precache). The generator now carries the reasoning inline so nobody re-adds them. If the variety i |
| `bug-2298` | user, third report: 'I cant hear the captains dialogue still, i see his mouth moving'. Then the user's own correct diagnosis: 'could it be that the dialogue audio that gets ducked for the cinematic sc | The cinematic duck in openal_channel::set_gain exempted MUSIC and standout CUES but never SPEECH: `if (s_sfxduck->value < 1.f && !bMusic && cueTier == 0) gain *= s_sfxduck->value`. coop_m3l1a_beachAudio fades s_sfxduck to 0.03 for a ~76s mu | Dialogue channels (CHAN_DIALOG / CHAN_DIALOG_SECONDARY) now take a FLOOR instead of the full duck, via s_sfxduckVoxFloor (default 1 = speech untouched by the cinematic duck). Same reasoning as the existing whistle exemption: an order is not |
| `bug-2297` | user: 'there were definitely errors related to playsound in the mission I was just playing' + 'I hear allies repeating lines like He's hit over and over again'. Live log: 125x dfr_attack_08h_1, 80x sn | THE MAPS-FIELD LOAD FILTER, sixth occurrence - and the console errors and the VO repetition are ONE bug, not two. dfr_attack_01a_2* has 3,654 declarations across the paks and every single one carries a maps field listing t1/t2/t3/m1l2a/m4l1 | New generator docs/tools/gen_coop_vo_override.py sweeps the pk3s and rewrites a marked block at the END of the mod's ubersound.scr (end-of-file placement is load-bearing: last declaration wins). 58 aliases: 12 numbered variants for each of  |
| `bug-2296` | user, twice: 'I dont hear the captains dialogue' then 'I cant hear the captains dialogue still, i see his mouth moving, either it's being overwritten by everything else or idk what' | Not a mixing problem, and the s_voxDuck sidechain added for the first report was treating the wrong cause. `<actor> say <alias>` drives the lipsync from the .skc/.lip pair and the audio from the sound ALIAS, INDEPENDENTLY - so a broken alia | Re-pointed 015g at the file that ships; declared streamed_ aliases for 030g and 032g against the shipping .wav; declared 109d/110f/111h/082g/035k, five fully-voiced m3l1 lines that ship as audio with .skc and .lip and were never given an al |
| `bug-2280` | console: 'ERROR PlaySound: sledgehammer needs an alias in ubersound.scr or uberdialog.scr' x115 in one playtest | workers.tik's sledge_hammer_action fires `18 sound sledgehammer` as a FRAME COMMAND. Every retail declaration of sledgehammer1..4 is gated to maps 'm2 dm moh obj train' or ' dm moh obj train', and the engine matches that field as a PREFIX o | sledgehammer1..4 re-aliased under the same names with maps 'm e t dm obj train'. More importantly the gate now closes the class: check_map_anims.py records which .tik declared each alias the script uses, scans those .tik files for `sound <n |
| `bug-2277` | docs/tools/check_map_anims.py found a SECOND `playsound explode_tank` that the first hand-fix of that same alias had missed | bug-2248 fixed one occurrence of a map-gated alias by hand and asserted a count of 1. There were two. A hand fix keyed on an exact indentation only ever fixes the sites that match that indentation. | Corrected to arty_exp_sand, and the class is now gated by a tool rather than by care: docs/tools/check_map_anims.py fails the build on any playsound whose alias has no declaration whose maps list prefixes the map. |
| `bug-2271` | user: 'lets ensure music doesnt start at the beginning of omaha specifically. it always tends to play starting tracks from frontline we added' | m3l1a was on coop_flmusic_mapAllowed's quiet-map list. The filler only starts when it believes the music channel is free, and it reads level.coop_musicCommand for that - which replace.scr::tmstart(loop) sets. Omaha's boat-ride ambience is s | m3l1a removed from the allow list. Omaha is not a quiet map and already has a scored moment of its own - m3l1a.scr starts sound/music/AfterTheDrop.mp3 about ten seconds before the ramp drops, with the SFX duck, so that track owns the scene. |
| `bug-2254` | user: 'Weird as soon as I launched I heard a bunch of dead noises instantly' | coop_flankCutDown killed 8 of the 24 flank men between 1.2s and 3.8s after level start. coop_mod/deathvox.scr gates death voices at 1500u (near band) / 3500u (far band), and the flank boats sit ~3100u from the player's start position in hig | The cut-downs now wait for coop_runInShelling (i.e. the run-in to be under way) and then a further 12 + randomfloat 150 seconds, so they spread over ~2.5 minutes. Rate cut from 1-in-3 to 1-in-4. The doomed man also threads coop_flankWalk wh |
| `bug-2248` | pre-ship audit: coop_shermanFire played alias 'explode_tank', which is declared maps "m2l2b " and would never load on m3l1a | The maps field on an alias is a LOAD filter in this engine, not documentation. An alias whose map list does not include the current map simply does not exist there, and playsound on it is silent with no error. Three aliases written from mem | All three corrected against Pak0's ubersound.scr before shipping: arty_exp_sand for the tank report and the bluff shelling ('m e t', 1000/8000), and a randomised arty_exp_water1..3 for the distant water columns. Rule reinforced: verify an a |
| `bug-2235` | FEATURE (user, 'yes to 1-3'): the three ranked survivors of the Omaha chaos workflow - unattended wounded, a boat crew that reacts to the near miss, and cover impacts that stop. | Feature request. Ranked by impact per unit of work by the workflow synthesis, after 26 agents surveyed and adversarially vetted the options. | (1) B5 THE WOUNDED - six unattended casualties in coop_beachVignettes, three per clip, using unarmed_pain_flooridle / unarmed_pain_kneesidle. Both .skc carry header flag 0x20 = ANIM_LOOP, so anim.scr takes its pause branch and they loop wit |
| `bug-2198` | roughly one water impact in four plays a DIRT explosion instead of a water one | ubersound.scr:1640 declared the catch-all alias arty_exp_water pointing at sound/weapons/explo/Exp_Dirt_01.wav, scoped to every m/e/t map, while arty_exp_water1/2/3 (real water wavs) are scoped to m3l1a and to a list of e-maps. Alias lookup | Repointed the catch-all at sound/weapons/explo/exp_water_01.wav rather than renaming it out of the prefix. Renaming was the other option and is wrong here: maps outside m3l1a and the e-list have ONLY the catch-all, so moving it out of the p |
| `bug-2176` | MP voice-command wheel produced no sound, no subtitle, no chat line and no radar blink on every campaign map of all three games | TWO independent inert-makers, and fixing either alone still ships it dead. (1) SCOPE: the 230 wheel aliases (5 voice types x 46 messages) at uberdialog.scr:31116-31419 all carried retail's deathmatch-only map scope. bLoadForMap (scriptmaste | Rescoped all 230 aliases to maps "m e t dm obj train " plus the trailing `always` keyword. `always` is the real load guarantee: RegisterAlias (scriptmaster.cpp:562) and ClientGameCommandManager::Alias (cg_commands.cpp:4396) both test bAlway |
| `bug-2173` | m4l0 was the only campaign map with no _precache.scr - assets registered at runtime instead of during the precache pass | 55 maps/*_precache.scr exist; diffing against the 64 scripts that call coop_mod/main.scr::main left only m4l0 (plus test_bob_foy and the co_lobby maps, which are legitimately exempt). m4l0 is a real campaign map - listed in maplist.scr and  | Generated maps/m4l0_precache.scr: exec coop_mod/precache.scr + exec global/loadout_precache.scr + 80 shared cache lines (the intersection of m4l1/m4l2/m4l3, kept in m4l1 order) + 13 map-specific models derived from map_entities/m4l0_entitie |
| `bug-2161` | FEATURE: hit markers - neutral on hit, red on kill, with a confirmation tick | Ranked #1 by feel-per-effort in the gunfeel plan and never built. Research corrected two assumptions: (1) the 'cut content hitmarker' is only an AUDIO alias pointed at null.wav - a sweep of all 17 retail paks found NO marker art at all, jus | Server sends the notify from Sentient::Damage instead - the same choke point the headshot cue uses, and for the same reason: rank-and-file AI carry the aihandler health buffer, so the player's bullet only wounds them engine-side and the rea |
| `bug-2020` | [user] 'some of these damage noises from me getting shot are of females' - and the investigation found 1,623 audio alias refs that were silent for every player except the dev machine | TWO stacked causes. (1) The morning pain fix filled 16 dead null.wav slots by spreading the pool across SIX voice actors (dfr_damage_01/02/03/04/05/08) - but player_pain voices THE PLAYER, one character, so takes from other actors (some fem | 1,040 unique files (47.0 MB) copied into the mod source tree at the exact alias paths so build.ps1 packs them (repo policy: binaries ship via pk3, not git - zero tracked wavs, deliberate). Pain + death pools rewritten to actor 01 only (11+1 |
| `bug-2001` | Three defects in the weapon-handling foley shipped earlier the same day | (1) MISSING ALIASES: CoopGunFoley("safe") fires on ADS-in for every weapon class, but only coop_gf_rifle_safe01..04 were ever sliced - grep for pistol/smg/mg _safe returns 0. Alias_FindRandom returns NULL and the sound layer then tries to r | (1) Route the classes with no safety take to "hsoft", which every class has. (2) Replace the hash with an explicit if/else table, which cannot drift the way an ad-hoc hash can. (3) Delete the duplicate call. |
| `bug-1863` | flak bomb timer ticking isn't working right its skipping beats or something | coop_churchFlak played the alias `bombtick`, which does not exist. Only `bombtick1` and `bombtick2` do, and the engine random-picks between numbered variants of a pool name. bombtick2 is aliascached maps "m1l2b m6l2a m4l2" so it does NOT lo | Play `bombtick1` explicitly - the variant whose maps list ('m1 m2 m3 m4 m5 m6 train t2l1 t1l3') actually covers m3l3. |
| `bug-1774` | 28 build-mode explosion/smoke emitters placed for the blackout, plus missing explosion sounds | feature work - the user placed the emitters and asked for one-shot blast FX to play with the detonation, smoke/electrical to persist afterwards, and explosive sounds which they did not place. | Split by lifetime: 24 one-shot emitters fired in three waves ~0.3s apart so it reads as a chain, each removed after 8s so 24 entities are not held for the map's life; 4 persistent (2 smoke, 2 electrical fire) spawned and never touched. Soun |
| `bug-1697` | Unknown parameter for coop_alarm_yell alias - the new German alarm shout was rejected outright by the alias parser | The alias was copied byte-for-byte from the retail definition of DENGM_E1L4_GS8607 apart from the name and the map scope, INCLUDING its `forcesubtitle "Alarm! Alarm!"` clause - and the engine rejected it with 'Unknown parameter'. That token | Dropped `forcesubtitle` and its string. The SHOUT is the requirement and the caption was not, so the caption goes rather than risk the alias never registering. Everything else is unchanged: same mp3, same soundparms (160/2100, which is what |
| `bug-1672` | 436 sound files loaded MID-GAMEPLAY in ~2s after spawn, and 695 of 1600 server sound-index slots burned - coop was sitting 15 slots from a hard ERR_DROP | sndcache.scr precached the voice pools through the SERVER: script `cache` -> gi.soundindex() (g_utils.cpp:1604) -> a CS_SOUNDS configstring -> the client registering it in CG_ConfigStringModified (cg_main.c:579). Two costs. (1) It consumed  | Declare the four pools with `aliascache` in ubersound/ instead: 484 coop_deathvox + 135 coop_flvo converted here, gurgle (13) and headshot (1) already done by the hitch audit. aliascache maps to AliasResource + CacheResource, which precache |
| `bug-1556` | User: 'a lot of my sfx seems to be lower than they used to be even with the dial turned up... the typewriter noise when you get a challenge unlocked, mg heat, breathing when sprinting, injured sound' | The SFX-slider gain branch treated CHAN_LOCAL and CHAN_LOCAL_SOUND as menu chrome ('menu / UI - master only'), so they never got the s_sfxvolume multiplier. Almost all coop player-feedback audio rides those channels - Player::PlayLocalSound | CHAN_MENU alone stays master-only; CHAN_LOCAL / CHAN_LOCAL_SOUND now fall through to the s_sfxvolume multiplier, matching the rule already stated in that function's own comment ('SFX = everything that isn't music, dialogue, or ambience'). S |
| `bug-1555` | User: 'weird they only spawned after the flaks blew up' (rear garrison) + 'dont hear the explosion sound either when the bombs go off' | Two separate mistakes. (1) I hooked coop_rear_defenders to coop_flak88_complete, so the rear was empty for the entire fight and only populated once the guns were already down - backwards for 'defend the rear area'. (2) obj_dm.scr only plays | (1) Garrison hook moved to coop_flak88_objective, alongside coop_flak88_defenders, so they are in place while the squad fights toward the guns. (2) coop_flak88_watch now plays explode_flak88 on the gun at detonation - that alias is maps "m  |
| `bug-1548` | User: 'I dont hear the sounds from these flaks firing at the end of this mission' | models/statweapons/flak88turret.tik fires alias flak_snd_fire, but every flak_snd_fire1..4 variant carries maps "m3l2 m5l2 m5l3 t1l3" - m3l1b is in NO flak fire alias's maps spec, so the alias never loads there. Proven from the live log: ze | Added m3l1b to the maps spec of flak_snd_fire1..4 in the m3-series block (ubersound.scr:823-826). |
| `bug-1517` | User: Grillo does not play his go on without me line (M2L1_grillo_dies) when he dies on m2l1 | coop_mod/deathvox.scr::muteNativeVO is threaded generically from aihandler.scr on EVERY AI death to suppress native German/Italian death VO - it stops CHAN_VOICE(4) every frame for ~1s after death. M2L1_grillo_dies ("Powell, go on without m | muteNativeVO now skips stopsound 4 (only) for any entity with self.coop_keepVoiceLine == 1; channels 7/8 still muted unconditionally since story lines do not use those. m2l1.scr sets self.coop_keepVoiceLine = 1 on Grillo at the top of s1_sc |
| `bug-1516` | User: Grillos dialogue cuts off at the end of m1l3c when he arrives in the truck to pick you up | grillo_dialog (fired by a BSP trigger, setthread) used idlesay - fire-and-forget, nothing waits for it. the_end fires exec global/missioncomplete.scr with NO delay (a //wait 2.25 above it is commented out) the instant its own trigger fires, | grillo_dialog now uses say + waittill saydone (blocking) and sets level.coop_m1l3c_grillDone=1 on completion. the_end waits for that flag (bounded to 8s, in case the dialogue trigger never fired for some reason) before calling missioncomple |
| `bug-1509` | User: challenge unlock sound doesn't really pop, not sure if we ever ducked the audio for it | bug-1264 gave the promotion ping s_sfxduck treatment (per-channel, music exempt) but explicitly left the challenge-complete typewriter cue on the unducked mix - it had to fight the full ambient/combat bed instead of sitting on top of it. | chal_typewriter now ducks to 0.25 before the first key-strike and restores in the same stepped/guarded pattern as xp.scr::xp_ping_unduck (0.55 -> 0.8 -> 1.0, NULL-checked at every wait since s_sfxduck is CVAR_ARCHIVE and a mid-sequence disc |
| `bug-1502` | User: 'can we make it fade back to whatever the player's own custom default sound settings are' | m3l1a.scr's cinematic duck and RoomTransform.scr's secret-room reveal both hardcoded restore targets (0.9/0.6 for music/ambient) instead of the player's actual slider value, because the server cannot read a client's live cvar back - stuffte | New CG_UpdateScriptedAudioDucks() in cg_view.c: two independent per-channel (music/ambient) client-side capture-then-restore state machines, driven by whitelisted marker cvars (coop_duck<Channel>Trigger/Target/InDur/OutDur) a map script can |
| `bug-1501` | User: gunshots/menu clicks silent, only music/ambience audible; later: SFX/Music/Ambience sliders have no effect, only Master volume works | m3l1a.scr's Omaha ramp-drop cinematic (coop_m3l1a_beachAudio) fades s_sfxduck 1.0->0.03 for a ~76s music-takeover sequence with ZERO interruption guard (unlike xp.scr's promotion duck, which learned this lesson and NULL-checks at every wait | Added a per-respawn safety-net stufftext (coop_mod/player.scr, alongside the existing s_volume/tinnitus baseline restore) that unconditionally resets s_sfxduck to 1 on every (re)spawn, mirroring the established pattern. Root scripting gap i |
| `bug-1486` | m2l1: 23 plays of a bogus alias literally named 'playsound'; the real line dfr_M2L1_202m never heard | Duplicated playsound token in the exec argument list. The word 'playsound' is consumed as the sound-name parameter and the real alias shifts into the second slot, the dowait flag. | FIXED 2026-08-06, deployed. maps/m2l1.scr:297 - removed the duplicated token; now 'exec coop_mod/replace.scr::playsound dfr_M2L1_202m'. Signature is playsound <sound> <dowait> (coop_mod/replace.scr:937). |
| `bug-1477` | ~3700 dead sound events sampled across the sweep. tank_vehicle_crash alone is 3115 of them (84%) - the single largest content gap in the whole sweep. Remaining ~600 spread across door_wood_locked1(99) | MIXED - the original 'blanket t-series scoping regression' theory does NOT hold uniformly, corrected on re-investigation. THREE distinct causes found: (1) tank_vehicle_crash - genuinely UNDEFINED under that exact literal name; only a differ | PARTIALLY FIXED 2026-08-06, deployed. Added 'aliascache tank_vehicle_crash sound/null.wav ...' scoped to every theater (ubersound.scr, next to the tank_vehicle_crash1 sibling) - resolves the 84% majority (3115/3700+ hits). sound/null.wav is |
| `bug-1398` | User: 'I placed some sound effects on the map, please grab those' - 11 placed emitters would not survive a map reload | coop_mod/save/snd_<map>.dat is WRITE-ONLY by design: coop_snd_place composes paste-ready script text and coop_snd_save writes it, but nothing in the mod ever reads it back. There is no coop_snd_load. So a build session's emitters live only  | Baked all 11 emitters from snd_e3l4_arena.dat into a new arena_spawnSounds label, threaded from main after 'level waittill spawn'. Copied the confirmed-working recipe at maps/co_lobby4.scr:59 (spawnLobby4Objects, itself baked from snd_m1l3c |
| `bug-1325` | limping plays the RUN footstep samples | CG_FootstepMain picks the sample by SURFACE only - snd_step_<surface> IS the run sample; iRunning only scales volume (0.25 walk). The 6 injured anims carried autosteps_walk, so a limper emitted quarter-volume RUN steps + the 30% gear jingle | REVISED same day per user ('just use the walk sound'): autosteps_walk RESTORED on all 6 injured anims - it plays the surface sample at WALK volume (0.25x, cg_specialfx.cpp:677-685), which IS the engine's walk sound; there is no separate sam |
| `bug-1272` | t3l1: no mortar or truck sounds, no allied dialogue, and 47,496 lines of console spam from one alias | PAK PRECEDENCE. The project launches com_target_game 2, so the mount order is main -> mainta -> maintt and Breakthrough content WINS over Spearhead content at the same path. Two consequences on this Spearhead map: (a) models/human/animation | Shipped Spearhead's level_t3l1.tik in the coop pk3 (which sorts last as zzzzzz_ and therefore wins) with a provenance header. Added t3l1 to every alias the engine reported missing, sourcing map lists from the SH paks, and restored six grana |
| `bug-1264` | Challenge completion had no distinct sound; promotion ping was buried in the mix | Challenge completion played coop_ui_click - the same generic click used for menu paging and the Service Record panel - so the moment had no payoff. For promotions, an earlier attempt (xp_ping_duck, removed 07-18) dipped s_volume, which is t | Challenge: added coop_type_key / coop_type_return aliases over retail's own objective-typing wavs (sound/items/mptypekey.wav and Shell_DrumBack_01.wav from Pak3.pk3) with the full 'm e t dm obj train' map list - the upstream objective_text1 |
| `bug-1183` | REGRESSION I INTRODUCED: 'Server disconnected for unknown reason' on m1l1 spawn, twice in a row. Client went CS_PRIMED -> CS_ACTIVE -> CS_ZOMBIE within the same second. The engine's pending-server-com | MAX_SOUNDS 1280 -> 1600 (bug-1180). Every configstring set AFTER the gamestate has been sent is delivered to the client as a RELIABLE COMMAND (SV_SetConfigstring -> SV_SendConfigstring -> SV_SendServerCommand emits `cs <idx> "<text>"`; the  | Reverted MAX_SOUNDS to the known-good 1280 and documented the reliable-command constraint inline so the next attempt does not repeat it. Rebuilt and redeployed openmohaa.exe + game.dll + cgame.dll together. MAX_CONFIGSTRINGS stays at 8192 ( |
| `bug-1161` | m1l1: definitely not hearing the truck driving in the intro (user playtest) | $introtruck2 (the Opel truck the driver/passenger ride in, loopsound'd every intro frame via m1l1.scr:309) plays alias truck2_run, which was `aliascache truck2_run sound/null.wav ...` - a silent placeholder that was apparently never filled  | Repointed truck2_run to sound/vehicle/veh_truck_move.wav (confirmed present in main/Pak3.pk3; same file already used by the coop-added coop_truck_run/truck_run aliases elsewhere), keeping its existing soundparms. NOTE: truck1_papers (also m |
| `bug-1141` | user: lag spikes in gl2 sessions; console shows **S_LoadSound: sound/coop_deathvox/dv_NNN.wav etc at combat moments | Random-pool one-shot voices are NOT precached: first play of each variant does a synchronous disk load+decode mid-frame (S_LoadSound at play time) = hitch on kills/chatter, recurring deep into a session because pools are large. Additionally | QUEUED: precache the voice pools at map load (script cachesound sweep or engine-side register pass at CG init) + fix/fallback the den_death_* aliases. Verify by grepping a post-fix session for zero at-play S_LoadSound lines. |
| `bug-933b` | User challenge exposed my wrong triage: the frontline stingers (sting_objective/wave/end) and 4 coop_amb ambience wavs have NEVER played - 'Couldn't load sound' at every load since shipping 07-13 | All 7 files were STEREO (and the amb set also 48kHz); the engine's OpenAL sound loader only accepts MONO (16-bit PCM, 22050 family) for these alias paths - every known-loading coop sound is mono/22050. Registration failed -> silent forever. | All 7 downmixed to mono 16-bit + the amb set resampled 48000->22050 (linear interp); stereo originals archived in scratchpad (NOT packed); snd pk3 repacked+deployed. VERIFY: next session log should show S_LoadSound sting lines at the office |
| `bug-849` | FEATURE: add a continuous wet blood-leak loop sound on bleeding corpses (retail oil-barrel leak audio), complementing the occasional death-rattle gurgle | Corpses had only the occasional death-rattle gurgle; the user wanted a steady quiet under-body blood trickle matching the visible tier-2 bleed-out drip. Retail oil-barrel leak alias liquid_leak -> sound/mechanics/Mec_WaterLoop_03.wav (a wet | Copied Mec_WaterLoop_03.wav from main/Pak3.pk3 into the mod as sound/coop_gurgle/coop_bloodleak.wav (own it, no retail alias touch). Added TUNED alias coop_bloodleak in ubersound/coop_gurgle.scr (soundparms vol 0.4, minDist 120, maxDist 900 |
| `bug-836` | FEATURE: no wet bleed-out gurgle/death-rattle SFX near fresh corpses - atmospheric gap; user wants a subtle wet gurgle to sometimes play near dead bodies for a few seconds after death, matching the vi | No such sound/system existed. Modeled on the death-voice system (deathvox.scr) but as a separate, quieter, shorter-range, delayed, occasional positional layer riding the corpse bleed-out window. | Added ubersound/coop_gurgle.scr (12 aliases coop_gurgle1..12, soundparms vol 0.55 / minDist150 / maxDist1200 / auto loaded) + sound/coop_gurgle/cg_gurgle1..12.wav (12 curated wet death-rattle SFX, 22050 mono s16, darkest/quietest/lowest-fre |
| `bug-601` | Frontline audio STILL static after the EA-XA v1 'fix' (user ear-test on validation clip) | The codec is EA-R1 (the variant ffmpeg's demuxer auto-picked originally - dismissed because the DEMUXER errored on the PS3 BE fields, not the codec): every SCDl channel block begins with TWO LITTLE-ENDIAN s16 SEED samples (LE seeds inside B | decode_eaxa_r1_channel + decode_schl_song_r1(be=False for seeds): flatness 0.0014 (vs 0.13/0.29), centroid 1324Hz, UNCLIPPED peak 17952 - every wrong decode slams 32767, clipping is itself a wrongness signal. All 514 re-decoded again, sting |
| `bug-600` | The 'fixed' Frontline audio (ffmpeg forced adpcm_ea_xas) was ALSO garbled - spectral centroid ~3.9kHz / flatness 0.29 = broadband mush, shipped in the first score-layer build + audition kit | The codec was never XAS: PS3 Frontline SCHl streams are EA-XA v1 (15-byte/28-sample frames) in SCDl blocks with BIG-ENDIAN fields (nsamples + per-channel offsets). ffmpeg's ea demuxer reads the PC little-endian layout, so EVERY codec choice | Custom pure-python EA-XA v1 decoder (frontline_eaxa.py, algorithm from ffmpeg ADPCM_EA verbatim + persistent per-channel history, desync guard on coef>3). Validated: full 281s duration (XAS decode had silently lost 36s), spectral flatness 0 |
| `bug-599` | Entire Frontline audio extraction was DIGITAL SILENCE (valid headers/durations, zero samples) | vgmstream misidentifies the PS3 HD remaster's EA SCHl streams (labels them EA-XA v1) and decodes zeros; the actual codec is EA-XAS. The size>1000 'OK' check passed because WAV headers+silence have full length - never validated CONTENT. | Decode with ffmpeg: `ffmpeg -c:a adpcm_ea_xas -f ea -i <split>.asf out.wav` (forced codec; the ea demuxer alone guesses adpcm_ea_r1 and fails). Boundary 'invalid packet' warnings are harmless - verified 0 mid-track silence gaps. LESSON: alw |
| `bug-156` | User reports no audio at launch; suspected the console 'stop all sounds' line and 'rate=0 width=0' as the cause | NOT a mod bug. Engine side healthy: s_volume 0.9, OpenAL device opened, music 'playing music/m3l2.mus'. OpenAL opened {default} = 'SteelSeries Sonar - Gaming (Virtual Audio Device)'. A Sonar virtual device only outputs if the Sonar app is r | Device routing, not code: ensure SteelSeries Sonar is running + Arctis on, OR set Windows default playback device to a real device, OR force a physical device via the latched cvar s_openaldevice (e.g. seta s_openaldevice "OpenAL Soft on Hea |
| `bug-153` | User reports no audio at launch; suspected console 'stop all sounds' and 'rate=0 width=0' as the cause | NOT a mod bug. Engine side healthy: s_volume 0.9, OpenAL device opened, music 'playing music/m3l2.mus'. OpenAL opened {default} = 'SteelSeries Sonar - Gaming (Virtual Audio Device)'. A Sonar virtual device only produces sound if the Sonar a | Device routing, not code: ensure SteelSeries Sonar is running + Arctis on, OR set Windows default playback device to a real device, OR force the engine to a physical device with the latched cvar s_openaldevice (e.g. seta s_openaldevice "Ope |
| `bug-116` | t1l2 flak88 cannon fires (anim plays) but produces no fire/reload sound | flak_snd_fire*/flak88_fire*/flak88_reload aliases were map-gated to 'm3l2 m5l2 m5l3' (+ e-series copies) which excludes t1l2, and no bare flak_snd_reload existed at all; the gun 'fire' anim (sound flak_snd_fire) and gag 'sound flak_snd_relo | Added t1l2-gated copies of flak_snd_fire1-4, flak88_fire1-4, flak_snd_reload, flak_snd_reload1, flak88_reload pointing at wavs verified present in main/Pak3 (Flak88Fire1/3/4, TankCannonFire1) + mainta (t34_reload) |
