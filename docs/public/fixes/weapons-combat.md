<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# Weapons & combat

[<- back to all fixes](../BUGFIXES.md)

**66 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-1716` | spawned officer had a head and arms but no body | Spawned with gun "Walther P38". Our officer TIKs comment out the top-level body skelmodel and load it INSIDE each `case weapon` block while hands and head load unconditionally. The cases spell it "Walter P38" - one t, retail's own misspelli | Use "Walter P38" exactly as the TIK and BSP entity data spell it. |
| `bug-1706` | spawned with m1 garand, fired a shot, then it turned into a silenced pistol with the m1 garand skin over top of it | coop_silencedDrawn was implemented as a SECOND spawnWeaponAssert thread (from the deferred coop_giveSilencedExtra) racing the spawn path own assert aimed at the loadout primary. Two asserts fighting; the loser left the client viewmodel desy | Give the silenced pistol in the spawn block before the desired-weapon choice and set local.desired to it, so there is one assert and one raise. Guarded the last-weapon branch with desired == NIL. Deferred helper now early-exits on coop_sile |
| `bug-1621` | ^~^~^ Script Error : unknown animation 'MG42_crouch_idle' / 'MG42_crouch_shoot' in 'models/human/german_wehrmact_grenadier.tik' - repeated during the 11:09-11:14 m2l2b session. | CONFIRMED (wf_9aeb9ad5): per-map TIKI includes gating, not per-weapon cases and not a regression. new_generic_human.tik pulls human_mg42.tik ONLY inside per-map 'includes <mapname...>' blocks, resolved at TIKI load by case-insensitive prefi | added '$include models/human/animation/human_mg42.tik' to the mod's UNCONDITIONAL include set in new_generic_human.tik (the established mp44/bar/bazooka/thompson recipe, ~30 aliases); stale 4095/12-bit comment refreshed (cap is now 8192/13- |
| `bug-1581` | User: 'I see the flaks in build mode, but these arent the anti aircraft flaks i am thinking of from the breakthrough campaign (E.G. the glider drop mission).' | Two separate things, both real. (1) DEFECT: flak_emplacement spawned models/statweapons/flak88turret.tik ALONE at ground level. That tik is only the upper half of the AA-campaign 88 - retail always pairs it with flak88base.tik (both maps th | (1) flak_emplacement now spawns flak88base.tik at ground and the turret at ground + 56*scale, registering both as one 2-piece structure so undo still removes it as a unit. (2) Added two new STRUCTURES (COMPOSED) items, 7 aagun_emplacement a |
| `bug-1572` | placed MG42 nest gunner mounted but never fired | A mounted gunner HOLDS the gun but nothing drives it. weapturret.cpp AI_ThinkActive gates firing on owner->m_Enemy and the 'startfiring' that begins a burst comes from global/mg42_active.scr - the driver every scripted nest uses (45 invocat | Turret and gunner now carry targetnames in the '<turret>' / '<turret>_gunner' convention that mg42_active binds on (self.gunner = $(self.targetname + "_gunner")), and the nest threads global/mg42_active.scr::mg42 on the GUN once both exist. |
| `bug-1553` | Script Error x100s: "Failed execution of command 'startFiring'/'stopFiring'/'setAimTarget' for class 'Animate' Targetname '88mm_weapon1'" | m3l1b's $88mm_weapon1/2 are class **Animate**, not TurretGun - so the entire turret API is unavailable on them. The pre-existing startFiring/stopfiring pair had therefore NEVER worked, and my first fix (spawn a script_origin + setaimtarget, | Dropped the turret API entirely. flak88turret.tik declares `fire_scripted 88turret_fire.skc`, and that animation carries the frame command `first sound flak_snd_fire` - so `local.gun anim fire_scripted` produces the recoil AND the report in |
| `bug-1551` | 'COOP_BINOC_CHECK: player fired weapon, weapongroup=...' printed on EVERY shot | Debug gi.Printf left in Weapon::Fire with no cvar gate at all, so it cannot be turned off at runtime. gi.Printf is Com_Printf - it prints regardless of `developer`. This shipped inside game.dll in release v1.2.2, meaning every player gets o | Deleted COOP_BINOC_CHECK and the COOP_BINOC_FIRE print; the binocular trace/ExecuteThread logic is untouched. game.dll rebuilt. Needs a redeploy and should go out as a 1.2.3 hotfix. |
| `bug-1481` | t2l2: turret nest script threaded onto a PathNode - 7x Failed execution of command for class 'PathNode' | Two MG42 handlers claim the same nest. global/turret.scr::main:15-17 grabs every entity whose targetname is literally 'mg42' and threads mg42_start on it; on t2l2 that entity is a spawner MARKER (info_pathnode), not an Actor. This answers t | FIXED 2026-08-06, deployed. global/turret.scr::mg42_start now guards against threading gunner_think onto a non-Actor target (classname != 'Actor'), with a diagnostic print instead of the error spam. Root cause traced fully: t2l2's nest targ |
| `bug-1435` | Ride audit: e3l4 convoy - one gunner death left the .30cal empty AND locked for the rest of the intro ride | coop_playerJustRespawned was an empty stub; the engine auto-detach on death vacated the slot behind a locked turret and level.coop_e3l4JeepGunner went stale (unguarded dismount warp). | Stub filled: gated on level.playeronjeep==1, vacant-gun re-seat using Bunker1's own mount recipe, gunner var refreshed. |
| `bug-1429` | User: 'if I die on the jeep i respawn back at the start and cant move... im now soft locked' (e3l1 medic jeep ride) | The medic jeep ride (BritHQ.scr::StartPlayerMedicRide) seats one player on the .30cal and sets level.playerInJeep, but has ZERO death handling: a death respawns the player at the pinned start spawn while the ride's replace.scr::physics_off  | maps/e3l1.scr coop_playerJustRespawned now ride-aware: physics_on/takedamage/immune-vehicle the respawner, re-seat them on the .30cal if the gunner slot is vacant (attachturretslot 0 + COOP_TURRET_MAN_F, the m1l3b/bug-309 recipe), else glue |
| `bug-1420` | User: 'Id rather us go with the s93 bar bipod, the model in general looks better' | Feature: the S93 pack shipped a complete BAR (bar_lmg skeleton, s93_bar + s93_bar-bipod textures, bar/fire_bar/reload_bar skc anims) that NO tik referenced - fully orphaned assets discovered while answering whether the BAR used the S93 skin | Swapped our live bar.tik setup{} to the S93 model: path bar_lmg, 'surface all shader s93_bar' + 'surface BAR-bipod shader s93_bar-bipod' (the pack's own audited bare-name convention), reload clip-hide frames moved from stock bar4 to the S93 |
| `bug-1370` | user: 'it works all the way to the jeep dropping me off, i get dropped off and the jeep drives off, but im stuck standing here' - e3l4 Bunker2, third iteration of the dismount defect. | The exit position was an INVENTED offset rather than an authored one. v1 was ( origin + (0 0 80) ) = the jeep's roof (bug-1367). v2 was a 96-unit lateral offset - a guess about where free space is, and at the Bunker2 stop it drops the playe | Exit on the vehicle's own authored dismount tag: local.jexit = $startjeep gettagposition "passenger_enter" (+8 z), with the lateral offset kept only as an unreachable fallback. That tag is placed by the artist for entering/leaving this exac |
| `bug-1367` | user: 'I get detached from the jeep, and now im stuck standing on it, not sure if the jeep is supposed to take off or not' then 'im soft locked for real now in this area where im stuck standing on top | PlayerGetOffJeepTurret dismounted with 'detachturretslot 0 ( $startjeep.origin + (0 0 80) )' - 80 units straight UP, i.e. onto the jeep's roof. It was copied from maps/t2l2.scr:1468, where it is harmless because that ride is OVER and the pl | Exit BESIDE the vehicle instead: local.jexit = $startjeep.origin - ( 96 * ( angles_toleft $startjeep.angles ) ) + (0 0 24), passed to detachturretslot AND assigned to the gunner's origin afterwards so the dismount cannot leave them parented |
| `bug-1293` | [user] 'I seemed to have lost all of my ammo when I triggered dbno and revived myself' | bug-913 recurring through a window its own fix did not cover. The ammo snapshot loop (itemhandler::coop_ammoSnapshot) ticks every 0.5s and is gated only on coop_dbno_active and coop_ammoRestorePending. bug-913 added that Pending flag so a s | Raise coop_ammoRestorePending BEFORE the takeall in BOTH revive paths (medkit self-revive and dbno_ai_revive), so the zeroed pools can never be snapshotted. That transfers ownership of the flag out of coop_ammoRestore, which exposed a secon |
| `bug-1258` | Player ejected from the jeep .30cal at e3l4 map start; lobby leaks g_inactivespectate 0 and g_forcerespawn 1 into the mission | (a) Bunker1.scr used doUse to seat players. Vehicle::DriverUse (fgame/vehicle.cpp) gates its slot switch on g_gametype->integer == GT_SINGLE_PLAYER or min_type == 2, so in coop only the DRIVER slot is honoured; turret and passenger are skip | (a) Replaced doUse with the shipped m1l3b recipe (unlock, attachturretslot 0, perferredweapon, lock, forcelegsstate COOP_TURRET_MAN_F), seating a single gunner; dismount now uses detachturretslot with an explicit exit vector, never vec_zero |
| `bug-1208` | User: 'my helmet does not show in third person' (live helmet switcher, coop_mod/helmet.scr). | TWO independent faults; everything else in the chain was verified CLEAN first (do not re-investigate): the server DOES send the client its own attached children (SV_BuildClientSnapshot adds the own client via SV_AddEntToSnapshot, which sets | (1) cg_modelanim.c: hoisted !bThirdPerson out to gate the whole condition - `&& !bThirdPerson && (!cg_drawviewmodel->integer \|\| cg.snap->ps.stats[STAT_INZOOM])`. Bit-IDENTICAL in first person (!bThirdPerson is already true there) so the v |
| `bug-1202` | German AI shout a wounded-begging line when a grenade lands instead of a grenade warning | den_fear_08e is the engine's 'incoming grenade!' alias (den_head_grenade_?02 in AAGeneric/BTGeneric_dialogue_german*.tik, fired by Actor Anim_Say -> anim/say_grenade_sighted.scr). Retail AA points it at den_suicide_11b/11d/12a/12b/13a/14a/1 | Repointed den_fear_08e to the Grn_001a/b/g + Grn_007a/b/e/g grenade reel (plus den_fear_08a) in BOTH the mod's uberdialog.scr (m dm obj) and the mod's ubersound.scr aliascache block (m e t). Also aliascached den_suicide_03c (the other 'gren |
| `bug-1053` | m5l1a/b storming 4006/3415 weather-blend errors that NONE-ERR metric (cast-none only) reported as 0 | (1) metric only counted "Cannot cast none", missing "binary applied to incompatible types none/float" (weather blend), "applied to NIL" (turret), "Waypoint to entity" (playerCansee). (2) weather.scr transition blend (weatherpattern:) + weat | weather.scr: default windtime/shuttertime/raindensity/rainvolume/thundertime if NIL at all 3 loop threads. turret.scr: NIL-guard spawner_activate return + pass local.ent[i].origin to playerCansee (cansee accepts vector). vehicles: !(playert |
| `bug-1026` | LIVE-BOOT AUDIT: t2l2 (audit-graded A- clean) throws 265 script errors on coop boot: 36x Couldn't load models/nil.tik, missing models/vehicles/panzerwerfer42.tik x4, 'must specify explicit classname f | TBD (investigate): addon MG42 nest gunner AI + panzerwerfer rocket-arty vehicle fail to resolve models -> unmanned nests, missing vehicle. Map still reaches coop-ready. | OPEN - needs investigation. Map is degraded not dead. |
| `bug-942` | e1l2 invisible walls near the gun emplacements persisted after mine WEAPONCLIP fix; one-way blocking, jumpable, in open plateau areas | addon_turretweapon_german_AXW15cmCannon (artillery1/2/3) spawns via TIK-init fallback (unknown classname -> statweapons/AX_W_15cm.tik 'classname FixedTurret'); the TIK ships setsize (-80 -80 0)(80 80 80) and that 160x160x80 box stays SOLID. | InitGun (runs for artillery1/2/3): self setsize ( -48 -48 0 ) ( 48 48 80 ). Trace-scan verified: 56 entity-blocked grid segments -> 20 (= the honest mount box). Persists through destroyed-model swap; gun stays shootable/plantable. |
| `bug-938` | THE REAL invisible walls (user: low, jumpable, in open areas/paths, absent in retail SP video; NOT the placed objects - model audit clean; NOT playerclip - the noPlayerClip experiment failed and let p | BSP-verified: e1l2 has 33 engine trigger_landmine entities, ALL damageable=1, scattered across open ground incl the exact gun-emplacement coords the user kept hitting. SetDamageable(true) -> setContentsSolid = every armed mine is an invisib | SetDamageable(true) now sets CONTENTS_WEAPONCLIP instead of SOLID: still in MASK_SHOT (shooting mines to clear works), player/AI movement passes through, stepping onto a wired mine still detonates via trigger touch. coop_noPlayerClip revert |
| `bug-913` | DBNO/self-revive re-gives hand back weapons with DEFAULT ammo, not the reserve the player had when downed | coop_ammoRestore (exact-ammo respawn, bug from 07-09 system) was only called on the player.scr respawn path; the two revive-in-place chains never invoked it. Also a race: the 0.5s snapshot loop resumes at revive (health restored, dbno_activ | Both revive chains now thread coop_ammoRestore after their weapon re-give (same convention as player.scr:970). Added coop_ammoRestorePending flag: restore raises it before its wait, snapshot loop skips while set, cleared on every restore ex |
| `bug-897` | After a DBNO revive/respawn on e1l2 the player's SECOND primary (Thompson 50rd, models/weapons/thompsonsmg.tik, picked up in-level as a 2nd primary) was lost; only the original loadout came back. | When a player has armory picks (coop_loWeapons), managePlayerInventory (itemhandler.scr:745-748) FORCES useDefault=true, so spawnInventory (1271) reads coop_loWeapons ONLY and ignores coop_inventoryWeapons. A 2nd primary picked up in-level  | giveWeaponLoadout now deep-copies coop_inventoryWeapons into a fresh group.loCarry array BEFORE resetPlayerWeapons nils it (picks path only). spawnInventory's picks branch calls the new mergeCarryWeapons(picks, carry) helper, which returns  |
| `bug-863` | Bake tonight's (2026-07-18 21:xx) STANDING-ONLY ADS tuning pass into s_adsGunTune[] per-gun table | User dialled ADS iron-sight STAND offsets for 21 guns in-game via adssave; values only live in qconsole.log until baked into the s_adsGunTune[] table. Correlation trap: each adssave belongs to the gun equipped AT that timestamp (nearest pre | Added 19 NEW rows (STAND tuned, CROUCH = inherited default the adssave printed since this pass was standing-only): Silenced Colt .45, Silenced Walther P38, Silenced TT-33, Silenced Beretta, Silenced Luger P08, Walther PPK, Luger P08, Nambu  |
| `bug-802` | User: the weapon change at the beginning of a new level (armory loadout replacing the map default) must be INSTANTANEOUS at spawn - the player visibly spawned holding the map-default weapon and then s | TWO stacked delays. (1) HOST pick availability: the host's picks only reached the server via the t+4s/t+15s name-bus resend volleys, so the first spawnInventory ran with empty coop_loWeapons flags -> map default kit given -> loadout_regiveW | (1) loadoutpick.scr::loadout_hostSeed (new): host-gated (bug-715 rule - coop_lo* are server-global = the host client's archive), launched from player.scr::manageSetup right after host detection; replays getcvar coop_lo1..4 through the norma |
| `bug-800` | User: the weapon change at the beginning of a new level (armory loadout replacing the map default) must be INSTANTANEOUS at spawn - the player visibly spawned holding the map-default weapon and then s | TWO stacked delays. (1) HOST pick availability: the host's picks only reached the server via the t+4s/t+15s name-bus resend volleys, so the first spawnInventory ran with empty coop_loWeapons flags -> map default kit given -> loadout_regiveW | (1) loadoutpick.scr::loadout_hostSeed (new): host-gated (bug-715 rule - coop_lo* are server-global = the host client's archive), launched from player.scr::manageSetup right after host detection; replays getcvar coop_lo1..4 through the norma |
| `bug-648` | scoped G43 has terrible rate of fire, should fire like a semi auto rifle | S93-imported g43sniper.tik shipped with firedelay 0.4 / dmfiredelay 0.5 (120rpm cap in coop DM) - sniper-ish balance numbers, not semi-auto cadence. Engine gate confirmed: Weapon::ReadyToFire is purely m_fLastFireTime + fire_delay (BT proto | Created mod override hzm-mohaa-coop-mod/models/weapons/g43sniper.tik (packs into zzzzzz_co-op_hzm_mod_assets_tex.pk3 which alphabetically loads after zzzzz_xw_weapons.pk3 and wins - same ordering rule as bug-494): firedelay 0.4->0.15, dmfir |
| `bug-623` | Player intermittently holds guns BACKWARDS; suicide temporarily fixes it ('out of nowhere im holding my guns backwards ... killed myself and it fixed it ... now its broken again') | Weapons-on-back pass (bug-619 UpdateCoopHolsteredWeapons) can re-holster a weapon that was detached while ALREADY holstered - its angles are the holster rotation (0/185/-25). AttachGun's holstering branch UNCONDITIONALLY saved this->angles  | AttachGun holstering branch now saves lastAngles ONLY when !lastValid (never overwrites a pending restore state). game.dll rebuilt. |
| `bug-619` | Weapons-on-back only visible until a long gun is raised ('weapons dont seem to stay on your back. I noticed the first time') | Retail's ActivateWeapon clears the ENTIRE holster tag when raising a weapon (reserves the spot for its own future putaway) and weapons given-but-never-drawn are never holstered - so the back emptied whenever a long gun was in hand and staye | New Sentient::UpdateCoopHolsteredWeapons() pass (one inactive carried weapon per free holster tag, occupancy-checked) called at the end of ActivateWeapon/DeactivateWeapon and from ReceivedItem/RemovedItem - whatever you are NOT holding is n |
| `bug-618` | Player lost their armory-picked pistol after a DBNO revive ('ive somehow lost my pistol after the weapons were given') | The DBNO revive re-give chain was: registered coop_inventoryWeapons -> level.coop_weaponLoadout (map default) -> garand/colt failsafe. The ARMORY PICKS tier (flags[coop_loWeapons]) was missing. An armory regive NILs coop_inventoryWeapons be | Inserted the picks tier into the dbno revive chain between registered inventory and map default (mirrors managePlayerInventory priority, gated on coop_loadoutLocked). |
| `bug-616` | Holstered/stowed weapons invisible on all characters (players and AI) - weapons vanish on switch instead of riding the back/hip | Retail shipped the complete visible-holster system (Sentient::DeactivateWeapon calls AttachToHolster on every weapon switch, ActivateWeapon un-holsters, mike_torso.st fires deactivateweapon for players, per-gun holstertag/offset/angles in 7 | Uncommented the 4 holster lines in the mod's 27 overridden weapon TIKs, added class-template blocks (kar98/p38 values) to 10 more (G43/SVT/Carcano x2/Moschetto/Breda/L42A1/PIAT/Vickers/Beretta), uncommented in all 32 xw-pack TIKs (backup zz |
| `bug-586` | Armory loadout generator: all vanilla guns parsed with no stats (bars stuck at 5/5/5/5/5); grenade ammo topup targeted nonexistent pools | Two tik-format traps: (1) vanilla weapon tiks spell stat overrides as SEPARATE tokens 'dm bulletdamage 24' while the xw pack merges them 'dmbulletdamage' - regex only handled the merged spelling; (2) grenade tiks hide ammotype behind $inclu | fnum/fspread/fammo try 'dm <key>' then 'dm<key>' then plain then 'sp <key>'; grenade roster ammo pools hardcoded (frags=grenade, smokes=smokegrenade); getAmmoType dedup classes: mills=agrenade (mirrors m2frag), nebel/m18=smokegrenade (own c |
| `bug-500` | Swept every mod in the dev dir for weapons coop lacked; 13 net-new guns were unimported (11 S93 silenced variants + Lee-Enfield sniper + M1 Carbine). | 'Extra WW2 Weapons'=='S93 pack' (S93=author tag SOARES93). The original import chose 18 guns but pulled in ALL net-new S93 files (models/textures/shaders/sounds) - so the 12 skipped silenced/sniper weapons' ASSETS were already in the xw pk3 | scratchpad/import13.py: added the 12 S93 tiks (names fixed e.g. 'Silenced MP40', globally-unique orders 112-116/212-216/313-316) + full-imported the M1 Carbine (carbine.skd+anims+hobbscarbine textures+carbine.shader, order 217). Registered  |
| `bug-495` | FG42 invisible + fires single shot only. | Vanilla FG42.tik is unfinished dev scrap (its own tik ends '// this weapon is probably going to be scrapped'): built from Springfield internals - `semiauto` + clipsize 5 (single-shot), every animation (idle/reload/fire/rechamber) reuses one | Removed FG42 from giveall.scr and from the weapon challenge families (gen_weapons.py) - rifle families 10->9. Regenerated challenges.scr + the disconnected menu. Can be re-added if a proper model+anim set is imported. |
| `bug-494` | ~32 imported weapons (all 13 KUN + 18 S93/xw + It_W_Carcano) given by 'give all' but NOT selectable in the weapon wheel - player scrolled through only 39 of 71 guns. | Each imported weapon inherited its vanilla parent's `rank <order> <power>` ORDER value (first number). The client weapon-select wheel keys ONE entry per GLOBAL order slot (not per-weapontype: type100smg at order 130 collided with webley the | scratchpad/fix_ranks.py rewrote the first rank number of all 31 imports + 2 vanilla to globally-unique orders (pistols 162-176, rifles 211-286, smg 312-326, mg 412-432, heavy 612-642), repacked the xw/kun pk3s, edited carcano->236/breda->41 |
| `bug-484` | give-all only yields ONE gun per ammo type - the 18 S93 guns, extra rifles/pistols, and smoke variants never appear | THE overarching give-all cap (separate from the earlier anim/texture/cap fixes). coop_mod/itemhandler.scr::addWeaponToInventory DEDUPES held weapons by ammo type ('we cant have two weapons of the same ammo type') and its gate notStartGrenad | give_all_weapons now saves level.coop_itemHandler, sets it 0 for the whole give (self give all + a 0.5s settle so every per-item handler sees itemHandler=0 and bails BEFORE the dedup/delete), then restores it. Restored all 3 national smoke  |
| `bug-483` | S93 imported guns not showing up in give-all (e.g. greasegun.tik 'Failed to load animation models/weapons/greasegun/mp40.skc') | Same class as the KUN bug (482): most S93 weapons borrow their animations from a vanilla weapon (arisaka<-kar98, nambu<-p38, type100<-highstandard, greasegun<-mp40, g43sniper<-g43, mp44scoped<-mp44, springfield_unscoped<-springfield/kar98,  | scratchpad/gen_xw_anims.py: indexed skc/skd across all paks by basename (preferring models/weapons/ paths), and for every S93 tik copied each referenced anim not already present into the weapon's folder. 20 anims recovered, verified ALL 18  |
| `bug-482` | give-all hotkey not giving the new (KUN) guns; log: 'TIKI_InitTiki: Failed to load animation .../garand_reload.skc' -> 'No valid animations found in models/weapons/bazooka_kun.tik' -> 'Could not spawn | The KUN weapon import (gen_kun.py) relocated each weapon's model folder to models/weapons/<gun>_kun/ but copied ONLY the files present in the KUN pack's folder (the HD mesh .skd + a single .skc). The tiks reference reload/fire/empty animati | scratchpad/gen_kun_anims.py: indexed all .skc/.skd across the vanilla paks (case-insensitive - the tik path 'm1_Garand' vs pak 'm1_garand' tripped a case-sensitive extract), and for each _kun tik copied every referenced .skc that wasn't alr |
| `bug-464` | All-weapons debug (give all) missing the new guns + some trilogy guns, in SP and MP | Two issues: (1) FG42.tik was never in giveall.scr / give_next_weapon at all. (2) MAX_WEAPONS=64 (q_shared.h) caps the weapon configstring/itemindex pool; give-all's ~49 guns PLUS the map's own AI/pickup weapons can exceed 64, so the tail (t | Added FG42 to giveall.scr + give_next_weapon rifle list. The tail-drop needs MAX_WEAPONS bumped (64->~96) - a full engine rebuild (exe+cgame+game) + friend version bump - RECOMMENDED, awaiting user OK (not done this turn). |
| `bug-459` | Service Record per-category URC: BACK button overlapped the CAMPAIGN category tab | 5 category tabs at width 118 starting x=14 stride 124 put the last tab at x=510..628; BACK at x=566..626 overlapped it in the 640px menu. | Reflowed the button row to 6 slots of width 99: tabs at x=8+j*103 (8,111,214,317,420), BACK at x=523..622, all clearing 640. Regenerated all 5 pages. |
| `bug-458` | bash heredoc `python - <<'PY'` failed: unexpected EOF while looking for matching quote | Python string content passed through a quoted heredoc contained apostrophes/quotes that the shell still tried to balance; challenge titles like "One Shot, One Kraut" and "Officer's Sidearm" broke the heredoc parse. | Stopped piping Python via heredoc. Wrote the generator to a real .py file with the Write tool and ran `python gen_sr.py`. Rule: never feed non-trivial Python through a bash heredoc; write to a file first. |
| `bug-378` | Coop lobby: player spawns holding rifle at his side instead of at parade rest (holster reported OK but rifle stayed in hands; self.gun NIL throughout). | lobbyPoseAndFreeze called replace.scr::holster, which maps to Player::HolsterToggle (player.cpp:8354) - a TOGGLE keyed on WeaponsOut(). The F3 instant-deploy uses a forced 'respawn' that skips weapon selection (no dm_primary via UserSelectW | Replaced the toggle 'holster' call with 'self safeholster 1' (EV_Player_SafeHolster -> Sentient::SafeHolster, sentient_combat.cpp:616): ensure-holstered, state-preserving - puts away if out, no-ops if already away, never draws on a fresh sp |
| `bug-369` | co_lobby1: host still has to CLICK to spawn - auto-deploy never fires. | lobbyAutoSpawnLoop latched coop_lobbyJoined=1 BEFORE confirming the deploy succeeded, and never retried. If the first skipTeamAndWeaponSelect missed (host at entnum 0 / weapon-menu re-push race), the host stayed spectator forever. | Removed the one-shot latch; retry skipTeamAndWeaponSelect every 0.5s scan while dmteam=='spectator' (self-healing). (Phase C plan agent.) |
| `bug-322` | .30cal gunner body faces mount-time direction (not the gun) and stands inside the gun | UpdateRemoteControl: pedestal turrets without a tag_seat hit the bare else branch - pOwner->setOrigin(origin) at the TURRET origin, angles never set, so the body kept whatever yaw it had when mounting. | No-tag_seat + player branch now faces the body with the aim yaw (m_vUserViewAng[1]) every frame and parks it coop_vehTurretBack (ARCHIVE, def 14) units behind the gun along that yaw. Pairs with the MG42_stand manning anims (r8). |
| `bug-315` | No fire sound for the gunner when shooting the .30cal in third person | the bug-301 viewmodel skip EARLY-RETURNED out of CG_ModelAnim - that also skipped the viewmodel's client TIKI frame commands, which carry the owner's FIRE SOUND (vanilla owners always hear the gun via the viewmodel entity, not the world gun | draw-only skip: bCoopHideDraw flag gates the R_AddRefEntityToScene + shadow calls (the same seam RF_DONTDRAW uses) while commands/sounds keep processing |
| `bug-310` | Right-corner blind-fire: gun stays glued to the RIGHT hand while the anim shoots with a left-hand pose - rifle points at the ceiling while firing | mp44_wall_blindfire_right.skc poses the LEFT hand as the shooting hand around a right corner, but MOHAA weapons attach to tag_weapon_right permanently - the gun follows the flailing right hand skyward | QUEUED (1-line candidate): right-corner row -> reuse the OVERHEAD crate spray (coop_blindfire_low: right-handed, side-neutral, has fire frames) until/unless a right-handed right-corner anim is found |
| `bug-309` | Jeep .30cal manning pose STILL not playing (screenshot: world gun visible OK, weapon holstered on back, plain arms-hang standing idle) | SUSPECTED (unverified): the VEHICLE_TURRET_* / TURRET_* states are never ENTERED - vanilla entry edges gate on 'IS_USING_VEHICLE IS_USING_TURRET !HAS_WEAPON' and the jeep gunner's flag combo does not satisfy either row (m_pVehicle vs m_pTur | QUEUED: instrument the live legs state on a turret (temp gi.Printf on state change or g_showstate equivalent), then fix the entry edges (or force the state from EnterTurret/TurretMove like forcelegsstate does). Do NOT guess condition semant |
| `bug-305` | Wall blind-fire sprayed bullets straight into the wall the character leans on (screenshot: decals at own shoulder); no side-awareness; no way to aim from cover | blindfire fired along raw view basis from the in-pose muzzle (rounds clip the wall plane); only a LEFT around-corner anim existed and nothing detected which side the corner was on; RMB was wired as cover-exit not aim | COVER v2: (1) anchored sustain - m_vCoopCoverNormal stored at entry, sustain traces vs the anchor (view-independent); (2) side probe - sideways step + re-trace toward wall -> m_iCoopCoverSide (left ties win), drives COOP_COVER_OPENRIGHT sta |
| `bug-304` | Jeep .30cal 3P: (a) player just stands in unarmed_idle while firing ('bullets from his eyes'), (b) the world gun is INVISIBLE to the gunner | (a) vanilla VEHICLE_TURRET_USING/TURRET_USING legs = unarmed_idle (nobody ever saw a 3P vehicle gunner); (b) vanilla filters the WORLD gun from the manning client entirely (SVF_NOTSINGLECLIENT + singleClient=owner, weapturret.cpp P_CreateVi | (a) new aliases coop_turret_stand_idle/_fire -> weapon_mg42/MG42_high_idle\|firing.skc (standing hands-on-grips; binary append) + both turret legs states use them, fire row keyed on ATTACK_PRIMARY_BUTTON; (b) u_view3p CVAR_USERINFO mirror ( |
| `bug-301` | Jeep .30cal in 3P: the gun model renders skewered through the player's head; jeep mount looks empty | TurretGun::P_CreateViewModel (weapturret.cpp:1836) spawns <gun>_viewmodel.tik attached to the OWNER'S 'eyes bone' with RF_ALWAYSDRAW - the classic 1P turret gun overlay. In 3P, local-player attachments get glued to the VISIBLE body (cg_mode | cg_modelanim.c: before the attach branch, skip drawing entirely when (parent == local client && bThirdPerson && TIKI name contains '_viewmodel'). 1P keeps the overlay (it IS the gun view) |
| `bug-296` | Third person on jeep .30cal: camera glitched inside the player model | cg_3rd_person draws the own body (cg_modelanim.c:1219) but the PMF_CAMERA_VIEW block in CG_CalcViewValues unconditionally copied the turret's bound gun-eye camera over the chase vieworg -> camera sits at the gunner's chest inside the drawn  | skip the PMF_CAMERA_VIEW vieworg/angles copy when (renderingThirdPerson && PMF_TURRET); CG_OffsetThirdPersonView gets a bTurret3p path that uses cg.camera_angles (authoritative gun aim) as chase reference and bypasses the CF_CAMERA_* transi |
| `bug-256` | 3P + sniper rifle + ADS hold glitches: shoulder camera ease fights the scope's forced first-person on scope-in/out | Scoped weapons zoom on the SAME button as ADS (server ToggleZoom on BUTTON_COOPADS). Pre/post-zoom frames (before STAT_INZOOM arrives / after unzoom with button still down) satisfied CG_AimingDownSights -> shoulder envelopes + wheel capture | CG_ActiveWeaponHasScope() (config-string weapon name vs the trilogy's complete zoom-TIK list: KAR98 - Sniper, Springfield '03 Sniper, Enfield L42A1, SVT 40, G 43, FG 42 + binocular items) gates CG_AdsStagedOn() to FALSE -> 3P sniper ADS sna |
| `bug-252` | e3l3 AB41 jump-out: solid turret gun floating at head height over the landing zone; players wedged until they crouched out from under it | detachturretslot with an explicit exit position on an UNMANNED slot fell through to the vanilla path: FindExitPosition teleported the GUN entity to the eject spot, set it SOLID_BBOX, and freed the slot (which also broke the later queryturre | DetachTurretSlot v6: an explicit-position detach is now a crew-eject-only order. Player rider -> full dismount teardown (v5). No/AI rider -> pure no-op return: gun stays mounted, slot intact (vehicle keeps driving gun pos, query/hide/remove |
| `bug-229` | MG42 still forced 3rd person even with coop_turretThirdPerson 0; multiple blind camera fixes to TurretGun::P_ThinkActive had no effect | UNCONFIRMED - root-cause investigation in progress. Ruled OUT: (1) stale deploy - engine loads game.dll ONLY from exe-dir/fs_basepath = GOG ROOT (Sys_LoadDll), never from maintt\; root game.dll is current and contains the FP camera code (ve | NOT YET FIXED - added two-sided diagnostic (coop_turretDebug 1, default-on in autoexec for next test): SERVER prints ^~^~^ TURRETCAM with the camOrg it actually sets + owner classname (weapturret.cpp after the camera branches); CLIENT TURRE |
| `bug-139` | MAGS magazine counter not visible bottom-right | cgi.R_DrawString expects VIRTUAL 640x480 coords (scaled internally by cgs.uiHiResScale, see CG_HudDrawElements line 656); CG_DrawMagazines passed actual pixel coords, so the string was multiplied by the scale and drawn far off the right edg | pass virtual 640x480 coords (sx=640-62, sy=480-30) with scale cgs.uiHiResScale |
| `bug-138` | random opaque square at screen centre in-game (mistaken for crosshair/DoF) | stripping the 60 ammo-HUD menus dropped each menu's 'bgcolor 0 0 0 0' line, so every empty menu rendered its SOLID background box (~261x263 = a square) at the menu default position. Not the DoF or crosshair - r_dofBlur 0 and ui_crosshair 0  | re-added 'bgcolor 0 0 0 0' to all 60 stripped hud_ammo_*.urc so the empty menus are transparent and draw nothing; pk3-only redeploy |
| `bug-110` | Hold-breath was backwards: holding Shift made the gun SWAY and releasing it steadied (opposite of intended) | Used (cmd.buttons & BUTTON_RUN) as 'Shift held'. BUTTON_RUN is the run/walk SPEED state, not the raw key: with default always-run it is SET while running and CLEARED while the walk key (Shift) is held. So the test was inverted. | Inverted to bShift = (buttons & BUTTON_RUN) ? qfalse : qtrue, so holding Shift (walk) = hold breath = steady. Also removed the breath HUD bar per user request. |
| `bug-107` | Spearhead/Breakthrough weapons go invisible when aiming down sights (RMB); base Allied Assault guns are fine | The AIM state runs 'viewmodelanim charge' which plays <weaponNamePrefix>_charge on the FPS hands tiki. SH/BT weapons had no <prefix>_charge alias in fps_anims_*.txt, so the lookup missed and the engine fell back to a bare officer/flak 'idle | Added 14 <prefix>_charge aliases (enfield/mosin/SVT/g43/carcano/delisle, sten/ppsh/moschetto, vickers/breda, beretta/webley/nagantrev), each a copy of that gun's existing <prefix>_idle line with the first _idle->_charge (so the .skc path st |
| `bug-106` | Spearhead/Breakthrough weapons go invisible when aiming down sights (RMB); base Allied Assault guns are fine | The AIM state runs 'viewmodelanim charge' which plays <weaponNamePrefix>_charge on the FPS hands tiki. SH/BT weapons had no <prefix>_charge alias in fps_anims_*.txt, so the lookup missed and the engine fell back to a bare officer/flak 'idle | Added 14 <prefix>_charge aliases (enfield/mosin/SVT/g43/carcano/delisle, sten/ppsh/moschetto, vickers/breda, beretta/webley/nagantrev), each a copy of that gun's existing <prefix>_idle line with the first _idle->_charge (so the .skc path st |
| `bug-105` | In crouch ADS the hand+weapon sit low/off-centre with no way to move them toward screen centre (crouch had only pitch/yaw/roll rotation, no screen translation) | The renderer screen-shift that recentres the whole weapon view (hands+gun) was driven only by standing r_weaponshiftx/y; crouch had no equivalent. The per-gun model.axis rotation in cg_modelanim moves only the gun, not the hands, so rotatio | Added cg_adsCrouchShiftX/Y (and migrated standing to cg_adsShiftX/Y); cg_view CG_CalcFov now feeds r_weaponshiftx/y = cg_adsShiftX/Y + (PMF_DUCKED ? cg_adsCrouchShiftX/Y : 0) each frame. cgame-only, no renderer rebuild. Nudge SHIFT mode now |
| `bug-098` | DEBUG give-weapon binds (give-all + per-category cycle) don't give the gun; log: 'givePlayerWeapon ... is already holding a weapon of class rifle - you must take this first!' | The engine holds ONE weapon per category and REJECTS a same-class give (game.item::givePlayerWeapon errors 'already holding class X, take it first') - it does NOT auto-swap. So the cycle never swapped (player already had a rifle) and give-a | Added 'self takeall' (EV_Sentient_TakeAll, works from script - bypasses the cheat gate like noclip) before the give in give_next_weapon (cycle) and give_all_weapons. Cycle now: takeall -> give+equip next gun. The coop wrapper's built-in gam |
| `bug-091` | User: MG42 cooldown pauses when a player leaves the gun - heat freezes while unmanned and only resumes when someone remounts | The heat build/bleed logic lived only in TurretGun::P_ThinkActive (the MANNED think). When a player dismounts, TurretGun::Think dispatches to ThinkIdle (unmanned, barrel-droop) which never touched m_fHeat, so heat froze. m_fHeat persists ac | Added unmanned cooling to the !owner branch of TurretGun::Think: m_fHeat -= 15/s and clear m_bOverheated at 0, so it cools on its own (~7s) whether or not anyone picks it back up. FL_THINK persists after dismount (proven by the barrel-droop |
| `bug-089` | User: 'the thompson ADS thing did not change anything' and 'speed is not slower at all in ADS' - recent fixes seemed to do nothing in-game | The fixes were edited in source but NEVER BUILT/DEPLOYED - the running game still had the old pk3/binaries. Compounded by an earlier real bug: ads.scr used command syntax 'local.player moveSpeedScale 0.4' -> 'unknown command' -> the whole s | Corrected ads.scr to property-set 'local.player.moveSpeedScale = 0.4' (sets speed_multiplier[0], multiplied into ps.speed every frame - player.cpp:4090/12712), then rebuilt game.dll (fgame) and ran build.ps1 to redeploy pk3 + autoexec + cga |
| `bug-072` | No ADS: gun does not raise / no zoom when holding RMB. Log: TIKI_InitTiki: Random animation name 'garand_adsraise' should end with a number | The new ADS viewmodel aliases were written with 'weight 1.0'. The TIKI loader treats any anim with a 'weight' as a member of a RANDOM selection set, which must end in a digit (idle1, idle2...). 'garand_adsraise' has no number suffix, so the | Removed 'weight 1.0' from every adsraise/adsfire line in fps_anims_{rifle,smg,mg,shotgun,pistol}.txt (kept crossblend). A single (non-random) alias registers fine without weight. |
| `bug-036` | Wrong condition in logic | Condition was: if (GetTag("tag_seat", &bone_or) | Changed to: if (g_gametype->integer != GT_SINGLE_PLAYER && pOwner-) |
| `bug-lean-ads-wrong-branch` | lean+ADS 'still the same, not able to see down my front sight' after a fix was supposedly applied | The earlier lean-roll damp was placed in CG_OffsetFirstPersonView's 'if (bUseWorldPosition)' branch (~388), which is NOT the first-person path, so it never executed. The lean the player actually sees in FP comes from line 1254 (CG_CalcViewV | Damped BOTH live FP sites when CG_AimingDownSights(): line 1254 roll by cg_adsLeanRoll (def 0.25), and the 476-483 lateral shift by new cg_adsLeanShift (def 0.2). Both live-tunable. |
