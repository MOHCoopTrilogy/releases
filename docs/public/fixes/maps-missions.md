<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# Maps & missions

[<- back to all fixes](../BUGFIXES.md)

**133 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-2402` | coop_navalGunfire's header comment described a version of the code that no longer exists after this session's fix. | The BUDGET paragraph said 'One entity at a time, removed after 1.2s, so this costs a single slot no matter how long the mission runs.' bug-2393 gave the report its own dummy.tik carrier living ~6.5-7.0s, so there are now two entities per vo | Both paragraphs rewritten to the measured values, each naming the bug that moved it. Caught by the vet pass, not by review. |
| `bug-2391` | user, three times: 'still floating blood all over barbed wire and seawall areas' | coop_beachDead's 46 blood pools were the one blood site on the beach with NO ground guard at all - a bare `droptofloor`, no obstacle nudge, no perched test - while every other blood and prop placement on m3l1a routes through coop_placeGroun | Each pool now measures against the ground datum after droptofloor (coop_nearGroundZ, falling back to coop_groundZ) and is removed if it sits more than 8 units above it - the same measured tolerance used everywhere else on this beach. 12 of  |
| `bug-2379` | user: 'Can we have more huggins boats come in and drop off troops fully animated etc after we've already gotten thru the cinematic sequence?' | n/a - new. coop_extraBoats places its boats ONCE at map load and they ride in bound to $higgins_fleet, the mover that carries the opening wave; by the time the player is ashore that mover has finished, so nothing new ever arrives. | coop_lateBoats: six waves after the handoff, one every 45-75s, each an origin-stepped run-in from y -3400 to -2450 over 14s (moveto no-ops on a script_model) then coop_boatLand VERBATIM for the ramp, the doomed-boat shell, coop_disembarkSqu |
| `bug-2363` | user, escalating: 'several helmets are floating in the air over the water still' -> 'there are somany floating helmets dear god' -> screenshots of a STRETCHER and PLANKS hovering over DRY SAND. | SOLVED BY MEASUREMENT AFTER TWO WRONG DIAGNOSES, BOTH MINE. (1) I first blamed coop_wreckDrop's float path and removed it - helmets can never reach that path. (2) A workflow then measured the cause as obstacle clip hulls and recommended pad | Tolerance 40 -> 12, which is the measured separation between a real seating (within about 10 either side of the datum, that being the datum's own noise) and something standing on an obstacle. Result, measured on the next boot: placed=83 rej |
| `bug-2361` | user: 'is it possible to also make our higgins boat destroyed after we get thrown out of it? or is that baked in?' then 'I'd love for it to slowly sink beside us and keel over while on fire' | n/a - new. NOT baked in: $higgins1 is an invisible script_origin pivot (ScriptOrigin : ScriptSlave : Mover) that the whole rig binds to, so it takes the full mover vocabulary, and after m3l1a.scr:2499 no thread owns it. | coop_higginsSink: a 15s gate past the handoff, then four mover legs that heel 38 degrees, trim 6 bow-up and settle 26 units, each leg a SINGLE blended move (translation and rotation accumulate into one LinearInterpolate, so they run togethe |
| `bug-2358` | user: 'their isn't much in the way of diziness going on which would add to the confusion' | THERE WAS ALMOST NONE, AND MY OWN COMMENT HAD THE RULE BACKWARDS. The client restarts the sway only if the new value EXCEEDS the live severity OR 5000ms have elapsed since the last start (cg_view.c:662). The re-arm lands ~3.1s after the bla | 0.85 -> 2.4, and the inverted comment replaced. Severity is unclamped and scales both amplitude and duration, so 2.4 beats the live 1.0 and restarts immediately, and coop_dizzyTime * 2.4 = 24s at the shipped 10 - covering the whole 17.4s be |
| `bug-2357` | user: 'random floating helmet above the water when we get out of the water, looks like a ranger helmet' | MEASURED, by two independent BSP raycasters replaying coop_wreckDrop over m3l1a.bsp across 300 map loads: 0.4-0.66 wreckage props per load came to rest ABOVE the water plane, up to 120 units clear of it, and ~75% were inside the footprint o | Both guards added: coop_dropSpot with pad 45 before the trace, and the perched test (discard if the trace result is more than 40 above the nearest-static / y-band ground datum) after it. Both constants are measured, not chosen: pad45/tol40  |
| `bug-2350` | user: 'WHen we end up under water the camera does this up and down bobbing, I think it should be slower movements looking around at actual events' | The hold's key ring spanned 10 units of z (-562..-572) and was walked 2.5 times across the 12 seconds - five vertical round trips, which is a bob, not a drowning man. | z spread 10 -> 3 units, so height is effectively held and what remains is the LOOKING. Yaw arc widened 83..101 -> 74..108 so the head turns across the scene instead of nodding at one spot, and the ring is walked ~1.2 times instead of 2.5, s |
| `bug-2345` | Outstanding since the first ramp-cinematic batch: the user's bonus ask to 'show people getting killed/drowning in the camera view looking toward shore' during the underwater hold was never built. A gr | It was carried as a bonus item across several batches and dropped each time. It was also not really buildable before: the underwater beat was 1.5s of tumbling, so there was no time in which to look at anything. | coop_underwaterDead + coop_underwaterKill, now that the hold is 12s on the seabed at pitch -6..-31 (looking up and toward shore). Seven already-dead - four face down at the surface, silhouetted against the light, three crumpled on the sand  |
| `bug-2340` | user: 'there should definitely be dead bodies and guns in there... maybe a typewriter somewhere... things that show weight has drowned people' | n/a - new content. | coop_beachWreckage, ~120 props. Dropped as CLUSTERS (a body plus 2-4 pieces of his kit inside 90 units) rather than an even scatter, because a cluster reads as a man who went down and a scatter reads as litter. Seated by `trace start end 1` |
| `bug-2339` | user feature request: 'it would be cool to also see bullets entering the water during our cinematic moment where we fall into the water.' | n/a - new content. | coop_waterBullets: bh_water_hard at the surface plus two water_trail_bubble beads descending, in bursts of 4-8 at 60-130ms with a 0.35-1.2s gap (a drizzle reads as rain; bursts read as a machine gun). THE DETAIL THAT MAKES IT WORK: both eff |
| `bug-2328` | user: 'found more assault engineers just standing in the open.' | The engineer watchers were asked for the pose `watching01` and the engine answered `unknown animation 'watching1' in models/human/dday_engineer.tik` - NOTE THE MISSING LEADING ZERO. That is the random-alias digit strip: a TIKI alias carryin | Swapped to trench_idle_up, a genuine 16.67s LOOP already proven on this map by the firing line, and which reads as exactly what the man is for: watching the bluff for the demolition party below him. |
| `bug-2323` | user: 'the injured guys holding their shoulders only sometimes lay down and hold their wound, most of the time they are standing in the open doing it' | The kneeling branch. <wg>_pain_kneesidle is upright from the waist, so at any distance it reads as a man STANDING and clutching himself rather than as a casualty - and it was 1 in 6 of the injured behaviours. There are no standing pain anim | Kneeling reduced from 1-in-6 to 1-in-10, with everything it loses redistributed to the two floor branches - flat on the sand and collapse-to-floor. A man down on the sand is what was asked for and what the beach should look like. |
| `bug-2317` | user: 'the guys moving the crates are standing around and not moving' (with screenshots of carriers frozen holding crates), then 'all guys just standing around including the box guys' | coop_pickLeg returns NULL when all 14 candidates (2 rings x 7 bearings) are blocked by the obstacle table. coop_advanceLeg then reports 0, and the caller's response to 0 is to wait and try again - FROM THE SAME SPOT, against the same table, | A desperation ring: when the full ladder fails, a 90-unit hop with the obstacle test dropped (at that range he cannot end up deep inside anything and the engine's own collision still stops him) but the dry-land test KEPT so nobody shuffles  |
| `bug-2311` | user, twice: 'one is trying to carry an injured soldier into the barbed wire' then 'same friendlies are stuck walking straight into the barbed wire at teh seawall, including the dude carrying the frie | These two pairs were NEVER part of the under-map/pathing fix (bug-2302) and could not be: they issue NO movement order at all. The drag and carry animations carry a root delta, so the men travel along their FACING for as long as the animati | New coop_haulPair steers by the only channel available - WHICH WAY THEY FACE - testing the lane ahead against the same 401-row obstacle table coop_legClear already uses (so wire, hedgehogs and Belgian gates are one test), widening to +-60 d |
| `bug-2308` | user: 'because the screen is white we don't really see how we ended up outside of the boat' + 'when the higgins door comes down they all run out the entrance' | TWO defects in the cinematic I had just built. (1) FADE ORDER, my bug: I wrote fadeout -> waitthread coop_rampFly -> fadein, and waitthread BLOCKS for the whole 2.2s arc, so the screen stayed fully white for the entire flight and cleared on | (1) fadein moved before the arc so it runs concurrently: 0.12s at full white (a blast flash, not a transition) then a 0.45s clear, readable about a quarter of the way into the throw. (2) coop_rampGore now cuts rangers 1-2 down IN the doorwa |
| `bug-2306` | user feature request: 'Could we make the ramp open more cinematic? Like it opens, guys get instantly butchered in front of you and than an explosion inside sends you up and over the side dramatically, | FEATURE, not a defect - but it rests on one discovery that made it cheap and safe: THE BUTCHERY ALREADY EXISTED AND NOBODY WAS THERE TO SEE IT. Retail threads rangers 1-5 to their deaths in the ramp mouth at t+1.1 and they finish dying arou | NOTHING PHYSICAL HAPPENS TO THE PLAYER, which is the whole reason it is safe for eight at once: every coop player on this map is ALREADY looking through a per-player Camera entity while his body sits glued, hidden, notsolid, physics_off and |
| `bug-2282` | user: 'see random guns sitting on top of barbed wire, must be some of your placed debris' | coop_placeGround's perched test only ran below y 2400, because the y-band height profile it used is meaningless on the bluff (the 4800..5200 band spans -344 to -553). The barbed wire runs y 3190..5913 - entirely inside the region where the  | The obstacle generator now carries each static's Z. A static sits ON the ground, so the nearest one within 500 units is a LOCAL ground reference valid anywhere on the map, bluff included - no profile needed. coop_nearGroundZ returns it; the |
| `bug-2279` | depthscan2.py: EOF depth = 1 (must be 0) on coopified.scr, immediately after removing the prisoner scene | The removal sliced from s.rindex('//=====', 0, i) - the last divider BEFORE the scene's comment block. Every label in this file has TWO '//=====' dividers, one above the label line and one below it, so the nearest preceding divider belongs  | coop_beachCarry restored verbatim. The near-miss never reached a build: docs/tools/depthscan2.py caught it in the same command. LESSON: when cutting a labelled block out of this file, anchor on the label name and walk forward to its own '}e |
| `bug-2278` | FEATURE + TOOLING: user asked for the remaining sweep scenes, conditional on confidence that they would ship unbroken | Four classes of silent failure had each shipped this session and none produces a Script Error: an animation alias that does not resolve on this map (the actor just stands), a LOOPING alias followed by `waittill animdone` (the thread parks f | docs/tools/check_map_anims.py: walks the model $include chain honouring TIKI_ParseIncludes' PREFIX rule and $mapspec, honours the random-alias digit strip, reads loop/one-shot and duration from the SKC header (flags at offset 8 - offset 12  |
| `bug-2274` | user: 'if these are the crates they are moving they are tiny... there are actual animations to move giant crates from other missions, thats what I was suggesting' | THE ANIMATION EXISTED AND WAS ONE $include LINE AWAY. models/human/animation/scripted/crate_carry.tik ships crate_pickup1/2, crate_walk, crate_putdown1/2, crate_drop and crate_stand_idle - a full authored crate work party whose crate_pickup | One $include added to the `includes m3l1a` block. coop_carryCrate rewritten to crate_pickup1 -> crate_pickup2 -> moveto 'anim/crate_walk.scr' <info_waypoint> (moveto plays the named anim script for the whole leg, and a waypoint target avoid |
| `bug-2268` | user screenshot: an ally standing on the gunwale of a wrecked Higgins boat; then 'floating dead bodies' | An obstacle table can only catch what is IN it, and a Higgins hull is a spawned ENTITY, not a static model - no amount of extending the static table would ever have caught it. Same for crates we place ourselves, corpses, and other actors. | coop_groundZ: the expected sand height by y, piecewise from the mean z of 365 ground-sitting statics in 400-unit bands. coop_placeGround rejects any placement resting more than 40 units above it and shoves it sideways to retry. Limited to y |
| `bug-2266` | user: 'Got several allies literally just standing out in the open on the beach too' and 'theres an alarming amount of allies just standing on the beach doing nothing under fire' | TWO CAUSES. (1) 19 vignettes were posed with a bare `anim <name>`; most of those aliases are one-shots, so ANIMDONE fires and the actor falls back to standing - the third time this exact fault shipped. (2) The poses themselves were wrong: s | General coop_vigHold: play the alias, waittill animdone, play again - a true loop never fires animdone so the thread parks and the pose holds, which makes one call correct for both kinds. It yields the moment ai_execute_todo claims the acto |
| `bug-2265` | user: 'some guys just standing in the water doing nothing, and running around without pushing up the beach' / 'is the ammo crate dropping troops here?' | The work party pickup was at y -1450..-1750 and the dump only 900-1400 further on, so the whole carry ran between y -1750 and -100. The measured beach profile puts that band at z -510..-586: the entire party was ankle-to-thigh deep in the s | Dump moved onto dry sand (y ~400, the band retail's own shingle_wave_spawn markers sit on) and the haul now runs DOWN to the water and back, across the player's advance. Charge band moved to y -950..-1450. |
| `bug-2264` | user: 'Still have floating allies on top of obstacles and floating blood in areas' | TWO HALF-FIXES THAT EACH MISSED THE OTHER'S CASE. coop_dryGround tested WATER DEPTH only, so an actor droptofloor'd onto a hedgehog passed it. coop_dropClear tested OBSTACLES only and was wired into six prop sites and zero actor sites. The  | One primitive, coop_placeGround: nudge XY clear of obstacles, droptofloor, and if still below -520 step shoreward and re-test obstacles. Every actor site and every remaining bare droptofloor routed through it. Threshold derived from the mea |
| `bug-2262` | user, repeatedly over months: 'We still have some blood floating over the tanktraps on omaha' | THERE WAS NO CLEARANCE TEST IN CODE ANYWHERE. Entity::droptofloor (fgame/entity.cpp:2669) is a BOX trace against clipmask with range MAP_SIZE, so it stops on the first collidable thing below - and the tidal flat is carpeted with collidable  | The obstacle set is now GENERATED, per the OpenWolf rule that an extractable inventory must never be hand-written: docs/tools/gen_m3l1a_obstacles.py reads LUMP_STATICMODELDEF (lump 25, stride 164, origin at offset 128, lump directory at byt |
| `bug-2261` | adversarial audit: 'THE WORK PARTY TAKES FROM A SHARED BUDGET WITHOUT PAYING INTO IT' | coop_beachWorkParty spends six coop_vigSpawn slots and never raised level.coop_vigCap, unlike coop_boatBurn (+2) and coop_flankLandings (+26). It only worked because flankLandings' +26 goes unspent - that label spawns directly. If anyone ev | The label raises coop_vigCap by its own 6. Crates and dropped kit now go through coop_dryGround and coop_dropSpot like everything else. |
| `bug-2260` | adversarial audit: gore, blood and dropped kit stop dead at y 1191 - the trench, the breach and all three bunker interiors have none | coop_beachGore's 26 cluster coordinates all sit in the landing band (y -2431..1191), and the scattered-kit loop reads the SAME anchors with only +/-150u jitter, so it inherited the identical dead stop. The map runs to y 6336 and the endgame | 20 more clusters covering y 2125..5179, solved against the 229 obstacle statics with >=130u clearance and >=430u separation from each other so they read as separate men rather than a carpet. |
| `bug-2259` | adversarial audit: 'THE DEAD ENTITIES ARE DRAWABLE, NOT MERELY INVISIBLE' + the working distant-explosion stream is gated behind the broken half's toggle | (1) Every fx model that behaves here carries `rendereffects +dontdraw` in its own TIKI init server block - fx_mortar_water.tik does. models/fx/bh_water_lite.tik and bh_water_hard.tik carry an EMPTY server block (read out of Pak0 to confirm) | The spawned water-hit entity is given `rendereffects "+dontdraw"` at the call site, since a retail tik cannot be edited. coop_runInWaterDistant got its own gametype guard and its own coop_runInWaterDistOn toggle. |
| `bug-2258` | adversarial audit: 'THE FLASH IS EFFECTIVELY INVISIBLE' + 'THE LOOP STARTS BEFORE THE PLAYER HAS LANDED' | Two faults in coop_shermanFire. (1) The muzzle flash was models/emitters/firegood.tik, which is a CAMPFIRE - fire_ring2.spr at scale 0.20 with scalemin 0.10 - placed at the entity origin (i.e. behind the hull, see bug-2257). At the 5,700-7, | The flash is now fx_mortar_dirt.tik - the same burst the shell impacts use, visible at range - placed 190 units forward of the hull centre along the tank's own yaw, at +96 z. The loop now waits for coop_runInShelling to start and finish bef |
| `bug-2257` | adversarial audit: 'THE MASKING PREMISE IS BROKEN BEFORE THE TRANSLATION EVEN MATTERS - the fireball is not on the tank' | models/static/vehicle_shermantank.tik's QUAKED box is (-288 -64 0) (24 64 160): the mesh runs 288 units BEHIND the entity origin and only 24 in front, so the origin sits at the extreme rear of the hull and its geometric centre is ~132 units | coop_shermanBody returns the hull's geometric centre, rotating the -132 local-X offset by the tank's own yaw; the mask and the fireball now use it. |
| `bug-2256` | adversarial audit: 'THE ONE SOLVED TABLE HAS ALREADY REGRESSED, AND ITS COMMENT NOW LIES' - four Higgins hulls added by eye under a header asserting the whole table was verified against LUMP_STATICMOD | coop_extraBoats was expanded 5 -> 9 ashore hulls to fill the empty middle of the beach. The four new x/y positions were chosen by eye, but the table header - written for the original five after bug-2209 - claims '>=460u clearance from any B | The four are re-solved, not re-guessed: a grid search over the proven ashore band (y -1200..-2500) maximising distance to the 229 obstacle statics, height from the mean of statics in the 430-1100u ring (the same height rule as the original  |
| `bug-2253` | user: 'seeing tons of allied actors standing on top of the higgins boats that are moving and stationary... just standing there' (with screenshots) | coop_boatBurn's wreck crew was posed with a bare `anim fallen`. `fallen` is a ONE-SHOT: it plays, ANIMDONE fires, and the actor returns to its idle, which is standing. coop_vigKeep exists precisely to re-trigger such a pose on a loop and wa | Two per wreck instead of four, posed via `thread coop_vigKeep "fallen"`, pushed 300-560u clear of the hull instead of 0-210u, and the label raises level.coop_vigCap by its own 2 so it cannot starve the vignettes. |
| `bug-2233` | coop_seasickMan would have been silently stomped within a second or two of firing: retail drives EVERY ranger in the boat from higgins1_crouch / _look / _flinch, and it fires the flinch FIVE times dur | Caught by the workflow's synthesis agent, which noticed the conflict because coop_seasickMan was written AFTER its survey lanes had read the file. I had checked that $higgins1_ranger4 was untouched between m3l1a.scr:446 and :2484 and conclu | level.coop_seasickPlaying, set around the anim in coopified.scr, and the four $higgins1_ranger4 lines in higgins1_crouch/_look/_flinch wrapped in if(level.coop_seasickPlaying != 1). Smallest possible guard: only ranger4, only for the 4.5s h |
| `bug-2231` | USER: 'did we ever figure out anything about bloody limbs being scattered around?' - asked on 2026-08-30 as 'If there is any way we could place limbs all over the beach that would be dope', and never  | It was researched in July and the research says real severed limbs are not available, but the finding never reached the beach and no fallback was built - only bodies (coop_beachDead) and pools (coop_beachBlood). The two blockers, from _rese | Built the fallback that same document recommends - 'a generic red meat chunk reused N times, reads as blown apart at gameplay distance without per-limb modeling'. coop_beachGore scatters 20 CLUSTERS (2-4 chunks each, one larger piece per cl |
| `bug-2219` | User: 'no blood in the water'. The log said WATERBLOOD slicks=14, so they were spawning. | Placed where the player never goes. The previous pass deliberately pushed the slicks OUT of the landing lane (x -1000..1400) and into the seaward half to dodge the shoreline's deformVertexes flap - but the lane is the only water the player  | 44 slicks at scale 1.6-3.8, spread THROUGH the lane over y -2150..-1250 (still seaward enough that the measured flap there is sub-unit) and raised +6 instead of +2 so a wave crest cannot swallow one. Height still comes from the measured wat |
| `bug-2216` | Every 'entity-pool guard' in the new Omaha code waved a failed spawn straight through to .origin/.notsolid/.anim, turning one spawn failure into four script errors per iteration. | NIL != NULL. A failed spawn yields 'none', which reads as NIL, and if(local.x != NULL) is TRUE for NIL - so the guard passed. This is the documented trap (guard maybe-unset entity handles with BOTH) and it was written 32 times in one sessio | All 32 post-spawn guards now read if(local.x != NULL && local.x != NIL), and coop_fxCleanup tests both on entry and before remove. Mechanical pass keyed on the line following each '= spawn'. |
| `bug-2213` | FEATURE (user): Omaha should read as a warzone - fires around the bunkers with cinematic depth and varied fire types, rounds hitting the water, return fire on the bluff, explosions inland, heavy fire  | Feature request, not a defect. | Five new cosmetic systems, all gated on level.gametype != 0 and all individually switchable: coop_waterShots (bursts of small splashes near players still in the water), coop_returnFire (bursts walking across the real emplacements, all missi |
| `bug-2211` | being behind these tanks also do not actually [protect] ... it has to do with omahas specific pain and cover system | THE USER DIAGNOSED THIS, and it invalidates two of my earlier fixes. Nothing on this beach kills you with bullets: mg42_damage_player applies damage DIRECTLY, and the only thing that ever suppressed it was isInCover, which walks $cover_trig | isInCover now also returns true when NO emplacement has line of sight to the player. The authored trigger volumes are kept as the first test; after they fail, up to six sighttraces run from the bunker and middle MG42s to the player's chest, |
| `bug-2209` | lets make sure none of these higgins boats are actually embeded in the maps objects... a few seem to be | 5 of 6 ashore boats were inside beach obstacles (minepoles, ramp triplelegs) - measured at 42, 70, 71, 79 and 139 units from a static, against a hull ~218x700. The method was the fault: each boat's ground height had been derived from its NE | Re-solved from LUMP_STATICMODELDEF: require >=430u clearance from any static, and take the height from statics in the 430-1100u BAND instead - near enough to be the same stretch of sand, far enough not to be the obstacle. Five clean spots a |
| `bug-2204` | m3l1a: the three biggest blasts on the beach land with a completely still camera in coop | All six jitter_* labels carry `if($player.size > 1){ end }` (m3l1a.scr:8333/8349/8365/8382/8399/8417). The guard is correct on its own terms - they drive global/earthquake.scr, which shakes EVERY player wherever they are standing - but it l | Added a coop-gated earthquake_radius beside each of the three calls, centred on the blast entity that is already in scope ($higgins2, $bangalore_explosion1a, $bangalore_explosion3a). Radius-based, so the men near the blast are thrown and th |
| `bug-2202` | these enemies skins used to have massive bullet holes in them when they got shot, seems like it barely happens now. We definitely changed something | My own regression, from the bug-2191 fix earlier the same day, and it reintroduced bug-735. I changed the wound-prop standoff from (locRadius + 1) to (len + 1) to stop the prop being dragged inward on secondary-sphere hits. But `position` i | Take whichever standoff is LARGER: standoff = max(len, locRadius), then push to standoff + 1. That keeps the old clearance everywhere it mattered and only pushes further out on the secondary-sphere and remapped-head hits that were genuinely |
| `bug-2199` | sequence_6_spawn_shingle has no re-entry guard while its sibling sequence_7_spawn_shingle does | sequence_6_spawn_shingle is reached by waitthread from two branch labels. Each branch begins with a nottriggerable/delete block, which closes almost everything - but not a same-server-frame double touch by two players, because Trigger::Trig | Mirrored the sibling's level.thread[] guard at the top of the label. Preventative - not tied to a specific report. |
| `bug-2196` | the tank still doesn't protect me from shots (user playtest, after the bug-2187 solidity fix) | My own regression in the bug-2187 fix. I wrote the bounding box as `setsize (0 - 58 0 - 115 0) (58 115 110)` - five tokens, not a 3-component vector - because I over-applied the project's 'no bare negative in parens' parse-killer rule. That | Rewrote both calls in the proven `( -58 -115 0) (58 115 110)` form. |
| `bug-2195` | i dont see the bunker areas getting shelled either (user playtest) | coop_inlandShelling, added 2026-08-28, never put a single visible burst on the bluff, for two independent reasons. (1) It spawned models/fx/explosionartilleryshell.tik and called `anim start`; that TIKI's animations block contains ONLY `idl | Rewritten to walk the ten mg42 emplacements the player is actually looking at ($mg42_left1/2, $mg42_leftbunker1/2, $mg42_middle1/2, $mg42_right1/2, $mg42_rightbunker1/2), scattering +/-210u around each. Uses models/animate/fx_mortar_dirt.ti |
| `bug-2191` | bullet holes do not bake into enemies, they float around the body (user playtest) | WRONG IN THE FIRST WRITE-UP, corrected here. The suspected cause was the offset maths at sentient.cpp:3035 snapping the prop onto the primary hit sphere. That line is a ~1u NO-OP for the common case: `position` is trace.endpos, i.e. the ray | sentient.cpp, game.dll only. (a) Chunks get their own lifetime cvar coop_goreChunkLife, default 6s, instead of borrowing coop_decapLife. (b) Bone resolution moved ABOVE the slot recycle - its two returns used to sit after it, so on a full b |
| `bug-2187` | the tanks you placed on omaha beach are not actually solid (user playtest) | coop_beachWrecks spawned the three Sherman wrecks with 'notsolid', a deliberate choice made on 2026-08-28 to avoid an AI pathing hazard on a beach the scripted rangers cross. In play it reads as broken scenery long before it reads as consid | The wrecks are solid, with an explicit hull-sized setsize instead of the model default. Yaw is now FIXED per spot instead of '(55 + randomint 70)': a SOLID_BBOX entity is axis-aligned in world space (SV_ClipMoveToEntities only routes angles |
| `bug-2179` | Objective Bonus Drop never ran on any map - 232 lines behind a commented-out hook, disabled by a comment describing a defect that was already fixed | main.scr:178-180 disabled the hook with 'its pickup re-granted on every touch -> infinite grenades'. That comment describes a pre-guard draft: objective_drop.scr:128-131 already sets flags[coop_smoke_taken] and removes the trigger, and :151 | Re-enabled the hook (main.scr:185) and derived nine real coordinates from map_entities/<map>_entities.txt - each the BSP origin of the entity that map's own script hands to global/objectives.scr as the objective position. Added a coop_find_ |
| `bug-2162` | user: 'my game crashed as I loaded the map' - ERROR: coop_mod/player_Torso.st: Expecting ':' on line 89 | MY EDIT. Two lines of the comment block added with the bug-2157 cover/brace fix shipped WITHOUT their '//' prefix, so the statemap parser read prose as state rows and rejected the file, which takes the whole torso statemap down on map load. | Restored the two '//' prefixes. More importantly, wrote docs/tools/stcheck.py, because the check that was run (depthscan2.py) CANNOT catch this: it scans brace depth, and the braces were balanced the whole time. This is the documented TRAPS |
| `bug-2109` | Prone reload: torso stands up off the ground. coop_proneSpineReload had no useful effect at any value. | Bone-bending compensates for a standing animation, and past a certain deviation it cannot win. A reload animation lifts the chest to work the bolt and seat a clip - far more than a firing animation - so the correction that fixed shooting (c | User-approved swap to the real body-space animation. New alias coop_prone_reload -> weapon_pistol/prone/pistol_prone_reload.skc (the ONLY prone reload in any pak) plus a first-wins `coop_prone_reload : POSITION_TYPE "prone"` line in RELOAD_ |
| `bug-2108` | 'i still canot prone on all terrain same exact issue. I also sometimes get stuck in prone where space or control should put me back into crouch' -> then, after the fix: 'NOW IT SEEMS LIKE EVERYTIME I  | THE EXIT CONDITION WAS THE DESIGN, NOT THE TRACE. TickCoopProne left prone whenever the crouch key was merely NOT held (`if (!bCrouchKey)`), which silently required the player to hold crouch forever to stay down. It was masked because the s | Exit rewritten as EDGE-TRIGGERED: releasing crouch only ARMS the exit; standing up requires a deliberate new crouch press or jump. Time-based escape removed as unnecessary - each press re-runs the honest trace, so a blocked player retries b |
| `bug-2087` | Script Error : Field 'coop_fireStop' applied to NULL listener / command 'anim' applied to NULL listener / Cannot cast 'none' to vector - repeating. | The flak fire loop tested `while (local.gun.coop_fireStop != 1)` with no NULL guard. Once the gun was destroyed every line in the body threw, and Script Errors do not kill a thread in this engine, so it kept re-throwing on its randomised 4- | Guard the handle in the while condition (&& short-circuits), plus an in-body break and one after the wait, since the gun can be destroyed across it. |
| `bug-2042` | The new probe bus shipped with four defects of its own, two of which made its most important fields print a constant placeholder on every row. | P1: prb_actorSnap read `local.a.coop_actorActualHealth` as a direct PROPERTY. That name is written as flags["coop_actorActualHealth"] at 8 sites and as a property at ZERO, so hpReal printed -1 for every actor forever - the single field the  | P1 -> flags["coop_actorActualHealth"], plus stopPain and noLongPain (the two gates that strand a buffered actor at full engine health). P2 -> flags["coop_role"] + flags["coop_personality_set"]. P3 -> initialise the mask to 0-1, which can ne |
| `bug-1972` | converting an enemy at the church SOFT LOCKS the clear-the-church objective | coop_churchSpawnersLive gates on the spawners currentalive/teamvalue counters (M3L3.scr:7447-7460). A converted actor is still one of that spawners spawned actors, so currentalive stays > 0 forever even though he is now an ALLY and no longe | surrender.scr: convert now bumps a global level.coop_convertedAlive and threads convertedWatch to decrement it on death. M3L3.scr coop_churchSpawnersLive now SUMS the three spawner counters, subtracts the living converts, AND keeps a real h |
| `bug-1924` | m3l3: captain ramsey is sometimes delayed to chat with the paratroopers after the halftrack is destroyed | The conversation is double-gated: a BSP proximity trigger (scene1_waveterminate_trigger) a spread-out coop team can fail to walk, and scene1_airborne_cleanup_enemy's unbounded 'while currentalive != 0' wait - one stuck wave straggler stalls | coop_scene2Prompt thread (coop-gated, started with scene1): keys on the map's own death signal (level.halftrack_alive), stops wave spawning, grants a 12s mop-up grace, then fires the reveal line and scene2_npc_blocking directly. One-shot gu |
| `bug-1879` | the waves that spawn near the front of the church (where mg nests are and near where we drop down from the pipe) never seem to really push into the church area | They physically cannot. coop_churchWaveLane lanes 1 and 2 spawn at x~5400-5600, EAST of the barricade that blocks the old churchyard entrance, and the live log names it: Path not found in Actor::MoveToPatrolCurrentNode, Reason: unreachable  | coop_chAdvance now counts consecutive stalls and, after 3 polls (4.5s of moving under 24u - a man in a firefight shuffles more than that), PLACES the attacker at the proven lane 0 position with up to 120u of spread and resumes the advance.  |
| `bug-1858` | I dont think the nebelwerfer objective ever triggers after the paratroopers drop though because they arent currently firing, no enemies are spawning and shooting the mgs at me like they usually do | scene7 - the entire nebelwerfer finale (gun crews, MG nests, spawners, the guns firing) - is only reachable from a BSP trigger_once, and that trigger does not fire in coop. `thread scene7` at M3L3.scr:324 is commented out with the note 'cal | coop_churchObjectives now threads scene7 at the moment it raises the final objective (airborne landed). scene7 given the same re-entry guard scene6 has (level.coop_scene7Started, gametype != 0), so the BSP trigger and the chain are both saf |
| `bug-1856` | We need to amp up the nebelwerfer shots, it needs to be happening pretty aggressively | coop_churchBarrage fired ONE rocket per iteration then blocked in-line on its own 1.6s telegraph and 4s fx cleanup, so the true cadence was ~1 impact per 10s. It also modelled a Nebelwerfer - a six-barrel ripple launcher - as a single tube. | Split one rocket into coop_churchShell and thread it so a salvo ripples instead of queueing: 4-7 rockets 0.4s apart, then a 5-11s reload. Per-shell damage cap cut 260->120 and floor 40->25 because salvo shells can overlap; aggressive must n |
| `bug-1838` | coop_churchDist returned ~150 for every input; a wave spawn 1245u from the church centre reported dist=165 | The hand-rolled horizontal distance (cx/cy, dx*dx + dy*dy, sqrt) did not evaluate correctly in morlang and collapsed to a near-constant ~150 regardless of input. This invalidated EVERYTHING built on it: coop_churchGermans, coop_churchPlayer | Rewrote coop_churchDist onto vector_length( a - b ), the idiom this codebase uses 149 times, zeroing z on both sides to keep it a cylinder. Verified by boot: the same wave spawn now reports dist=1245.371 against a hand-checked 1252u, g stay |
| `bug-1836` | user: 'I can start the climb up but then it wont climb any futher up' / 'if I keep jumping i can get there but thats now how it should work' | SAME-FRAME RACE, found by measurement after two wrong guesses. The 'ladder' latch thread calls 'local.player forcetorsostate GET_ON_LADDER' and then starts status_check in the SAME frame. status_check's loop condition includes 'self.legs_st | status_check now WAITS for the latch before polling it: up to 20 frames for legs_state_using_ladder to become 1, with a NULL re-check after the wait, then the existing loop. The readout gained 'latchframes=N' so the wait is visible and a ge |
| `bug-1835` | user: 'I also cant climb the ladder, for whatever reason I can start the climb up but then it wont climb any futher up' | UNKNOWN - deliberately not guessed. Two fixes have already shipped on reasoning alone (bug-1828 yaw/height from the capture, bug-1831 a 22u standoff) and the symptom persists, which is the 3-strikes signal to stop proposing and start measur | NOT FIXED. Instrumented instead: status_check now records the climb's start z, peak z and end z and prints them against the ladder's own base and top as '^~^~^ LADDERCLIMB start= peak= end= base= top='. That separates 'never left the bottom |
| `bug-1830` | near-miss: was about to refactor 28 vector literals of the form ( 4016 0 - 967 0 - 328 ) believing the embedded '0 - N' had corrupted them | The 'bare negative in parentheses' parse killer (T1) applies to a parenthesised negative SCALAR expression like (-1). I had over-generalised it to vector literals and written every coordinate as ( x 0 - y 0 - z ), then on reading it back mi | Settled empirically instead of by refactor: temporary coop_vecprobe label printed the embedded form, a plain-negative form and the indexed components on a dedicated boot. ( 4016 0 - 967 0 - 328 ) prints ( 4016.000 -967.000 -328.000 ), ident |
| `bug-1822` | User: 'i spawned in and it asks me to pick a team now, that should never happen' then 'i cant spawn in'. Log: "Script 'maps/m3l3.scr' was not properly loaded" on every setthread. m3l3 unplayable. | A newly appended scripted-fence module (bug-1820) plus the objective-chain block contained morlang expression-level parse killers, which kill the ENTIRE .scr - so m3l3 ran with no script at all and produced the raw team menu (TRAPS T1 signa | Stripped both appended blocks and restored the retail objective init, keeping the bug-1817 tank-trigger fix. Verified by DEDICATED BOOT, not by scanners: 0 'was not properly loaded', 0 parse errors, reaches 'initialising coop weapon loadout |
| `bug-1820` | m3l3 objective #6 needs an invisible wall that blocks HUMAN PLAYERS ONLY and lifts when the nebelwerfer objective opens. Initial plan named cmpatch brush surgery, which cannot do it. | Two wrong assumptions in the first plan. (1) cmpatch/<map>.txt was described as the project's invisible-wall system; it is the opposite - cm_killbrush ZEROES a brush's contents (cm_load.c:835) so the e1l2 recipe DELETES unwanted collision a | Built a scripted polyline fence in M3L3.scr: coop_fence_arm / coop_fence_watch / coop_fence_sideof / coop_fence_neardist / coop_fence_down, over the five user-walked vertices (4 segments, 291u, flat at z ~= -307). Players only (it only ever |
| `bug-1802` | user: 'I think this map is supposed to be storming' - m5l1b had no weather at all in coop | m5l1b does not author its own rain - it INHERITS it, restoring level.raindensity / rainvolume / thundertime from game.* saved by m5l1a (continuegame, ~:1255). That block sits behind if(game.playerorigin != NIL), the SINGLE-PLAYER continuati | level.coop_weatherHold = 2 in m5l1b main, set before the native exec global/weather.scr (which reads level.weatherpattern). Holds a steady storm instead of the random build/clear walk, which is biased toward clearing and would leave most of |
| `bug-1780` | text audit: ~22 one-shot DEV: prints reachable in normal play, plus a dead block tagged removeme | 26 iprintlnbold_noloc('DEV: ...') sites across e1l2/e1l3/e1l4/e2l1/m1l2a/M1L3a/m3l1a/m4l2/m5l1b/m6l1c/developer.scr/old_friendly.scr. _noloc means no self prefix, i.e. broadcast to every player. Two carried informal/profane wording ('DEV: s | 22 converted to println. The 4-line dead block at m1l2a.scr:1683-1686 was DELETED instead - the papersthread label ends on the line above it, grep finds no caller, and the site was already tagged 'test - removeme'. All 14 touched files pass |
| `bug-1778` | text audit: the level-2 papers pickup is announced twice, by two systems, back to back | global/items.scr::add_item takes a second parameter that suppresses its own message. m6l2a passed only one argument, so nomessage was NIL, items.scr printed 'You have acquired level 2 papers!' (iprintln) and the map then printed 'Level 2 pa | Pass nomessage, the idiom already used by 10 other callers in this tree. The map keeps its single bold line. |
| `bug-1776` | the explosive charge models all need to disappear after the explosion (already the case with the 2 vanilla charges) | The two retail charges vanish because exploder.scr::explode owns them. The four exterior charge markers are script_models this map spawned, so nothing cleaned them up - a planted charge sat on the wreck for the rest of the mission. | coop_m6l2aClearCharges removes all four coop_m6SabMark entities from the detonation. |
| `bug-1775` | there should also be fires that play - dont use the quick/cheap fire models, use others | The sabotage wrecks burned with electrical_fire, a small electrical sputter rather than a fire; the obvious alternative tehao_cheapfire is literally the cheap one the user meant to avoid. | firesmoke.tik and fireandsmoke.tik - the two best-supported burning models in the retail data (12 and 7 shipped scripts respectively), both carrying smoke as well as flame. Mixed rather than three of one so the flames do not animate in lock |
| `bug-1769` | if you are loud and dont get to mission end and it blows up, cutscene should trigger, but then when it ends you still have to fight your way out | bug-1763 made the cutscene unconditionally end the mission, which was right for the window but wrong for the fuse expiry - that path should show the blast and then put the squad back in the fight. | coop_m6l2aEndCutscene takes an endsMission argument. 1 (window) fades to missioncomplete and the stats screen; 0 (fuse expiry) calls cueplayer, unfreezes, restores takedamage, removes the camera and clears coop_m6ObjDone so the ending watch |
| `bug-1768` | if you get to the end of the mission with the timer still going, you just see the area without a detonation, and when the cutscene runs out you get the rank up screen without ever seeing the explosion | The window branch latched level.coop_m6Blown = 1 before calling the cutscene, which made the cutscene's own `if( coop_m6Blown != 1 ){ detonate }` false. On the on-time path the charges therefore NEVER fired - the camera cut to an intact tow | Only the cutscene may latch coop_m6Blown. Upstream branches report and defer; the cutscene owns the detonation so the blast is always what the camera is pointed at. |
| `bug-1767` | stopwatch still starts at 10 seconds, goes down to 60, and THEN you get the charges set message | FOUND IT, and the user's own description named it: 'something is still triggering it to go thru 10 seconds before truly being triggered'. objective1_bombtracker still ran the RETAIL fuse - `stopwatch 8` plus a blocking `wait 8` - before rea | Removed the retail stopwatch and shortened the wait to 1s. The bombtick loopsound is kept - that is the charges arming and it should be audible - but the widget belongs to the escape clock alone, which now starts on the plant. |
| `bug-1766` | the huge tower now has no collision at all - we overcorrected | bug-1756 fixed a 301x390x1153 solid wall by going notsolid, which is the opposite mistake. A single box cannot describe a lattice tower: cover the footprint and you wall off the charge markers at its feet, remove it and the mast is a hologr | A CENTRAL COLUMN instead - setsize ( -48 -48 0 ) ( 48 48 1153 ). You cannot walk through the middle of the mast, and the corners where the legs and the charge sites are stay open; charge site 3 sits at local ( -95.5, -109.9 ) from the base, |
| `bug-1763` | cutscene should hold longer, players should be invulnerable once it triggers, and it should end the mission into the stats screen instead of dropping us back into first person getting shot at | The cutscene held 8s, left players damageable behind a locked camera, then handed the view back with cueplayer - leaving the squad standing in a town that had just gone hostile. | nodamage every player on entry (the same call developer.scr's god mode uses) - deliberately with no takedamage restore, since this label now ends the level and re-arming damage a frame before missioncomplete would only re-open the hole. Hol |
| `bug-1762` | the explosion should trigger at 120 seconds OR when I actually complete the mission, and the cutscene should be the mission end | The fuse expiry ran the full ending beat, which meant the cutscene - and therefore the mission end - could fire while the squad was still standing in the middle of town. | Separated the two events. The charges fire on EITHER trigger, guarded by level.coop_m6Blown so they can only go once. The cutscene belongs solely to reaching the window. Overstaying now costs the clean run rather than ending it: the charges |
| `bug-1761` | your coords drew the explosion when you enter the building you exit the mission from - there's an actual window you have to jump out of | The exit test was a 512-unit radius around ( 2088 -3240 400 ). That radius covers the whole exit building, so it fired the moment a player stepped inside rather than when they left through the window. The POINT was nearly right - the user's | User's own viewpos at the window, ( 2006 -3236 402 ), with the radius cut to 110u so it means the window itself. Radius kept in a local so it is tunable in one place. |
| `bug-1760` | the timer starts at like 10 seconds on the stopwatch - third report of the same symptom | The duration was never arriving. The clock called `exec coop_mod/replace.scr::stopwatch local.leg` - and EVERY working caller of that helper in the tree passes a LITERAL (`stopwatch 8`), never a variable. Units were verified end to end firs | Set it per player directly, the one form proven with a variable - officer.scr:4471-4477 does exactly this. Also prints the leg and remaining total each time, so the number is stated rather than inferred. NOTE bugs 1753 and 1759 both 'fixed' |
| `bug-1757` | Cutscene didnt play when things went loud / I got to the explosion point, it seems to be triggered by the wrong thing | coop_m6l2aQuietEnding opened with an alarm branch inherited from the pre-timer design: on level.alarm it printed 'quiet ending lost - detonating now' and called objective1_detonation + threat_condition_delta DIRECTLY, bypassing the ending b | Alarm no longer detonates. There are exactly TWO triggers, as specified: the fuse expiring, or the mission actually being completed. Going loud still costs the clean ending - level.coop_m6EscapeLate and the alarm state decide what happens A |
| `bug-1755` | there should also be fire near the exploded objects | The mast took the retail topple path, which only ever produced dust and no fire at all. The generator and power box did get an emitter, but a single sprite sitting exactly on a prop's base reads as a decal rather than as the thing burning. | One coop_m6l2aSabFire helper placing THREE electrical_fire emitters on a small spread, called for all three wrecks including the mast. Left running for the rest of the mission - the burning site is the mark the squad leaves on the town. |
| `bug-1754` | the tower falls the wrong way | sc_p_radiotowerleg's `fall` animation topples along the model's own axis, and the leg was spawned at angles ( 0 0 0 ) - so the mast always fell in one fixed direction regardless of what is around it. | Yaw the entity that plays the animation, from cvar coop_m6TowerFallYaw (default 0), applied to both the falling leg and the fallen wreck so they agree. A cvar and not a literal because the correct value is something you look at rather than  |
| `bug-1751` | parse error: syntax error, unexpected TOKEN_PLUS (maps/m6l2a.scr, 2693) - whole map script failed to compile | Wrote `println "text" + var + "text"` without parentheses. Morpheus needs println( ... ) around a concatenation. One bad expression parse-killed the ENTIRE file - the map booted with no script at all: 'Couldn't parse maps/m6l2a.scr', then ' | println( ... ) with parens. IMPORTANT GAP THIS EXPOSED: depthscan2, linecheck and quotecheck ALL passed the broken file - they check brace depth, line shape and string termination, none of which is expression syntax. A live boot with develo |
| `bug-1746` | user: 'lets not require you to take a callsign... all you have to do now is use the radio itself not kill the guy' | Objective 2 was 'Take the Signals Officer's call sign', completed by KILLING him, with the transmission as a separate objective 3 at the same location. Two objectives and a mandatory kill for what is one action at one place - and it forced  | Objective 2 becomes the transmission itself, text 'Transmit false orders from the radio set to divert the town's reinforcements.', marker on the set, completed when the transmission finishes. Slot 3 retired. Removed all three coop_m6ObjCall |
| `bug-1742` | piano guard still bypasses body to investigate the radio - after bug-1740 was supposed to fix exactly that | bug-1740 fixed only half the problem, and the wrong half. It stopped coop_m6l2aRadioFind from PICKING a man who already had eyes on a corpse, but nothing re-evaluates an errand once it has begun. The RADIOFIND probe added in that fix is wha | New coop_m6l2aRadioBodyAbort watcher threaded alongside the walk, polling the same live coop_bustEyesOn stamp the picker uses so the two halves cannot disagree. On a fresh stamp it sets coop_radioAbort and re-issues walkto at the actor's CU |
| `bug-1740` | instead of stopping at the body the piano officer walked past him to the radio I turned on, even though it said he was investigating the body - radio seemed to take precedence again | coop_m6l2aRadioFind picked purely on alive + type_disguise officer/sentry + nearest within 1200. Nothing told it that its best candidate was at that moment looking at a corpse, so a radio distraction could pull the investigator straight off | bust.scr's body sweep now stamps local.a.coop_bustEyesOn = level.time every 0.5s tick for any actor who can currently see the corpse - a timestamp, not a boolean, so it expires on its own when he looks away and needs no second array walk to |
| `bug-1717` | Script Error : Cannot set a read-only variable (patrol watchdog) | Read local.a.enableEnemy to test whether an actor was mid radio errand. enableEnemy is SETTER-ONLY: writing is legal, reading throws once per tick. | Track our own readable flag (coop_onRadioErrand) set and cleared by coop_m6l2aRadioGo. |
| `bug-1715` | Script 'maps/m6l2a.scr' was not properly loaded - whole map ran with no script | A comment line lost its // prefix (written as tab + 'here becomes...'). Raw prose in code position - syntax error, unexpected TOKEN_COMMA - which kills the compile of the WHOLE file. All three scanners passed it: none asks whether a line is | Restored the //. Swept for other prose-without-// lines (none). Scanner gap: linecheck.py should reject a non-comment line that parses as prose. |
| `bug-1713` | Script file compile error: Couldn't parse 'maps/m6l1c.scr' - syntax error, unexpected TOKEN_LEFT_SQUARE_BRACKET at (maps/m6l1c.scr, 1842): local.p = [local.i] | SELF-INFLICTED, and a trap worth keeping. A patch was applied via `python -c "..."` in DOUBLE quotes, so bash expanded $player to the empty string before Python ever saw the source. `local.p = $player[local.i]` reached disk as `local.p = [l | Restored the two lines. Standing rules from this: never put morlang source containing $ inside a double-quoted shell command - use a Write-tool script file or a single-quoted heredoc; and after deploying any map script, check the server log |
| `bug-1656` | after talking to a player the m2l2a scientist returns to a spot noticeably further from his table than where he started - 'looks funky' | sciwalkto walks him to $table via global/walkto.scr -> moveto, which pathfinds to the NAV NODE nearest the table rather than to a fixed spot. His first trip (scientistidle, at map setup) starts from his spawn; the trip after a conversation  | remember the first arrival (level.coop_sciSpot / coop_sciAngles) and correct back to it on later arrivals, but ONLY within 160u so it is a sub-stride nudge rather than a visible teleport; beyond that he is left where the pathfinder put him. |
| `bug-1652` | a papers checker opened fire on the player and NOTHING escalated - no alarm, no other actor aggroed (user: 'you'd think that would set everyone off') | MEASURED this run. The player-disguise guard is evaluated against the PLAYER's is_disguised only, with no term for 'a german is already shooting at him'. At 00:07:16 ALARMTRIP fired with enemyDisguised=1 and our guard blocked it (ALARMTRIP  | OPEN - Phase C design input. No Phase A change; recorded before implementation so the T5 exclusion is revisited deliberately rather than discovered in a playtest |
| `bug-1643` | weldinginterupt's re-entry guard is dead code | spelling mismatch: the guard tests self.inturpupted (:1255) but the flag is set as self.interupted (:1259), so the guard never sees its own write | align the spelling (planned inside Step 22b); also cower (:1214-1223) has no guard at all and ends in a raw self attackplayer - NOT YET IMPLEMENTED (found by master-plan vetting round 3, not by a playtest) |
| `bug-1608` | m2l2b: the bomb nearest the manifest could not be planted AT ALL after the manifest gate was added. | Both plant triggers are trigger_useonce in the BSP (setthread plantthebomb / plantthebomb2). The new manifest gate refused by simply printing and ending - which CONSUMED the single shot. Walking up to a charge before taking the manifest per | The gate now re-arms its own trigger ( / triggerable) before ending, turning a one-shot refusal into a retryable one. General rule: never end out of a trigger_useonce thread without re-arming, or the refusal is permanent. [SUPERSEDED 2026-0 |
| `bug-1558` | Self-inflicted: a scripted replace patched the WRONG label - coop_flak88_spawn_defender instead of coop_rear_spawn | Both labels contain the identical line `local.sol exec global/enable_ai.scr`, and the edit anchored on that line alone. The assault troops that runto the player were being registered into the rear-garrison roster, inflating the backline tot | Anchored on a longer unique span (the line plus its surrounding block), removed the misplaced registration, restored it in coop_rear_spawn, verified the enclosing label programmatically rather than by eye. |
| `bug-1557` | User: 'I just completed the mission but im not convinced i actually cleared the backline of all enemies, I just destroyed the flak' | The backline objective completed on a DEATH COUNTER: coop_rear_dead incremented from a per-actor waittill-death watcher, compared against coop_rear_total which only incremented on a SUCCESSFUL spawn. Any actor that failed to spawn, landed i | Replaced counting with a LIVE HEADCOUNT. coop_rear_spawn now stores each actor in level.coop_rear_actor[]; coop_rear_headcount polls that list every 2s, counts isAlive, and completes only at zero with the guns already down. The remaining co |
| `bug-1500` | t3l2 could never be measured: every attempt showed 4 connected spectators, 0/104 triggers, only 2 script labels ever ran, walker never started. Survived every fix that unlocked m2l2a/e1l4/t2l3 and bur | OFF-BY-ONE-POSITION IN OUR OWN HARNESS CODE. t3l2 is index 54 of 54 in the rotation. coop_maptest_tick's end-of-list check (if local.next == "" -> print MAPTEST_COMPLETE, end) sat ABOVE the Phase-3 covwalk block, so for the LAST map in the  | FIXED 2026-08-06, deployed. The Phase-3 covwalk block is hoisted ABOVE the end-of-list check (it uses nothing from local.next - data-flow clean, reviewer-verified; coop_maptest_skip has zero readers so stuffing it later changes nothing). MA |
| `bug-1483` | e2l3: 6-error cascade per tank at FinalHouse.scr:335-342 UPDATE 2026-08-06 (live verification pass): the FindTrigger guard I added stops the 6-error cascade INSIDE activateTank, but does NOT stop a NE | activateTank has no NIL guard on the result of global/FindTrigger.scr::FindTrigger, which falls through to 'end local.returntrigger' with the variable unset when nothing matches. | PARTIAL - do not treat as closed. The FindTrigger NIL guard (maps/e2l3/FinalHouse.scr:334-343) is deployed and does stop its own 6-error cascade, confirmed live. NOT done: none of the 10 call sites (lines 281-331) guard their use of activat |
| `bug-1470` | 118 triggers never fired across 31 maps; entity-key analysis showed only 11 had any gate (10 MONSTERS-only, 1 facing angle) - the other 107 had no reason not to fire | The walker visited ONE point per trigger: the bbox centroid. Two flaws. (1) A brush trigger's bounding-box centre can lie entirely OUTSIDE the volume for L-shaped, hollow or wrapped brushes, so the teleport missed the trigger completely. (2 | Manifests rebuilt with WORLD bounds (origin + model bounds) for 4,334 brush triggers. Walk lists now sample up to 5 interior points per trigger - centre plus 30%/70% along any axis longer than 192 units - and stand near the volume FLOOR rat |
| `bug-1467` | m5l3/m3l3 restart loop survived FOUR suppression fixes; the reload always landed right after COVWALK START, and none of the guards ever logged a hit | The walk list itself. bug-1464 excluded level-transition triggers from the FORCE lists but not from the WALK lists, and the walker teleports a player INTO each trigger volume - so it dropped a player straight into trigger_changelevel 'level | Walk-list generator now applies the same exclusion as the force lists (class changelevel/exit, or name/setthread matching missioncomplete\|missionfailed\|briefing\|endlevel\|levelchange\|transition): 13 transition triggers excluded, 1924 ke |
| `bug-1464` | m3l3 restart loop persisted after both missioncomplete doors were guarded - restart fired the instant COVWALK FORCE BEGIN ran | THIRD door, and this one was mine. The ENGINE-side forcealltriggers skips level-transition classes, but the generated SCRIPT-side named-trigger force lists had no such filter - so covwalk::forcefire happily fired m3l3's transition trigger b | Force-list generator now excludes any trigger whose CLASS matches changelevel/exit or whose setthread/targetname matches missioncomplete\|missionfailed\|briefing\|endlevel\|transition. 9 transition triggers excluded across the trilogy, 900  |
| `bug-1463` | Sweep: m3l3 reloaded 7 times in 10 minutes then MAPTEST_PAUSED 'no player connected' (rcon showed 4 clients connected the whole time) | bug-1452 suppressed mission end in coop_mod/missioncomplete.scr, but that is only ONE of the two doors: many maps - m3l3 among them - exec global/missioncomplete.scr directly. The sweep's force-fire therefore kept triggering real mission tr | Same guard added to global/missioncomplete.scr (no-op + COV MISSIONCOMPLETE_SUPPRESSED log under coop_maptest 3). Lesson: when suppressing an engine-facing path for a test harness, enumerate EVERY entry point - the coop wrapper and the reta |
| `bug-1452` | User: 'I see one screen in main menu and another sitting at the mission complete screen' | forcealltriggers fires EVERY trigger in the level, which necessarily includes the mission-end trigger. missioncomplete.scr::main then ran the real end sequence: clients were thrown to the MISSION COMPLETE screen, and from there to the main  | missioncomplete.scr::main and replace.scr::missionfailed early-out while coop_maptest 3 is active, logging COV MISSIONCOMPLETE_SUPPRESSED / COV MISSIONFAILED_SUPPRESSED - which doubles as a completability datapoint (this map CAN reach missi |
| `bug-1440` | Sweep Pass 1 launch: black screen, no MAPTEST/COVWALK output despite coop_maptest=3 confirmed via rcon | coop_maptest_tick has TWO cvar guards; the phase-3 patch widened only the mid-tick stop check. The ENTRY guard (!= "1" -> end) killed the tick silently before its first println. Compounding: the run monitor waited for a walker heartbeat tha | Entry guard widened to accept "3"; monitor rebuilt with a stall detector (no map advance within 6 min = STALLED event) so silent failure becomes a visible event. |
| `bug-1407` | User: 'the collission for these long sandbag semi circles is still off' | props.scr reused cover.scr's arc offsets - exact for the SMALL semicircle - and stretched them onto the LARGE model with one uniform coop_propArcScale of 1.5. The TIKIs' QUAKED blocks give the true bounds: small (-32 -80 0)(56 88 56), large | Per-axis map: x=(x+32)*1.363636-56, y=(y+80)*1.190476-96. Collider half-width 20 small / 26 large. coop_propArcScale demoted to a fine-tune multiplier defaulting to 1.0. NOTE: a sweep of all 168 trilogy BSPs found the model placed in NONE o |
| `bug-1405` | User: 'even with the jeep gate removed i cant walk thru it' | A gate is TWO things: the visible func_rotatingdoor pair AND a separate player-clip brush entity. Bunker2.scr:416 removes the clip ($clipB2Gate remove) when the gate opens - a beat inside the objective chain the arena never runs - so deleti | arena_clearGateClips removes $clipB2Gate at map start, guarded on BOTH NULL and NIL. User confirmed walkable. |
| `bug-1396` | User: 'I removed the gate entities. I still cant open or remove this door though' (.30cal room door, e3l4 arena) | The door is not an entity. Parsing the e3l4_arena.bsp entity lump (lump 14, 4519 ents) shows 28 door/gate entities map-wide: nearest to the arena checkpoint (-82 -5200 362) is 'jeepgate' at 1170u (already removed by the user), then nothing  | Gated arena_unlockDoors behind 'coop_arenaUnlockDoors 1', default OFF. The blanket unlock could never have affected this door and was pure risk. Kept as opt-in because the Init* threads do lock frontdoor2/librarybackdoor/armoireroomdoor/jee |
| `bug-1377` | e3l4 Bunker2 total mission softlock: the defend-the-bunker wave gate can never be satisfied, so the gate never opens and the map cannot be completed. | VANILLA TYPO at Bunker2.scr:649 (retail maintt/pak1.pk3 maps/e3l4/Bunker2.scr:582 is byte-identical): '$bunker2parade.totalguys = local.wave1 + local.wave2 + wave3 + 5' - 'wave3' is missing its local. prefix. A bare identifier in expression | Added the local. prefix (totalguys = 40). Then bounded EVERY remaining unbounded 'while (...){ waitframe }' in the file - 9 in total: the wave gate, the two MG-node approaches (a coop player standing on the MG node blocks the runto forever) |
| `bug-1369` | e3l4 ROOT CAUSE 2 of 2: global/jeepanim.scr::PassengerGetInJeep dies on its first executable line on this map, so gotSupplies could never pass 1 even with pathing healthy. | jeepanim.scr:44 does `gettagposition "passenger_enter0 1"`. That tag exists on models/vehicles/jeep/jeep.skd (the AA jeep) but NOT on models/vehicles/willys/baseframe.skd, which is what e3l4's $startjeep (vehicles/willys_playerturret.tik) u | Added maps/e3l4/Bunker1.scr::passengerGetInWillys - a faithful copy of the vanilla routine with the correct "passenger_enter" tag, so the walk-in animation is preserved (unlike forceCrewIntoJeep, which teleports and stays the failsafe-only  |
| `bug-1335` | user: 'Wait whats that incoming' fires too early - supposed to follow the 'glad to see you made it' line | JP2705 is the NEXT line of the retail rendezvous conversation (the interruption to JP2704's mid-sentence 'We--'), not a final-battle line; I had placed it at StartEndBattle | moved: chained 4.5s after JP2704 in dialog_over_here; removed from StartEndBattle. PH2904 confirmed correct at the rail-bridge beat by the user |
| `bug-1291` | [user feature] 'when the player gets really low health they should start playing the same limp animation the actors do, and you should see the limp in first person (camera should imitate that as you m | Feature, not a defect. Designed by a 9-agent research+critique pass, then materially REDESIGNED after the critiques' central objection turned out to rest on a false premise (bug-1290): the proposed 'peak health this life' signal was said to | SERVER (game.dll) is the single authority. Player::TickLimp - health/max_health < coop_limpStart (0.30), on the ground, not dead/downed/vehicle/turret - sets m_bCoopLimping, and stuffs coop_limpView to the OWNING client ON CHANGE ONLY. Call |
| `bug-1289` | [user] t3l1: 'Again i can move in intro, im stuck there for a bit... you should be frozen until that open truck gets destroyed really' | NOT a broken lock - the bug-1286 watchdog works exactly as intended, and the probe proved it: heartbeats every 0.5s continuously from ~7.75s through release at t=18.250, i.e. the full authored 10.5s with no gap. The user's 'im stuck there f | Hold the lock to the beat the user actually wants. gags/t3l1_enemyspawn.scr::JeepExplosion sets level.coop_introTruckBlown = 1 immediately after the explosion model is spawned and playing (so the player is still frozen while they watch it g |
| `bug-1287` | [user request] t3l1: 'It would be nice to give the other 4 russians a bit more HP at the beginning so they dont die so easily' | Not a defect - a balance consequence of bug-1282's correct fix. The soviet-ally exemption in aihandler.scr::initialisePainVars deliberately keeps Soviet actors OFF the coop damage buffer so scripted deaths land as authored. Right for the tw | Set $startingsoldier1-4 .health = 250 alongside the map's own 'gun' assignments (t3l1.scr:222-225), where the map already configures these actors. Verified safe: the soviet branch of initialisePainVars sets coop_actorStopPainHandler and 'en |
| `bug-1218` | ^~^~^ Could not find label 'level_end_trigger' in 'maps/m3l2.scr' | A Trigger entity does setthread 'level_end_trigger' but maps/m3l2.scr contains no label of that name - only '$level_end_trigger remove' (an entity targetname of the same string). The trigger's thread can never run. | FIXED. Added the level_end_trigger label to maps/m3l2.scr. The BSP entity is a trigger_multiple named level_end_trigger with "setthread" "level_end_trigger" and "wait" "3" (map_entities/m3l2_entities.txt:1937), so it had always been calling |
| `bug-1184` | m1l1 truck-bed allies render CONTORTED/shredded - limbs stretched into spikes, faces flattened (user screenshots shot0036-38). Captain renders correctly; the three privates do not. | OPEN - not yet identified. What IS established: (a) this is skeletal/pose corruption, NOT an angle error, so the earlier 'upside-down' framing and the +180 roll correction applied for bug-1173 were both wrong; (b) the 39 'Channel named <X>  | REVERTED my own unverified +180 roll correction on guy01/guy2/guy3 (bug-1173) - it was based on a gimbal-lock theory I never confirmed, the live result is contortion rather than a clean rotation, and the untouched Captain still renders corr |
| `bug-1066` | reconnect_sweep.ps1 reported REJOIN-FAIL on all 54 maps (rejoin CS_ACTIVE count never increments) despite reconnect working in resilience.ps1 | The sweep drops P4 mid-map via Stop-Process (force-kill leaves a stale player4_home OpenMoHAA.pid), then RELAUNCHES P4 with +connect WITHOUT purging that pid. The relaunched P4 hits the blocking Abnormal-Exit modal dialog (sys_main.c:255, s | Purge player4_home OpenMoHAA.pid right before the mid-map P4 relaunch in reconnect_sweep.ps1. Note: the crashAfterDrop=False result across 47 maps IS valid (server survives client drops, 0 crashes); only the rejoin metric was bogus. Re-run  |
| `bug-1027` | LIVE-BOOT AUDIT: e3l4 coop boot: 'Script maps/e3l4/outro.scr was not properly loaded' + 251x binary-op-on-none cascade. BT campaign ending/credits broken. | SUPERSEDED - the compile failure no longer reproduces and the recorded cause ('outro.scr fails to compile/load') is stale. Fixed incidentally by one of the 2026-08 e3l4 rounds (bugs 1379/1382/1384/1398/1426/1427), which is why nothing ever  | VERIFIED CLEAN 2026-08-17 by dedicated boot with developer 1 (compile errors are gated behind it). A playerless dedicated boot does NOT reach the script: e3l4.scr threads Outro.scr at line 44, AFTER waitForPlayer on line 28, so the file is  |
| `bug-1022` | COOP-CONVERSION AUDIT: t2l3 DEAD ON LOAD in coop - $mortar_turret0 (t2l3.scr:110) runs on unspawned addon turret, NULL-listener throw kills map main thread before begin/objectives/waves. t2l3.scr:542  | Self-converted map references addon $-names only the SP engine spawned; no e3l3-style shim. | PROPOSED (_research/coop_conversion_audit.md): spawn native granatwerfer + PanzerIV/halftrack/truck stand-ins at BSP origins, or guard the blocks. :110 CONFIRMED, :542 suspected. |
| `bug-950` | walls still felt at spots INSIDE deployed strip zones ('basically the same spots'); west canyon wall run also blocked (outside zones) | TWO parts. (1) PREDICTION GAP: the zone strip lived only in server pmove (player.cpp); cgame prediction kept stock MASK_PLAYERSOLID, so the client predicted the covered wall and fought the server = mushy still-there walls inside zones. This | cg_predict.c mirrors the coop_clipStripZones strip (PLAYERCLIP\|FENCE) using the shared cvar store (listen host); canyonW zone widened into the full west-wall run (-6700 -5250 -4950 -1700 70 1100, 20 brushes contained, 0 visible fences); en |
| `bug-910` | unknown command: getmins / (coop_mod/coop_placements.scr, 209) / Couldn't compile 'coop_mod/coop_placements.scr' -> baked cover objects missing on ALL maps (m1l1, m1l2a, e1l2) | getmins/getmaxs are registered EV_GETTER events (entity.cpp:680) - property syntax only (local.e.getmins). The framedefer solidity refactor used COMMAND syntax (local.e getmins), which the compiler rejects as unknown command, parse-killing  | Property syntax at all 3 sites: local.mn = local.e.getmins / local.mx = local.e.getmaxs (m1l1, m1l2a, e1l2 blocks). |
| `bug-873` | e1l2 (Hideout.scr) coop: exit doors $opendoordoor re-lock on the FIRST player crossing $deletetrigger4, sealing a lagging player inside the munitions bunker seconds before the finale radiusdamage (aud | At Hideout.scr:462-463 the ObjectiveEscape end-sequence did `$deletetrigger4 waittill trigger` then immediately `$opendoordoor lock`. $deletetrigger4 (trigger_multiple at 4320 4320 496) fires on the FIRST player to cross; in coop that re-lo | Before the `$opendoordoor lock`, inserted a coop approach-gate that holds the re-lock until no live player remains near the doors: `while (waitthread coop_mod/replace.scr::withinDistanceOf $opendoordoor 600){ waitframe }`. Radius 600u cover |
| `bug-872` | e1l2 (Hideout.scr) coop softlock: mission never completes - DoOutro approach-gate at line 471 loops forever, outro dialogue + fadeout + exec global/missioncomplete.scr e1l3 never run (audit E-01, P0). | The [205] coop port swapped the SP `while (vector_length($player.origin - $commander.origin) > 800)` test for `while (waitthread coop_mod/replace.scr::withinDistanceOf $commander 800)` but forgot to negate it. replace.scr::withinDistanceOf  | Added the missing negation: `while (!(waitthread coop_mod/replace.scr::withinDistanceOf $commander 800))`. This matches every other coop approach-gate in the mod (e1l3/Boating.scr:210,227; e3l2/prisoner_section_1.scr:294; e1l4/PostShip.scr: |
| `bug-818` | REGRESSION: end-of-mission rank-up debrief card skipped / 'moves on too fast' at REAL mission ends (user: 'we were only supposed to change that for the briefing missions'). Introduced by bug-804. | NOT a cross-map guard leak (ruled out with evidence: qconsole.log prints 'Service record:' from xp_identify on EVERY map load = player flags wiped each map, and the transition log shows 'Unloading Game'->'Trying to load game.dll'->InitGame  | coop_mod/missioncomplete.scr::main: added an idempotency guard at the top - if(level.coop_missionCompleteRan==1){end}; level.coop_missionCompleteRan=1. Only the FIRST completion runs the debrief + the single bsptransition; a duplicate fire  |
| `bug-788` | User: 'the rank up timer is on in the briefing and it's adding more time to skip than just the 5 seconds' - after the all-ready skip on a briefing map the XP debrief card runs anyway | Briefing maps advance via endbriefing -> exec global/missioncomplete.scr -> coop_mod/missioncomplete.scr::main, which unconditionally waitthreads xp.scr::xp_summary before bsptransition. xp_summary blocks ~coop_xpSummaryTime (28s, autoexec) | coop_mod/missioncomplete.scr: skip xp_summary entirely when level.coop_mapname containsText 'briefing' (bug-722 guard pattern) or level.coop_lobbyMap==1; maptest skip folded into the same local.coop_skipDebrief flag. xp_flush still always r |
| `bug-752` | Wrong reference: 739 should be 750 | Used "739" instead of "750" | Changed 739 â†’ 750 |
| `bug-749` | Incorrect value in code | Had "spectator" | Changed to 's compile - condition must be one line if( loc |
| `bug-624` | e1l2 invisible wall blocks the side path after leaving the intro jeep | InitLevel spawns 9 solid script_object clip brushes (clip1-9, a retail dev-patch guardrail marked 'TODO: test') that are NEVER removed - they permanently wall the side path once players dismount. | playerExitTank (Intro.scr) now removes $clip1..$clip9 when control is handed back (intro glues players to the jeep anyway, so the guardrail is moot post-intro). |
| `bug-319` | stamina runs out mid-sprint in 3P -> player drops to WALK animation at run speed (skating) | Shift = sprint key means BUTTON_RUN arrives CLEAR while Shift held (legacy walk semantics). The speed branch kept run SPEED via the coop_sprint fallback, but the legs statemap RUN conditions still saw the clear bit and picked WALK anims. | After TickSprint in ClientThink: when coop_sprint is enabled and BUTTON_RUN is clear (and Alt COOPWALK not held), re-assert BUTTON_RUN in last_ucmd so statemap + ClientMove both see RUN. |
| `bug-313` | Server crash while peeking from cover: 'Possible infinite loop in state AIM/STAND/COVER_TORSO' ERR_DROP | peek routed through the VANILLA AIM state ('AIM : COOP_COVER_PEEK'); AIM's own exit web bounced AIM->STAND->COVER_TORSO->AIM within one evaluation pass = engine infinite-loop guard kills the server. LESSON: custom features must not transiti | new cover-owned COVER_PEEK_TORSO state (aim posture via torso-twist, AIM's fire-dispatch rows copied, exits ONLY to COVER_TORSO/!peek or STAND/pose-gone); COVER_TORSO peek edge now targets it |
| `bug-307` | Server crash on blind fire: 'Can't find player animation coop_blindfire_wall_r' -> 'Possible infinite state loop' ERR_DROP | TWO layers: (1) user's test at 23:03:43 raced the alias-relocation deploy (pk3 landed 23:04:13 - tested the OLD pack; the relocation itself was correct); (2) a missing anim in an IN-GRAPH statemap row is FATAL (infinite-loop stopper ERR_DRO | fire-frame block (1/3/5/7 server fire) added to coop_blindfire_wall_r inside the section; deploy-completion now announced with a timestamp so tests can't race packs. LESSON: any statemap-referenced anim MUST resolve or the server dies - alw |
| `bug-299` | Server crashed at map load: player_Legs.st Unknown condition 'COOP_COVER' line 57 (ERR_DROP) | CONCURRENT-AGENT DEPLOY RACE: the cover-system agent edited the statemap (ships in code.pk3) while I ran build.ps1 for the XP deploys - pk3 picked up its half of the feature before its engine-condition registration was in the exe I had buil | rolled FORWARD: rebuilt exe after the agent's player_conditionals.cpp registration landed (conditions now exist, return false until wired = inert), redeployed. RULE: while any agent has the MOD TREE checked out for edits, every build.ps1 ru |
| `bug-062` | Wrong reference: solid should be notsolid | Used "solid" instead of "notsolid" | Changed solid ÃƒÂ¢Ã‚Â†Ã‚Â’ notsolid |
| `bug-055` | t2l2 seated passengers: (a) take no damage, (b) stuck in crouch can't stand, (c) truck can't drive with a passenger | (a) glued passengers were set notsolid = bullet traces skip them (only turret gunner stayed solid). (b) CROUCH_IDLE->STAND statemap transitions all require CHECK_HEIGHT 'stand'; CondCheckHeight stand-up G_Trace startsolids for a pinned seat | Passengers set solid after duckableglue (take damage). Truck set notsolid like halftrack (solid passenger no longer blocks drive; vehicle can't be crushed by/collide with passenger). Engine: CondCheckHeight returns true early for m_pGlueMas |
| `bug-053` | Wrong reference: nodamage should be takedamage | Used "nodamage" instead of "takedamage" | Changed nodamage ÃƒÂ¢Ã‚Â†Ã‚Â’ takedamage |
| `bug-048` | Wrong condition in logic | Condition was: if ( level.coop_dbgSeat >= 5 ) | Changed to: if ( level.coop_dbgSeat >= 2 ) |
| `bug-044` | Wrong operator: != should be == | Used "!=" instead of "==" | Changed operator != ÃƒÂƒÃ‚Â¢ÃƒÂ¢Ã‚Â€Ã‚Â ÃƒÂ¢Ã‚Â€Ã‚Â™ == |
