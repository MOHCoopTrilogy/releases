<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# UI, HUD & menus

[<- back to all fixes](../BUGFIXES.md)

**220 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-2389` | Earlier user ask, found unfulfilled by the audit sweep: 'cap challenge targets at about 750 rather than 1000 - 1000 for any challenge is too much.' | The cap was applied to the GENERATOR only. Four hand-written chal_def lines were never touched and still shipped at 1000, 2500 and 5000 - one of them sitting on the literal 1000 the user objected to. | q_500/q_1000/q_2500 are an escalating LADDER, so they are re-spaced 250/500/750 rather than all flattened to the cap - three identical rungs would have been a worse answer than the original complaint. fac_heer_3 to 750. Descriptions rewritt |
| `bug-2352` | user, twice: 'also the objective menu isnt updating correctly with all new objectives' and later 'the survive objective doesnt appear in the objective menu'. | GENERAL, NOT PER-MAP, AND IT AFFECTED EVERY REGISTER-THEN-ACTIVATE MAP. add_objectives threaded the toast from the TOP of the function, where local.objective is still exactly what the caller passed. Maps register an objective's text at stat | Toast moved below the read-back switch, with a NIL/empty guard. One edit, and it restores on-screen objective announcements on every map that uses the two-step form, not just Omaha. |
| `bug-2349` | user: 'the survive objective doesnt appear in the objective menu' | SUPERSEDED BY BUG-2352 - and my first fix was wrong. I changed coop_landingObjective to the two-step form (register text at status 1, activate at status 2) because the smoke objective uses it. The audit then showed the smoke objective's tex | See bug-2352. The per-map two-step change is retained because it is correct once the general defect is fixed. |
| `bug-2337` | user feature request: 'a new challenge called "Not the Actual Events" Signal the bomber to provide concealment to reach the shingles. triggered by calling the signal radio on omaha.' | n/a - new content. | chal_def 'cc_bomber_smoke' campaign, target 1, reward models/coop_helmets/coop_helmet_29thnet.tik (an asset already proven present). The stat is written from exactly ONE place - a chal_team_bump at the coop_smokeSignalDone line in coopified |
| `bug-2322` | user: 'I think the first objective should read "Survive the beach landing" once we land in water it becomes "Signal the smoke barrage."' and 'Get to the shingle objective should be triggered after the | FEATURE. Constraint found while implementing: global/objectives.scr is a literal switch with cases 1-8 and NO default, and index 0 underruns (EventAddObjective subtracts one with no lower bound, so it writes below the objective configstring | Slot 1 is REUSED with a text swap, which is what the user described anyway: coop_landingObjective raises 'Survive the beach landing.' at the boat ride, pointed at the waterline (a real location matters - a zero vector makes the engine fall  |
| `bug-2222` | Prototype B's own board key read 52 MAPS and 'ALLIED ASSAULT - 6 missions, 32 maps'; mission 6's chips ran b, d, c, e left to right. Prototype C had no loader cfg at all. | The aggregate counts were written by hand while the chips themselves were generated, so the two drifted - the board actually places 53 chips across the trilogy and 33 in Allied Assault. The m6l3c and m6l3d rects were transposed. C was opene | Counts corrected from the chip commands themselves; m6l3c/m6l3d rects swapped; new ui/proto/coop_proto_c_open.cfg matching A and B - loads both pages, blanks coop_protoTitle/Sub/Art, re-seats 'main' on the stack so BACK has somewhere to lan |
| `bug-2200` | i dont think the smoke objective text worked at all, i also dont see the objectives appearing on the screen per usual here (m3l1a, user playtest) | My own regression. To make the smoke objective sort FIRST I moved it to index 0, reasoning from the engine HUD, which does iterate from CS_OBJECTIVES + 0. That was the wrong HUD. The objective panel players actually see is the coop one, and | Renumbered instead. The coop panel has exactly 8 slots and retail uses 7, so the smoke objective takes 1 and the retail chain shifts 1-7 -> 2-8: 26 call sites in m3l1a.scr, 2 in coopified.scr, plus the three challenge keys that match on the |
| `bug-2177` | Teammate radar was direction-only: every out-of-PVS teammate drew pinned to the rim regardless of actual distance | SV_PackNonPVSClient normalised the teammate delta to a UNIT vector whenever length > 0 - which is always. x,y are already (delta / com_radar_range), so the normalisation destroyed all distance information before it reached the wire. CL_Unpa | sv_snapshot.c: clamp instead of normalise - only divide by length when length > 1, so anything inside the radar keeps its true position and only out-of-range mates pin to the rim. Bit layout unchanged (msg.cpp:3380), so no client-side decod |
| `bug-2175` | Objective toast wipes the team-revive HUD mid-revive - reviver holds [USE] with no prompt and no progress bar for the rest of the bleed-out | objectives.scr::coop_obj_toast owns HUD slots 135-142 and tears the whole range down unconditionally when it finishes. Its guard is one-directional: it waits at the top only if somebody is ALREADY down when the toast starts, so a player who | dbno.scr: the monitor loop now watches level.coop_objToastBusy and clears local.shown on its falling edge, forcing one full re-issue of text and alpha on the next tick. Worst case is a 0.1s flicker instead of a permanently blank HUD for a 9 |
| `bug-2169` | the '[DOWN] <name>' teammate label is a single global slot, drawn once and never redrawn | THREE defects in one feature, all invisible in single player. (1) dbno_marker_manager wrote HUD slot 31 as a hard-coded global constant, so with two men down the second overwrote the first's label, and reviving EITHER blanked the label for  | Per-downed-player slot chosen from the only free slots left in the 27-39 DBNO band (30/36/37 alongside 31 - 27,28,29,32,33,34,35,38,39 are taken), which is exactly enough for four players, with labels stacked vertically by player index so t |
| `bug-2168` | NEW OBJECTIVE toast and the DBNO team-revive HUD fight over slots 135-142 (bug-1680, reported 2026-08-10, never repaired) | objectives.scr::coop_obj_toast owns 135 (heading), 136-141 (computed 136+line) and 142, and blanks 135-142 on teardown. dbno.scr's team-revive channel owns 135 (reviver text / 'Hold [USE] to heal'), 136-137 (reviver bar), 138 (downed text), | Relocation was not available: _research/hud_slot_map.md records the >=100 fade-exempt band as full ('Genuinely free: Nothing'). Made them disjoint IN TIME instead - coop_obj_toast now waits, up to 12 s, while any player has coop_dbno_active |
| `bug-2137` | build break: error C2601 'Player::TickCoopStress': local function definitions are illegal (x3) | COMMENT SWALLOWED A CLOSING BRACE - the same trap this project has hit repeatedly. A scripted edit appended a trailing // comment to a one-line body 'if (!pHold) { pHold = gi.Cvar_Get(...); }', producing '...); // comment }' - the brace end | Comment moved onto its own lines above the statement, brace restored. Diagnosed with a running brace-depth scan over the function - the same technique the .st depth scanner uses, and the reliable way to find this class of break, since the e |
| `bug-2107` | C2601 'local function definitions are illegal' - twice in one session, from a generated edit. | A scripted edit appended a // comment to a single-line `if (!p) { p = Cvar_Get(...); }`, placing the comment BEFORE the closing brace so // swallowed the `}`. Braces then balanced one short and the next function definition became 'local'. B | Put the trailing comment AFTER the closing brace. Diagnose with a running-depth scan reporting depth at each column-0 definition - it names the first function seen at depth != 0, and the true culprit is the edit just above it. |
| `bug-2098` | Upscale-coverage audit reported 'UI/menu - 641 still retail' with mohmenu/voodoo as the largest single block (241 files). Acting on that as written would have been wasted work. | `textures/mohmenu/voodoo/` is the LEGACY 3dfx Voodoo low-resolution fallback set. Verified rather than assumed: 70 .urc files reference it, INCLUDING the mod's own advancedoptions.urc / audio options.urc / main.urc - but every single voodoo | No change. Recorded so the next upscale pass does not spend a GPU run on dead assets. The genuinely safe menu targets are the PHOTOGRAPHIC ones only - briefing plates, credits, dmloading, slideshow, roughly 90-140 files - because the rest o |
| `bug-2097` | zzzzzz_co-op_hzm_mod_assets_tex.pk3 was shipping 1,252 *.pre_upscale_nobuild backup files - 200 MiB on disk, 87.3 MiB inside the pk3 - to every client on connect, for art the engine can never load. | The upscale pipelines back originals up as `<name>.pre_upscale_nobuild` (bug-1917: never overwrite a gitignored binary without a backup). That is correct. But build.ps1's only filters were $excludeTop (two dev dirs), $excludeNames (three ex | Added `$_.Name -notlike '*.pre_upscale_nobuild'` to the Get-ChildItem filter, with the reasoning inline. Measured before/after: 7,007 -> 5,755 files, 1,255.67 MB -> 1,168.35 MB. Exactly 1,252 files and 87.32 MB, matching the audit's predict |
| `bug-2079` | 3 Elite weapon challenges award models/player/american_army.tik, which player.scr:1319 calls the 'free ungated roster default' - so they pay nothing. | cosmetic_gatedBuild is an ALLOW-LIST of cosmetics that REQUIRE unlocking; anything absent from it is free to everyone. american_army is deliberately absent (it is the default uniform). Three high-effort Elite challenges - wpn_garand_e 'The  | PARTIAL. Closed the detection gap permanently: docs/tools/check_challenges.py now cross-references every cosmetic reward against cosmetic_gatedBuild and reports NO-OP REWARD, and build.ps1's summary filter was widened so the finding actuall |
| `bug-2078` | USER: 'can you confirm that the Loadout is correct for helmets, guns and uniforms? some unlock conditions said unlock through challenges or rank but don't specify which challenge or rank for unlock co | Three separate lock messages all shrugged the same way and none could do better, because nothing in the codebase joined the two halves of the answer. The requirement for any gated item lives in EITHER challenges.scr (chal_def's 7th arg maps | Added docs/tools/armory_unlocks.py, which sweeps both authorities plus the three item rosters (loadoutroster.scr guns, helmet.scr coop_helmetTik + coop_armorySkins) and the gate allow-list (cosmetic_gatedBuild), joins them, and emits coop_m |
| `bug-2068` | MY OWN REGRESSION, caught by the user asking 'what exactly if anything did we lose feature wise from the fix?' - the rewritten coop_notargetWatch would have silently revoked BUILD MODE's FL_NOTARGET ( | bug-2064's watcher was rewritten to read the flag back and write whenever it disagreed with what the RIDE wanted. That reads as a tidy re-assert and is actually a STOMP: FL_NOTARGET on a player has THREE owners - this watcher, coop_mod/buil | A watcher may assert its own claim and must never revoke anyone else's. want=1 asserts only when the flag is known to be OFF (have==0), or once on the edge when the getter is absent (have==-1, older game.dll) so a stale build can never osci |
| `bug-2067` | [user, live] 'I get my inventory twice, once when I spawn, and then again a few seconds later.' The kit is issued THREE times per spawn on m1l1 (KITSPAWN/KITGIVE x3), two of them carrying an 'Armory:  | THE DEBOUNCE IS NARROWER THAN THE THING IT DEBOUNCES. loadout_resend replays the client's archived picks in TWO volleys, at t+4s and t+15s - eleven seconds apart, and the comment at the commit site says the staggered tokens 'must trigger on | loadout_resend stamps flags['coop_loJoinResend'] on entry and clears it on exit, and loadout_regiveWatch now waits that window out before its existing 1.5s debounce. The coop_loRegivePending latch already guarantees a single watch, so volle |
| `bug-2036` | 'dedicated smoke BROKEN' was carried as a standing fact and shaped a whole session's approach - it is FALSE, the dedicated boot works | A stale note ('dedicated smoke BROKEN -> verify via user listen log') was being treated as current. It caused every verification this session to be deferred to the user, and directly contributed to shipping bug-2034 - a guard that was never | Re-tested empirically instead of trusting the note. omohaaded.exe boots clean on m1l1: reaches 'Server: m1l1', CM_LoadMap, and 'initialising coop weapon loadout for maps/m1l1.scr' - i.e. coop init - with 0 Script Errors and 0 'not properly  |
| `bug-2022` | [user] 'the nebelwerfers do WAY too much damage from insane radius... im deep inside the church and they are somehow hitting me' | coop_churchShellHurt applies barrage damage by PURE 3D DISTANCE (<=320u falloff) with no occlusion test. The loop is hand-rolled specifically so germans can be skipped (radiusdamage cannot filter), but the hand-rolling lost the one thing En | sighttrace gate before the falloff math: (impact + (0 0 40)) -> (player.origin + (0 0 40)), skip the player entirely when blocked - retail radiusdamage semantics (no LOS = no damage). Same +40 chest-lift recipe as officer.scr::coop_spread_p |
| `bug-2015` | [user] 'any idea why I am randomly getting damaged ... i am near the churhc area' then 'it took damage away multiple times until it killed me' | coop_churchShellHurt applied the church barrage with a RAW health write - local.p.health = local.p.health - local.f - instead of the damage event. That bypasses Sentient::Damage entirely, so every piece of feedback a player reads was skippe | Routed through the real damage event: `local.p damage $world local.f $world local.pos local.dir ( 0 0 1 ) 0 0 9 0`, MOD_EXPLOSION=9, with direction = vector_normalize(player.origin - impact) so player.cpp:3705 damage_yaw = dir.toYaw() puts  |
| `bug-1996` | [user] 'Im seeing the texture over my screen problem we once solved before, not sure how we did. But I think we had to turn something off (maybe it was msaa or DOF)' | CG_DrawZoomOverlay initialises bDrawOverlay = qtrue and ONLY the final else-branch ever clears it. The Spy Camera / Binoculars / Bombing Run arms set a zoomType and fall straight through, so their overlay drew whenever that item was the ACT | Gate every zoom type on cg.snap->ps.stats[STAT_INZOOM] before the type select. The generic arm keeps its additional <= 30 test (which distinguishes a rifle scope from a wide binocular fov); the dedicated arms only need 'is the zoom live at  |
| `bug-1994` | audit_menu_cvars.py first run reported 8 DEAD menu controls; all 8 turned out to be false positives | Four blind spots in the first version. (a) Dereferences were counted only across files that themselves mention the cvar API, but registration and use routinely live in different files - r_fastdlights is registered in tr_init.c and read in t | Scan every source file for derefs; index bare Cvar_Get; fold case on lookup; count Info_ValueForKey reads; and treat a pulldown whose entries carry their own command as command-driven rather than dead. Re-run: 84 working, 3 display labels,  |
| `bug-1993` | [user] 'WE should have removed 3D Grass settings from the coop field settings forever ago. Please also make sure all of the field settings are actually functional.' | The 3D grass feature was disabled long ago on the user's own verdict and r_grass defaulted to 0, but the FIELD SETTINGS checkbox was left on the sheet - a control whose only honest position is off. More generally there was no way to tell a  | Removed the 3D Grass label and checkbox and re-spaced the remaining 10 rows on a uniform 30px pitch (they were 32px at the top and 24px at the bottom, crowding the buttons). Added docs/tools/audit_menu_cvars.py, which resolves each menu cva |
| `bug-1992` | [user] 'using middle mouse to switch shoulders in ANY view (behind cover,any third person view) also isnt smooth, camera should move from left to right behind the player so you should effectively see  | TWO faults. (1) The side offset was already eased, so the swap was not a teleport - but easing a lateral offset moves the camera along a STRAIGHT CHORD from +side to -side, and that chord passes through the player's head at the midpoint. Th | Bulge the chase distance by (1 - \|sideSign\|) - zero at either shoulder, maximum mid-sweep - turning the chord into an arc that bows out BEHIND the player, so the swap shows the soldier's back exactly as asked. New cvar cg_adsShoulderArc ( |
| `bug-1991` | [user] slash-key view toggle: 'there is seemingly no difference between the two when you use the / key... but if you enable it in control settings you get locked to the free cam version of third perso | cg_freecam was missing from whiteListedVariables[]. The three-mode cycle in thirdperson.scr (first person -> free cam -> chase) was correct all along and switches modes by stuffing BOTH cg_3rd_person and cg_freecam, but only cg_3rd_person w | Added cg_freecam to the whitelist. Removed the now-conflicting 3RD PERSON FREE CAM checkbox and its plaque from advancedoptions.urc per the user (the toggle should own the choice, not a separate sticky setting), and reset the archived cg_fr |
| `bug-1977` | XP progress bar: the gold fill is massive and does not fit inside the bar - reported repeatedly, 'fixed' repeatedly, kept coming back | The rect arithmetic was never wrong. Measured from the art: the plate groove is rows 28..99 of the 128-tall xpbar_base (fractions 0.219..0.781), and the fill at y33 h5 inside a plate at y30 h12 sits WITHIN that. The rendered result disagree | Stopped fixing it by arithmetic. The channel inset is now baked into the ARTWORK - xpbar_fill and xpbar_shine are rebuilt 128 tall with the gold occupying only rows 28..99 and transparency elsewhere - and the fill is drawn with the IDENTICA |
| `bug-1947` | ui_wiring_audit BUILD BLOCKED: DEAD VSTR coop_lofinprevs1-4 never assigned <- ui/loadout/sNsel.cfg (after wire_mv2 run for MV wave 3) | wire_mv2 section 2 rewrote loadout_finish WHOLESALE from a template snapshotted before the loadout deep-fix wave - re-running it for wave 3 stomped 75 lines of later in-place fixes (coop_loFinPrevS slot re-preview echoes, coop_loSetBusy ser | git-reverted loadoutpick.scr, re-applied only the fid ceiling (13->19 for the 10-variant springfield) as a surgical edit, and RETIRED wire_mv2 section 2 (replaced with a comment; loadout_finish is maintained in-place). The ui_wiring_audit g |
| `bug-1938` | variant button cross-wired into slot 1's ring from any other slot - corrupted slot 1's archived finish; slot re-entry always showed the base gun | Deep-trace viewer-F5/F6/F8.2: all 12 variant-host pages hard-coded coop_loMvPN to the _s1 ring, so a tile inspect on another active slot sent VARIANT clicks through slot 1's chain (whose cfgs seta S1F/FA1); nothing could re-preview an appli | Pages no longer touch the active pointer (owned solely by s<n>sel per slot click; gen_loadout regenerated); server arms coop_loFinPrevS<slot> at the exact gun+fid page on every success (finish/variant/standard/wrap) and s<n>sel replays it l |
| `bug-1936` | rapid armory clicking silently dropped every action after the first - one name-bus token per batch, no close-commit for weapons/finishes | Deep-trace F4/F6: playerNameCommand dispatches the lowest-index token per ~0.75s batch and the name clean destroys the rest; helmets/skins had close-commits (bug-773), weapons/finishes had none - menu and server drifted for the whole sessio | loadout_closeLoadoutCommit replays all 8 archived recipes (A1-4 then FA1-4) at armory close behind token-free polls; NEW idempotent-quiet exits in loadout_setBody and loadout_finish make un-dropped replays silent (also kills the join-volley |
| `bug-1935` | the loadout seed push could permanently overwrite a returning player's archived picks with garand/colt | Deep-trace sync-F9: loadout_seedPushUI's first push (spawn+5s) exec'd the FULL commit cfgs unguarded - their setas overwrote coop_loA1/A3 (the replay recipes) and the vstr re-committed the seed over real picks that landed in between. | Chips-only seed cfgs (plain set - display only, no archives, no re-commit) and the slotId guard on BOTH pushes. |
| `bug-1934` | finishes leaked across resets and gun re-picks - a new gun inherited the old gun's variant fid (model lottery) | Deep-trace F11/sync-F3: loadout_clear and the gun-commit path never touched coop_loFin1-4; reset.cfg never cleared the client half; orphaned FA archives deny-beeped at every join. | clear NILs the fids; a new gun commit resets the slot to standard on both sides (server flag+chip echo, and the generated commit cfgs clear the archived chip+recipe); reset.cfg clears the finish family and inspect registrations. |
| `bug-1933` | denied finishes/variants kept claiming applied in the menu forever; stale fids replayed and denied at every join | Deep-trace F7/F9: every deny path in loadout_finish left the client's already-seta'd chips standing (weapon denies always reverted), and the direct-replay miss for a fid the gun lacks ended SILENTLY. | loadout_finRevert pushes the server's current fid back into both archived cvars on all six deny paths and the silent replay-miss. |
| `bug-1932` | every armory open destroyed the archived finish recipes and slot-card registrations - picks visually vanished, cold-menu slot cards dead | Deep-trace sync-F1/F2: init.cfg plain-set coop_loFA1-4 to empty and stomped coop_loInspectS1-4 at every open; set on an archived cvar keeps the archive flag, so the wipe got SAVED at quit. The server InspectS self-heal cannot reach the disc | The eight wipe lines are removed - the archives are the truth; a fresh profile just no-ops. Server echoes still correct them after every validated apply. |
| `bug-1931` | regiveWatch 'no picks' guard was dead code - a picks-less rebuild handed out the map default kit under an applied toast | Deep-trace F4: '.size == 0' never matches - unset/empty arrays report NIL/-1 (bug-909 comment in-repo). A sidearm-only or cleared loadout slipped through, stripped the registry, and spawnInventory fell to the map-default branch. | Proven replace.scr idiom: NIL test first, only positive size means picks. Also apply-trace F10: the loadoutLocked bail now says WHY ('kits are scripted on this mission') instead of dying silently after a success toast. |
| `bug-1930` | an armory apply made while dead or DBNO vanished - revive re-gave the OLD kit | Deep-trace F3: loadout_regiveWatch ends silently on dead/DBNO (guards run BEFORE the registry strip), the revive chain prefers the untouched coop_inventoryWeapons registry, and nothing ever re-kicked. | The watch now QUEUES (flags coop_loRegiveQueued) instead of dying; dbno's revived-branch re-stamps dirty and re-arms the watch; a full respawn clears the queue since the spawn path reads the same rebuilt list. Also the DBNO sidearm hands th |
| `bug-1927` | the other skins (gold, etc.) are not showing in the viewer when you select them | The 36 fin<fid>_s<slot>.cfg apply files (a one-off strip-builder product, NOT gen_loadout output) archived the pick and sent the bus token but never fired the finish preview - the finp<id>_<fid>.cfg previews existed and coop_loFinP1-7 were  | All fin cfgs now fire their preview: fid 1-7 vstr coop_loFinP<fid>, fid 0 vstr coop_loOpenInspect (back to the standard gun). Wiring-audit note: these files are hand-patched one-offs - folding them into gen_loadout is recorded in OPEN.md. |
| `bug-1926` | you still dont get the updated weapons or weapon variants after you hit done in loadout in game | TWO stacked causes. (1) The regive re-issues coop_loWeapons, which only loadout_rebuild bakes (loadout_finResolve swaps each slot to its finished variant INSIDE rebuild) - and loadout_finish never re-ran rebuild after changing the fid, so t | loadout_finKick now waitthreads loadout_rebuild before stamping dirty; the 120s gate and the coop_loFinKick flag are deleted outright; m2l2a (the real stealth concern) sets level.coop_loadoutLocked = 1, the documented per-map lock the gate  |
| `bug-1923` | it didnt actually update my gear when I hit done (finish/variant applies never reached hands even after the 120s-gate fix) | The bug-1919 fix opened the 120s gate but NOTHING knocked: loadout_finish never stamped coop_loDirty nor spawned loadout_regiveWatch - only loadout_set (weapon picks) did. The FinKick flag sat unused until the next full weapon pick. Worse,  | loadout_finKick helper (cosmetic mark + Dirty stamp + debounced watch spawn) called from all four SUCCESS paths of loadout_finish (standard, finish 1-7, variant, cycle-wrap-to-standard); pre-gate flag placement removed. |
| `bug-1919` | its saying equipped for some of it in the game text but my model isnt changing - finish/variant applies toasted 'equipped' but the carried weapon never updated mid-mission | loadout_regiveWatch refuses ANY regive when level.time > 120 and the player is not in the lobby ('picks saved - they apply from your next mission') - a guard against join-time kit bursts arming disarmed stealth starts. The finish/variant re | loadout_finish sets flags[coop_loFinKick]=1 before its regive kick; loadout_regiveWatch bypasses the 120s bail when the flag is set (a finish/variant swaps a gun the player already carries, so the stealth concern does not apply) and clears  |
| `bug-1918` | user: 'when i try switching variants it forces the switch back' (in-game armory, VARIANT cycling visually reverts instantly) | Stufftext ORDER in loadout_finish's confirm: `set coop_loPrev <variant>` was sent BEFORE `vstr coop_loOpenInspect` - and OpenInspect re-execs the gun's p-page, whose first lines reset coop_loPrev (and coop_loNm) to the BASE tik. The server' | Page first, then overrides, in all three apply paths (finishes 1-7, wrap-to-standard, variants 8-13): vstr coop_loOpenInspect, THEN set coop_loPrev <variant>, THEN set coop_loNm <label> (nameplate now names the variant/finish too - it previ |
| `bug-1916` | (caught pre-ship) finish strip: 14 of 51 guns' finishes would have been silently dead - table lookup missed | Morpheus string-array keys are CASE-SENSITIVE. The generated variant table was keyed on disk tik casing (It_W_Beretta.tik) while the roster stores gives lowercased (it_w_beretta.tik), so level.coop_skinGive[give][fid] returned NIL for every | The emitter now keys the table on the give AS THE ROSTER SPELLS IT (case-insensitive match in Python, emit the roster's exact string). Verified 0 case mismatches, 40/51 exact (the 11 others are bind-only guns not in the armory). Lesson: whe |
| `bug-1908` | user: 'Still seeing NIL when I spawn into the map and I think I have the default - there are script errors in console'. Log: "Script 'coop_mod/loadoutroster.scr' was not properly loaded" / "Couldn't p | loadoutroster.scr DID NOT COMPILE AT ALL. The six new weapons were added to loadout_weapons.tsv with an empty `tab` column, and the generator rendered that straight out as `level.coop_loRosterTab[69] = ` with nothing after the '='. An assig | Filled the tab column (rifle=0 sniper=1 smg=2 heavy=3 pistol=4 grenade=5, derived from the weapons that already worked) and regenerated; the diff is exactly the six lines. Then closed the class: gen_loadout.py now refuses to render an empty |
| `bug-1906` | user: 'I didnt get my loadout either when I got in game, it says NIL on the left text where it usually tells me my weapons' / 'head just floats in the air where the body was before it falls' | Two things. (1) roster_get is a THIRD id-keyed table, and the one the SPAWN path reads to turn a saved loadout id into a weapon. It only had cases 01-69, so any new gun came back with every field unset: no weapon given and NIL in the loadou | Six roster_get cases added, values copied from each gun's closest sibling. The settle think is no longer scheduled: it only ever existed to stop the head sinking through the floor, which was really the SOLID_NOT bug (bug-1903), and with the |
| `bug-1904` | user: 'New challenge "The Hunt" - Successfully defend the Church without it being contested once.' | Feature request. No new measurement was needed: the church fight already computes level.coop_chG every half second - the count of THIS PHASE's attackers inside coop_chZone, the breach radius - and drives the progress bar from it. 'Contested | level.coop_chContested latches on any tick where coop_chG > 0, read AFTER coop_churchRate since that is what refreshes coop_chG. coop_churchHuntAward fires only on the survived-the-duration path and refuses if coop_chOver is set (overrun is |
| `bug-1902` | user: 'selecting any of the new weapons doesnt put them into your loadout slots. Also if these are locked by challenges or ranks they arent appearing locked in loadout with the unlock conditions' | SELF-INFLICTED by bug-1896's fix. The armory has TWO halves and I generated only one. The ui/loadout/ pages draw the grid; loadoutroster.scr::roster_ids is the list the SERVER walks in loadout_ui_exportUnlocks to push, per weapon, 'seta coo | gen_loadout.py now generates the roster too, so both halves come from one table and cannot drift apart again; `check` reports 'unlock roster OUT OF SYNC' and fails. Two defects were caught during the fix and both would have shipped silently |
| `bug-1896` | C96 and Johnson are not selectable in the armory picker (user asked for 'in the Loadout system as well') | The armory is not a data list. It is 69 literal tiles in coop_loadout.urc, each with hand-placed rect coordinates in a fixed grid, its own hovercommand page (reqNN.cfg), its own select page (tNN.cfg), a lock overlay tilelkNN gated on coop_l | GENERATOR RECONSTRUCTED as docs/tools/gen_loadout.py, validated by exact reproduction: it regenerates all 319 per-weapon files for the existing 69 weapons byte-for-byte, and `build` changes zero files on disk. Source of truth is docs/tools/ |
| `bug-1895` | SELF-INFLICTED: "Script 'coop_mod/challenges.scr' was not properly loaded" + "syntax error, unexpected TOKEN_ELSE" - the whole challenge system dead on the first boot after the weapon wiring | My insertion script built the re-chained line as '\telse' + anchor.lstrip('\t') where the anchor began 'if(' - concatenating to 'elseif(' with no space. One missing character killed the compile of a 230 KB file and with it all 350 challenge | elseif( -> else if(. The lesson is the process, not the typo: my structural scanner reported 0 findings, build.ps1's scrlint reported 755 files clean, and the challenge validator still counted 350 challenges with writers and rewards - ALL T |
| `bug-1871` | feature request - healing a teammate should grant XP (user believed it already existed) | FEATURE. It did not exist and was not merely unhooked: medkit_use is self-ONLY - it takes one player, tests THAT player health and heals THAT player, with no target parameter anywhere in medkit.scr. The only teammate-facing recovery was the | Built medkit_teamheal_monitor / _pick / medkit_teamheal / _abort. Aim-free proximity (96u) at an OTHER player who is alive, not spectator, NOT downed and below max health, then hold USE for 100 frames. Costs the HEALER a medkit (user chose  |
| `bug-1867` | LOCALIZATION ERROR: 'Locate and destroy the' does not have a localization entry | The new slot-6 objective text was written as "Locate and destroy the Nebelwerfers" without the trailing period. Retail's own string - the one that HAS a localization entry - is "Locate and destroy the Nebelwerfers." with the period. | Matched both sites (objective text and toast) to retail's exact localized string. |
| `bug-1862` | I also dont think the compass is leading you to objectives correctly | global/objectives.scr::add_objectives takes FOUR args - index, status, text, and local.location - and the compass points at that fourth one. coop_churchObjectives passed only three for every primary (slots 1-5), so those objectives had no l | Added a location to all six primary calls: tank (2926 -485 -62), churchyard crossing (4167 -1115 -231), church (1551 -2370 -255), flak gun (1874 -3725 -306), and the church again for signal/hold. |
| `bug-1859` | We will need new spawn checkpoints, the one that exists spawns you by the old entrance to the church area that I have blocked off | m3l3_update4 placed all eight spawns on the x=5287 row - the original churchyard approach, which the coop route now blocks off with coop_churchHoldLine and the heavy_pipe ramp crossing - so dying at the church respawned you outside your own | Three checkpoints re-walked from live viewpos and wired to the objective chain rather than to scene triggers: update4 = (4167 -1115 -231) yaw 271 on 'Clear the church'; NEW update6 = (1492 -2098 -197) yaw 263 once the church is secured; upd |
| `bug-1855` | I dont feel like theres enough pressure in the wave systems for one player, they aren't trying too hard to get inside the church | coop_churchWaves used coop_reinf_count with base 6 / per 3. That helper scales around a TWO-player baseline, so a SOLO player got 6 + ((1-2)*3) = 3 attackers alive for the whole set-piece. The 4-per-wave send cap on a 3s poll could not refi | base 14 / per 5 / cap 28 (solo 9, 2p 14, 3p 19, 4p 24), send cap 8, poll 3s->2s, and coop_chAdvance re-issues its runto every 1.5s instead of 3s. All exposed as cvars coop_chWaveBase/Per/Cap/Send and seeded in coop_defaults.cfg. |
| `bug-1847` | user: 'The Church text is not centered, and the bar itself is ugly' / 'the smoke you are using is also obnoxious it should be the same kind of smoke we already use for the signal drop' / 'Your smoke p | Four presentation faults. (1) The bar label used a ZERO-WIDTH rect with align center, so the engine anchored the rect at screen centre and drew the string rightward from there - half a word off. (2) The bar was a flat grey rect with a red s | Bar moved to the free 175-195 run (175 frame / 176 track / 177 fill / 178 label, registered in the slot map), every element sharing one width and axis so they cannot drift; fill shifts green->amber->red with progress. Smoke switched to plan |
| `bug-1842` | user: 'i just placed a new ladder that works PERFECTLY from build mode' / 'your ladder was broken af'; and 'this ladder is always where the new trigger for the church guards to spawn... they did not b | TWO faults. (1) The scripted ladder was in the wrong place. The user's working build-mode placement captured as ( 4010.860 -911.426 -334.536 ) yaw 358 h 128; the scripted one was ( 4015.875 -966.873 -327.909 ) - 55u off along Y and 6.6u hig | (1) Adopted the user's captured position verbatim and deleted the standoff. (2) Added coop_gate_scene6_* wrappers - 'level.coop_chGuardsGo == 1 OR the retail istouching volume' - and routed all five spawn control loops (spawndetector1/2, mg |
| `bug-1833` | user: 'church meter filling up and no one is here but me' then 'the church has been overrun'; user hypothesis 'could it be the enemies that havent spawned in, I nocliped over here' | Live readout at the moment of the complaint: 'CHURCH g=3 in=1 rate=120 prog=23520 cap=24000', with the player measured at ( 1624 -2417 -197 ), i.e. 87u from the church centre. So the contested model was working exactly as designed - in=1 co | Split the single radius into two, because the two questions are different: coop_chZone = BREACH (attackers, default 500 - a german 900u out is walking to the church, not in it) and coop_chHold = DEFENDING (players, default 1100 - a player 9 |
| `bug-1832` | user: 'i dont even see the place to put the ladder now like I used to' | I acted on 'the object over the barbed wire spawns in, that should get triggered by the start of the objective itself' by moving coop_churchLadderMarker off main and into coop_churchPhase - without first checking WHICH object sits over the  | Restored 'if( level.gametype != 0 ){ thread coop_churchLadderMarker }' to main. Left coop_churchWireBoard alone; it was already correct. Lesson: before relocating an entity on a positional complaint, resolve WHICH entity by coordinate - two |
| `bug-1829` | user: 'Church got overrun really fast and i dont think anyone was in it' / 'me going in didnthelp' | coop_churchDefense was a ONE-WAY RATCHET. The tick did 'if(rate>0){prog+=rate}else{prog-=drain}' and coop_churchRate only returned 0 when ZERO germans stood within the zone - a zone spanning the whole scene6 street, so during the battle it  | Made it a contested capture point. coop_churchRate now returns a SIGNED delta and the loop just adds it. New coop_churchPlayersIn counts defenders in the same zone as the germans, so each defender cancels one german: net>0 fills at net*60 ( |
| `bug-1828` | user: 'also the ladder doesnt place correctly. its not positioned exactly as I put it in build mode so you cant climb' | coop_churchLadderMarker called coop_mod/ladder.scr::main with a yaw and height I invented (192 / 160) instead of the ones build mode captured (358 / 128). A 166 degree yaw error builds the climb volume against the wrong face of the wall, so | Read the three values out of the save capture and pass them verbatim: yaw 192->358, height 160->128. Comment records that a ladder is placed FACING INTO the wall, so yaw decides which side the climb volume lands on and is not cosmetic. |
| `bug-1826` | maps/M3L3.scr parse-killed again: 121 'was not properly loaded', coop init never reached, after adding the MG42 belt helpers. | NEW T1 TRIGGER, not previously documented: ARITHMETIC IN A COMMAND ARGUMENT POSITION. `$scene6_mg1 coopammo 0 - 1` - passing an expression where a command expects a literal argument - kills the whole file. This is distinct from the already- | Compute into a local first: `local.unl = 0 - 1` then `$scene6_mg1 coopammo local.unl`. Verified by dedicated boot - 0 not-properly-loaded, 0 script errors, coop init reached. Caught within one boot because the church defense is being built  |
| `bug-1824` | FEATURE: m3l3 objective #6 - defend the church until the airborne arrive. Built after bug-1822 parse-killed the map, so built differently. | n/a - feature work. Recorded because the METHOD is the lesson: bug-1822 shipped a whole subsystem in one pass, all three scanners passed, and it parse-killed the file so m3l3 loaded with no script at all (raw team menu). | Built in NINE pieces, each followed by a DEDICATED-SERVER BOOT before the next was written - 0 'was not properly loaded' at every step. Pieces: (1) coop_churchDist cylinder distance; (2) german/player zone counters; (3) threshold 2+2*player |
| `bug-1773` | That One Map No One Played was tied to the wrong mission; two new m6l2a challenges requested | cc_no_one_played bumped on mapname m6l2a and its description said 'Board the train on The Communications Blackout' - but m6l2a ends by escaping the town through a window; the train is boarded on m6l2b, The Schmerzen Express. | Re-tied to m6l2b and the description corrected. Added cc_final_broadcast (The Final Broadcast - use the call sign to divert reinforcements before the blackout, awarded in coop_m6l2aDivertReinforcements) and cc_stealing_show (Stealing the Sh |
| `bug-1764` | the text about containing overlaps with the use radio text | Different HUD slots do not stop two lines colliding if they draw at the same place. The new transmit prompt (121) used y -58, which is exactly where the Phase C contain prompt (250) draws - and a guard by the radio can be containable, so bo | Transmit prompt moved to y -42, clear of the contain line at -58 and the body warning at -74. Recorded in _research/hud_slot_map.md, which tracks slots - the lesson is that it needs to track POSITIONS too. |
| `bug-1758` | a Press Use to Transmit False Signal should come up when you are right by the radio | The transmit trigger was silent - the set glowed once the call sign requirement was dropped, but nothing told the player the radio was interactive or which key. | coop_m6l2aTransmitPrompt on HUD slot 121, live only between objective 2 opening and the orders being sent. 121 chosen against _research/hud_slot_map.md: free block between the m2l2a Naxos 118-120 and 126, and >= 100 so the engine HUD fade c |
| `bug-1756` | I can't plant one of the explosives on the tower. I think the object itself has some kind of invisible wall around the legs | coop_m6l2aProp always spawns solid, and I replayed build mode's measured box for the mast verbatim: 301 x 390 x 1153 units. A single box that size around a LATTICE tower is a wall nobody can walk through. Charge site 3 at ( -2538.399 -1170. | Spawn the mast notsolid. A scenery mast has no business blocking movement, its real legs are thin, and the charge markers are deliberately placed at its feet. Note the build-mode box is a MEASUREMENT of the model's extents, not a statement  |
| `bug-1753` | the timer/stop watch still starts at 15 seconds | NOT root-caused from code - no stopwatch anywhere in the mod is set to 15, and this map has exactly two callers: the retail `stopwatch 8` in objective1_bombtracker and the new escape fuse. replace.scr::stopwatch was checked and correctly lo | Clear the widget before setting the escape clock - stopwatch 0 is the engine's documented clear (player.cpp:1467 'use 0 to clear the stopwatch') - then a frame, then the real fuse, so exactly one countdown can be running. Also prints the fu |
| `bug-1749` | the tower is tipped over already... it needs to be standing, right now its laying down like it was already destroyed and i havent even planted bombs yet | I passed freeze=1 to coop_m6l2aProp for sc_p_radiotower.tik. That runs coop_m6l2aFreeze, which calls stopanimating - and on an ANIMATED tik whose anim set contains the collapse, stopping the animation parks it in a fallen pose. The mast the | freeze=0, matching exactly what build mode emitted. Rule: when replaying a build_*.dat placement, replay the flags it emitted too - build mode's own output is the reference for how that model wants to be spawned. |
| `bug-1745` | preventive: build-mode sabotage targets and the interact radio were not precached | Models placed via build mode and then spawned by script were absent from the map's precache list. A model rendering correctly in build mode proves it EXISTS in the pak, not that it is precached - the cold-boot spawn is a different path. | Cached radio_military_pulsing, sc_p_radiotower, sc_p_radiotowerleg, sc_p_radiotowerfallen, generator and static_powerbox2. static_powerbox2 has no retail script reference anywhere, so it was the most exposed of the set. |
| `bug-1744` | you replaced my broadcast radio but didn't remove the old radio... so its sitting inside of it | The first implementation of the 'make the radio glow red once the call sign is taken' request SPAWNED a second, pulsating radio (models/miscobj/radio_military_pulse.tik) at exactly level.coop_m6l2aMainSet and left the user's build-mode-plac | The set is now ONE prop that changes its own model, using the pulse/no-pulse pair MOHAA ships for this exact purpose (the same convention as electrical_switch_pulse/_nopulse and redvalve_pulse/_nopulse): models/miscobj/radio_military.tik sp |
| `bug-1718` | build-mode props in the wrong place; train schedule stayed on the table after pickup | coop_m6l2aProp never replayed build mode's saved setsize, call sites passed level vars including ARRAY SUBSCRIPTS as thread arguments, and the helper returned nothing so pickups had no handle to remove the model. | Helper takes mins/maxs and applies setsize, returns the entity, and every call site passes a literal vector from build_m6l2a.dat. |
| `bug-1689` | user: 'The Naxos text appears when you are downstairs underneath the room itself', 'I didn't see the officer warning text until I was right on top of him', 'the radius for his body should also be on t | EVERY PROXIMITY TEST IN THIS FEATURE WAS A SPHERE. vector_length is a 3D distance, so a 512u 'room' radius reaches straight through the floor and a 320u 'standing over the body' radius catches a player one storey up. Three separate sites ha | All three sites now test a HORIZONTAL distance plus a 96u vertical band. The horizontal distance is taken with vector_length on both points flattened to z=0 via a vector literal (locals inside a literal are proven - props.scr:407, tracescan |
| `bug-1680` | LATENT: objectives.scr's NEW OBJECTIVE toast occupies HUD slots 135-142, colliding head-on with the DBNO team-revive channel (135-140) and the XP micro popup (142-144). Found while measuring the slot  | objectives.scr:87-109 draws its toast header on 135 and its wrapped lines on a COMPUTED base, `local.slot = 136 + local.line`, plus 142 at :117. dbno.scr owns 135-140 for the team-revive channel (reviver text 135 + bar 136-137, downed text  | NOT FIXED - reported, not repaired, because moving a live objectives widget is a blind bet (TRAPS T3 UI corollary: the only oracle is the user's screenshot) and it is unrelated to the work in flight. What WAS done: _research/hud_slot_map.md |
| `bug-1679` | user: 'going into spectator makes messages like Hold Use to heal player get stuck on screen if they were spectating a DBNO player... I am guessing that is the case for many other messages like that' | TWO causes, and spectating was only the trigger the user happened to notice. (1) THE TEARDOWN NEVER COVERED CANDIDATES. dbno_teamrevive_monitor draws the availability prompt 'Hold [USE] to heal player' on slot 135 for every player in its CA | THREE changes. (1) dbno.scr candidate scan now also requires `dmteam != "spectator"` and `flags["coop_isActive"] == 1`. (2) dbno_teamrevive_monitor's exit tail now walks $player fresh and clears slot 135 + the coop_revivePrompt latch for EV |
| `bug-1678` | user: the seeded default loadout is correct in play but the LOADOUT TAB still renders empty | bug-1675's loadout_seedDefaults wrote flags["coop_loWeapons"] DIRECTLY. That array is one of THREE things a real pick maintains, and it is the only one the spawn path reads - which is exactly why the guns were right and nothing else was. Th | The seed now runs the SAME path a real click runs, so the three cannot diverge. Server side: two `waitthread loadout_set local.player <slot> <id> 1` calls ('01' M1 GARAND -> slot 1, '48' COLT M1911 -> slot 3), which validate against the ros |
| `bug-1675` | a brand-new player's armory loadout is EMPTY, so they silently get the map's default mission weapons instead of their starting unlocks | flags["coop_loWeapons"] is ONLY ever written by loadout_commit (loadoutpick.scr:394) and cleared by loadout_clear (:358/:411). Nothing seeds it, so a player who has never opened the armory has an empty array. giveWeaponLoadout then behaves  | New loadout_seedDefaults, called once per player from player.scr's per-player setup. Seeds m1_garand + colt45 (the starting unlocks) when the picks array is empty, and NEVER overwrites a real choice - it early-exits if any pick exists, and  |
| `bug-1670` | LATENT SEVERE: chal_ui_export queues ~1005 reliable commands per player into a 1024-deep ring every 30s - predicted remote-client disconnect with 'Server command overflow'. NOT yet observed, because a | chal_ui_export walks the WHOLE challenge catalogue and chal_ui_writeOne emits 3 stufftext (coop_uiD/coop_uiB/coop_uiN) per challenge on either branch. Independently verified: 335 chal_def entries x 3 = ~1005 reliable commands per player per | ['TWO changes in challenges.scr, both mod-script only - no engine rebuild, no protocol constant. (1) chal_ui_writeOne is now DIRTY-TRACKED: it builds a signature of exactly the values that appear in its three stufftexts (done:bar:progress:t |
| `bug-1668` | user: "I dont think we got the XP for reviving a player, at least it didnt pop up" - team revive completes but no XP popup | SOLVED, and the premise was wrong: XP was NEVER missing. Three probe rounds established it. (1) Caller side fine: XPPROBE revive callerOk=1 cls=Player. (2) xp_award IS entered: playerOk=1 cls=Player amt=10 init=1 enabled=1 - which also kill | Moved xp_micro_popup to slots 142-144 (fade-exempt, >=100) and rebased its row offset, which was computed as (slot - 73). Recorded in _research/hud_slot_map.md, including the correction that the XP system now owns 62-72 rather than 62-75. F |
| `bug-1661` | the reviving player sees no 'Healing Player' text and no progress bar, while the downed player's HUD is fine | the engine HUD fade multiplies the alpha of EVERY ihuddraw slot below 100 once the player reads as calm (cg_drawtools.cpp:648-656; slots >= 100 are explicitly exempt as persistent UI). A medic stands perfectly still holding a key, which is  | moved the whole channel to the fade-exempt block 135-140 (reviver 135-137, downed 138-140) and recorded it in _research/hud_slot_map.md with the reason, so nobody puts a hold-to-interact prompt below 100 again |
| `bug-1659` | 9 Script Errors in the 2-player test: 'Player entity is NULL for ihuddraw_alpha/ihuddraw_string' (solo baseline was ZERO) | per-player HUD labels run a display `while` loop and then clear their HUD slots afterwards - but the loop exits precisely BECAUSE the player became invalid (disconnect, spectate, map change), so the teardown then calls ihuddraw_* on a NULL  | wrapped every teardown that sits directly before a label's `}end` in `if( local.player != NULL )` - 8 blocks across 5 files, plus 2 hand-written regions in dbno.scr that the scan skipped because non-HUD statements followed the HUD lines. ch |
| `bug-1627` | Objective toast audit fixer 1: New Objective card never shown for one or more objectives on M1L3a, m5l2b, m1l2a, m5l1a (pre-player add_objectives, start toast lost), e2l1 (text-less RevealObj/NIL Upda | global/objectives.scr::add_objectives status-2 is the only toast channel; these maps either add objectives at map load before any player is connected (coop_obj_toast_all early-exits on player.size < 1) or flip status 2 with NIL/empty text ( | m2l2a recipe applied additively: coop_objToastStart label (waitForPlayer -> wait 2 -> coop_obj_toast_all 1 <text>) threaded from a level.gametype gate for the four start-toast maps; one gated waitthread coop_obj_toast_all N <exact registere |
| `bug-1626` | Objective toast audit fixer 3: New Objective card never shown for one or more objectives on m4l0, m6l3c, m3l1b, e1l1, e2l3, t1l3, t3l2 - pre-player add_objectives, bare (text-less) status-2 flips, or  | Three variants of one class: (1) m4l0/m6l3c/m3l1b add objectives at raw spawn before any player connects, so coop_obj_toast_all exits on player.size<1; (2) e1l1 RevealObj and t1l3/t3l2 flips call add_objectives N 2 with NO text, so the toas | Applied the m2l2a recipe: gametype-gated coop_objToastStart label (waitForPlayer -> wait 2 -> coop_obj_toast_all 1 <text>) threaded near the pre-player adds on m4l0/m6l3c/m3l1b/e2l3, plus gametype-gated coop_obj_toast_all lines with the exa |
| `bug-1625` | Objective-toast audit fixer 4: six maps never (or wrongly) announced objectives. M5L2A/m6l3e/M3L3 add their sole/first objective pre-player (raw level waittill spawn or setup thread before waitForPlay | global/objectives.scr::add_objectives status-2 with text is the ONLY channel that fires the New Objective card (hook -> coop_mod/objectives.scr::coop_obj_toast_all, which exits on NIL/empty text or player.size<1). Retail maps either add all | m2l2a recipe applied additively: (a) M5L2A/m6l3e/M3L3 got a coop_objToastStart label (waitForPlayer -> wait 2 -> coop_obj_toast_all 1 with the exact add text) threaded gametype-gated next to the pre-player add; M3L3 toast text matches the i |
| `bug-1602` | build_<map>.dat: the SESSION START banner ran together with the next block, swallowing its header comment (build_m1l1.dat:238). | The banner string had no trailing newline. It was collapsed to a single line by the bug-1283 fix (a REAL multi-line string literal had killed the whole file), and the terminating newline was dropped along with the line breaks. | Appended a \\n\\n ESCAPE to the banner. Escapes were never the bug-1283 problem - real newlines in the source were - and line 143 already uses \\n. Existing .dat files need no repair: every block starts with a // comment, so the only thing  |
| `bug-1601` | The unwired-challenge count was reported as 49 then 47; the real figure was 46, and three challenges reported as dead (cc_static_line, cc_desert_fox, cc_enemy_mine) were already wired. | The checker enumerated producer sites with glob.glob('maps/*.scr'), which does NOT match maps/<map>/*.scr. Every challenge bumped from a map SUB-script was invisible to it - and the BT/SH maps keep most of their logic in sub-scripts. cc_ene | Rewrote the checker to os.walk the whole tree (750 files vs the glob's partial set) and to strip // comments before matching, so a mention inside a comment is not counted as a producer. Rule: any cross-reference scan of this repo must walk, |
| `bug-1598` | Self-inflicted: a new challenge batch defined 'Latrine Duty' and 'Charges Laid', both of which duplicated already-shipped challenges (evt_six_flushes 'Six Flushes', obj_four_charges 'Four Charges, One | The duplicates were invisible during authoring because the shipped originals are in the 47-strong unbumped set (bug-1596): grepping the map scripts for an existing hook found nothing, which read as 'not covered yet' when it actually meant ' | Dropped both new definitions and pointed the new map hooks at the shipped stats instead, which fixes two of bug-1596's dead rows at the same time. Before adding any challenge, check chal_def by TITLE and by feat, not by whether a hook exist |
| `bug-1597` | cc_king_tiger ('Beutepanzer' - 'Steal the King Tiger with the tank crew still alive') awarded even when the tank crew had been lost. | The dispatch entry bumped the stat unconditionally on objective 4 completing. The 'with the tank crew still alive' half of its own description was never implemented, so the feat degraded to plain objective completion. | Gated on level.flags[tankcrewdead] != 1. m5l1b.scr:786 sets that flag when level.engineers drops below 2, which is the map's own 'You lost too many of your tank crew' fail state. |
| `bug-1596` | 47 defined challenges have no chal_bump/chal_team_bump/cc_award_clean site anywhere in the mod - they are visible in the Service Record and can never be completed. | The v1.2.1 batch (commit 7410b61) added ~50 chal_def rows from a proposal document but only the definitions landed - the per-map hooks that were supposed to bump each stat were never written. chal_bump early-exits when level.coop_chal_statN | Detected by a new consistency checker that parses every chal_def and cross-references the bump sites across coop_mod/, maps/ and global/. Two of the 49 were wired in this batch (evt_six_flushes at t2l1 toilet_easteregg, obj_four_charges at  |
| `bug-1588` | User (playing t2l3): 'its grabbing the new objective as defeat the high ranking officer... those are secondary objectives and shouldnt even appear here' and 'I got the official objective finally and i | The new-objective toast was hooked to coop_obj_push - the coop SIDE-objective bus, which carries exactly two lines, the officer and the radio. The maps' own objectives come through a completely different path (exec global/obj.scr -> global/ | Moved the hook to global/objectives.scr::add_objectives, right after the addobjective call, gated on status == 2 (in progress) so the hidden registrations every map does at start announce nothing. New coop_obj_toast_all fans out to active p |
| `bug-1567` | mg_nest_manned did not appear in STRUCTURES (COMPOSED) despite shipping correctly in the pk3 | bp_catalogRefresh hard-coded 'local.base = 4' - the number of built-in structures at the time it was written. It writes blueprint templates at base+1.. and then sets itemcount = base + k, so a fifth built-in was overwritten by the first bp: | catalog_init now publishes level.coop_build_structBuiltin (5) next to itemcount[41], and bp_catalogRefresh reads it, falling back to the old 4 only if unset. Deliberately does NOT fall back to itemcount: the thread can run more than once pe |
| `bug-1554` | User: 'none of my objects are here that I built' | NOT a bug - build mode is a CAPTURE tool, not a persistence layer. It writes paste-ready spawn blocks to coop_mod/save/build_<map>.dat and re-reads that file only to append to it; nothing loads the .dat at runtime, so placements are gone on | Baked all 34 placements from build_m3l1b.dat into a coop_rear_props label in maps/m3l1b.scr, threaded at map start (they are terrain, not an event). Documented the capture-vs-persistence distinction in the label header so the next session d |
| `bug-1543` | User: make sure all new helmet items are unlockable by challenges or ranks - thematic, not random | All 13 pieces added 08-07 (gas mask + the 12 from the pak audit) shipped FREE - no challenge rewarded them and none were in coop_cosmeticGatedTok, so everyone could wear them immediately. | Attached each to a thematically-matched existing reward-less challenge: SS officer hat<-Total De-Nazification, SS Mutze<-Chain of Command Broken, crusher cap<-High-Value Target, splinter camo<-Ghost in the Fog (counter-sniper), eyeglasses<- |
| `bug-1538` | User: saved fog in build mode on the North Africa/ocean map (m1l3c), but no profile file existed to apply | The fog panel's on-screen key hints were wrong in ALL FOUR positions. HUD slot 94 read 'KP_INS save / KP_DEL reset / KP_ENTER night / KP_5 mist', and coop_fog_monitor's arm message said 'KP_INS saves this map'. The real mapping comes from c | Corrected both strings to the actual build-mode keys ('KP_ENTER save KP_5 reset KP_INS night KP_DEL mist' and 'KP_ENTER saves this map'). The user's tuning session was unrecoverable - level.coop_fog* live only in the running map and the ser |
| `bug-1536` | User: are all challenges accurately registered to the most logical category? (self-audit requested) | cc_day_of_tiger ('Drive It Like You Stole It', a map_m5l2b completion marker - same pattern as 13 other map_X milestones, all correctly in 'campaign') was filed under 'vehicles' - its own id still says 'tiger', a leftover from an earlier ta | Moved all 4 to 'campaign'. Verified no duplicate chal_def ids exist across all 299 (dupe-checked while auditing) and the 5 weapon categories all group correctly by weapon class - no other misplacements found. |
| `bug-1535` | User (screenshot): 23 Service Record tabs crammed into two rows was unusable navigation - asked how a modern game would present this | N/A - design change. The old scheme gave every SUB-PAGE its own tab (RIFLES / RIFLES 2 / CAMPAIGN 1-5 / ...) instead of one tab per real category, so category sub-pagination directly inflated tab count as content grew. | Redesigned navigation to tab-plus-pager: catspec gained a 5th 'tab group' field, and the 5 weapon-family categories (rifles/snipers/smgs/pistols/support_weapons, 135 challenges) now share ONE 'WEAPONS' tab per a follow-up user request, step |
| `bug-1532` | User: the challenge unlock condition/reward layout was still bad after 3 rounds of inline-text fixes (truncate -> shrink -> stack) - wanted a real hover-to-reveal instead | N/A - design change, not a bug. Every inline attempt tonight (bug-1525, bug-1531) still made the reward compete with the description for the same line, just with progressively less-bad compromises. The user correctly identified that the row | Reward text ('Unlocks: X') removed entirely from the baked row image - description always gets its full natural width now, same code path whether or not a reward exists. Reward is revealed on hover instead, reusing the pinbtn<gi> Button tha |
| `bug-1531` | User: Service Record description still got cut off on a few rows even after the shrink-to-fit fix (bug-1525) - long description + long reward name together still didn't fit at any readable font size | bug-1525's shrink-to-fit had a 15px floor and fell back to ellipsis truncation below that - fine for most rows, but 3 challenges (obj_dog_green_present/obj_four_charges/obj_rode_it_in) combine an 80+ character full-sentence description with | Raised the inline shrink floor to a genuinely comfortable 19px (DESC_COMFORT) and, only for rows that still don't fit at that size, moved the reward onto its OWN line directly below the description (small 15px font) instead of sharing the r |
| `bug-1530` | User: Armory model-viewer text overlap fix (bug-1527) regressed / never actually worked | cosReqBg (the backing panel) and cosReq1-3 (the caption text) were both left at ordernumber 20, TIED with charRender (the model viewer, also 20) - the fix's own comment assumed same-tier ties break by declaration order (panel declared after | Gave every layer in this stack an explicit, distinct ordernumber instead of relying on tie-breaking: charRender stays 20 (back), cosReqBg is now 15 (front of the model), cosReq1/cosReq2/cosReq3 are now 10 (front of the panel). No ties left  |
| `bug-1529` | User (screenshot): MEDALS tab showed pure black, and the 'X/5 Pinned' summary was nowhere in the Service Record | The 'end.' token that closes the "coop_sr" menu block in the generated .urc was emitted as part of the BACK button's own list entry, which was written BEFORE bgMedals, the 12 per-medal lock-scrim overlays, and even the earlier pinSummary La | Moved 'end.' to be the true last write, after every remaining Label (bgMedals, medLock0-11, medLockIcon0-11, pinSummary). Confirmed by grep that 'end.' now sits after all of them in the generated ui/coop_sr.urc. |
| `bug-1527` | User (with screenshot): Armory model-viewer's unlock caption ('UNLOCK: Stalingrad Rifle-Only Server') overlapped/unreadable against a tall skin's legs | cosLk/cosReq1-3 (the lock icon + 3-line unlock caption) sit at rect y=332-381, inside charRender's own rect (12 46 134 348, y=46-394) - the character model's own geometry can render right where the caption text sits, and the layout below (s | Added a semi-opaque dark backing panel (cosReqBg) declared after the model Label but before the caption Labels, so it draws on top of the model and under the text - caption stays legible regardless of the worn skin's pose/silhouette. |
| `bug-1525` | User (with screenshot): 'All Six Home' and other Service Record rows had their description hard-truncated with '...' to make room for the 'Unlocks: X' reward text, losing real content | Earlier tonight (this same session) the reward text was baked inline on the same row as the description, with the description's available width computed by subtracting the reward's measured pixel width and ellipsis-truncating whatever didn' | Replaced the truncate-with-ellipsis loop with a shrink-to-fit loop: try progressively smaller description font sizes (down to a 15px floor) before ever truncating, so the full sentence stays readable at a smaller size instead of losing word |
| `bug-1524` | User: the native Multiplayer Options skin selector could overwrite the Armory-selected skin; wanted it blocked and routed to the Armory instead | Retail ui/multiplayeroptions.urc's 'Allies Player Model' button (stuffcommand "playermodel 0") cycles dm_playermodel via the native UI_PlayerModel_f/UI_ApplyPlayerModel_f commands (cl_ui.cpp:3512-3550), writing straight to dm_playermodel wi | Added hzm-mohaa-coop-mod/ui/multiplayeroptions.urc as a full override of the retail menu (mod ui/ takes priority over the mounted paks). Every widget is unchanged except the Allies-model cycle Button, whose stuffcommand now runs "exec ui/lo |
| `bug-1523` | Audit request: ~48 models/player/*.tik reward strings in coop_mod/challenges.scr chal_def calls (rifleman.tik, riflemannohelm.tik, submachine_gunner*.tik, support_gunner*.tik, american_army_29id*.tik, | REFUTED, not a bug - T11-shaped false premise. A pure git-tree file check (grep chal_def reward strings against hzm-mohaa-coop-mod/models/player/) genuinely finds all 48 basenames absent from that directory, and none are explained by a case | No code change to the reward strings, armory/lobby skin arrays, or model files - none was needed. Left a standing comment at coop_mod/challenges.scr's chal_def definition (~line 508) naming this bug id and the verification method, so a futu |
| `bug-1522` | User: pinned challenges should show a quarter-progress popup on the left (like the promotion bar), clear of anything else | The quarter-progress popup (chal_progress_popup, shipped 2026-08-04) already existed with exactly the requested look/position/fade, but fired for EVERY counter challenge (target>=4) crossing 25/50/75%, not just the player's 5 pinned ones -  | Added chal_pin_isPinned local.player local.cid helper (mirrors the inline pin-membership check already used by chal_pin_toggle/chal_pin_importUserinfo) and gated the chal_apply popup trigger on it - confirmed with user before restricting sc |
| `bug-1521` | User: 'Id like all of the skins added and ways to unlock them...' - 23-candidate trilogy NPC skin survey needed real unlock wiring | Named campaign NPCs (Ramsey, McMartin, Lyndon, etc.) had either no reward wired to their existing survival-tracking challenge, or a generic placeholder skin instead of their own likeness. The reward-application system (challenges.scr::chal_ | Ported 11 characters (Ramsey, McMartin, Johnson, Cappy, Hildebrandt, Wilson, Captain Ike, Claus, Burton, Gobbs, Whittaker) from their retail .tik files into flattened models/player/*.tik skins, wired into all three roster arrays, and attach |
| `bug-1519` | User reported Service Record unlock text overlapping descriptions and auto-generated reward names being unreadable (Ital Volhat, American Assault Engineer2) | bug-1511s reward-text draw assumed most long-description rows would have empty rewards (true for weapon pages, false for Campaign/Combat/etc, which have both long full-sentence descriptions AND populated rewards) - fixed x=1260 draw positio | Added REWARD_NAMES, a curated lookup table for all 180 unique reward values (verified no duplicates) with real display names, checked before falling back to the mechanical transform. Collision: reward width is now measured FIRST, then the d |
| `bug-1512` | User: unpinning boxes also doesnt remove the x in the checkbox | Double independent toggle. CL_PinToggle_f (client, bug-1503) computes the definitive new pin state instantly and writes it into 5 userinfo cvars, then (if connected) fires a name-bus token carrying only the catalogue INDEX, no state. Server | chal_pin_byIndex no longer calls chal_pin_toggle. It now calls chal_pin_importUserinfo (same trusted-adoption call chal_ensure already uses at connect) to adopt the client's userinfo as ground truth, diffs old vs new pin state for the iprin |
| `bug-1511` | User: I dont see where we mention what the Reward/Unlock is | chal_def's 7th argument (a skin/helmet/weapon .tik path or a perk_* keyword) was parsed into the generator's row tuple (rw) but never drawn anywhere in page_bg() - captured data with no display path. | Added friendly_reward_name() (strips path/extension/coop_ prefix, perk_ prefix, underscores to spaces, title-case) and draw an "Unlocks: <name>" line right-aligned on the description row, only when the reward field is non-empty (180/287 cha |
| `bug-1510` | User: check boxes for the service record appear too far to the left | pinbox/pinmk/pinbtn rects used RX+5 / RX+22 as raw SCREEN-space offsets, where RX (80) is the panel's own left screen edge - so the checkboxes sat inside the baked ornamental border/frame instead of next to each row's title text (which is p | Added PINX = round(sx(96)), a texture-space anchor converted the same way as every other row element, replacing the raw RX+5/RX+22 constants. Checkboxes now sit in the gutter between the decorative border and the title text. |
| `bug-1497` | PREMISE CORRECTION: 'Morpheus has no continue statement' is wrong. | `continue` IS a real keyword: lex_source.txt:315 emits TOKEN_CONTINUE, bison_source.txt:159 has ENUM_continue, and scriptcompiler.cpp:1201 generates code for it. Separately, `end` is NOT a lexer keyword - it lexes as an identifier and dispa | Recorded so future fixes stop contorting around a restriction that does not exist. |
| `bug-1484` | m1l2a: Can't find 'mom/mom.scr' x8 (reported against m1l1, actually fires on m1l2a) | The MoM debug/cheat-menu script is threaded UNCONDITIONALLY from the ossdoor: trigger label, but mom/mom.scr does not ship - there is no mom/ directory in the mod and no retail pk3 contains any mom*.scr. | FIXED 2026-08-06, deployed. maps/m1l2a.scr:742 - the unconditional 'thread mom/mom.scr::setup' is commented out, matching the precedent at coop_mod/main.scr:146 where the identical call was already disabled as unneeded. |
| `bug-1457` | User: 'there is a press use to enter jeep image on all screens even after the jeep map is over' | ui_addhud coop_jeepEnter draws a CLIENT-side HUD element that survives a map change. All three removal paths in m1l3b/M1L3a/t2l2 are gated on local.player.flags['coop_hudPeTeJ'] != NIL - a flag stored on the PLAYER ENTITY, which is destroye | manageSetup now sends ui_removehud coop_jeepEnter unconditionally when a player is set up on any map (removing an absent hud is a no-op). Class lesson: client-side UI state outlives the entity-side flag that tracks it - any ui_addhud needs  |
| `bug-1453` | User: 'seeing another team select screen' during the sweep (server showed all 4 clients connected, embodied and walking) | The walker only ran the join/menu-dismiss recipe for clients whose dmteam read 'spectator'. A client that carries a valid team across a map change - typically the HOST - was skipped, so its team menu stayed on screen and it never got the co | popmenu 0 is now sent to EVERY client at the start of every map cycle regardless of team (idempotent). Data integrity was never affected - teleports are server-side and the log proves all four were embodied - but a menu on screen looks iden |
| `bug-1425` | User: 'in build mode I just get giant cubes ... hard to tell what side the ladder will be facing and the exact size' | The ladder marker TIKs used shape_post - the same cube mesh every coop_build shape shares - so the build-mode GHOST (the only place these models are ever seen; placed ladders are beams) showed a featureless cube with no plane, facing, or la | Markers rebuilt on models/fx/destruction/wood_latice - the stock FLAT crosshatch panel - one per style with that style's own texture on both surfaces (idle uses wood_latice.skc, matching the stock tik). The ghost now shows the ladder's plan |
| `bug-1423` | User: 'after i place it and switch categories it seems to delete' - fifth light failure | Architectural, not another parameter bug. A blockdlight tempmodel is an INDEPENDENT CLIENT EFFECT, and four consecutive fixes (wrong command bug-1408, ms units bug-1409, dontdraw-vs-parentlink bug-1417, parent never recorded bug-1421b) were | Replaced the mechanism wholesale with the engine's NATIVE constantLight: lightRed/Green/Blue + lightRadius + lightOn (entity.cpp:3441+), networked in entityState, rendered by CG_EntityEffects (cg_ents.c:93) for every entity carrying it - dr |
| `bug-1421` | User: 'deleting hte ladder also doesnt work still' (after the bug-1419 fix deployed) + 'lights endlessly accumulate / deleting does nothing' | TWO independent faults. (a) LADDER: the bug-1419 patch anchored its ladderIdx insertion on the dumpline/count/save capture trio - which appears in THREE capture paths in buildmode.scr - and replace-first put the assignment into the STRUCT p | (a) moved the assignment into the ladder divert using a UNIQUE anchor (the scale capture line); removed the stray from the struct path. LESSON: anchors for generated edits must be unique strings, never shared idioms - assert count==1 before |
| `bug-1419` | User: 'Note on ladders if you delete them in build mode they dont actually delete' | The build divert spawned the ladder (trigger + solid clip + up to ~22 beams) without registering ANY of the pieces with undo - coop_build_undo only knows coop_build_ent[n], which is never set for ladders. KP_DEL cleared the capture line and | ladder.scr tracks every piece per ladder index (ladder_track) and ladder_remove deletes the set; buildmode records coop_build_ladderIdx[n] at place and undo calls ladder_remove for it. |
| `bug-1416` | Ladder capture block briefly written with REAL newlines inside string literals (odd-quote lines 626-638) | Python-generated script text: '\n' in the generator collapsed to a literal newline in the emitted MOHAA string - the same heredoc-class parse killer as bug-1363 et al (4th recurrence). A real newline inside a string literal parse-kills the  | Rebuilt the block with chr(92)-composed escapes so the emitted text is byte-identical to the proven sound-capture pattern (buildmode_sounds.scr:299). Caught by the odd-quote sweep BEFORE deploy this time - the sweep works; run it after ever |
| `bug-1415` | User: 'placing a ladder just puts down a cube' -> pointed at github.com/mohaa-moddb/ladder-spawning-mod: 'build this but put into build mode and give them the textures so they arent blue' | Two problems with the FuncLadder attempt: every coop_build shape primitive shares one cube mesh, so the ladder visual rendered as a cube; and the engine FuncLadder path was never play-verified. The referenced mod does it script-only: a trig | Ported near-verbatim to coop_mod/ladder.scr with the laser rails+rungs ALWAYS drawn as TEXTURED func_beams (beam.cpp 'shader'). ladder_state.scr = the ON_LADDER/OFF_LADDER flag hook. player_Torso.st: removed all 8 CAN_CLIMB_UP/DOWN_LADDER c |
| `bug-1414` | User: 'it seems like switching light models places them too' + 'deleting the light did not work' | A blockdlight tempmodel is an INDEPENDENT client effect with its own killTime (now 24h). Two consequences: (a) deleting the emitter prop left its light burning for 24h; (b) the build-mode GHOST is a real entity running the idle anim, so mer | parentlink inside the blockdlight parameter block (all 8 TIKIs). cg_tempmodels.cpp:578: a parentlinked tempmodel returns false - dies - the moment its parent entity is not currentValid. Deleting the prop kills the light; the ghost's strays  |
| `bug-1404` | Build-mode placements do not survive a map reload; found while checking the user's work after a crash mid-build | Two halves of one feature that were never joined. buildmode.scr:772 writes coop_mod/save/build_<map>.dat as paste-ready SCRIPT TEXT and nothing reads it back. props.scr:46 reads coop_mod/save/props_<map>.dat, a pipe-delimited DATA format, a | Added _research/buildmode_to_props.py: parses the build capture (model/origin/angles/scale/solid per spawn block) and appends to props_<map>.dat in the format props.scr already parses, deduping on model+position to 0.1u so it is idempotent, |
| `bug-1402` | _research/hud_slot_map.md listed 88-99 as free; fogmode.scr actually occupies 88-94 | The slot map is hand-maintained and fogmode.scr claimed 88-94 without updating it. Following the table would have put the arena deploy curtain straight on top of the fog HUD - exactly the co-display collision the table exists to prevent. | Added the 88-94 fogmode row and the 95-96 arena-curtain row. ALWAYS census the actual calls before claiming a slot: grep -rhoE "ihuddraw_[a-z]+\s+\S+\s+[0-9]+" coop_mod/*.scr maps/*.scr \| awk '{print $3}' \| sort -n -u |
| `bug-1382` | user: 'will need the door removed where the allied is holding in the room with the 30 cal, will need the rear gate removed that is used to drive out with the jeep' - arena blocked by map doors/gates. | Not a defect - a missing capability. Every func_door / func_rotatingdoor in e3l4 is UNNAMED (targetname '-' throughout map_entities/e3l4_entities.txt), so there is no $name to reference from script and no safe way to hardcode which one the  | Aim-and-delete: BACKSPACE removes the entity under the crosshair, END saves the list. The trace already returns ['entity'] and ['entityNum'] (scriptthread.cpp:7285-7312). Persistence is by ENTITY NUMBER - BSP entities spawn in a fixed order |
| `bug-1381` | user: 'I want to save all of my work (spawns, invisible walls, coop spawn, and objects) ... What do I need to do to not lose any of my work?' - two of the three save systems could not reload. | Only barriers had a real loader. (1) build_<map>.dat is an EXPORT - paste-ready 'local.m = spawn script_model' blocks meant to be hand-baked into a map script; buildmode.scr:142 reads it only to APPEND, never to respawn, so props vanished o | New coop_mod/props.scr: props_<map>.dat as 'tik\|x y z\|pitch yaw roll\|scale\|solid', with a real character-scan parser (no split() in this dialect) and prop_vec shared by both loaders. Wrote the real holdout_load parser + holdout_load_spa |
| `bug-1380` | user: 'Idk how to turn off the wallgun to start doing the rest of this' - the barrier tool shipped with no off switch. | Two omissions. (1) No disarm cfg existed at all. (2) wall_cmd_monitor never checked coop_wall, so even setting it to 0 left every bind live - F9 kept placing barriers while the user was trying to use holdout or build mode - and the preview  | wall_cmd_monitor now refuses every edit command while coop_wall != 1 and says so; the preview hides/shows on the gate instead of freezing; the preview loop always starts and self-gates each frame so the tool can be armed and disarmed with n |
| `bug-1364` | Pinned-row text pushed as 'set coop_cpN <title> <p>/<t>' - the three-space column padding collapses to one space on the wire. | Cvar_Set_f (cvar.c:936) takes its value from Cmd_ArgsFrom(2), which REJOINS the tokenised args with a single space. Multi-word values survive unquoted (which is why the coop_so1/so2 pattern works, and why bug-758 required unquoted), but any | Use a visible separator - ' - ' - instead of whitespace padding for the name/progress split. |
| `bug-1358` | user: more allied skins wanted; new skins need rank+challenge unlock criteria and must be accessible from the loadout | armory had 88 skins and only 76 baked loadout preview pages (s01-s76) - the 12 BA-pack skins added earlier were registered but UNREACHABLE in the loadout ring, and any further additions would have been too | installed 33 vetted community skins (AAAA archive + ModDB 1st Rangers) after re-verifying BOTH collision classes at install time (one archive, US Sniper SKIN.pk3, was rejected for a 'nopicmip' shader collision the report missed). Armory 88- |
| `bug-1353` | user: 'can bias be increased over 1? It seems that is the max' + what does skybox far do | farplane_bias is a DISTANCE IN WORLD UNITS (renderer: GL_FOG_START = farplane_bias, GL_FOG_END = farplane_distance, tr_backend.c:1480) but the editor treated it as a 0..1 fraction: 0.05 steps capped at 1, i.e. the entire control could only  | bias now steps 100u, capped one step below the fog distance (a start beyond the end inverts the ramp) and floored at 0; presets rescaled to ~30%/40% of their distance; skybox_farplane applied unconditionally; HUD relabelled 'FOG STARTS AT < |
| `bug-1351` | user: 'I am getting Frontline Sweeper credit when I'm killing Italian enemies' | chal_facHas called 'waitexec global/strings.scr::instr' - that file does not exist ANYWHERE (mod tree or any retail pak; the coop copy is coop_mod/strings.scr and its own header comment quotes the non-existent global/ path, which is what mi | chal_facHas rewritten self-contained (plain char walk, returns 1/0, no external dependency). Cost is irrelevant - chal_factionOf caches per model. Added a '^~^~^ FACTION <model> -> <faction>' log line (one per unique model per map, cache-bo |
| `bug-1346` | user request: challenges that track kills by specific enemy skins/actors (23 faction challenges) | feature - the kill hook classified by the KILLER's weapon only, never the victim | new AXIS FORCES category + 23 defs; chal_factionOf classifies the victim MODEL by substring (most-specific first, cached per model, non-troops excluded); hooked into chal_ai_killed; SR menu spec extended (gen_sr4.py) -> pages P15/P16. Mappi |
| `bug-1326` | user: spectate should count down at least 10 min - still 60s despite the bug-1258 fix | ui_startdmmap (UI_StartDMMap_f, cl_ui.cpp:3422-3480) RE-PUSHES g_inactivespectate/g_inactivekick (plus g_gametype, teamdamage, frag/timelimit, maxclients, hostname, cheats 0) from the archived ui_* menu cvars via Cbuf AFTER start_server.cfg | seed ui_inactivespectate 600 + ui_inactivekick 0 in start_server.cfg so the engine's own re-push writes the intended values. TRAP: any future set g_<X> in start_server.cfg for the re-pushed keys must seed the ui_ twin too |
| `bug-1283` | unknown command: " / ^~^~^ Script file compile error: Couldn't compile 'coop_mod/buildmode.scr' / Script 'coop_mod/buildmode.scr' was not properly loaded | A string literal written across four lines. The parser cannot do that: it read the opening quote as a bare command ('unknown command: "') and then failed the WHOLE file, because a compile error in this dialect is file-wide, not line-local.  | Collapsed both banners to single-line strings. Added a tree-wide scanner for the general defect - count quotes in the CODE portion of each line (comment- and string-aware) and flag any line with an odd count. It found these two plus two gen |
| `bug-1279` | t3l1: 'immediately after the scripted dialogue (truck blows up) I get access to guns, but they give me the default map guns like the ppsh, mosin, revolver... it isn't until I go dbno, die, respawn tha | The map's own SP kit script takes the armory loadout back. maps/t3l1.scr:52-59 runs 'exec coop_mod/replace.scr::takeAll' followed by six '::item weapons/...' gives (Nagant revolver, Mosin, PPSh, SVT, F1, RDG smoke) - and crucially that bloc | Made an armory loadout outrank a map's scripted SP kit, in the shims rather than per-map. Added two helpers to replace.scr: coop_armoryActive (player has picks and the loadout is not locked - NIL tested before .size, since an unset array th |
| `bug-1278` | e3l2: enemies from the open truck spawn right in front of the player and the rescued British POW appears at the wrong time | Downstream symptom of bug-1251, not an independent defect. maps/e3l2.scr:171-173 registered coop_watchDestroyable on $Cannon1/2/3 and their throbbing boxes; because that failsafe self-detonated ~0.3s into map load, $Cannon3_Throb was BlowUp | No separate change required - the coop_watchDestroyable arming guard (bug-1251) leaves the throbbing boxes intact, so 'show' succeeds at line 144 and the chain runs in authored order. Recorded so the linkage is not re-investigated as its ow |
| `bug-1271` | Sticky bombs do nothing on any objective - truck and tanks alike; timer appears briefly then never explodes | TWO separate things, one a real bug and one a misunderstood design. THE BUG: StickyBombUse did `self thread StickyBombHide self.myent` then `self show ; self takedamage`. StickyBombHide sets `nodamage` on EVERY entry of self.myent, and self | Extracted global/stickybomb.scr into the mod and changed the call to `self waitthread StickyBombHide self.myent`, making the ordering deterministic: disable the group first, then re-enable the one actually used. The missing stickybomb_plant |
| `bug-1269` | Script '<file>' was not properly loaded - shipped three times in one session (buildmode.scr, itemhandler.scr) | Two distinct authoring mistakes, both invisible to depthscan2.py because it only balances braces. (a) buildmode.scr: a nested `waitthread` used as an ARGUMENT - `waitthread ...::coop_fog_doCmd ( waitthread coop_fog_mapKey local.cmd )`. (b)  | Fixed both (split into two statements; single-line println). Wrote scratchpad/linecheck.py, which flags any line starting with a binary operator - validated at 455/455 real files passing, its single hit being inside a /* */ block comment in |
| `bug-1262` | Fog editor cfg silently destroyed build mode's numpad bindings, and vice versa | The first version of the fog tool shipped its own cfg binding all 16 numpad keys. buildmode.cfg binds the same 16. Whichever cfg was exec'd last won, leaving the other tool completely dead with no error. Self-inflicted, same session. | Fog is now a SUBMODE of build mode, riding the existing modeswap bus (the mechanism SOUND mode already uses): backslash cycles object -> sound -> fog. Added coop_fog_mapKey in buildmode.scr to translate build action names to fog actions, an |
| `bug-1261` | Build mode does nothing - no build view, hotkeys dead | coop_build_monitor was gated on coop_dev == 1, but coop_mod/start_server.cfg does 'set coop_dev 0' on EVERY server start - and starting a server is how a coop map is launched (the Apply button execs it). So the documented flow 'exec buildmo | coop_build_boot is archived and is only ever set by cfg/buildmode.cfg, so it is already a deliberate persistent opt-in; the gate now accepts EITHER coop_dev 1 OR coop_build_boot 1. Added a second BUILD_BLOCKED line naming the no-host case s |
| `bug-1259` | No briefings for any Spearhead or Breakthrough mission; e2l3 leaks a hidden invulnerable axis actor per tank; bomber.scr dereferences a deleted plane | (a) The three briefinge scripts in the mod were byte-identical retail copies: SP-only spmap, bare level waittill prespawn and spawn, single-entity $player references, and playmovie, which is ScriptThread::EventPlayMovie calling gi.SendConso | (a) Wrote all six briefings on the m-series briefing1.scr pattern; cinematic IS on the client whitelist (cgame/cg_servercmds_filter.cpp line 118), so the film is pushed per-player via stufftext and the coop ready gate advances the map. Regi |
| `bug-1252` | E3l2 soft lock: no PIAT, tank2 invincible, soldier will not follow, gate never opens. All SH/BT maps spawn with Colt45 and Garand. | The switch had no case for e2l2/e2l3/e3l1/e3l2/e3l3/e3l4/t1l1/t2l1/t2l3/t2l4/t3l1/t3l2 and ended with a TODO comment. Retail issued these in global/loadout.scr, but every block there is wrapped in if(level.gametype == 0) so none of it runs  | Added 12 map cases ported from global/loadout.scr (BT) and original-scripts/spearhead/global/loadout.scr (SH). Verified all 20 referenced weapon TIKs exist in the mod. Retail wrote 'ammo bazooka 2' for the PIAT but Uk_W_Piat.tik declares am |
| `bug-1248` | user: I placed objects in m1l2a but don't see them - did they get removed? | NOT DATA LOSS. build_<map>.dat is an EXPORT, not a save: buildmode.scr only writes it, and re-reads it solely to APPEND so a later session cannot clobber an earlier capture. Nothing respawns from it at map load. Verified exhaustively - a di | Copied all 7 captures into the sandbox homepath so future sessions continue the same capture. Baked m1l2a's 75 placements into maps/m1l2a.scr as a `coop_bakedProps` label, threaded (not waitthread) from main. Renumbered the coop_build #N co |
| `bug-1205` | Script Error: Can't find 'coop_modhelmet.scr' / Script 'coop_modhelmet.scr' was not properly loaded - thrown from coop_mod/loadoutpick.scr lines 436-437 every time the ARMORY menu is opened (qconsole  | loadout_menuCtl's ',w0o' (menu opened) branch called the cosmetic lock exporters with a BACKSLASH path separator: `thread coop_mod\helmet.scr::armory_skin_exportLocks`. The script loader swallows the backslash, so the path resolved to the n | Both lines changed to `coop_mod/helmet.scr::...`. Grepped the whole coop_mod tree for other backslash script paths - only one remaining hit and it is inside a comment (lobby.scr:1093, a Windows file path). |
| `bug-1185` | UI/HUD text quality is poor everywhere - user flagged it on the rank-up 'PROMOTED' card and on pickup messages ('Recovered 100 Health / Got 10 Rifle Rounds'), noting it 'appears in all areas'. | The shipped hi-DPI glyph atlases are ESRGAN AI-UPSCALES of the original low-res bitmaps (per the project's own cerebrum notes: '4x facfont-20', 'TEST 4x handle-23 [ESRGAN x4plus]'). Upscaling a bitmap font invents detail and produces uneven | Regenerated all four atlases by rendering Bahnschrift (a real vector font) directly into each atlas's existing cell rects, via _research/mkfont.py. Because RitualFont locations are design-space and normalised at load, redrawing inside the e |
| `bug-1127` | Menu text 'looks like hot garbage' at 3440x1440 ultrawide (both renderers) | virtualres-1 menus with no scalecvar scale by NON-UNIFORM (vidWidth/640, vidHeight/480) in UIWidget::SetVirtualScale (~2555) = 5.375x3.0 at 3440x1440 -> 1.79x horizontal glyph stretch + over-magnification. Renderer-agnostic (tr_font.cpp ide | PENDING (user chose 'menu center'): uniform scale + horizontal centering, spec being produced+adversarially verified by ultrawide-menu-center workflow (hit-testing must move with draw; 4:3 identity; wall-board/loadout-3D/HUD exempt). |
| `bug-1001` | User: 'casemate is a square box with a texture and thats it' / 'all of the items in the blueprints are small squares'. Session log: zero BUILD_BP_PLACED lines, shape tiks never loaded. Plus at build-m | Two issues. (1) UX: every STRUCTURES-category item (bunker builders AND bp: templates) previews as a lone ~16u box_concrete anchor-cube ghost with no indication that KP_ENTER constructs the real multi-piece structure - user judged the items | Ghost name for structCat now appends '[KP_ENTER = BUILD IT HERE]' + iprint hint on selection ('cube = aim marker'); session defaults init coop_build_name to empty string. Deployed. NOTE: actual shape-piece rendering (orientation risk from b |
| `bug-962` | buildmode dead ('not properly loaded'): structure-capture strings contained REAL newlines (patcher escape mangling) = whole-file parse kill; ALSO bunker solidify used unscaled bounds (agent review): s | Python patcher wrote literal 0x0A inside morfuscript string literals (line 423-427). Separately, solidify ignored per-entity scale (bug-829 class - getmins returns BASE bounds). | Escapes repaired to proper backslash-n; odd-quote line sweep added to the QA recipe; demo pillbox removed from e1l2 (user verdict); solidify multiplies by local.e.scale property (fixes mixed-scale pieces too). Compile verified headless (0 e |
| `bug-961` | Event 'damageable'/'enter'/'flickeralpha' does not exist (33/31/11x) in the 16:03 game.dll family - feared mine-solidity regression | Event registry lookups failing for EV_RETURN-typed keyval events in a heavily INCREMENTALLY rebuilt game.dll (a dozen rapid edit-build cycles that day). listener.cpp/trigger.cpp unmodified; NaN cherry-pick and the new markwall/killwall even | Clean rebuild deployed + functional verification. POLICY: after long incremental edit chains, build fgame/cgame/exe with --clean-first before deploying; verify with the mine-spawn error signature (grep "Event 'damageable'" = 0) in a headles |
| `bug-957` | mid-game rank-up bar 'still messed up' (user screenshot): chunky gold slab overhanging the XP bar frame + white smear on the empty track during PROMOTED | Pixel-measured the screenshot against the code rects (x/5.375, y/3.0 virtual mapping): the 'gold block' is at (250..275, 29..42) = slot 66 rank-up halo, NOT the fill (fill 263..276, 33..38 was correct all along). xpbar_glow.tga had NO radia | Regenerated xpbar_glow.tga as a true radial halo (white RGB, cos^2 alpha falloff 255->0). Art-only; also improves the ceremony (slot 70) and debrief glow reuses. Ships with next deploy. |
| `bug-848` | Feature request: on the end-of-mission debrief, list everything unlocked during THAT map, reset each map. | No per-map record of unlocks existed; unlocks flowed through chal_add_unlock into the lobby pending queue (persistent) but nothing showed them at the mission-end debrief card. | challenges.scr::chal_add_unlock now ALSO appends each genuinely-new unlock display name (reused chal_unlock_displayName) to a per-map, per-player accumulator player.flags['coop_mapUnlockList'] ('~'-joined), auto-resetting via a level.coop_m |
| `bug-829` | Build-mode GEOMETRY box placed solid at max scale 8.0 is not actually solid (players/AI walk through it) and cover system cannot trace against it; also feels too small - reported by user. | MOHAA script scale command only resizes the VISUAL mesh (engine Entity::setScale sets edict->s.scale only, never touches edict->r.mins/r.maxs). A script_model marked solid uses SOLID_BBOX from its BASE bounds, so a scale-8 box renders ~128u | In coop_build_place, after scale+solid, when a non-actor object is placed solid at scale != 1.0, read the BASE bounds off the already-loaded ghost (ghost.getmins/getmaxs - the freshly spawned model model keyvalue is POSTED so its getters ca |
| `bug-819` | Mid-game rank-up bar (xp_hud_popup, top of screen) filler STILL too big for the brass frame after the bug-805 fix deployed (user re-screenshotted). | bug-805 IS deployed (verified: coop_mod/xp.scr in both the homepath and basepath zzzzzz_co-op_hzm_mod_code.pk3 is byte-identical to the working copy, clamp lines present) - so 'fix didn't ship' is ruled out. The bug-805 fill clamp (0..120)  | xp.scr xp_hud_popup: head glint (slot 65) rect y32 h7 -> y33 h5 at BOTH the setup draw and the animated loop draw, seating it in the channel exactly like the fill (263 33 w 5). Fill/shine (slots 63/64) left at y33 h5 - they already sit insi |
| `bug-808` | Lobby unlock-list feature hardening: connect-time rank backfill (xp.scr::xp_rank_unlock 0..rank, threaded from xp_identify) could race chal_ensure's unlock-set load; chal_add_unlock would then dedup a | chal_add_unlock read flags[coop_chal_unlocks] directly without guaranteeing chal_ensure had loaded it; the xp_identify->xp_rank_unlock path is a separate thread that can run before the challenge save is deserialized. | chal_add_unlock now starts with waitthread chal_ensure local.player (early-outs once loaded; the loading-singleton guard makes the threaded-backfill wait until the load completes, no deadlock since the backfill is its own thread). Also fixe |
| `bug-807` | Wrong reference: 796 should be 805 | Used "796" instead of "805" | Changed 796 â†’ 805 |
| `bug-805` | Mid-game rank/progress bar filler drawn too big for the brass bar frame (ultrawide). | Measured xpbar_base.tga (1024x128): groove channel = x71..x998, y28..y95 (fractions 0.069/0.976, 0.219/0.75). Two fillers violated it: (a) xp_hud_popup head glint hx=w+259 -> at full bar (w=120, the state a rank-up forces for ~1s before the | xp.scr xp_hud_popup: explicit int clamp 0..120 on the fill width; glint capped at hx<=375 (opaque core stays inside the channel); fill/shine y 34->33 and glint y 33->32 (centers in the measured channel, same proportions as the user-approved |
| `bug-803` | User: 'I picked a skin I didn't have unlocked yet, it forced me back to the one I did have unlocked in lobby, but once I got into the actual level I have the skin I haven't unlocked yet.' (locked-skin | The v3.2 armory skin PREVIEW pages seta'd dm_playermodel + coop_loSkin on every page VISIT - merely browsing a LOCKED skin archived it. Every armory APPLY path correctly refused locked skins (armory_skin_set gate, spawn re-force cosmetic_is | (1) gen_loadout3.py: skin preview pages no longer write dm_playermodel or coop_loSkin at all (per-page asserts + a global no-emitted-file-touches-dm_playermodel assert); both cvars are now written ONLY by the server's gated success path hel |
| `bug-801` | User: 'I picked a skin I didn't have unlocked yet, it forced me back to the one I did have unlocked in lobby, but once I got into the actual level I have the skin I haven't unlocked yet.' (locked-skin | The v3.2 armory skin PREVIEW pages seta'd dm_playermodel + coop_loSkin on every page VISIT - merely browsing a LOCKED skin archived it. Every armory APPLY path correctly refused locked skins (armory_skin_set gate, spawn re-force cosmetic_is | (1) gen_loadout3.py: skin preview pages no longer write dm_playermodel or coop_loSkin at all (asserts enforce it, incl. a global no-emitted-file-touches-dm_playermodel assert); both cvars are now written ONLY by the server's gated success p |
| `bug-787` | User design REVERSAL (pre-release, supersedes bug-759/772 hidden-locked redirects): armory skin AND helmet pickers must cycle ALL entries again, with locked ones marked by a lock icon + unlock-require | bug-759/772 hid locked cosmetics via server-pushed per-page redirect chains (coop_loSkinN/P<NN> + NA/PA archived, 56+68 cvars + 112/136 init.cfg seed/replay lines); user reversed the design for release. | GENERATOR: s/h pages hard-link literal neighbours again (DIRECT full wrapping ring; init.cfg seeds coop_loCharN/P + coop_loHelmN/P to the seed pages' real neighbours; ALL 260 redirect seed/replay lines removed - asserted 0 stale coop_lo(Ski |
| `bug-786` | User (pre-release): remove the FIT button from the armory header - the fit-tune panel is a dev tool and must not ship visible | The FIT toggle Button (fitToggle, rect 484 8 68 16) was emitted unconditionally into the armory header. | gen_loadout3.py: fitToggle emission (FITBTN/reg/W block) removed; header spot left unregistered. ALL fit-tune machinery kept console-reachable: `toggle coop_loFitUI 0 1 ; exec ui/loadout/fitbinds.cfg` (or plain set coop_loFitUI 1) opens the |
| `bug-782` | Wrong operator: >= should be <= | Used ">=" instead of "<=" | Changed operator >= â†’ <= |
| `bug-781` | Incorrect value in code | Had "FIT toggle button missing" | Changed to "FIT toggle button must not be emitted (removed fo |
| `bug-773` | User playtest (co_lobby1): picking a HELMET in the armory (opened from the lobby) does not update the lobby character/mannequin; pick also seemed not to stick. Also: post-ready launch countdown should | THREE stacked causes, all evidenced in qconsole.log 07-18 10:45. (1) MISSING HELMET CLOSE-COMMIT: loadout_menuCtl's close branch ('x') re-fired only the SKIN pick (vstr coop_loASkin) - the helmet half of the bug-755 recipe was never added,  | (1) loadoutpick.scr: close branch now also threads new loadout_closeHelmCommit (wait 0.75 + token-free-name wait, then stufftext 'vstr coop_loAHelm') - the exact loadout_resend stagger, so the ,hn token is not dropped behind ,sn/,w0x (first |
| `bug-771` | Wrong reference: 758 should be 767 | Used "758" instead of "767" | Changed 758 â†’ 767 |
| `bug-770` | Wrong reference: 758 should be 767 | Used "758" instead of "767" | Changed 758 â†’ 767 |
| `bug-762` | Wrong reference: 754 should be 756 | Used "754" instead of "756" | Changed 754 â†’ 756 |
| `bug-761` | Wrong reference: 754 should be 756 | Used "754" instead of "756" | Changed 754 â†’ 756 |
| `bug-758` | qconsole.log spam 'Cvar coop_loLkA65..69 does not exist.' / 'Cvar coop_loCmt65..69 does not exist.' - armory locked-weapon padlock/commit cvars never created client-side; deny/one-per-class chip clear | Same wire truncation as bug-736: 7 stufftext sites wrapped values in escaped quotes (and 3 of them were also ;-joined multi-statement). Player::EventStuffText sends stufftext "<cmd>", so the first embedded quote ends the client's Argv(1) ea | Three recipes, all one-statement-per-stufftext: (1) space-containing values sent UNQUOTED - client Cvar_Set_f rejoins argv 2.. via Cmd_ArgsFrom(2), and cg_servercmds_filter only validates the variable name then skips to the next ';'/newline |
| `bug-757` | change request: remove Helmet Next/Prev and Emote (salute/at-ease/stretch) bindable actions from Options>Controls; keep Armory/lobby helmet flow and console access | helmet cycling moved to the Armory/lobby wardrobe and emotes were deemed not worth Controls-menu real estate; the 5 binditem entries (,e1x ,e2x ,e3x ,hnx ,hpx) were leftover keybind surface | removed 5 binditem lines from ui/BIND.SCR (Coop Emote: Salute/At Ease/Stretch, Coop: Helmet Next/Prev); updated stale autoexec.cfg [305] comment to console-only wording. Bus untouched: variables.scr indices 23-25/35-36 and player.scr dispat |
| `bug-755` | User (co_lobby): (1) picking a skin + hitting DONE doesn't update the lobby character/mannequin; (2) re-opening the armory shows the default skin again (picker resets) | TWO causes. (2) RE-OPEN RESET: ui/loadout/init.cfg reseeds coop_loChar to american_army + the CharN/P chain seeds on EVERY open and nothing replayed the persisted skin page (the weapon side has coop_loOpenInspect; skins had no equivalent -  | gen_loadout3.py: (a) ported bug-740 into the emitters (skin pages seta coop_loSkin + coop_loASkin absolute token; URC skin buttons 'vstr coop_loCharN/P ; vstr coop_loASkin'; asserts updated - bare ,sn/,sp now asserted ABSENT, per-page locks |
| `bug-751` | Wrong reference: 739 should be 750 | Used "739" instead of "750" | Changed 739 â†’ 750 |
| `bug-750` | SERVICE RECORD no longer opens (user report + shot0222: lobby PREV/CLOSE/NEXT buttons with NO panel). Log: parse error: syntax error, unexpected TOKEN_LOGICAL_AND | chal_menu_refresh (added 07-17 live-refresh) split its while() condition across 3 lines with && at the START of the continuation lines. The Morpheus parser ends the statement at the newline, so a LEADING logical operator is a syntax error - | Joined both conditions onto ONE line: challenges.scr chal_menu_refresh while(), ab41_scene.scr AB41 istouching if(). Verified: loose-file override in homepath -> co_lobby1 boots with 0 parse errors / 0 not-properly-loaded, and name-bus toke |
| `bug-739` | SERVICE RECORD no longer opens (user report + shot0222: lobby PREV/CLOSE/NEXT buttons with NO panel). Log: parse error: syntax error, unexpected TOKEN_LOGICAL_AND | chal_menu_refresh (added 07-17 live-refresh) split its while() condition across 3 lines with && at the START of the continuation lines. The Morpheus parser ends the statement at the newline, so a LEADING logical operator is a syntax error - | Joined both conditions onto ONE line: challenges.scr chal_menu_refresh while(), ab41_scene.scr AB41 istouching if(). Verified: loose-file override in homepath -> co_lobby1 boots with 0 parse errors / 0 not-properly-loaded, and name-bus toke |
| `bug-736` | Briefing READY-UP overlay + player names never appear (3rd report); log spams 'Cvar coop_gate_* does not exist.' | coop_gateSet wrapped the cvar value in escaped quotes: stufftext 'set coop_gate_x "1"'. Player::EventStuffText transmits the command as stufftext "<cmd>" (player.cpp SendServerCommand 'stufftext \"%s\"'), so the embedded quote ENDS the wire | coop_gateSet now sends the value UNQUOTED (safe: one statement per stufftext; client Cvar_Set_f uses Cmd_ArgsFrom(2) so multi-word values like names/tally arrive whole, runs of spaces collapse). Also added a bounded force-resend: every 7th  |
| `bug-722` | Audit: watching a BRIEFING counted as completing a mission (briefing1-6 route through missioncomplete with the NEXT map's name) - ms_1 earnable from the couch. | chal_mission_complete bumped map_/missions unconditionally; briefings pass the next map's name so the gate had to key on level.coop_mapname. | Early-out when the REAL current map contains 'briefing'/'co_lobby' or level.coop_lobbyMap==1. |
| `bug-721` | Audit: teamkilling allied AI advanced every kill-family challenge (total kills, headshots, melee, weapon mastery). | chal_ai_killed had no victim-team gate. | Early-out unless local.victim.team == "german". |
| `bug-718` | Self-review: reorg left duplicate coop_chal_catLabel['campaign'] (old 'CAMPAIGN FEATS' overrode new 'CAMPAIGN'); plus 8 dead old-category label lines. | The reorg apply script's catLabel-block regex was non-greedy and stopped at the FIRST catLabel line, leaving the old per-category label lines (325-333) in place; line 333 catLabel['campaign']='CAMPAIGN FEATS' won over the new line 323 'CAMP | Removed the 9 stale old-category catLabel lines (325-333); the new complete block (315-324) stands. |
| `bug-716` | Self-review: client one-per-class registry went stale when the SERVER cleared a primary (join-resend path). | loadout_setBody's server-side one-per-class clear blanks the other slot's chip cvars but never touched the client's archived registry (coop_loWho_<class>/coop_loUnregP<S>); the join resend clears server-side without running the client commi | When loadout_setBody clears the other primary, also stuff the client registry in sync (coop_loUnregP<other>='', coop_loWho_<class>=exec clrP<slot>.cfg, coop_loUnregP<slot>=exec unreg_<class>.cfg). Values mirror the client commit cfg so it's |
| `bug-715` | Self-review: on a listen server the host's armory skin bled onto every skinless remote player. | coop_loSkin is a SERVER-GLOBAL cvar = the host client's skin. manageAliveSpawning + loadout_resend fell back to getcvar coop_loSkin for players whose coop_armorySkin flag was NIL; on a listen server that is the host's skin, which passes arm | Gated the coop_loSkin fallback (both the spawn re-force and the resend capture) to the HOST only (flags['coop_isHost']==1, entnum==0). Remote players rely on the server-authoritative per-player coop_armorySkin flag. |
| `bug-714` | Self-review: Scripted Havoc ('2+ kills with a single exploding barrel') could be earned with TWO separate barrels within 1s. | chal_ai_killed barrel-burst counter used a time-only per-killer window, not the specific barrel; two barrels each killing 1 enemy within 1s counted as 2. | Also key on the inflictor barrel entity (coop_barrelBurstInf): a different barrel or an expired window starts a fresh count. All victims of one blast share the same inflictor + frame. |
| `bug-713` | Self-review: real-time challenge-panel refresh loop could re-show a closed panel (paced redraw re-paints slots the close/autoclose just cleared). | chal_menu_refresh re-runs chal_menu_draw every 0.5s; chal_menu_draw pages itself with waitframe every 4 rows and only rechecked player!=NULL, never page/token, so an in-flight redraw finished painting after the panel closed. | Added an optional local.guard arg to chal_menu_draw; the in-game + refresh callers pass 1, the LOBBY caller omits it (lobby never sets coop_chalMenuPage so it must not self-abort). Guarded pacing loop bails if coop_chalMenuPage != local.pg  |
| `bug-712` | Briefing ready-up prompt + player-name roster + ready checkmarks never show on the campaign briefing screens (ultrawide listen-server host); health bar + compass DO show. | The briefing's ONLY visible ready-up path is the coop_gate_* client-cvar bridge that the slide URCs (ui/briefing1a-g.urc) read via enabledcvar/linkcvar (the ihuddraw roster is drawn behind the fullscreen briefing menu = invisible, per bug-6 | Moved the coop_gate_* cvar-bridge push OUT of the fragile secondary coop_readyGateHud thread and INTO coop_readyGateStart's own arm/ready-check while loop - the same guaranteed-running loop that enables F-to-ready and computes the tally (it |
| `bug-707` | Armory does not stop equipping two of the same weapon CLASS into Primary 1+2, and does not stop equipping guns not yet unlocked. | Both rules (one-per-class clear + unlock gate) live ONLY in loadoutpick.scr::loadout_setBody, which runs server-side via the name-bus token dispatch. The DISCONNECTED main-menu armory has no server/no $player, so tile clicks just seta the c | Mirrored both rules CLIENT-side (parse-safe, single-level quotes only). LOCK GATE: t<id>.cfg now commits via 'vstr coop_loCmt<id>' instead of 'vstr coop_loCcur'; coop_loCmt<id> is archived per-gun by the server in loadout_ui_exportUnlocks ( |
| `bug-684` | Briefing ready-up (roster + 'Press F to Ready Up') never shows, though health bar/compass/score DO show in the briefing. | The briefing map (maps/briefing/briefing1.scr:14) calls `drawhud 0` for the cinematic. That suppresses the CUSTOM ihuddraw slots (100-116) the ready gate draws its roster + prompt into. readygate.scr forced cg_huddraw_force 1 (which re-show | Added `drawhud 1` in coop_readyGateStart when the gate activates, so the custom ihuddraw ready-up renders during the coop briefing. |
| `bug-683` | Changing skin in the armory loadout does nothing (SKIN < PREV / NEXT > buttons unresponsive; HELMET buttons work). | The operator render (charRender) was made a MOUSE RESPONDER for drag-to-rotate (AllowActivate). Its rect '12 46 134 374' ends at y=420, which OVERLAPS the SKIN buttons at y=396-411, so clicks on SKIN hit the operator (drag) instead of the b | Shrank the charRender rect to end 2px ABOVE the skin buttons (height = SKINL[1]-2-(CHAR[1]+16) = 348). Operator is ~7% smaller; skin buttons now receive clicks. |
| `bug-641` | Briefing screen hides the Ready Up prompt + player roster entirely (user: 'you don't see any of it') | Briefing slides are FULLSCREEN URC menus (bgfill 0 0 0 1) - the menu layer always draws over ihuddraw, so readygate's slots 100-116 can never show during a briefing regardless of slot number. The coopgate_* UI overlay lived in missionbriefi | Ship mod overrides of the 9 retail slide URCs (ui/briefing1a..g.urc extracted from main/Pak0.pk3) with the coopgate_* overlay resources appended as the LAST widgets of each menu - in-menu file order = draw order, and only one slide is visib |
| `bug-628` | On-screen Service Record / challenge-complete toast text too big | chal_toast_show used facfont-20 for the title + verdana-14 header (already shrunk once on 07-12 but still too large per user). | CHALLENGE COMPLETE verdana-14->verdana-10, title facfont-20->verdana-12, subtitle verdana-12->verdana-10; rects tightened. |
| `bug-626` | Armory operator soldier does pull-ups/pushups on nothing; loadout tabs overflow ('HEAVYPISTOLNADES'); inspect hint clipped both sides | charRender used modelanim americanselectionidle = a RANDOM 7-anim pool (idle/salute/pullups/pushups/jacks). Tabs were 6-across at 40 wide from x=306 -> ran to x=551, past the 306..468 weapons panel. | New coop_loadout_idle alias (single plain idle clip). Tabs now 2 rows of 3 (tab_rect) inside the panel width. Model raised + shifted right; inspect/card weapon previews shifted right; skin+helmet cycle buttons added to operator panel. gen_l |
| `bug-622` | Debrief rank insignia look aliased / low quality | Source art was hard-edged (alpha mid/hard ratio 0.032, 67 colors - drawn without AA) and gets minified 128->56 (debrief) / 13 (popup) without mips. | Regenerated all 22 insignia supersampled (drawn at 1024, Lanczos to 128; scratchpad gen_ranks2.py) with the same cream/gold language; oak leaves + colonel eagle hand-authored after the first parametric attempt read as blobs. |
| `bug-620` | e1l2 minedetector (mission-critical map gear) stripped by armory regive / DBNO revive / respawn takeall ('are we taking the mine detector in e1l2?') | Map gave the detector as a ONE-SHOT give; every loadout source (registered inv, armory picks, map default) is applied after takeall with no mission-item concept, and picks REPLACE the map loadout array so addWeaponToEveryone would not survi | New level.coop_missionItems tier: itemhandler.scr::addMissionItemToEveryone registers + gives now; spawnInventory appends the list to a COPY of whatever loadout source won (avoiding in-place growth of shared arrays); dbno revive gives it af |
| `bug-595` | Armory picks STILL not carrying to m1l1 (round 2) - and lobby LOADOUT button dead: common root suspected | OPEN. Facts: picks ARE archived (the LIVE config is homepath maintt/configs/omconfig.cfg - maintt/omconfig.cfg is a stale 0-byte decoy from Jul 4!); the regive worked in-lobby ('your loadout has been applied'); but ZERO server->client deliv | Diagnosis build: loadout_resend now fires TWO volleys (t+4 and t+15 - the late one lands after the client is active) with visible 'Armory: requesting saved picks (pass N)' iprints. If pass-2 requests show but no equips follow, transport is  |
| `bug-594` | Armory 3D preview upside down and running off-screen left after the 'corrected' framing | Reframed rendermodel params derived from the CL_Draw3DModel bounds math (modeloffset z+44, modelscale 0.25-0.40) render inverted/off-frame - the offset interacts with the camera transform in a way the static math doesn't capture. The ORIGIN | Reverted to the screenshot-proven framing; generator comment forbids re-deriving without a live screenshot. |
| `bug-592` | Armory picks did not carry over to mission 1 (user spawned with map default kit) | Two stacked failures: (1) the pick re-request lived at the TAIL of detectCoop (after fov + wait 3 + authtoken) and never executed on m1l1 (zero armory iprints all map - anything upstream ending that thread kills the tail); (2) even when it  | loadout_resend moved to its own thread launched in manageSetup next to detectCoop (nothing upstream can strangle it); loadout_set now schedules a DEBOUNCED kit re-issue (loadout_regiveWatch: 1.5s quiet, alive/not-DBNO/not-locked guards, gat |
| `bug-591` | Armory chips showed M1 GARAND in BOTH primary slots while the server had denied the second (user screenshot shot0087) | The assign cfg setas the client chip cvars BEFORE the server validates; the server's duplicate-primary denial then rejected the pick, leaving client display and server state out of sync. Any server-side denial of an already-seta'd pick desy | Removed the duplicate denial (server validation must never reject what the client UI already committed unless it can push a correction); loadout_rebuild collapses p1==p2 to one copy instead. |
| `bug-588` | Mouse stuck/dead after exiting the armory loadout menu (user report) | lobbyui loadout_open turned the engine lobby-cursor bridge OFF before pushmenu and relied on the CLOSE button's ,w0x bus token to turn it back on - any other exit (ESC) left the bridge off: dead cursor, dead lobby buttons. | Keep the lobby cursor bridge ON while the menu is open (menu holds the real mouse; the ihuddraw cursor just freezes - no usercmd deltas reach the bridge), so EVERY exit path restores control. loadout_menuCtl 'x' cursor-restore kept as a saf |
| `bug-553` | End-of-mission rank progress bar is 'massive' / 'way larger', 'doesn't fill up right', bar at the top of the screen messed up | HUD slot collision at mission end. The top gain popup (xp_hud_popup, slots 62-69), promo ceremony (xp_promo_ceremony, 70-73) and micro popup (xp_micro_popup, 73-75) share slots with the debrief card (xp_summary_card, slots 63/64/65/69/70/71 | Added level.coop_xp_summaryActive gate. xp_summary sets it =1 (only when the card shows, local.t>0) BEFORE the awards and =0 after 'wait local.t'; added a 'wait 0.5' before the card so any in-flight popup fades + clears slots 62-69 first. x |
| `bug-538` | Objective bar empty on m5l1b (and after respawn / campaign map transitions) | coop_objectivesManage only re-syncs MAIN objectives on player-COUNT change; respawn/transition doesn't change count -> bar never populated. | Per-spawn re-sync in manageAliveSpawning: clear coopObjectiveSetupDone + reset coopObjectivesPlayers so the manager re-pushes active objectives. |
| `bug-537` | Challenge completion popup re-shows already-unlocked challenges when a new one completes | chal_toast_pump restarted at queue index 0 each run and never advanced a shown marker. | Persistent coop_chalTShown high-water mark; each title shown exactly once. |
| `bug-534` | Service Record menu shows duplicate/ghosted count text (e.g. '35/40' overlapping a dim '0 / 40') | page_bg baked a fallback '0 / <target>' number into the page texture AND the URC overlays a live count Label (linkcvar coop_uiN<gi>) at the same spot. Once persisted challenge progress exists, the live 'N/T' renders on top of the baked '0 / | Removed the baked d.text('0 / '+tgt) in gen_sr4.py::page_bg. The live coop_uiN label is now the single source of the count. Regenerated the 12 SR page textures + coop_sr.urc. (Caveat: a fresh install with no persisted progress shows no numb |
| `bug-526` | user: the briefing ready-up ('skip feature') isn't showing over the briefing screen | the ready-gate roster/prompt draw via ihuddraw (cgame HUD layer). Each briefing slide menu does bgfill 0 0 0 1 - a fullscreen opaque UI-layer fill that sits ABOVE the cgame HUD, so the ready-gate is completely hidden. The functionality (F r | drew the ready-up in the UI layer: coop override of the shared ui/missionbriefingback.inc (included by every briefing slide) with cvar-gated elements (prompt + tally + 4-slot rank/name/green-check), gated by enabledcvar coop_gate_active. re |
| `bug-502` | Console auto-opens over the Field Report at launch even after removing the main.urc force line; user's ui_console stuck at 1. | UI_Init created the mini-console under `if(ui_console\|\|developer)` and never called setShow(false), so with ui_console=1 (console enabled in Advanced Options) a console box dropped at every launch. The fakk_console was already created hid | Removed the mini-console creation from the ui_console block in UI_Init - it's created only when ui_minicon is set (UI_CheckRestart already handles that). ui_console=1 now just enables the ~ console without auto-showing. Rebuilt openmohaa.ex |
| `bug-501` | Service Record disconnected menu: progress numbers (0/20, 5/10, 1/1) missing on all challenges. | Numbers came only from linkcvar coop_uiN<i> Labels, but those archived cvars were NEVER populated in the user's config (0 entries) - the in-game chal_ui_export never persisted. So the menu showed blank tracks + no numbers. | Bake '0 / <target>' beside every bar as an always-visible default (dim brass); the live coop_uiN Label still overlays real progress when a session populates it. Also made the fills glossy xpbar-style (item 7) on the menu textures + live pan |
| `bug-493` | Live Service Record panel (chal_menu_draw) hard-caps at 18 rows via fixed ihuddraw slots; per-weapon revamp created categories of 20/22/39 rows that would overflow into footer/bar-fill slots (rows sil | chal_menu paged 1:1 by category (coop_chal_catN) and chal_menu_draw drew ALL rows of a category into slots 156+r..174 (footer) with a hardcoded 'while(r<18)' blank loop. Fine when every category <=18 rows; the 90 per-weapon challenges (pist | chal_init builds a flat page map (coop_chal_pageCat/pageOff/pageSub/pageSubN, pageN) sub-paginating each category into ceil(n/18) pages. chal_menu cycles 0..pageN; chal_menu_draw resolves page->(cat,off), counts matches and only draws seen> |
| `bug-423` | Incorrect value in code | Had "UPDATE v1.1.41 - FIELD REPORT" | Changed to "UPDATE v1.1.42 - FIELD REPORT" |
| `bug-413` | bind LEFTBRACKET / RIGHTBRACKET would silently fail to bind the bracket keys for build-mode anim browse | MOHAA/idTech3 Key_StringToKeynum (cl_keys.cpp:402) returns str[0] for any single-char string and only has named entries for SPECIAL keys in keynames[] (KP_*, SEMICOLON). There is no LEFTBRACKET/RIGHTBRACKET keyname, so a multi-char unknown  | Use the literal character: 'bind [ "set coop_build_cmd animprev"' / 'bind ] "...animnext"' (and 'unbind [' / 'unbind ]' in buildmode_stop.cfg). Caught before shipping by reading the keyname table + Key_StringToKeynum. |
| `bug-401` | Script Error : Failed execution of command 'ihuddraw_alpha' for class 'Player' - lobby bottom-left roster (name/rank/ready checkmark) never appeared | lobbyRoster used the ENTITY-PREFIX form ($player[v] ihuddraw_alpha slot / local.viewer ihuddraw_shader ...). ihuddraw_* are NOT Player methods - they are GLOBAL commands taking the player as the FIRST ARG. Every draw call threw and set noth | Converted all ihuddraw_* in lobbyRoster to global form: ihuddraw_X <player> <slot> <args> (matches lobbyHideHud 'ihuddraw_alpha self slot 0' and xp.scr 'ihuddraw_shader local.player 62 ...'). |
| `bug-366` | co_lobby1 lobby: HUD icons (ammo/grenade/XP bar/rank star) still show despite lobbyHideHud; also the lobby soldier holds his rifle at the ready instead of an at-ease pose. | lobbyHideHud only cleared ihuddraw slots 20 + 27-36 and only re-asserted 8x (4s). Coop actually draws HUD across slots 20..72 (items/XP/rank at 62-72, drawn by xp.scr/itemhandler.scr), and those threads re-draw on update, so the icons leake | lobbyHideHud now hides the WHOLE 20..72 range in a loop and re-asserts continuously while coop_lobbyActive. First-pass pose: lobbyPoseAndFreeze does 'self takeall' before freeze so the torso drops to an unarmed idle (no rifle). Full hands-o |
| `bug-349` | lobby: player spawns in FIRST PERSON with gun (had to click to spawn), not locked to the static camera; also stray opel truck + compass showing | Camera attach ran from a 0.5s poll that raced the spawn - the spawn finalized to first-person AFTER my attach, undoing it. Also no auto-spawn (coop team-select requires a click) and the HUD compass showed. | Attach camera + lock from the coop_playerJustSpawned/Respawned callbacks (co_lobby1.scr opts in; fires post-spawn) with a 0.15s beat before the toggle-style trigger_camerause doUse so it holds. Auto-spawn via main.scr::skipTeamAndWeaponSele |
| `bug-333` | Build mode never activated - 'I dont think i saw the build mode text'; log shows '^~^~^ BUILD_BLOCKED coop_dev is not 1' | coop_dev CANNOT be armed via the saved config: start_server.cfg:32 runs 'set coop_dev 0' at EVERY coop server start (menu Apply), which executes AFTER omconfig.cfg is read and stomps any archived 'seta coop_dev 1'. The build monitor threads | Added 'set coop_dev 1' inside buildmode.cfg (runs after the map is up, so it wins over start_server.cfg), plus it already re-applies all 16 numpad binds. Now a single in-game 'exec coop_mod/cfg/buildmode.cfg' does everything - no restart, n |
| `bug-314` | XP bar misassembled on ultrawide: textured layers each shifted differently, bar not centered; text offset was correct | ihuddraw 'center' horizontal alignment computes TEXTURED-QUAD x-offsets differently from STRING draws (different code paths) - on 3440x1440 the quad layers land far from the true center while the text lands right. Negative-offset-from-cente | all 8 slots relaid in ABSOLUTE virtual coords with left/top alignment (virtual center = x 320: base 210, fill/shine 227, glint 221+w, glow 184, plate 196, emblem 198, text 438) - deterministic on every aspect ratio. RULE: never use center a |
| `bug-300` | XP HUD showed 'NIL' text (also deemed ugly/persistent by user) | two issues: (1) xp_identify was re-entrant - a kill award racing the connect-time identify could run a second instance / read half-initialized flags (rank NIL -> rankAbbr[NIL] renders 'NIL'); (2) design: persistent lower-left rank line was  | identify singleton (coop_xp_identifying flag; second caller waits) + xp_award NIL-guard on totals; persistent HUD REPLACED by xp_hud_popup: top-center emblem + 200px animated bar (eases 25%/frame into rank bracket) + accumulating '+N XP', a |
| `bug-298` | coop_mod/xp.scr compile error line 106: unknown command 'userinfo' -> whole XP system dead (no HUD bar, every xp.scr:: call logs 'not properly loaded') | the XP research agent claimed script can read userinfo via 'player userinfo' + info_valueforkey (cited player.cpp:1484) - NO such script command exists; one bad line = entire file fails to compile (scr all-or-nothing) | engine now PUSHES cl_guid onto the player entity as self.coop_guid in G_ClientUserinfoChanged (g_client.cpp, next to the u_shoulderaim parse; ScriptVariableList::SetVariable(name,const char*) overload verified); xp_identify polls self.coop_ |
| `bug-261` | ALL chat + feed text (dmbox AND gmbox) invisible on screen; independent of coop_hudFade; messages still reached the console log | The HUD-fade early-out in UIWidget::Display gated on m_local_alpha <= 0.02. The dm/gm message boxes are CREATED with base alpha 0 BY DESIGN (Create(..., 0) -> setBackgroundAlpha -> m_alpha=0: transparent container whose TEXT is drawn in Dra | Early-out rescoped to the fade MULTIPLIER only (m_hudFadeMul <= 0.02) - the faded-HUD end-state guarantee still holds (the multiplier is what zeroes faded panels), while designed-zero-alpha widgets keep their Draw path. RULE: an optimizatio |
| `bug-255` | HUD fade round 3: mission gadget models (TNT/radio/papers, hud_items top-right) refused to fade while everything else did | Every hud_items.urc widget declares 'fadein 0.2'. A widget with m_fadetime runs Motion() on EVERY Display, and a finished fadein rewrites m_alpha = 1.0 each frame - stomping the fade value written into m_alpha before paint. Menus without fa | New UIWidget::m_hudFadeMul (default 1.0) + SetHudFadeMul(); Display() computes m_local_alpha = m_alpha * parent_alpha * m_hudFadeMul AFTER Motion() ran, so urc fade sequences and stat_alpha logic compose with the HUD fade instead of overwri |
| `bug-254` | HUD fade round 2: only the health statbar fill faded; stat number text, panel art, and the huddraw score/icons stayed solid | Three independent draw paths ignore widget alpha: (1) UIFakkLabel::Draw sets m_font->setColor(m_foreground_color) raw for playerstat/configstring text (UILabel::Draw DOES multiply m_local_alpha - only the Fakk subclass forgot); (2) DrawStat | cl_uistd.cpp: all 3 raw setColor sites multiply m_foreground_color.a * m_local_alpha; DrawStatbar sets its base color explicitly from m_local_alpha and scales the flash by it. uiwidget.cpp Display(): early-out at m_local_alpha <= 0.02 (guar |
| `bug-157` | Main menu 'broken' after the last 2x upscale: fly-out preview images (BRIEFING world map, desk previews, OPTIONS panels) render soft/blurry | The 2x upscale pass swept in 29 VANILLA main-menu chrome textures (bigmap_h, desk_lit/loadsave/personalrecords, options_video/audio/controls/advanced, projector_lit, instant_action_game, *_sign, multiplayer_h/options_h, m_*button, loadingba | Confirmed all 29 are stock (present in base paks: Breakthrough maintt/pak1+pak2, fallback main/Pak1/Pak2/Pak6EnUk). Removed the mod's overrides (moved the 29 .tga to scratchpad/upscaled_menu_backup) so the engine falls back to vanilla via n |
| `bug-144` | Upscaled menu UI broken: massive unreadable settings text, oversized/soft buttons, hover glitches, green broken load bar | AI upscale (Real-ESRGAN) enlarged menu UI image dimensions 2x-4x; MOHAA sizes some menu widgets by their texture dimensions, so enlarged buttons/labels/bars/hover-masks render too big / misaligned. Photographic fullscreen backgrounds (drawn | Downscaled 26 layout-sensitive widgets back to original base-game dimensions, preserving AI-cleaned content, via a manual uncompressed bottom-left TGA writer (ffmpeg's targa muxer emits RLE type-10 + top-origin which MOHAA's strict loader r |
| `bug-133` | User: 'When I go in dbno, die and respawn in the boat level I dont start with the right equipment I originally spawn with' - t1l3 DBNO death -> respawn hands out the default M1 Garand instead of the B | coop_mod/loadout.scr::main is a switch(level.script) that sets each map's framework respawn loadout (level.coop_weaponLoadout/ammoLoadout, consumed by itemhandler.scr::giveWeaponLoadout on every spawn). It had a case for maps/t1l2.scr (Brit | Added case "maps/t1l3.scr": as a shared fall-through into the existing t1l2 British-kit case body in coop_mod/loadout.scr (matches the e2l1/e2l3 shared-case pattern). Framework now hands t1l3 the Webley/Enfield/Sten kit deterministically on |
| `bug-132` | Function not marked async | Function uses await but wasn't declared async | Added async modifier |
| `bug-088` | Heal / self-revive progress bars rendered grey/black and never showed their green colour | ihuddraw_color MULTIPLIES (modulates) the ihuddraw_shader texture - cgame CG_HudDrawElements does R_SetColor(vColor) then R_DrawStretchPic(...hShader) (cg_drawtools.cpp:643/667). Both bars used shader textures/mohmenu/black.tga (RGB 0,0,0), | Created a 4x4 opaque-white TGA textures/hud/coop_bar_white.tga and repointed both bars (medkit self-heal + dbno self-revive, slot 34 bg + slot 35 fill) to it. bg tinted 0.12 grey, fill tinted 0.1 0.9 0.1 green. Also shrank 200x16 -> 100x6 a |
| `bug-armory-roster76` | ARMORY skin roster stale at 28 entries while the LOBBY A/D cycle offers 76 (incl. the 3 geared soldiers rifleman/submachine_gunner/support_gunner + nohelm variants) - armory could neither preview nor  | armory_skin_build was hand-copied from an older 28-skin lobby roster and never reconciled after the lobby list grew to 76; the UI page generator SKIN_LIST mirrored the same stale 28 | Rewrote armory_skin_build to the lobby's exact 76 names in identical order (lobby.scr coop_lobbySkins[1..76]); updated gen_loadout3.py SKIN_LIST to the same 76 (+assert NS==76) and re-ran it: emits s01-s76 full Next/Prev ring, seed page s34 |
| `bug-020` | skcHeader_t/skdHeader_t etc. undeclared; then C2118 negative subscript at skx_format.h:167; then LNK2005 Com_Error already defined in misc_utils.obj | su44's 2012 md5_2_skX targets the old qcommon/tiki_local.h struct names (skdHeader_t/skcHeader_t) that this fork renamed/split. Then my sizeof guard used wrong constant (52 vs actual 48 for skcHeader_t). Then tool_stubs.c duplicated Com_Err | Created self-contained skx_format.h mirroring the fork's on-disk layouts (skeletor/*_file_format.h + tiki_shared.h) under su44's names; included it instead of tiki_shared.h; fixed skcHeader assert to 48; trimmed tool_stubs.c to Com_DPrintf  |
