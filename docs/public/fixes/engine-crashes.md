<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# Engine & crashes

[<- back to all fixes](../BUGFIXES.md)

**223 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-1797` | when I change my audio device in the audio settings and hit Apply it doesnt update | The Audio menu OUTPUT DEVICE popup was a baked snapshot of one machine enumeration, selected BY INDEX (s_setdevice N). The real enumeration order had since changed, so three of the seven entries pointed at the wrong device: picking Arctis N | s_setdevice now accepts a device NAME as well as an index - a pure integer still means an index (back-compat), anything else is matched case-insensitively as a substring against s_alAvailableDevices via Cmd_ArgsFrom, so spaces and parens pa |
| `bug-1796` | Renderer-side engine changes never reached the running game | The binaries deploy list carried renderer_opengl1.dll only, but the live install runs gl2 (cl_renderer opengl2, and PLAY-GL2.bat force-sets it). Same shape as bug-1634, where deploying to the GOG root alone never reached the live install. S | Added renderer_opengl2.dll to the build.ps1 binaries list, and registered r_desktopfullscreen in renderergl2/tr_init.c to match gl1 exactly. |
| `bug-1795` | Screen capture records the game black. SignalRGB matches colors on the main menu and loading screens, then the lighting turns off as soon as gameplay starts. Fraps, NVIDIA, SteelSeries, OBS all fail. | Not a capture-software problem - a presentation-path one. The engine presents through legacy wglSwapBuffers. With a borderless window covering the screen EXACTLY (r_desktopfullscreen 1 at desktop resolution) and vsync off (r_swapInterval 0) | Added GLimp_MakeCaptureSafe(): after window creation, when fullscreen AND r_desktopfullscreen, fetch the HWND via SDL_GetWindowWMInfo and apply WS_EX_LAYERED + SetLayeredWindowAttributes(alpha 255). A layered window cannot be promoted to an |
| `bug-1747` | there aren't any subtitles when the radio transmission plays, there were supposed to be subtitles that show exactly the same way as vanilla does it | NOT an alias defect - the aliases carried `subtitle "..."` correctly all along. The engine's SERVER-side subtitle path is hard-gated to single-player: fgame/entity.cpp:3781 opens `if (g_gametype->integer == GT_SINGLE_PLAYER && ...)`, and co | No engine change. The DRAWING half is not gated: client/cl_uiview3d.cpp View3D::DrawSubtitleOverlay reads subtitle0..3 and renders facfont-20 / URed with a per-line fade computed from string length, and cg_servercmds_filter.cpp explicitly w |
| `bug-1724` | a whole session's engine and script work was never actually in the game | game.dll was built at 23:53 but the live copies were 18:22, and the pk3 was packed at 23:26 while maps/m6l2a.scr was written at 00:07. level.stealthnative = 1 against the old binary is not even an error - Level has no such setter there, so  | Deployed and verified: both game.dll copies now contain the stealthnative event string, and all four changed scripts byte-match the repo inside the pk3. |
| `bug-1699` | user: 'Since we turned off depth of field the weird texture bug from yesterday had disappeared but its also disappointing because I want DOF to work' | THE FIX WAS REAL AND NEVER APPLIED. bug-1629 correctly identified S2 - coop_defaults.cfg shipping r_ppDoFFocus frozen at 744.985718, a focus plane leaked from a tuning session, against an engine default of auto (tr_init.c:1490) - and correc | Two parts. (1) coop_defaults.cfg r_ppDoF 0 -> 1 with the rationale and the revert instruction recorded inline; r_ppDoFFocus stays 0 with an explicit 'never re-seed a tuned plane here' warning. NOT forced in autoexec.cfg - r_ppDoF is menu-ow |
| `bug-1667` | "slow server" icon flashes almost permanently for every client - persisted even after moving to a DEDICATED server that renders nothing | Sys_PlatformInit's timeBeginPeriod block - and the `static UINT timerResolution` declaration itself - were wrapped in #ifndef DEDICATED. A dedicated server therefore NEVER raised the Windows timer resolution and ran at the ~15.6ms default,  | Removed the #ifndef DEDICATED guards around the timerResolution declaration, the timeGetDevCaps/timeBeginPeriod call and the matching timeEndPeriod. A dedicated server needs the fine timer MORE than a client, not less. Built the omohaaded t |
| `bug-1663` | user question: what drives the blinking 'slow server' phone-jack icon at the bottom of the screen, plus occasional latency flare-ups for the client | ANSWERED, not a defect. cg_drawtools.cpp:180-207 draws gfx/2d/net.tga bottom-centre when `cgi.GetCurrentCmdNumber() - CMD_BACKUP + 1` yields a usercmd whose serverTime the server has still not acknowledged - i.e. the client has sent a full  | probe shipped: sv_lagProbe 1 prints ^~^~^ SVFRAME (worst/mean/over-budget count per second) and ^~^~^ SVLAG + per-client SVLAGCL at the exact moment the icon broadcasts. Lives in the EXE (server code), not game.dll. OPEN: discriminating run |
| `bug-1658` | 2-player test: Player2 cannot SEE the blueprint collectibles that Player1 sees | ENGINE BUG, found 2026-08-10 by reading Entity::SVFlags (entity.cpp:4127-4190). The handler declares `int singleClient = 0;`, resolves `ent = ev->GetEntity(i+1)` for +singleclient, NULL-checks it - and then NEVER USES IT. `edict->r.singleCl | assign singleClient = ent->entnum in both the +singleclient and +notsingleclient branches (entnum IS the client number for a player). VERIFIED IN A 2-PLAYER TEST 2026-08-10: Player2 now sees their blueprint copies. NOTE: this command was br |
| `bug-1634` | disguise tracer produced ZERO output during a live papers interaction; auto-cover never observed working in play | the LIVE install is G:\mohaa-gl2\openmohaa.exe which loads game.dll/cgame.dll/renderer from G:\mohaa-gl2\ (process-module listing proved it) - all engine binaries deployed to the GOG root since 08-08 15:41 never loaded. Extends bug-1633 (sa | build.ps1 now deploys cgame.dll/game.dll/game.pdb/renderer_opengl1.dll to BOTH roots (gl2 live + GOG release-parity); CLAUDE.md corrected; string-scan (grep -qa coop_coverAuto game.dll) is the fast is-it-in-this-binary check |
| `bug-1633` | deployed cfg changes silently never reached the engine (r_ppDoF stayed on; DoF-off fix invisible) | the live launch profile runs with fs_homepath G:\mohaa-gl2\home; homepath cfgs SHADOW basepath copies, and stale autoexec.cfg (08-09 00:46) + coop_defaults.cfg (08-08 23:05) sat in G:\mohaa-gl2\home\maintt while build.ps1 only deployed to G | build.ps1 cfg deploy now targets all three dirs (GOG maintt, APPDATA maintt, G:\mohaa-gl2\home\maintt when present) |
| `bug-1629` | random mission starts look muddy/washed out with a ghost texture that moves with the camera (not map specific) | PARTIAL (workflow suspect S2, confirmed shipping): coop_defaults.cfg shipped r_ppDoF 1 with a frozen tuning-session focus plane (r_ppDoFFocus 744.985718 @ 0.55 intensity) - engine intent was DoF off by default (tr_init.c:1490). Remaining su | coop_defaults.cfg: r_ppDoF 0, r_ppDoFFocus 0 (auto). NOTE: seta values already archived in existing installs' configs - a migration force-set is needed before release if bisect confirms |
| `bug-1622` | Feature, not a bug: auto-cover. User ask verbatim: 'crouch behind cover and after a second it will automatically put you behind cover like modern games do'. | The shipped cover system (engine-side TickCoopCover, LOW pose) engaged only via the bus-26 bind. Research (docs/proposals/cover_attach/) established the tick already validates per-frame and the request flag is the single entry point. | AUTO ENGAGE inside TickCoopCover's not-requested branch: while crouched (PMF_DUCKED) + still + alive/unmounted, a dwell timer accumulates; at coop_coverAutoDelay (0.9s default) the SAME m_bCoopCoverRequested flag the bind sets goes true spe |
| `bug-1620` | m2l2b: touching a bomb site BEFORE taking the Enigma manifest permanently bricks that bomb - it cannot be planted afterwards. User-reproduced 11:05 playtest: 'Recover the Enigma manifest first.' at 11 | bug-1608's re-arm never worked, for two independent reasons. (1) Wrong entity: '$bombplant triggerable' targets the bomb PROP (script_model) - the BSP trigger_useonce carries NO targetname at all (map_entities/m2l2b_entities.txt: trigger at | On first refusal each site spawns a PERSISTENT replacement trigger_use (engine default count -1, never self-consumed) at the BSP trigger origin with the same setthread, using the same inline-spawn recipe as coop_exit (m2l2b.scr:30), sized ( |
| `bug-1610` | Couldn't find image file for shader enigma / enigmatext - Enigma machine still invisible in the 2026-08-08 23:35 playtest, after coop_enigma.shader was authored. | NOT a shader defect - a build-ordering artifact. The playtest client loaded at 23:34 and quit at 23:37:11; coop_enigma.shader was only packed into zzzzzz_co-op_hzm_mod_code.pk3 at 23:38:06. The session ran the build from BEFORE the shader e | No change to the shader for this entry; rebuilt and redeployed so the shader is actually present (pk3 stamp 23:47:50). Verified by reading the entry back out of the deployed pk3. |
| `bug-1566` | cdb.exe not present - documented crash-analysis recipe no longer works | The recorded recipe points at C:\Program Files\WindowsApps\Microsoft.WinDbg_*md64\cdb.exe (installed 07-19). It is gone from this machine; neither Bash globbing nor a PowerShell recursive search finds it, and no Windows Kits copy exists. | Parse the minidump directly in Python instead - scratchpad/dmp.py (exception record, module list, stack scan) and scratchpad/nodes.py (resolve globals and walk live arrays out of the memory ranges). Resolve RVA to symbol with the linker .ma |
| `bug-1565` | game crashed on 'exec coop_mod/cfg/navbake_m3l1b.cfg' - ACCESS_VIOLATION read in droptofloor | Two different questions were collapsed into one predicate. CoopNodesAreBulk() was keyed on bulkNavMemory, which CoopPrepareRuntimeRebuild() deliberately nulls. The first coop_navnode ran prepare; the coop_navbuild at the end of the cfg then | Split the predicate. CoopNodesAreBulk() (bulkNavMemory != NULL) still means 'allocation is still coming out of the block' and gates CoopNavDropNode's prepare. New CoopBulkOwnsNodes() (startBulkNavMemory != NULL) means 'nodes LIVE in the blo |
| `bug-1563` | latent heap corruption at map change after creating path nodes on a .pth map | FreePathNode() frees an individual node only when bulkNavMemory is NULL. Nulling it to allow runtime node creation therefore also flips teardown behaviour: the map-end ResetNodes() would call gi.Free on EVERY node, including archive-loaded  | CoopFinishRuntimeRebuild() restores bulkNavMemory after CreatePaths(), putting FreePathNode back to its no-op behaviour. Runtime nodes then leak into the level pool, which is freed wholesale on map change. Player::CoopNavRebuild pairs prepa |
| `bug-1561` | latent heap corruption: creating path nodes at runtime on a map that loaded a baked .pth | PathSearch::AllocPathNode decrements bulkNavMemory by sizeof(PathNode) with NO bounds check (navigate.cpp:2300). When a map loads a .pth, ArchiveLoadNodes allocates ONE bulk block sized for exactly that node count and every node lives insid | Added PathSearch::CoopPrepareRuntimeRebuild()/CoopFinishRuntimeRebuild(). Prepare drops the archived connectivity and nulls bulkNavMemory so new nodes go to gi.Malloc, WITHOUT freeing startBulkNavMemory or the node objects - ClearNodes() wo |
| `bug-1560` | coop_navrec recorded 1363 nodes but no maps/m3l1b.pth was written and the AI still could not path the rear area | EventCoopNavRec called PathSearch::CreatePaths() bare. CreatePaths opens with 'if (m_bNodesloaded) return;' and that flag is set for the rest of the map once the graph is built at load (navigate.cpp:2534), so the call was a silent no-op: th | Added Player::CoopNavRebuild() = ClearNodes() + CreatePaths(), used by both EventCoopNavRec's OFF branch and EventCoopNavBuild. CreatePaths now runs to completion and ArchiveSaveNodes writes maps/<map>.pth to homepath. |
| `bug-1547` | User: 'lets remove the frozen effect on screen for when it snows it looks really bad' | Frost-on-lens (bug-1158) accumulated an eased r_ppFrostAmt on the PRECIP_SNOW branch of CG_CoopPrecipType whenever a sky trace succeeded, driving frost_fp.glsl in renderergl2. Visually poor in play. | Deleted the cgame publisher outright rather than defaulting the toggle off - tr_postprocess.c gates the draw on r_ppFrostAmt > 0.001f and nothing writes it now, so the effect is inert on every client even with a stale archived r_ppFrost 1.  |
| `bug-1515` | User: m1l3c ocean water blend looks worse than before, wants it reverted; also asked to extend the water far out | The 2026-07-29 gl2 fix (bug-1242/1249) made alphaGen tCoord + tcMod wavetrant actually function on gl2 (previously parsed-but-ignored) - m1l3c ocean uses textures/misc_outside/northafrika_shoreline, one of the three shaders that fix explici | New mod-owned shaders/coop_water_overrides.shader defines textures/misc_outside/northafrika_shoreline (same name = takes priority over the stock Pak0.pk3 copy) with every alphaGen tCoord and tcMod wavetrant line removed - reproduces the pre |
| `bug-1499` | e1l1 sweep runs died abruptly at ~10-12k triggers with NO log line - misread as clean exits because renderer-shutdown prints appeared earlier in the log (those were the normal SERVER-INIT renderer res | THE SWEEP DELETED WORLDSPAWN. forcealltriggers force-fired e1l1's unnamed end-of-level trigger_vehicle (brush model *10). A forced fire bypasses TriggerVehicle::respondTo (TriggerStuff always answers world activates, trigger.cpp:454) and a  | FIXED 2026-08-06, deployed (game.dll hash-verified to gl2+GOG; pk3 rebuilt). Two layers: (1) ForceAllTriggers now skips Vehicle trigger classes, matching the ChangeLevel/Exit/Door precedent - coverage denominators must treat vehicle trigger |
| `bug-1494` | 5x "ERROR: func_door::MoveTo : No speed is defined!" each followed immediately by "----- Server Shutdown (Server crashed: ...) -----" | SELF-INFLICTED BY THE SWEEP HARNESS. The line immediately preceding every one of the five crashes is '^~^~^ COV FORCEALL func_door exploder' - our own forcealltriggers command force-activating a func_door that has no 'speed' key. func_door: | FIXED 2026-08-06, deployed (game.dll hash-verified to gl2+GOG). ForceAllTriggers now skips Door trigger classes alongside the existing ChangeLevel/Exit skip. Verified: zero 'No speed is defined' crashes in any run since deploy. |
| `bug-1492` | The FIRST map of every sweep launch came up with coop uninitialised: "invalid waittill spawn for 'Level'" at coop_mod/replace.scr:105, "Cannot cast 'array' to listener" at maps/m2l2a.scr:78 and coop_m | g_gametype is a LATCHED cvar. coop_mod/start_server.cfg ends with 'ui_startdmmap 2', which sets it - but the engine then logs 'g_gametype will be changed upon restarting' and the value does not APPLY until the next map change ('Gametype cha | FIXED 2026-08-06. Added '+set g_gametype 2' to the host command line in SWEEP-4P.ps1 so the cvar is already latched before the first map loads. Related but separate: bug-1491 made inCoopMode stop caching a zero, which fixes recovery on a mi |
| `bug-1478` | 190 spawn-time vehicle/item sounds resolve to NOTHING on EVERY map: snd_pickup x105 plus vehicle sounds | SV_SpawnServer wipes the global sound-alias table (CM_LoadMap -> Alias_Clear) and then spawns all map entities BEFORE parsing ubersound/*.scr. The alias table is empty for the entire entity-spawn pass, so any Sound() issued from an entity's | OPEN. Parse the alias files before the entity-spawn pass, or defer spawn-time Sound() calls until after. |
| `bug-1459` | User: 'how long will it take to time out' - answer: never; the sweep deadlocked on m2l2a with zero COVWALK output for 7+ minutes | Every sweep timeout lives INSIDE covwalk/maptest gates, but a map whose main script dies never sets level.coop_mainScriptLoaded, so coop_maptest_tick blocks upstream in waitForMainScript and none of those gates is ever reached. All the care | Harness-level stall watchdog: if no COV MAPDONE appears for 7 minutes it writes SWEEP_STALL_WATCHDOG (naming the stuck map) into the log and kills the run so it cannot hang silently. Design lesson to keep: a timeout is only as good as the c |
| `bug-1441` | Sweep relaunch: all 4 clients showed 'Abnormal Exit' windows, no qconsole.log, no FPRINT heartbeat - looked like a startup crash | NOT a crash: taskkill /F leaves maintt/OpenMoHAA.pid behind, and Sys_InitPIDFile (sys_main.c:242) then shows a MODAL 'start with safe video settings?' dialog. The process sits on the dialog forever - before the log file is opened - so every | Harness preamble purges home*/maintt/OpenMoHAA.pid before launching. Diagnosis recipe: enumerate the dialog's child windows via Win32 GetWindowText - UIAutomation returns no Text elements for this dialog. |
| `bug-1424` | User: 'ladders seem to place invisibly now' - climb worked, no visual | CORRECTED (the .spr theory was WRONG - the user had wood-TEXTURED beams working, which disproves it). SetBeamShader's appended '.spr' is only a CacheResource hint; the shader registers from the RAW name via gi.imageindex and the client auto | Textured beams restored: style token -> texture path (wood flrwood1_rep, rusty rustflat, clean argent2d2, metal drkgry1flt), shader from path, colour white, alpha 1, scale 2.5. Registry/undo (bug-1421) untouched. If invisibility recurs, dia |
| `bug-1417` | User: 'Placing the light no longer seems to actually create lights' - third light regression, after the parentlink fix | parentlink and an invisible emitter were mutually exclusive. The parentlinked update path fetches R_GetRenderEntity(parent) and KILLS the effect if NULL - and an RF_DONTDRAW parent is never added to the render scene (cg_modelanim.c skips R_ | cgame engine fix, both parentlink blocks: when R_GetRenderEntity is NULL but the centity is currentValid, fall back to its networked currentState origin/angles instead of killing. The !currentValid kill is untouched, so effects still die wi |
| `bug-1378` | feature: holdout arena authoring needs invisible barriers that block HUMAN PLAYERS ONLY - enemies, bullets and thrown grenades must pass straight through. | Not a defect - a missing capability. Script can only call 'solid', which is CONTENTS_SOLID and blocks everyone including the AI the arena is meant to funnel in. No script path exists to any other contents value. | Added entity event 'coop_playerclip' (EV_Entity_CoopPlayerClip) setting CONTENTS_PLAYERCLIP + SOLID_BBOX. Verified against bg_public.h:633-646: MASK_PLAYERSOLID CONTAINS CONTENTS_PLAYERCLIP (players blocked) while MASK_MONSTERSOLID, MASK_SH |
| `bug-1371` | user: 'is the tank on this map invincible? ive hit it so many times with a bazooka and it doesnt explode' - e3l4 bunker4tank01. Log: repeated "VEHZOMBIE ent=187 tn='bunker4tank01' hp=-279 deadflag=2 - | DrivableVehicle::Killed (fgame/vehicle.cpp:7184, HZM coop bug-724 block) does: ScriptVariable *pv = level.vars->GetVariable("coop_vehKills"); int n = pv ? pv->intValue() : 0. coop_vehKill_monitor READS level.coop_vehKills every frame withou | Assign level.coop_vehKills = 0 in coop_vehKill_monitor before the read loop, so the variable is a real int by the time any vehicle dies. Durable hardening still wanted engine-side: the intValue() call should type-check (pv->GetType() != VAR |
| `bug-1357` | Imported MOHAA skin packs silently override stock/mod shaders and stock player tiks | 2002-era community skin pk3s reuse stock shader block names and stock models/player tik basenames. The coop pak mounts last, so an imported name WINS: User-tr_fighter_pilot redefines 15 viewsleeves* shaders (every first-person sleeve in the | Vetting pass before any import: (1) extract top-level shader block names from the pack scripts/*.shader and diff vs hzm-mohaa-coop-mod/scripts/*.shader AND vs retail paks; (2) diff models/player/*.tik basenames vs stock tiks - a match repla |
| `bug-1356` | user: menu advertised 'Weapon skin' unlocks that do not exist; and every unused skin/helmet should have a way to unlock | 57 challenges carried cosmetic_*/attach_* reward tokens that NOTHING consumes - cosmetic_isUnlocked is only ever called with helmet tiks and player-skin tiks; challenges.scr's own header calls them ids 'a FUTURE loadout picker can read'. Th | (1) SR generator only labels rewards the mod actually grants (weapon/headgear/uniform), right-aligned on the title line, omitting rather than truncating. (2) all 57 placeholder rewards repointed to real cosmetics, ordered so campaign feats  |
| `bug-1324` | user: limping after hurt plays running AND sprinting sound effects when holding sprint | TickLimp requires groundentity but TickSprint's !m_bCoopLimping gate has no ground term - every airborne frame (jump/stair edge/knockback) cleared the limp flag, so held Shift+W fired genuine sprint bursts mid-limp (gear loop, SPRINT_FORWAR | split the flag: m_bCoopWounded = pure health-fraction test (no ground term); m_bCoopLimping = wounded && grounded (consumers unchanged). wantSprint and the pant now gate on !m_bCoopWounded |
| `bug-1323` | user: AB41 rail tank + opel starttruck2 both sit at negative health, smoking, alive - airstrike 'didnt do shit'; probe: starttruck2 hp=-150, AB41 previously -50 | Entity::DamageEvent (entity.cpp:2705) early-outs on health <= 0, and script 'hurt' routes through the same path (HurtEvent -> Damage). Any vehicle reaching health<=0 without its death VISIBLY completing is entombed forever: no shot, blast o | Vehicle::CoopZombieRescue(): at the top of Vehicle::EventDamage AND a new DrivableVehicle::EventDamage wrapper (its response table mapped EV_Damage straight to Entity::DamageEvent, bypassing Vehicle). If health<=0 && takedamage!=DAMAGE_NO:  |
| `bug-1320` | STANDING RULE: 'I don't ever want blood wiped from any model' - heals and gore_reset visibly wiped blood tiers off AI | CoopGoreHeal credits m_fCoopGoreDamage back and CoopGoreUpdateSkinTier recomputes DOWNWARD; the no-arg script gore_reset hard-cleared the skin bits (officer canteen heal, aihandler, DBNO revive all route through these) | tier is now monotonic within a life under new cvar coop_gorePermanent (default 1): downward re-tier suppressed; no-arg gore_reset still zeroes the damage counter + gib mark but leaves painted blood. Player respawn unaffected (Player::Respaw |
| `bug-1310` | FEATURE: bake the user's e2l1 crash-site build (62 placements) into the map script so it survives outside build mode. | build_e2l1.dat is an EXPORT - nothing respawns from it at map load (bug-1248 class). The user's C-47 debris field (6 wreck pieces, 22 fire/smoke/spark emitters, 1 dead paratrooper, 1 drag mark, 1 misc) existed only in the capture. | coop_bakedProps label appended to maps/e2l1.scr with the capture body verbatim (single session, SESSION marker stripped), threaded (not waitthread) from main after main.scr::main - the m1l2a recipe. 31 placements verified spawn-for-spawn in |
| `bug-1306` | LATENT: spectators kept suppression / on-hit blood screen tint - the 'dead/spectating' guards in cg_view.c tested h <= 0 only. | Player::Spectator() sets deadflag = DEAD_NO and health = max_health, so a spectator's STAT_HEALTH stays at max and the h <= 0 guard never fired. Latent until the scripted suppression floor made it reachable in normal play (die + spectate du | bAlive = h > 0 && snap && !(pm_flags & (PMF_SPECTATING \| PMF_INTERMISSION)), used by BOTH the suppression clear and the s_coopHit / r_ppHit clear (the second h<=0 site had the identical hole). |
| `bug-1304` | gl2 fog endgame: effects erased-or-unfogged over sky (r_globalFogSky trade), geometry pale against unfogged sky in the glider, fog mixed in the wrong colour space vs gl1. | The screen-space fog pass structurally cannot fix these: it scores pixels by the depth buffer, so a no-depth-write surface is scored at whatever is BEHIND it (sky = erased or skipped), and the sky itself cannot be fogged without erasing spr | FORWARD PER-FRAGMENT GLOBAL FOG (r_globalFogForward, default 1). ApplyGlobalFog in lightall_fp + generic_fp reconstructs eye distance from gl_FragCoord.z with the same projMat terms as the screen pass - zero new varyings, zero new permutati |
| `bug-1300` | gl2 e2l1: distant trees/flak guns/aircraft render as pale flat cutouts; 'fog isn't hiding distant objects'. Immune to every fog/DoF/bloom/exposure/tonemap/anisotropy/MSAA cvar. | renderergl2 never implemented ANY of MOHAA's four alphaGen distance-fade modes. tr_shader.c:1576-1590 parses distFade/oneMinusDistFade/tikiDistFade/oneMinusTikiDistFade and tr_staticmodels.cpp:399-420 coarse-culls on them, but the alphaGen  | Ported all four modes. Per-vertex pair (distFade/oneMinusDistFade) computed CPU-side into tess.color alpha by new RB_FillDistFadeAlpha(), called next to RB_FillModelLightingColors before the VAO upload; gated by new shader_t::needsDistFade  |
| `bug-1299` | gl2 e2l1: distant trees/objects render as bright white shapes; fog does not hide distant geometry. The globalfog_fp.glsl clamp fix changed nothing. | r_globalFogPreTone defaults to 0, so RB_GlobalFog runs AFTER RB_ToneMap. gl1 computes GRADE(mix(scene,fog)); gl2 computes mix(TONE(scene),fog) - fog mixed in the wrong colour space, so it cannot lift the horizon and distant geometry keeps i | The bug-1299 clamp in globalfog_fp.glsl removes the ~2x overshoot that blocked the pre-tone order; with it r_globalFogPreTone 1 yields TONE(mix(clamp(scene),fog)) - structurally identical to gl1. Pending live A/B before baking the default t |
| `bug-1298` | [user] gl2: 'distant trees look white', 'objects outlined in white', 'texture popin you couldn't see before gl2' - SOLVED: MSAA | MULTISAMPLING interacting with alpha-tested cutout surfaces. Isolated by BISECT, not by theory: disabling the entire gl2-only feature set cleared it, and restoring everything EXCEPT r_ext_framebuffer_multisample / r_ext_multisample kept it  | NOT YET FIXED IN THE RENDERER - currently mitigated by MSAA 0, which costs all edge AA. A proper fix (correct cutout handling under multisample resolve, e.g. alpha-to-coverage) is pending design + review. PROCESS NOTE, THE REAL LESSON: SIX  |
| `bug-1296` | [user] e2l1: 'trees and the plane look kinda highlighted in white' with r_globalFogSky 0; buried/flat with r_globalFogSky 1 - both states wrong | The sky exemption's EPSILON, not the exemption's existence. `zw >= 0.99999` is meant to mean 'nothing wrote depth at this pixel', but WINDOW DEPTH IS NONLINEAR: 1-zw ~= zNear*(1/d - 1/zFar). With r_znear 4 (tr_init.c:1888) an epsilon of 1e- | One line: epsilon 1e-5 -> 1e-7 (`zw >= 0.9999999`). Shrinks the slab to 1.6 / 10 / 62 units at those far planes - below one pixel of depth precision - while still catching true no-geometry pixels, since qglClearDepth is exactly 1.0 (tr_init |
| `bug-1295` | [user] gl2 sky 'looks low res and pixelated' (t3l1), and 'the sky looks really ugly now' once fog stopped masking it (e2l1) | A gl1/gl2 port divergence in ParseSkyParms. gl1 loads the six sky faces through R_FindImageFileOld passing its shader_force32bit argument (renderergl1/tr_shader.c:2033) - UNCOMPRESSED 32-bit. gl2 built its imgFlags as IMGFLAG_MIPMAP\|IMGFLA | Added IMGFLAG_NO_COMPRESSION to the imgFlags in renderergl2 ParseSkyParms, matching gl1. Both the outerbox and innerbox loads share that same imgFlags variable, so one change covers all twelve faces. Costs VRAM, not quality. DEPLOY NOTE: th |
| `bug-1273` | Skybox looks pixelated/broken after the t3l1 crash fix | Self-inflicted. While chasing the t3l1/t3l2 crash I added an SF_FACE-only filter to R_Sky_AddSurf as belt-and-braces, on the theory that a wrongly-typed surface was reaching SurfIsOffscreen2. That theory was wrong - the real cause was a 16- | Reverted the filter - collect every surface exactly as gl1 does, since gl1 renders this correctly and performs no type filtering at all. The genuine fix (const glIndex_t*) and the bounds guard in tr_main.c both remain. |
| `bug-1268` | Suppression effect barely visible when being shot at | coop_defaults.cfg shipped 'seta r_ppSuppressAmount 0.068768' against a renderer default of 1.0. Every suppression effect - vignette, blur, desaturation, tunnel darkening - is multiplied by that value, so the whole system was running at roug | Raised to 0.55. Chose a middle value rather than 1.0 because the oddly precise old number looks like a baked live-tuning result rather than a mistake; the exact revert command is recorded in the cfg comment. On-hit blood was NOT implemented |
| `bug-1265` | Players can mount paks, cannons, flaks and MG42s while DBNO | Nothing published DBNO state to the engine, and dbno.scr contained no turret or vehicle references at all - a downed player was not even dismounted from a gun they were already manning. The script-side cannonThink.scr mount driver only test | Added a coop_setdbno engine setter on Player mirroring the existing coop_setcover pattern (Event decl, response map entry, handler, member, IsCoopDbno accessor), mirrored it from all 13 DBNO set/clear sites in dbno.scr / player.scr / medkit |
| `bug-1254` | Renderer leaks every GLSL program, texture, VAO and FBO on each disconnect or map load; one session logged R_Init x4, GLSL_InitGPUShaders x4, GLSL_ShutdownGPUShaders 0, RE_Shutdown 0 | gl2 calls the whole R_Init() from RE_BeginRegistration, but the four GL deletors live only in RE_Shutdown, which the disconnect path never reaches (CL_Disconnect_f to CL_FlushMemory to CL_StartHunkUsers to CL_BeginRegistration; CL_ShutdownA | Added a symmetric GPU teardown at the top of RE_BeginRegistration, guarded on tr.registered and placed before Hunk_Clear and R_Init: R_ShutDownQueries, FBO_Shutdown, R_GoreShutdown, R_DeleteTextures, R_ShutdownVaos, GLSL_ShutdownGPUShaders, |
| `bug-1249` | m3l1a water: dead-straight waterline against the sand, and a pale additive band where the surf shader meets the open ocean (gl2 only) | alphaGen sCoord/tCoord are PARSED by gl2 (tr_shader.c:1712-1714) and consumed by NOTHING - one grep hit in the whole renderer, the parser. baseColor[3] therefore kept its 1.0f initialiser and every such stage drew FULLY OPAQUE. gl1 implemen | Ported RB_CalcAlphaFromTexCoords into generic_vp CalcColor as a per-vertex ramp: f = (alphaMax-alphaMin)*coord + alphaMin, clamped to [alphaConstMin, alphaConst]. Reads RAW attr_TexCoord0 (pre-tcMod) as gl1 does. New UNIFORM_ALPHAGENPARAMS  |
| `bug-1245` | m1l2b: German officers render with salmon-pink/absent trousers; corpses show teal torso + pale blue boots. INTERMITTENT | OPEN. Surfaces receive a COMPLETELY WRONG but valid shader (not a missing one). DISPROVEN so far: (1) missing gore-tier shaders - all exist incl. retail officer_pants; (2) gl2 skin-offset divergence - skinNum + (*bsurf & 3) is byte-identica | NOT FIXED. Next: watch for recurrence on the current exe; if it recurs, capture screenshot + viewpos + full qconsole.log and compare a gl1 load of the same spot. |
| `bug-1242` | user: the water is ugly (on gl2) | gl2 never implemented the MOHAA-specific tcMods TMOD_WAVETRANS / TMOD_WAVETRANT / TMOD_BULGETRANS that renderergl1 has (gl1 tr_local.h:406, tr_shader.c:599-604, tr_shade.c:1485). `tcMod wavetrant` appears 36 times in misc_outside.shader and | Ported both wave tcMods to gl2. Enum values APPENDED at the end of the TMOD_ enum (inserting mid-enum would renumber existing values). Parser branches mirror gl1. gl1 applies these per-vertex as a uniform texcoord translation by EvalWaveFor |
| `bug-1240` | a new .glsl file link-errors as an unresolved fallbackShader_* symbol | shaders.cmake:1 is a bare file(GLOB_RECURSE ... *.glsl) with NO CONFIGURE_DEPENDS, so an incremental build never re-runs the glob and the new shader is never stringified into a .c - the extern declaration then has nothing to link against. | Re-run `cmake .` in the build dir after ADDING a .glsl file. Editing an existing one is fine; only creation needs it. |
| `bug-1239` | user request: camera motion blur in gl2 | rend2 has no motion blur at all - net-new pass. | Rotation-only camera blur at the HEAD of RB_HZMScreenFx (before FXAA, so FXAA cleans the tap stepping). Motion vector by REPROJECTION of the previous frame's forward vector into the current view basis (immune to Euler wraparound, unlike ang |
| `bug-1237` | r_cubeMapping produces zero probes and renders nothing on every MOHAA campaign map | THREE independent defects stacked. (1) R_LoadEntities - the only writer of s_worldData.entityString/entityParsePoint - is inside the `#if 0` block of Quake3 loaders the MOHAA loader replaced, so entityParsePoint was NULL and every R_ParseSp | (1) Bridge s_worldData.entityString from ri.CM_EntityString() (the collision model already held it; R_Sphere_InitLights was already consuming it for the sun). (2) New R_PlaceCubemapsAuto places probes on info_pathnode - 52,607 across the 54 |
| `bug-1233` | user request: add chromatic aberration to the injured effects that get a bit worse with diziness | The dizzy strength was computed inline inside the dizzy pass, so the chromab pass in a DIFFERENT function had no access to it; caching would have made the value depend on which pass ran first. | Extracted RB_HZMDizzyAmount() (recomputes from r_ppDizzy/Amount/Start + r_ppHealthFrac + coop_dbnoView, order-independent). Chromab now sums an artistic term (r_ppChromaticAberration, opt-in) and an injury term (r_ppDizzyChroma, default 0.6 |
| `bug-1230` | User: 'dead bodies seem to have shadows constantly changing from light to dark on them as I move' ... 'its more of whether I move my CAMERA'. Appeared after enabling r_charShadows 1. USER CONFIRMED FI | LIGHT-SPHERE SLOT ALIASING - a cache/index scope mismatch introduced by the gl1->gl2 port. backEnd.numSpheresUsed is reset at the top of RB_RenderDrawSurfList, i.e. ONCE PER DRAW-SURF LIST. trRefEntity_t::sphereCalculated - the flag asserti | Added r_sphereCacheScope (CVAR_ARCHIVE, default 0 = today's behaviour byte for byte). backEnd.sphereListId increments beside the per-list counter reset; the allocation stamps trRefEntity_t::sphereList with it; the reuse test additionally re |
| `bug-1229` | gl2 live play: 'dead bodies seem to have shadows constantly changing from light to dark on them as I move'; user clarified it tracks CAMERA movement, not translation. r_charShadowDist 0 and r_charShad | Light-sphere SLOT ALIASING - a cache/index scope mismatch introduced by the gl1->gl2 port. backEnd.numSpheresUsed is reset at the top of RB_RenderDrawSurfList (tr_backend.c:779), i.e. once per DRAW-SURF LIST, but trRefEntity_t::sphereCalcul | Added r_sphereCacheScope (CVAR_ARCHIVE, default 0 = today's behaviour byte for byte; NOT CVAR_LATCH per bug-1181, NOT CVAR_CHEAT per bug-1156). Set to 1 it scopes the per-entity sphere cache to the list that built it: new unsigned trRefEnti |
| `bug-1226` | Latent (upstream, NOT fixed): tr.refdef.pshadows always points at backEndData->pshadows[0] with no per-scene base offset, unlike tr.refdef.dlights which uses r_firstSceneDlight. Two WORLD scenes in on | RE_BeginScene sets tr.refdef.pshadows = &backEndData->pshadows[0] unconditionally (tr_scene.c:476), while the dlight equivalent one line earlier correctly offsets by r_firstSceneDlight. The drawsurf command copies the POINTER, not the array | NOT FIXED - documented only. Harmless in MOHAA today (one world scene per frame; RDF_NOWORLDMODEL previews build no shadows, and both pshadow dispatches are gated on !RDF_NOWORLDMODEL). Recorded because r_hzmDlightShadows makes this LIVE co |
| `bug-1225` | Latent trap: any CPU-side texture analysis in renderergl2 gated on 'picFormat == GL_RGBA8' silently skips ~17% of this install's world textures (361 of 2183 non-decal entries across the four AA_HD_Pro | R_LoadImage tries '<name>.dds' FIRST whenever r_ext_compressed_textures is on (the default), and tr_image_dds.c hands the DXT blocks straight to GL with picFormat set to a GL_COMPRESSED_* constant - so the diffuse pixels never exist on the  | Added R_HZM_GenerateNormalMapFromSource: for a compressed diffuse whose path the filter claims, decode a NON-DDS sibling purely as a height source by walking imageLoaders[] directly (that table has no dds entry, so it cannot recurse into th |
| `bug-1224` | Latent defect for ANY future attempt to give characters screen-space sun-shadow receive in gl2: with r_charShadows 1 every sunlit actor would darken himself with his own cast shadow. | tr.screenShadowImage (the sun shadowmask lightall samples under LIGHTDEF_USE_SHADOWMAP) is resolved from tr.renderDepthImage, which is filled by the MAIN-view z-prepass - and bIsCharacter surfaces are excluded from that prepass by RB_DepthF | Actively SUPPRESSED rather than left to switch itself on: useSunShadow now carries '&& (!stageCharLit \|\| r_charLightShadow->integer)', with r_charLightShadow default 0. Also collapsed the permutation-index test and the texture-bind test o |
| `bug-1223` | Latent: every bIsCharacter actor would have cast NOTHING into any projected-shadow (pshadow) depth map, making r_hzmDlightShadows invisible for AI and players even when working correctly. | RB_RenderDrawSurfList computed allowChars = isShadowView && r_charShadows->integer && backEnd.viewParms.shadowCascade > 0. A pshadow view is VPF_DEPTHSHADOW with shadowCascade == 0 (its viewParms are Com_Memset to zero), so RB_DepthFillSkip | Added viewParmFlags_t VPF_PSHADOW (0x100), set ONLY by R_RenderDlightShadowMaps via R_RenderPshadowMapRange(first, num, VPF_PSHADOW). allowChars is now isShadowView && ((r_charShadows && shadowCascade > 0) \|\| ((flags & VPF_PSHADOW) && r_h |
| `bug-1219` | WARNING: SV_FindIndex overflow (max=1280, start=2100): sound/... (243 occurrences on m3l2) | m3l2 exceeds the 1280-slot sound index (MAX_SOUNDS was raised 1024->1280). 243 sounds - coop_deathvox, coop_flvo, coop_gurgle, multiplayer dialogue, coop_headshot, coop_rain_loop - are dropped and will be silent on that map. | NOT FIXED - detected 2026-07-29 by _research/regression harness (S0 engine.capacity). Either raise MAX_SOUNDS again or trim per-map sound registration on m3l2. |
| `bug-1217` | Five latent engine defects documented-but-not-fixed in bug-1208's fix note, swept in one pass. No user-visible symptom reported for any of them; found by inspection during the helmet third-person hunt | FIVE independent items, each re-confirmed against current source (line numbers had all moved since bug-1208 logged them). (1) cg_modelanim.c now :1661-1670 - the attached-model 'inherit the parent's view visibility' pair wrote RF_THIRD_PERS | FOUR fixed, ONE deliberately left alone after inspection. (1) FIXED cg_modelanim.c: masks are now (RF_FIRST_PERSON \| RF_THIRD_PERSON \| RF_DEPTHHACK). PROVEN BIT-FOR-BIT INERT on today's data and shipped as a latent-only correctness fix, n |
| `bug-1214` | Silent out-of-bounds write + out-of-bounds skinning reads in the skeletal morph-weight pool (skeletorMorphCache). No log line, no crash - deformed heads/faces on characters added late in a busy scene. | skeletorMorphCache[MAX_SKELMORPH] is the sibling of TIKI_Skel_Bones[MAX_SKELBONES]: same producer (R_AddSkelSurfaces, once per entity PER VIEW), same reset point (TIKI_Reset_Caches, once per SCENE). When MAX_SKELBONES was raised 20000 -> 13 | MAX_SKELMORPH 12800 -> 131072 in tiki/tiki_mesh.h so exhaustion is unreachable before the guarded bone pool bails (512 KB BSS, same cost basis as the bone-pool raise). Added the missing explicit bounds check in renderergl1/tr_model.cpp R_Ad |
| `bug-1211` | Setting r_ppSSAO 1 under renderergl2 turns the ENTIRE screen black. No crash, no GL error. | SSAO GENERATION was nested inside the r_depthPrepass gate in RB_DrawSurfs (`r_depthPrepass->integer \|\| isShadowView`), and additionally inside that block's `if (!isShadowView)` arm - so a shadow view could not produce it either. The SSAO  | TWO parts. (1) SAFETY: new backEndState_t::ssaoValid, cleared at the top of every non-shadow RB_DrawSurfs and set ONLY by the pass that actually writes tr.screenSsaoFbo; the composite now additionally requires it. That alone downgrades the  |
| `bug-1210` | renderergl2 rendered three full sun-shadow cascade depth passes every single frame and threw all three away, at r_depthPrepass 0. | The cascade DISPATCH condition was `glRefConfig.framebufferObject && r_sunlightMode->integer && !(rdflags & RDF_NOWORLDMODEL) && (r_forceSun->integer \|\| tr.sunShadows)` - it did NOT test r_depthPrepass. Every CONSUMER of what it produces  | Added `&& (r_depthPrepass->value \|\| R_CharShadowsActive() \|\| (r_shadowDebug && r_shadowDebug->integer))` to the dispatch so it agrees with its consumers. Correct in BOTH prepass states: r_depthPrepass 1 -> renders exactly as before, no  |
| `bug-1207` | Every launch (and every vid_restart) with developer 1: "Couldn't find image file for shader sun" and "Couldn't find image file for shader gfx/2d/sunflare". | renderergl2/tr_shader.c CreateExternalShaders() (lines 4989/4991) unconditionally registers the two Quake3/rend2-legacy shader names "sun" and "gfx/2d/sunflare" at renderer init. MEASURED: neither name is defined in ANY .shader file and nei | Data-only: new hzm-mohaa-coop-mod/scripts/coop_sunflare.shader defines both names pointing at MOHAA's OWN retail sun art - textures/sprites/flare_main.tga (main/Pak0.pk3), which is exactly what the game's native sun flare uses (Pak0 scripts |
| `bug-1198` | Two macro-vs-literal couplings that would fail silently on the next raise: MAX_SOUNDS vs the 11-bit sound_index wire field, and the pure-pak checksum arrays in SV_VerifyPaks_f vs MAX_STRING_TOKENS. | (a) MSG_WriteSounds/MSG_ReadSounds hard-coded 11 bits for sound_index. The 'never exceed 2048 without widening both sides, it SILENTLY TRUNCATES' rule existed only as a comment above MAX_SOUNDS - nothing enforced it, which is exactly the fa | (a) Added #define SOUND_INDEX_BITS 11 next to MAX_SOUNDS with '#if MAX_SOUNDS > (1 << SOUND_INDEX_BITS) #error', and both msg.cpp call sites now use the macro instead of the literal. Emitted bits are unchanged (11), so this is NOT a protoco |
| `bug-1197` | SILENT (latent): a map with more than 4096 static models would render the ones past index 4095 with the wrong transform and could corrupt the shader field of the sort key. | R_AddStaticModelSurfaces packs the static-model loop index into the drawsurf sort key's entity field (tr.shiftedEntityNum = i << QSORT_ENTITYNUM_SHIFT / QSORT_REFENTITYNUM_SHIFT). That field is 12 bits, so index 4096+ both aliases onto anot | Loud one-time PRINT_WARNING in R_LoadStaticModelDefs (both renderers) when numStaticModels exceeds the encodable ceiling, naming the count, the ceiling and the bit width. Not clamped - the models are legitimately in the BSP and dropping the |
| `bug-1195` | Repeated vid_restart leaked font slots until the table hit MAX_LOADED_FONTS and fonts silently stopped loading; the sgl font table could also be written past the end of the array with no guard at all. | Two defects. (a) R_LoadFont_sgl guarded the WRONG counter: it tested s_numLoadedFonts (which belongs to s_loadedFonts[]) while writing s_loadedFonts_sgl[s_numLoadedFonts_sgl]. A .RitualFontList registers several sgl faces per list entry, so | gl1+gl2: guard s_numLoadedFonts_sgl instead of s_numLoadedFonts in R_LoadFont_sgl. gl1: reset s_numLoadedFonts = s_numLoadedFonts_sgl = 0 at the end of R_ShutdownFont, matching gl2. |
| `bug-1194` | Raising MAX_SFX_INFOS in snd_local_new.h silently did nothing - the guard in load_sfx_info was written against the literal 1000. | load_sfx_info tested 'if (number_of_sfx_infos == 1000)' - a hard-coded literal instead of MAX_SFX_INFOS, AND an exact-equality test rather than >=, so any path that stepped over the value would have walked off the end of sfx_infos[]. It als | Test 'number_of_sfx_infos >= MAX_SFX_INFOS' and report with Com_Printf naming the limit and the source file (global/sound<N>.txt), since dropping sound definitions must be visible without developer 1. Swept the rest of the sound code: snd_d |
| `bug-1191` | gl1/gl2 divergence found while chasing oversized sprites: renderergl2 never re-initialises shader.sprite.scale after zeroing the global shader, so any sprite shader without an explicit 'spritescale' k | renderergl1/tr_shader.c does 'Com_Memset(&shader,0,sizeof(shader))' then immediately restores 'shader.sprite.scale = 1.0;' (line 3416). renderergl2/tr_shader.c does the same memset at line 3881 but never restores the 1.0 default. sprite.sca | Add 'shader.sprite.scale = 1.0;' after the memsets in renderergl2/tr_shader.c, matching renderergl1/tr_shader.c:3416. Delegated to the gl2 ghost-view agent along with the fix it is investigating. |
| `bug-1187` | Characters render CONTORTED/disfigured (m1l1 truck passengers most visibly). Silent - nothing is logged. | entityState_t.frameInfo[N].index was transmitted in 12 BITS = max animation index 4095, but MAX_TIKI_LOAD_ANIMS is 8192 and qcommon/tiki.h's own comment states new_generic_human.tik ALREADY exceeds 4095 after the force-load pack additions.  | Widened frameInfo[0..15].index from 12 to 13 bits (8192, exactly matching MAX_TIKI_LOAD_ANIMS) in entityStateFields_ver_15 ONLY - that is the Breakthrough protocol table this fork runs under com_target_game 2. entityStateFields_ver_6 (AA) a |
| `bug-1186` | No error message by design - this was a SILENT failure. Entities past the 1024th in a single server snapshot were discarded with no Com_Printf, no warning, no trace of any kind; they simply did not re | MAX_SNAPSHOT_ENTITIES (file-local define, sv_snapshot.c:269) was still the 1024-era value - the LAST unraised member of the bug-934 snapshot-capacity family. bug-934 raised the client parse ring (MAX_PARSE_ENTITIES 2048->8192, client/client | (1) MAX_SNAPSHOT_ENTITIES 1024 -> 2048, matching MAX_ENTITIES_IN_SNAPSHOT and bound by MAX_GENTITIES (GENTITYNUM_BITS 11 = 2048, q_shared.h:1667) instead of an arbitrary literal. Because SV_AddEntToSnapshot dedupes per svEntity via snapshot |
| `bug-1181` | Game closed instantly when enabling Ambient Occlusion and pressing APPLY in the post-FX menu. Access violation 0xC0000005 in renderer_opengl2.dll InitShaderEx+0xE7, stack: UIFont::CheckRefreshFont ->  | NOT the SSAO code - the crash never reaches it. I had added `ui_checkrestart` to the post-FX APPLY button (bug-1177) so the latched AO toggle would apply immediately. ui_checkrestart issues vid_restart FROM AN OPEN MENU; gl2 then tears down | Reverted the post-FX APPLY button to plain `popmenu 0` and relabelled the AO row 'Ambient Occlusion (needs vid_restart)'. AO is applied on the next natural renderer init (map load or a manual restart) instead of the menu forcing one. Also f |
| `bug-1179` | REGRESSION I INTRODUCED (the ACTUAL cause of both m1l1 load crashes): 'ERROR: SV_FindIndex: bad start index 4260' -> 'Server crashed: SV_FindIndex: bad start index 4260' during Actual Spawning Entitie | I raised MAX_SOUNDS 1280 -> 2000 to stop the SV_FindIndex overflow WARNINGS, on the (agent-supplied, unverified-by-me) reasoning that the binding limit was the 11-bit sound_index wire field (2047). WRONG limit. The real ceiling is the CONFI | (1) Reverted MAX_SOUNDS to the known-good 1280 and rewrote its comment to document the REAL constraint (the configstring layout, with the arithmetic) so the next person does not repeat the 'but the wire field has room' mistake. Rebuilt and  |
| `bug-1177` | All five Ambient Occlusion controls in the post-FX menu are silent no-ops on the gl2 renderer (user: 'Do AOsettings even work anymore') | r_ppSSAO / r_ppSSAORadius / r_ppSSAOIntensity / r_ppSSAOBias / r_ppSSAODepthAware are registered and consumed ONLY by renderergl1 (grep for r_ppSSAO in renderergl2/ returned zero hits). gl2 had only upstream rend2's bare on/off r_ssao, with | Wired all five to gl2 using the same lever names as gl1 so ONE cvar set drives both renderers. (1) Registered the five in gl2's R_Register beside r_ppDoF; r_ppSSAO is CVAR_LATCH there (gl1 has it live) because it gates SSAO buffer allocatio |
| `bug-1172` | SELF-INFLICTED: build.ps1's normal, pre-existing behavior (always deploys cgame.dll + renderer_opengl1.dll to the REAL GOG root as part of packing the mod pk3s) picked up tonight's gl2-sandbox-motivat | Did not account for build.ps1's automatic GOG-root cgame.dll/renderer_opengl1.dll deploy (documented, intentional behavior for NORMAL mod-content workflow) when reusing the same build outputs for tonight's gl2-sandbox engine experimentation | Immediate remediation: temporarily reverted MAX_SOUNDS (2000->1280), MAX_ENTITIES (4095->1023), MAX_TIKI_ALIASES (8192->4095) in source, rebuilt cgame.dll + renderer_opengl1.dll from that reverted state, deployed directly to the real GOG ro |
| `bug-1171` | Postfx menu Tonemap/Grade checkbox + Grade 0-4 slider do nothing on gl2 - user: 'Do AOsettings even work anymore, or Tonemap/Grade?' | gl2's HZM-parity grade path was only gated on the console-only, unexposed-in-any-menu r_tonemapMode==1 - r_ppTonemap and r_ppGrade (what the menu actually controls) were never read anywhere in gl2 at all, and neither cvar is set by any ship | Tonemap/Grade: fixed - RB_ToneMap's gate is now `r_tonemapMode==1 \|\| r_ppTonemap->integer \|\| r_ppGrade->integer` (matches gl1's OR logic, tr_postprocess_gl1.c:750-751) and applies the same 4 grade presets (neutral/warm-Normandy/cold-Ard |
| `bug-1170` | Postfx menu 'Muzzle Heat' slider doesn't respond to dragging - user: 'What does muzzle heat setting do? it doesnt let me adjust the bar' | The slider's linkcvar, r_ppMuzzleHeat, is NOT a setting - it's cgame's live, per-frame decaying gunfire-heat intensity (cg_view.c: cgi.Cvar_Set("r_ppMuzzleHeat", ...) every frame). Any manual drag was overwritten by the next frame's publish | Removed the broken slider/label. Overall muzzle-heat strength is already covered by the adjacent 'Heat Haze Amt' slider (r_ppHeatAmount multiplies both ambient heat haze and muzzle heat per tr_postprocess.c). Kept 'Muzzle Radius' (r_ppMuzzl |
| `bug-1169` | gl2 sun rays visible on m1l1's night sky (starry, no visible sun) - user: 'STILL see random sun rays in the sky, this is a NIGHT map' | m1l1 worldspawn ships sundirection/suncolor ("10 15 30", nearly black) purely to tint ambient/sphere lighting toward moonlight - the sun bridge (bug-1154) correctly treats this as 'a sun exists' for lighting purposes, but RB_SunRays only ev | RB_SunRays now computes sun luminance from tr.sunLight and (a) returns immediately below a threshold (48, comfortably above moonlight-tier ~15 but well below a real daylight/dusk sun) and (b) scales ray intensity (the existing `mul` blend f |
| `bug-1168` | Engine-limits audit follow-up: gl1 (shipping renderer) still capped at MAX_ENTITIES=1023 per-scene refEntities while gl2 independently grew its equivalent array to 4095 chasing an unrelated bug; MAX_T | MAX_ENTITIES: gl1's own drawsurf sort-key packing (tr_main.c QSORT_ENTITYNUM_SHIFT=8, entity field masked '& 4095') already budgets 12 bits/4095 values, matching REFENTITYNUM_BITS - the 'can't be increased without changing drawsurf bit pack | Raised MAX_ENTITIES 1023->4095 in tr_types_new.h (benefits gl1, the shipping renderer, most - a busy multi-player coop scene with up to 128 corpses + ~80-cap scaled AI + effects could plausibly exceed 1023). Raised MAX_TIKI_ALIASES 4095->81 |
| `bug-1167` | Suspected memory-corruption cluster during one long live session: repeated 'G_SoundCallback: entity 1023 gone' spam, 'SV_FindIndex overflow (max=1280, start=2100)' with a corrupted filename ('...amb_c | UNDER INVESTIGATION (Workflow wf_410ba04d-5b8, 4 parallel angles: sound-owner/SV_FindIndex array bounds, sphere-light list population, gore-instance entity-slot-reuse write-safety, and a systemic re-audit for any array still sized below MAX | ROOT CAUSE FOUND by two independent investigation agents converging on the same answer: code/client/cl_invrender.cpp:160 (CL_Draw3DModel, the armory/loadout/helmet-switcher UI 3D-preview widget) hardcoded `ent.entityNumber = 1023`. Under th |
| `bug-1166` | CMDTRACE heartbeat line spamming qconsole.log every ~256 buffer swaps regardless of r_cmdtrace value (user flagged this twice - once earlier in the session, again after it was apparently missed) | Leftover temporary debug instrumentation from the bug #73 (gun-over-menus) investigation, explicitly commented 'REMOVE with the rest of the SKEL* scaffolding' in its own source. The heartbeat print (`^~^~^ CMDTRACE heartbeat swaps=%d cvar=% | Fully removed all CMDTRACE scaffolding: the g_cmdtraceFrames global + its heartbeat/arming/per-command-print block in tr_backend.c's RB_ExecuteRenderCommands, and the three IMM2D trace call sites in tr_draw.c (Draw_StretchPic, Draw_TilePic) |
| `bug-1165` | coop_postfx MORE button just goes back to the previous (Video/settings) menu instead of advancing to page 2 | All three pages' MORE/BACK buttons used stuffcommand 'popmenu 0 ; pushmenu <target>' in one command. This is a DOCUMENTED engine limitation already discovered and written up for a different menu (bug-461, coop_sr.urc): MenuManager::PopMenu  | Removed the combined pop+push pattern everywhere in the 3 postfx pages: MORE buttons are now pure `pushmenu coop_postfxN` (stacks the next page on top, leaving the current page on the stack underneath); BACK buttons are pure `popmenu 0` (po |
| `bug-1164` | gl2-only flickering/shimmering on thin decorative geometry (wall trim spikes, m1l1 and other maps/objects per user); not present on gl1 (no shadow maps) | The sun cascade shadow depth pass (VPF_DEPTHSHADOW, added this session for bug-1156) applies no slope-scaled polygon offset when rendering scene geometry into the shadow maps - only a small FIXED receiver-side bias exists (shadowmask_fp.gls | Added r_shadowMapBiasFactor/r_shadowMapBiasUnits cvars (CVAR_ARCHIVE, default 4/4 - NOT CVAR_CHEAT, would be silently clamped on a listen server) and apply them via qglPolygonOffset/GL_POLYGON_OFFSET_FILL specifically during the depthFill b |
| `bug-1160` | Hitting MORE under Post-Processing effects menu overlaps with Suppression control (user screenshot, live playtest) | The God Rays/Low-Health FX/Suppression FX toggles + Sun Shaft/Suppress strength sliders were appended to the bottom of page 1 at y=392-464 without ever moving the button strip (MORE/DEFAULTS/CANCEL/APPLY), which stayed at its original y=414 | Moved lblSupp/cbSupp to y=420/418, the two strength slider rows to y=448, and the whole button strip to y=478; grew background rect to 40 70 560 450 and the menu to 640 530. |
| `bug-1155` | gl2 was binding ordinary DIFFUSE textures as specular maps on lit surfaces - a mild but real wrongness in the shipped gl2 image. | r_specularMapping defaulted to 1. rend2 auto-probes for a '<name>_s' texture on any lit stage with no explicit specularMap. MOHAA ships no specular maps, but it does ship 39 files whose names end in _s that are ordinary diffuse textures, 34 | Default r_specularMapping to 0 for MOHAA, with an in-code note explaining the _s false-positive and when to set it back to 1 (only if genuine specular maps are ever authored). |
| `bug-1154` | Every rend2 sun-driven feature was dead on MOHAA: r_drawSunRays pointed at a hardcoded sky point on every map, and tr.sunShadows (cascade shadow maps) could never turn on. gl1 is affected too - its go | rend2 only learns the sun from a q3map_sun / q3gl2_sun SHADER directive. MEASURED: a scan of all 513 .shader files across main+mainta+maintt finds ZERO of either. So tr.sunDirection kept the hardcoded fallback (0.45,0.3,0.9) set in RE_LoadW | Bridge s_sun into tr.sunDirection / tr.sunLight immediately after R_Sphere_InitLights. Two correctness details: (a) OVERBRIGHT - s_sun.color is 0-255 and has ALREADY been multiplied by tr.overbrightMult (tr_sphere_shade.cpp:1277-1279) while |
| `bug-1152` | User: 'I cant adjust the msaa from 4x to 8x, it simply doesnt change when i select it' (gl2 build, Advanced Graphics menu). | The MSAA value plate was linkcvar'd to r_ext_multisample. Under renderergl2 the engine CLAMPS that cvar to a max of 4 - renderergl2/tr_init.c:1389 ri.Cvar_CheckRange(r_ext_multisample, 0, 4, qtrue) - and the qconsole log proves it fires on  | Point the aadisplay label at r_ext_framebuffer_multisample. The pulldown sets both cvars to the same value on every pick, and the FBO cvar is never clamped, so it is the honest record of the chosen level and reads correctly under BOTH rende |
| `bug-1151` | gl2 was missing every GAMEPLAY-DRIVEN post-FX gl1 has: low-health desat + red vignette + heartbeat, the suppression tunnel-vignette, heat haze and the localized muzzle shimmer. The Advanced GFX / coop | Same as bug-1149/1150 - only the tonemap/grade stage of renderergl1's RB_PostFxApply had ever been ported. The cgame-published driving signals (r_ppHealthFrac, r_ppSuppress, r_ppHeat, r_ppMuzzleHeat/X/Y/Radius) are renderer-agnostic and wer | Byte-ports of gl1's LOWHEALTH_FS / SUPPRESSION_FS / HEATHAZE_FS with gl1's CPU-side ramps copied exactly - including the low-health quadratic ramp, the 0.35 onset floor at the threshold and the heartbeat rate 1.8 + hurt*1.6. Dispatched from |
| `bug-1149` | gl2 has none of gl1's HZM post-FX chain. gl1 ships bloom / god rays / DoF / sharpen / FXAA (r_pp* in the Advanced Graphics menu); gl2's RB_PostProcess had only rend2's own SSAO (default off), the glob | Only the tonemap/grade stage of gl1's chain had ever been ported. The rest of renderergl1/tr_postprocess_gl1.c (RB_PostFxApply: SSAO -> DoF -> bloom -> god rays -> grade -> FXAA -> sharpen -> heat haze -> rain) has no gl2 equivalent. | First stage ported: BLOOM. glsl/bloom_bright_fp.glsl and glsl/bloom_blur_fp.glsl are byte-ports of gl1's BRIGHT_FS and BLUR_FS; RB_HZMBloom in tr_postprocess.c runs bright-pass -> H blur -> V blur -> additive composite using the existing tr |
| `bug-1148` | gl2's ported gl1-parity colour grade (r_tonemapMode 1) had no effect, and the whole fog/darkness thread was chasing a phantom +26 near-band luma excess on e2l2. | TWO faults stacked. (1) UNIFORM NAME: FBO_Blit passes the grade as UNIFORM_COLOR, and GLSL_InitUniforms resolves uniforms by the NAME in tr_glsl.c uniformsInfo, which is u_Color - but the fragment shader declared `uniform vec4 u_Grade`. No  | Renamed the shader uniform u_Grade -> u_Color (with an in-file note on why the name is load-bearing) and corrected the tr_glsl.c comment. Added r_toneMap / cg_drawviewmodel / cg_draw2d to the explicitly-forced cvar list in the boot cfg AND  |
| `bug-1147` | gl2: animated 2D shaders (menu/HUD art with tcMod scroll/rotate, rgbGen wave, animMap) do not animate - the 2D shader clock is frozen at whatever the last 3D scene left behind. | Mis-ported guard. Set2DWindow sets `backEnd.projection2D = qtrue` near the top and then, at the bottom, tests `if (!backEnd.projection2D)` before refreshing backEnd.refdef.time / floatTime - a dead branch that can never be true. renderergl1 | Capture the previous state into a local `wasIn2D` before raising backEnd.projection2D, and gate the time refresh on that - exact gl1 semantics, and the projection2D/last2DFBO assignment that RB_SetGL2D's early-out depends on is left untouch |
| `bug-1146` | gl2 renderer static zone (TAG_STATIC_RENDERER) grows by ~53 MB and ~69,600 blocks on EVERY vid_restart - i.e. every Advanced Graphics apply. Measured pre-fix via rcon meminfo: 57.6 MB / 69,517 blocks  | RE_BeginRegistration's pre-clear block (R_IssuePendingRenderCommands / R_LevelMarksFree / R_TerrainFree / R_GoreLevelReset / ri.Hunk_Clear) sat behind `static qboolean s_bReregistration`. vid_restart UNLOADS and RELOADS the renderer DLL (cl | Fixed the precondition at its source instead of guarding the caller: R_LevelMarksFree now checks `if (!dcl_editmode \|\| !dcl_editmode->integer) return;`. The s_bReregistration static is deleted and the block runs unconditionally, matching  |
| `bug-1144` | gl2 task #73: the first-person weapon draws OVER fullscreen menus - the M1 Garand punches a hole through the ESC board, an unlit gun-shaped 'shadow' sits on the main menu with no world loaded, and the | renderergl2 never forced depth-test OFF for 2D stages. renderergl1 does: tr_shade.c RB_StageIteratorGeneric has `if (backEnd.in2D) GL_State(pStage->stateBits \| GLS_DEPTHTEST_DISABLE)`. gl2's RB_IterateStagesGeneric just did GL_State(pStage | gl1 parity in renderergl2/tr_shade.c: `if (backEnd.projection2D) GL_State(pStage->stateBits \| GLS_DEPTHTEST_DISABLE); else GL_State(pStage->stateBits);`. backEnd.projection2D is gl2's in2D equivalent (set by Set2DWindow/RB_SetGL2D, cleared |
| `bug-1140` | gl2 #73: first-person gun renders OVER the ESC menu / weapons bar; dark gun shadow over the main menu. gl1 clean. | NOT a draw-order bug and NOT a late second scene (user-session frame trace: menu-open frames contain ZERO DRAW_SURFS/POSTPROCESS - the client renders no scene under fullscreen menus). The gun is a GHOST: tr.renderFbo persists across frames, | R_Ensure2DClear(): the FIRST immediate 2D draw of a frame with no scene submitted (g_sceneThisFrame flag: set in RB_DrawSurfs, reset in RB_SwapBuffers) binds tr.renderFbo, disables scissor, and clears color once. Wired into Draw_StretchPic, |
| `bug-1139` | user: most advanced gfx and regular settings just crash my game now (gl2 sandbox); 2 dumps tonight, both AV at renderer_opengl2.dll GeneratePermanentShader+0x487 (identical rva twice) | PROBABLE (guard shipped, awaiting confirmation on next settings session): FixRenderCommandList opened with cmdList = &backEndData->commands then guarded if(cmdList) - a near-NULL OFFSET that is never NULL - so with backEndData NULL (the vid | FixRenderCommandList now returns early when backEndData is NULL (guard the base pointer, not the offset). Deployed to G:/mohaa-gl2 with the CMDTRACE heartbeat build. If the user crashes again at settings-apply, the new dump will symbolicate |
| `bug-1137` | gl2 MAX_MOD_KNOWN was 1024 (half of gl1's 2048): R_AllocModel returns NULL silently past the ceiling (RE_RegisterModel prints a warning but the model is just gone = permanently invisible for the sessi | gl1's tr_local.h had MAX_MOD_KNOWN raised to 2048 (engine-limits pass) but the gl2 renderer kept the stock ioq3 value 1024. gl2's pool is model_t* pointers (tr.models[MAX_MOD_KNOWN]) so the low cap saved almost nothing (8KB of pointers); mi | MAX_MOD_KNOWN 1024 -> 2048 in renderergl2/tr_local.h for gl1 parity. All other usages are macro-sized arrays or bounds checks (grep-verified): the SKEL* diagnostic arrays in tr_shade.c/tr_model.cpp/tr_main.c double in size, which is accepta |
| `bug-1136` | gl2 FixRenderCommandList (tr_shader.c ~3466): one-frame wrong-entity/wrong-shader flicker whenever a brand-new shader is registered mid-frame while a scene is already queued (RE_RegisterShader does no | Three defects: (1) the repack rebuilt drawSurf->sort as (sortedIndex<<QSORT_SHADERNUM_SHIFT)\|entityNum\|... but R_DecomposeSort returns entityNum ALREADY shifted down, so the repack scattered the entity index into the dlight/fog/low bits i | Repack now replaces ONLY the shader-index field and preserves every bit below QSORT_SHADERNUM_SHIFT verbatim: sort = (sort & ((1u<<QSORT_SHADERNUM_SHIFT)-1)) \| (sortedIndex<<QSORT_SHADERNUM_SHIFT). This is provably correct for BOTH packing |
| `bug-1132` | gl2 global farplane fog far too thin vs gl1 on e2l2 ('walls around the map stand out, vanilla covered that with fog'); gl1 mean luma 43.8 vs gl2 36.6; distant canyon walls crisp in gl2, dissolved in g | RB_GlobalFog reconstructed eye distance from r_znear (clamped >=1) + backEnd.viewParms.zFar taken from the SEPARATE RC_POSTPROCESS command's copy of tr.viewParms - neither is guaranteed to be the projection that actually rasterised the worl | Latch the fog state from the MAIN world view in a new RB_SetupGlobalFog() called from RB_DrawSurfs (same call site as gl1's RB_SetupFog): farplane bias/distance/colour plus projectionMatrix[0]/[5]/[10]/[14]. globalfog_fp.glsl now inverts de |
| `bug-1130` | @3x hi-DPI fonts rendered as SOLID BLACK BARS in gl2 (main menu buttons = black rectangles); rollback required | Every vanilla font sheet has an EXPLICIT shader def in scripts/gfx.shader (nopicmip+nomipmaps+blendFunc GL_SRC_ALPHA GL_ONE_MINUS_SRC_ALPHA+rgbGen identity; verdana-14 adds nodepthtest). The @3x names had no def, so R_FindShader built a def | Ship scripts/coop_fonts_hi3x.shader with the 13 @3x shader defs copied verbatim from vanilla bodies (map path -> @3x.tga). Belt-and-braces: gen_hidpi_fonts.py save_tga now writes the exact vanilla TGA byte layout (18-byte header, desc 0x00, |
| `bug-1129b` | Menu/UI text blocky ('dog water') at 3440x1440 even after uniform virtualres scaling: facfont soft/stretched, courier-16 in advanced_graphics + buttons extremely jagged | MOHAA fonts are tiny 256px bitmap atlases; verdana-12/courier-16/18/20/marlett are BILEVEL (1-bit alpha, zero AA) so 3x magnification shows raw staircases. A prior same-name 4x TGA override (gfx/fonts) helped little because it only Lanczos- | Hi-DPI font system: R_LoadFont_sgl in BOTH renderers now prefers fonts/<name>@3x.RitualFont when present (metrics byte-identical to vanilla -> zero layout drift since UVs are normalized and screen metrics are design-space; only gfx/fonts/<n |
| `bug-1128` | gl2 NO-SPAWN boot crash after my #77 fix round 1 (log ends at renderer init) | Self-inflicted: moved R_LevelMarksFree before R_Init in RE_BeginRegistration, but gl2 (unlike gl1) registers its cvars inside R_Init - on FIRST boot dcl_editmode was still NULL -> NULL deref. gl1 is immune because its cvars exist before Beg | static qboolean s_bReregistration gate: pre-clear block (frees + Hunk_Clear) only runs on re-registrations, never on first boot. |
| `bug-1126` | gl2: smoke grenade / smoke sprite lighting garbage (same class as red decals) | R_GetLightingForSmoke was an empty '// FIXME: unimplemented (GL2)' stub -> vLight left as uninitialized stack garbage (identical class to bug-gl2-reddecals R_GetLightingForDecal). | Ported gl1 tr_light.c:1115 adapted to gl2: R_GetLightingGridValueSingle + dlight accumulation loop + overbright multiply + 255-normalize + /255 (smoke consumer expects [0,1]). |
| `bug-1125` | gl2: 'I can't max draw distance' - Advanced Draw Distance slider (r_lodscale) reverts | r_lodscale registered TWICE in gl2 R_Register (line 1534 CVAR_CHEAT + line 1656 CVAR_ARCHIVE); Cvar_Get OR-combines flags (cvar.c:470) -> cvar became cheat-protected -> writes forced back to default under sv_cheats 0. gl1 registers once as  | Changed the line-1534 registration to CVAR_ARCHIVE (both regs now ARCHIVE). |
| `bug-1124` | gl2: most Advanced Graphics settings crash the game on apply (vid_restart); user could not apply MSAA 8x | gl2 never called ri.Hunk_Clear() in RE_BeginRegistration (gl1 does, tr_init.c:1908) so every runtime vid_restart leaked the entire TAG_STATIC_RENDERER zone -> zone exhaustion -> Com_Error fatal. Co-factor: R_ShutdownFont() never called in g | 3-round fix: (1) mirror gl1 order frees->Hunk_Clear->R_Init in RE_BeginRegistration + R_ShutdownFont in RE_Shutdown + font counter reset. (2) bug-1128: first-boot NULL-cvar crash - gl2 registers cvars INSIDE R_Init (gl1 registers earlier),  |
| `bug-1123` | In-game Anti-Aliasing (MSAA) setting had no effect under the gl2 (opengl2) renderer | The slAA Slider linkcvar'd only r_ext_multisample, the renderergl1 MSAA cvar. The gl2 renderer resolves MSAA from a different latched cvar, r_ext_framebuffer_multisample (renderergl2/tr_fbo.c; registered CVAR_ARCHIVE\|CVAR_LATCH in renderer | Replaced the slider with a discrete PulldownMenuContainer (Off/2x/4x/8x) using the bug-756 trans_click compact recipe; each addpopup runs 'seta r_ext_multisample N ; seta r_ext_framebuffer_multisample N'. Added a blank2 display Label (linkc |
| `bug-1063` | v1.1.53 server-crash fix (coop_wallProbe game.dll default 0) was NEGATED in the field by autoexec.cfg:42 seta coop_wallProbe 2 | Fixing a cvar DEFAULT in engine source (gi.Cvar_Get(name,"0",0)) does NOT change the runtime value if a shipped cfg setas it first. autoexec.cfg:42 (leftover from invisible-wall killwall tooling) shipped "seta coop_wallProbe 2", executed at | autoexec.cfg:42 -> seta coop_wallProbe 0. Confirmed live: deployed autoexec reads 0, WALLPROBE spam=0 on the running server, no crash dump. Synced player1_home autoexec to 0 so the harness replicates real config. Shipped v1.1.54. |
| `bug-1002` | c0000005 AV in openmohaa.exe (engine frame, from game!Entity::setModel <- ScriptModel::SetModelEvent) on first shape-kit tik load; qconsole last lines: 'DOWNGRADING TO OLD ANIMATION FORMAT ... shapes/ | TWO defects in md5_2_skX output, proven by a field-level walk of the engine load path (scratchpad shapekit/skx_validate.py). (1) THE CRASH: the tool writes surface ofsCollapse=ofsCollapseIndex=0, but TIKI_CacheFileSkel (tiki_skel.cpp:391-40 | Regenerated, converter output UNMODIFIED: (a) gen_md5.py v2 authors the md5 root joint AT the tool's hardcoded -90deg-X-roll convention (joint quat (0.7071068 0 0), w=-sqrt -> same rotation as QuatFromAngles(0,0,-90); this is also the retai |
| `bug-960` | killwall kills did not persist across map loads (discovered via failed loose-script smoke test: loose files with pk3-name twins are SHADOWED by the pk3 on this engine) | cm_killbrush appended to loose homepath cmpatch/<map>.txt - but FS gives the pk3's copy precedence over a same-name loose file, so the loose kill list was never read back at CM load. Kills worked live in-session only. | Dual-file design: shipped list stays cmpatch/<map>.txt (pk3); live kills go to cmpatch/<map>_local.txt (a name no pk3 ships), applied as an overlay ON TOP at every CM load. Seeding logic removed. User's existing loose file migrated to the _ |
| `bug-954` | STUCK-NOHIT x4 along rising canyon path: player pushing, not moving, NO obstruction within 48u at any probe height - invisible wall with no wall | Upstream OpenMOHAA bug (fixed there Dec 2025, commit e3a873b4, AFTER our fork): PM_ClipVelocity's zero-check was exact (in[0]==0 && in[1]==0); a velocity clipped to TINY-but-nonzero 2D length then hits VectorNormalize's minimum-length quirk | Cherry-picked e3a873b4: VectorLength2DSquared(in) < 0.005 tolerance in PM_ClipVelocity (players), MM_ClipVelocity (AI - also fixes AI freezing on slopes!), VM_ClipVelocity (vehicles). game.dll + cgame.dll (cgame compiles bg_pmove for predic |
| `bug-953` | probe capability gaps: silent terrain phantoms, overhead blockers, invisible floors, no player confidence feedback, no in-game fix loop | v4 radial-only probing required physical contact at walking heights and could only report, not fix. | Probe v5: STUCK detector (push-not-moving forensics incl terrain w/ shader+brush), CEIL probe (invisible overhead), FLOOR identity (standing on clip), HUD echo (coop_wallProbe 2), markwall bindable command (512u aim-trace forensic report),  |
| `bug-948` | e1l2 last artillery gun blowing up does not progress the mission; 39x 'Cannot cast none to float' at officer.scr:3771 (crater fires) + 1x at Game(Event:'BlowUp', Object:'ThrobbingBox_Explosive') | HZM tinnitus blast-ping code in RadiusDamage: pv->floatValue() on the player's coop_blastPing script var. When the slot exists as VARIABLE_NONE (a script READ of the unset field leaves an untyped slot), floatValue() throws ScriptException O | Type-safe read: only floatValue() when GetType() is VARIABLE_FLOAT/VARIABLE_INTEGER, else treat as -1 (always overwrite). game.dll-only; staged auto-deploy on game close. Stuck runs need a map restart. |
| `bug-947` | invisible walls STILL reported (4th round) with all three species fixes verified deployed; no viewpos data - location unknown | Deployed state verified correct (pk3 zones string + game.dll binary contains zone code + user session 07-21 09:39 postdates all deploys). Walls therefore exist OUTSIDE the plateau strip zones - census shows 2226 clip-only segments map-wide  | Instrumentation: coop_wallProbe (default 1) in the pmove hook - once per second per player, 8-direction 48u probes; pure-playerclip hits log '^~^~^ WALLPROBE clip x y z dir', invisible-solid-entity hits log '^~^~^ WALLPROBE ent num classnam |
| `bug-946` | e1l2 invisible walls STILL present after mine + artillery fixes (user report, 3rd species) | Full-map player-mask census (26350 blocked segments): 2226 clip-only. The artillery plateau carries a 225-segment retail SP-boundary playerclip web - a long diagonal fence line across open ground (the user's first viewpos faced straight int | REGIONAL playerclip strip: new cvar coop_clipStripZones (up to 8 'x0 y0 x1 y1 z0 z1' boxes, ';' separated) parsed in Player pmove setup (player.cpp, next to the coop_noPlayerClip hook); while the player stands inside a box, pmove tracemask  |
| `bug-945` | tracescan could not detect playerclip walls - script trace is MASK_SOLID only, blind to the exact contents that block players | EventTrace arg3 only toggled MASK_IGNORE_ENTS; MASK_SOLID lacks CONTENTS_PLAYERCLIP/does not match player movement mask, so clip-only invisible walls were invisible to every scan. | arg3 extended: 0=MASK_SOLID+ents, 1=world-only (retail), 2=player-movement mask (MASK_PLAYERSOLID & ~CONTENTS_BODY). game.dll-only change. |
| `bug-936b` | Afrika Korps face-mask (DAK facewrap) turns COMPLETELY WHITE on gore-tier corpses (user screenshot; uniform lacerations render correctly) | The gore3 generator blood-composited the ENV-REFLECTION map (textures/common/reflection1 - a 1.5KB sheen gradient) instead of the diffuse texture for the 3 MULTI-STAGE sheen materials (facewrap, gestapo_ledermantel, sspanzer_ledermantel) an | All 3 defs regenerated mirroring the ORIGINAL stage structure (env sheen stage untouched + diffuse stage swapped to a laceration-composited copy of the real cloth texture, ALPHA CHANNEL PRESERVED - the blendfunc depends on it). Diffuse sour |
| `bug-935` | Post-respawn crash (mine death, sweeper out): /GS stack-cookie abort in cgame!CG_AddPacketEntities+0x2de (first crash caught by the new cgame PDB) | TWO constant-space confusions exposed by the 2048 op: (1) cg_ents.c declared `qboolean processed[MAX_ENTITIES]` - but MAX_ENTITIES is the RENDERER per-frame refentity cap (1023, locked by drawsurf bit packing), while the array is indexed by | processed[] -> MAX_GENTITIES (+ loop bound); tr_scene clamp -> `>= MAX_ENTITIES` (the array's true size; over-budget refentities skip rendering gracefully). Exhaustive cgame audit: no other gentity-indexed array uses a foreign constant (cli |
| `bug-934` | Repeat mine-explosion crash after skel_index fix. FIRST dump with cgame symbols: abort in cgame!CG_ReadNextSnapshot -> CG_ProcessSnapshots (exe-side overflow error) | The 1024-era SNAPSHOT BUFFER family, untouched by the protocol op: MAX_PARSE_ENTITIES 2048 (client parse ring, power-of-two mask) wraps within ONE busy 2048-entity snapshot -> CL_GetSnapshot 'parseEntities exceeded' error -> abort; MAX_ENTI | MAX_PARSE_ENTITIES 2048->8192 (4 max snapshots of headroom, stays power-of-two); MAX_ENTITIES_IN_SNAPSHOT 1024->2048; server snapshot rings x4 (all 4 sites). exe+cgame rebuilt, hash-verified deployed (bak=openmohaa_pre_snapring_bak.exe). cg |
| `bug-932b` | Crash at mine explosion (detector out) after the 2048-entity op: c0000409 with unresolvable cgame frames; log shows live entities numbered 1025-1040 | tr.skel_index was a BARE [1024] array indexed by model->entityNumber (tr_model.cpp:1715/1718/1738) - a hardcode the protocol-op audit missed (only searched constant NAMES + wire masks, not bare 1024 array sizes). With entities now legitimat | skel_index -> [MAX_GENTITIES]. ALSO: cgame was the only binary building with no PDB (why its crash frames were never resolvable) - added /Zi + /DEBUG for Release in its CMakeLists; cgame.pdb now ships to GOG root beside game.pdb. Renderer + |
| `bug-930` | Crash (abort, c0000409 via G_SoundCallback, 'unknown non-std exception' in hzm_fatal.log) on the first map load after the GENTITYNUM_BITS 11 deploy | MY DEPLOY GAP: the 11-bit protocol set shipped as exe+cgame+game per the old MAX_MODELS discipline, but the RENDERER is also a protocol consumer (ENTITYNUM_WORLD/NONE comparisons in tr_backend/tr_main/tr_light parent-chain walks + a HARDCOD | tr_gore.c GORE_MAX_ENTNUM -> MAX_GENTITIES (symbolic); renderer rebuilt with the 11-bit constants and deployed. PROTOCOL DISCIPLINE UPDATED: a protocol-constant op ships ALL FOUR binaries - openmohaa.exe + cgame.dll + game.dll + renderer_op |
| `bug-929` | Feature/capacity op: GENTITYNUM_BITS 10 -> 11 (true 2048-entity pool), follow-up to the bug-927 root cause | Entity pressure on big coop fights (count-scaled battalions + corpses kept + dropped weapons) ran the honest 1024 pool near its ceiling; the mod always WANTED 2048 (the old cfg lie). | GENTITYNUM_BITS 11: MAX_GENTITIES=2048, ENTITYNUM_WORLD/NONE=2046/2047 (all symbolic consumers verified - zero hardcoded 10-bit masks in wire code; sound/entity writes all use the constant). PROTOCOL SET deployed together: openmohaa.exe (ha |
| `bug-927` | GRAND ROOT CAUSE of the weekend crash family: paratrooper crash dump (Entity::attach NULL parent) + log evidence (live actor AT slot 1022, weapon attach-to-oneself at 1022, both respawn primaries entn | The mod has shipped `set maxentities 2048` for years - but GENTITYNUM_BITS=10 hard-caps the wire at 1024 entities with slots 1022 (ENTITYNUM_WORLD) / 1023 (ENTITYNUM_NONE) reserved. The 2048 setting only DISABLED AllocEdict's overflow guard | (1) server.cfg + start_server.cfg: maxentities 2048 -> 1024 with explanatory comment. (2) AllocEdict: both overflow checks now ALSO clamp at i >= ENTITYNUM_WORLD (protocol ceiling) so no config can ever re-enable the stomp - true exhaustion |
| `bug-926` | Respawn kit STILL broken after mine death while detecting (invisible rifle + detector only). NEW diagnostics caught it: INVSTALE pruned ent=1022 + INVDUP refused thompson AND springfield BOTH reportin | 1024-entity pool at ceiling on e1l2 (count-scaled battalions + corpses kept forever + EVERY dead AI leaving 2-3 dropped-weapon entities forever since weapons-on-back). At the ceiling the respawn kit's spawned pickup entities land on collidi | (1) PRESSURE RELIEF: enabled the engine's native g_droppeditemlife=60 (dropped weapons auto-remove after 60s; pickup cancels the timer natively at weapon.cpp:3173/3237) - frees hundreds of slots on big maps. (2) EDICTHI allocator tracer (le |
| `bug-925` | Crash #6: c0000005 near-NULL in Sentient::NextWeapon+0x123 (weapon cycling through a corrupted inventory) - sentient_combat.cpp was MISSED by the bug-920 guard sweep | Same stale-inventory-entry family; the earlier 'comprehensive' sweep grepped only files already suspected - sentient_combat.cpp has 10 inventory walks (NextWeapon/PrevWeapon/BestWeapon/WorstWeapon/ammo scans), 8 unguarded. A truly global gr | NULL-skip guards at all 8 sentient_combat sites + sentient.cpp armor-scan straggler. Deployed with the bug-923 batch. |
| `bug-924` | LIVE e1l2: mine death (DBNO death_outran) -> respawn gives all log OK (Picked Up Colt/Thompson/Springfield + KITBACKFILL primaries=2 + REISSUE detector) but player ends with INVISIBLE unraisable sprin | DIAGNOSIS (patch proposed, not applied): (1) The bug-920 AddItem dup-guard is NOT the culprit - inventory stores raw entnums, so refusing an add whose entnum is already listed is idempotent for membership (G_GetEntity resolves the listed in | PROPOSED (see session report): Sentient::PruneStaleInventory() helper that REMOVES (not skips) NULL/non-Item slots + INVDUP/INVSTALE DPrintf diagnostics to finally catch the producer live; call from FindItem, NumWeapons, NextItem/PrevItem,  |
| `bug-923` | Weapons dropped by dying AI sometimes FLOAT and SPIN in mid-air forever (live evidence: pistol hovering+spinning next to a rock face on e1l2, at the height where the AI died on the slope above) | Drop origin = world position of the attach tag at the instant of death (hand, or Bip01 Spine2/Pelvis holster tags from the weapons-on-back system), item physics box = 24x24x14 (setSize -12,-12,-2 / 12,12,12 at weapon.cpp:2532). If that box  | NOT YET FIXED - investigation only, exact patch proposed in session report: (1) Weapon::Drop spawn-point clamp after setSize (weapon.cpp:2532): sweep the item box from owner->origin+(0,0,40) to the intended drop origin using the item's own  |
| `bug-920` | Fifth mine-death crash: exact dump = near-NULL IsSubclassOfWeapon in Player::UpdateStats (per-frame HUD walk) - crash migrated to the next unguarded inventory walk | TWO-PART: (a) Sentient::AddItem appends entnums with NO duplicate check while RemoveItem/~Item remove only ONE occurrence - any double-add leaves a permanently stale container entry that dangles when the item is freed (producer). (b) ~12 ad | AddItem now refuses duplicate entnums (root); every remaining walk guarded (NULL / NULL+type). Comprehensive sweep - no unguarded inventory.ObjectAt deref remains in fgame. game.dll+pdb deployed (bak=game_pre_dupguard_bak.dll). |
| `bug-919` | Crash reliably when the detector-holding player is KILLED by a mine. Exact dump: c0000005 read at 0xffffffffffffffff in Sentient::FindItem+0xad via Sentient::useWeapon <- EventUseItem (script `use`) | Inventory slot whose entnum was RECYCLED by the mine-blast entity churn to a live entity of a DIFFERENT class - the bug-915 NULL guard passes (pointer is non-NULL) and getName()/model reads through the wrong layout -> wild address (-1). Con | All FindItem* guards upgraded to (item && item->isSubclassOf(Item)); FreeInventory walk gained the same guard; coop_missionItemDebug retired to 0 in autoexec (probes no longer needed). game.dll+pdb deployed (bak=game_pre_recycleguard_bak.dl |
| `bug-917` | Third e1l2 crash: exact-symbol dump = NULL read at Entity::updateOrigin+0x7b via Actor::DoMove->setOrigin (children[] walk) | An attached child entity (holster/attachmodel churn on actors) freed without parent cleanup leaves a stale entnum in children[]; updateOrigin/setAngles/tag-scan loops deref G_GetEntity() unguarded. Same use-after-free family as bug-914/915  | Guards + slot-heal (children[i]=ENTITYNUM_NONE) in updateOrigin + setAngles children loops; NULL-skip in both tag-scan loops (~4471/4520); SafePtr checks in both glue loops. game.dll+pdb deployed (bak=game_pre_childguard_bak.dll). |
| `bug-916` | Runtime-spawned script_models using stock static-model TIKs (models/static/sandbag_large_semicircle.tik, nazi_crate.tik via coop_placements.scr) render saturated YELLOW on some faces / near-BLACK on o | static/* TIK shaders use rgbGen vertex (baked BSP radiosity). That only exists for MAP-PLACED statics (RB_StaticMesh, tr_model.cpp:1606-1629, fills tess.vertexColors from staticModelData + sets vertexColorValid). A runtime TIKI refentity dr | Engine (renderergl1, cgame side only - NOT built yet, parent to review/build): new RB_TikiEntityVertexColorFallback() in tr_shade.c before ComputeColors - when CGEN_VERTEX/CGEN_EXACT_VERTEX has no valid vertex colors AND we are drawing a TI |
| `bug-915` | Second crash while using the mine detector on e1l2 (after bug-914 fix). WinDbg on the minidump: c0000005 INVALID_POINTER_READ at game!Sentient::FindItem+0x95 | All five Sentient inventory iterators (FindItemByExternalName/Modelname/ClassName, RemoveWeapons, GetWeapon) deref G_GetEntity(entnum) guarded only by assert() which Release compiles out - one stale inventory entnum (item freed without owne | NULL-skip guards in all five inventory loops + weapon NULL guards in both conditionals. game.dll manual deploy (bak=game_pre_invguard_bak.dll) and game.pdb now shipped NEXT TO the dll so future WER dumps resolve to exact lines. |
| `bug-914` | Crash while using the mine detector on e1l2; log spams 'trigger landmine triggered by NULL entity' (Event doTouch, TriggerLandmine) dozens of times per frame before dying | G_Impact's FIRST EV_Touch block adds e2->entity WITHOUT the null-guard the second block has; when the minefield chain-reacts (mines/entities freed mid-frame during the sweep), queued touches resolve to freed edicts -> AddEntity(NULL) -> bot | g_phys.cpp G_Impact: guard first block with && e2->entity (mirror of second block). trigger.cpp: both Trigger::TriggerStuff and TriggerAll::TriggerStuff NULL checks changed from ScriptError throw to gi.DPrintf + return (a dead toucher is ig |
| `bug-905` | User: 'when I shoot people in the face I still sometimes dont see the bulletholes when they are living' - UV gore wounds land reliably on dead/settled bodies but often not on moving living enemies (fa | The exact wound projection (R_GoreSkelSurfaceCheck, Moller-Trumbore thin-ray at tr_gore.c:755-793) ray-tests the SERVER's bullet segment against the CLIENT's currently-animated/interpolated skinned triangles. On a static corpse the poses ma | Renderer-only (renderer_opengl1.dll), skin-only, no perf change. (1) Raised skin-snap radius: GORE_SKIN_SNAP_DIST 18->26 (tr_gore.c:61), clamp ceiling 40->64 (tr_gore.c snap loop), cvar coop_goreSkinSnapDist default '18'->'26' (tr_init.c).  |
| `bug-892` | CS_MODELS configstring pool (MAX_MODELS=1024) overflows on e1l2: once the 1024 model slots are full, further enemies/props register as model index 0 = INVISIBLE (see bug-891). Fix requires raising MAX | Two independent items bundled because they share the three-binary protocol rebuild: (A) the bug-866 blast-decapitation feature was in the working tree (never committed) and would be recompiled into game.dll, reintroducing the AI-twitch regr | PART A - REVERTED all bug-866 decap CODE from source (kept inert asset .tik files): removed HeadGibObject class decl (object.h) + impl (object.cpp CLASS_DECLARATION+ctor+HeadTouch) - both files now byte-identical to committed HEAD baseline; |
| `bug-868` | FEATURE: gunshot wounds on EXPOSED SKIN (face/head/hands) should read a bit bloodier than cloth wounds, without touching the dialed-back cloth/corpse blood or the dark #150200 palette. | The UV wound stamp was surface-agnostic: the best-hit is picked without regard to whether the hit surface is bare skin or uniform, so skin and cloth got identical stamp+splash sizes. | Added renderer-only skin detection R_GoreDiffuseIsSkin(image_t*) keying off the hit diffuse's imgName: skin = path contains '/faces/' OR basename contains 'hand' OR 'head' OR ('face' and not 'wrap'). Verified against the full stock(AA+SH+BT |
| `bug-853` | Integrate 36 WW2 MOHAARU custom-map objects into build-mode catalog (user approved all 36). | Feature request: mine winners from MOHAARU map packs and wire as placeable build-mode objects, following the 1936 integration pattern (cats 35/36 + coop_build_catIsCover hook). | Extracted 28 of 36 objects (tik+skd/skc/skb+textures, original internal paths preserved) from the 7 MOHAARU packs into hzm-mohaa-coop-mod; 179 files (10.9MB) + 99 verbatim shader blocks in new scripts/coop_mohaaru_imports.shader (0 name col |
| `bug-846` | Shellshock/tinnitus ear-ring does not play when grenades/explosions detonate NEAR the player - only on a big direct hit. Near-miss, behind cover, or blasts soaked up by the ~750 HP pool never ring. | The only trigger was DAMAGE-based: coop_mod/tinnitus.scr::coop_tinnitus_monitor polls player.health each frame and rings only when HP lost in one frame >= coop_tinnitusMinDamage (default 60). A blast that dealt <60 dmg (or 0 - RadiusDamage' | ENGINE HOOK (game.dll): added a blast-proximity ping at the top of RadiusDamage() in weaputils.cpp - the single choke point every explosion funnels through (grenade/rocket/barrel/landmine/script radiusdamage/vehicle wreck via CreateExplosio |
| `bug-817` | Gore TUNING pass (5 user requests on the bug-795 build, which is now 'too much'): (1) 'dial the blood back... put it to what it originally was, now it's way too much' (the bug-796 round-4 crank); (2)  | bug-796 (round 4 'super messy') over-cranked everything: uniform heavy tier soaked ~60-75% of the cloth (2-4 drench blobs + 14-20 patches + spray/smear cast-off + a measured-coverage top-up loop), light tier had 5-8 splotches; the corpse po | (1) gen_gore_skins.py blood_mask reverted to moderate bug-747/754-era coverage: light = 3-5 splotches + ONE gravity run; heavy = 1 large soak blob + 6-9 patches with runs. Removed the _spray/_smear cast-off primitives and the coverage top-u |
| `bug-809` | M1 Carbine buttstock renders solid white in-game (missing-texture white) | Original defect in the S93 xw-weapons import (present in ALL pak versions incl. pre-holster/pre-semiauto baks, NOT today's gore tex repack). carbine.tik maps surface skin9 (wood buttstock/handguard, surface 'SKL Skin9' in carbine.skd) to sh | Rewrote scripts/carbine.shader inside zzzzz_xw_weapons.pk3: carbine1 map + qer_editorimage now textures/hobbscarbine/carbine1.tga (valid RLE TGA 612x410 shipped in same pak). Rebuilt pak preserving all 362 entries/metadata; backup at _xw_we |
| `bug-796` | User round 4: 'even more blood (not just the holes) on their uniforms, make it super messy' | Light tier was 3-5 subtle blots with no runs; heavy tier ~1-2 blobs + 9-13 patches never reached soaked coverage on every texture; renderer kill-splashes were 2-3 and their size was silently flattened by the hole clamp cascade (kind-1 splas | gen_gore_skins.py: light = 5-8 splotches + guaranteed gravity run (clearly wounded); heavy = SOAKED chaos: 2-4 drench blobs + 14-20 saturated patches + long runs + new _spray (cast-off arcs) + _smear (drag chains) primitives + measured-cove |
| `bug-776` | User (3rd report, playtest 07-18 10:44): blood POOLING still not visible under corpses despite bug-754's new baked pool texture + rgbGen identity shader + bigger radii (pk3 + game.dll all deployed and | PROTOCOL SCALE CLAMP, not texture/shader/death-path. Full deployed chain audited end-to-end this session and every link is good: game.dll (Jul 18 10:29) contains the pool code (5 GOREPOOL strings match source), Sentient::ArmorDamage runs Dr | (1) decals.cpp Decal::setRadius: still sets s.scale (legacy fallback) AND side-channels the EXACT radius as whole units in s.surfaces[1] (8-bit netfield in ver_15, clamped 0-255; surfaces[1] was unused for ET_DECAL - only surfaces[0]=direct |
| `bug-756` | User: it's really difficult to select the Borderless Window setting - the Display Mode pulldown in Video Options barely responds to clicks | A PulldownMenuContainer's clickable band is NOT its rect: UIPulldownMenu::MouseDragged (uilib/uipulldownmenu.cpp) sizes the hit column by the MENUSHADER's native pixel width via getDescWidth -> Rend_GetShaderWidth. The dispmodelist overlay  | Two-layer: (1) MOD (works on current exe): created textures/mohmenu/trans_click.tga - a real fully-transparent 256x32 32-bit TGA - and pointed dispmodelist menushader+selmenushader at it; 256 native px >= rect width so the hit column spans  |
| `bug-754` | User: it's really difficult to select the Borderless Window setting - the Display Mode pulldown in Video Options barely responds to clicks | A PulldownMenuContainer's clickable band is NOT its rect: UIPulldownMenu::MouseDragged (uilib/uipulldownmenu.cpp) sizes the hit column by the MENUSHADER's native pixel width via getDescWidth -> Rend_GetShaderWidth. The dispmodelist overlay  | Two-layer: (1) MOD (works on current exe): created textures/mohmenu/trans_click.tga - a real fully-transparent 256x32 32-bit TGA - and pointed dispmodelist menushader+selmenushader at it; 256 native px >= rect width so the hit column spans  |
| `bug-753` | User: cannot pick my resolution from the pick list in Video Options (runs 3440x1440 ultrawide; qconsole.log 07-18 09:44 shows him stuck windowed at 640x480, then picking the list max 1600x1200; omconf | Three-part: (1) the vanilla resolution pulldown only offered the engine r_mode table 2..9 (all 4:3, max 1600x1200; r_vidModes in tr_init.c has no widescreen entries), so 3440x1440 was literally unpickable - the only path to it is r_mode -1  | ui/video options.urc: pulldown replaced with 11 modern entries (1024x768..3840x2160 incl 3440x1440), each writing the RECONCILED cvar set 'set r_mode -1; set r_customwidth W; set r_customheight H' (all CVAR_ARCHIVE\|CVAR_LATCH -> pick is ar |
| `bug-747` | 4-tier gore system barely visible in-game: no bloodholes, models not getting bloody, blood pools missing | FIVE stacked causes: (1) COLOR - every gore asset (skin overlays, UV woundstamp, drip streak/drop sprites, wound patch) was authored flat #150200 RGB(21,2,0) = near-black, invisible composited on dark uniforms (the ground shaders get away w | (1) layered wet-blood palette: kept #150200 base + dried rim but added MID(60,6,2)/HOT(100,10,4) eroded-band cores in gen_gore_skins.py (erosion halved for light tier after the first rerun produced zero bright px in _blood1), woundstamp reg |
| `bug-734` | gore tier 4: qglGetTexImage undeclared - renderergl1 has no GL readback entry point (qgl_linked.h define is dead code, GLE lists lacked GetTexImage) | gl1/gl2 GL functions exist only via the GLE pointer lists in renderercommon/qgl.h loaded by sdl_glimp.c; glGetTexImage was never in any list. renderergl1/qgl_linked.h (#define qglX glX) is not included anywhere, so its qglGetTexImage define | Added GLE(void, GetTexImage, ...) to QGL_DESKTOP_1_1_PROCS in renderercommon/qgl.h - auto-declared (tr_local.h), auto-defined + auto-loaded (sdl_glimp.c) on the desktop gl1 and gl2 paths; tr_gore.c NULL-checks it before use. |
| `bug-731` | gore tier 1 generator: HRRTM tiks re-declare uniform surfaces inside every 'case weapon' block; first-pass generator (a) appended blood skin lines only after the FIRST occurrence (weapon-specific case | TIKI setup parser executes only the matching case block, so every case path independently re-declares the uniform surfaces and each path needs the appended skins; skins ACCUMULATE per executed path so the 4-shader cap must be audited per-pa | append the _blood1/_blood2 lines after EVERY occurrence of a gore-able surface line (no dedupe pop), and added a per-path 4-skin overflow audit (top-level count + worst single case-block count per surface must be <= 4) that passes on all 51 |
| `bug-724` | Audit: vehicle challenges (Panzerknacker/Panzer Graveyard) counted FRIENDLY vehicle kills, and vehicles with removeondeath=0 never counted. | coop_vehKills bump sat AFTER the !m_bRemoveOnDeath early return and had no enemy filter (any Player attacker counted, incl. the squad's own King Tiger / allied escort trucks). | Moved the bump above the early return; skip when driver.ent is a Sentient on TEAM_AMERICAN (player- or allied-AI-driven = friendly). Unmanned vehicles still count. game.dll rebuild. |
| `bug-719` | Self-review #9: last player to shoot a durable barrel got kill/challenge credit even if a chain blast/AI/fire detonated it minutes later. | m_lastDamager (SafePtr) persisted for the barrel's lifetime, only overwritten by another Player hit; BarrelKilled always used it as the RadiusDamage attacker. | Added m_fLastDamagerTime (set in BarrelDamaged on a player hit, init -1000). BarrelKilled only honors m_lastDamager if (level.time - m_fLastDamagerTime) <= 3.0s, else attributes to the barrel (this). The killing shot's own BarrelDamaged fir |
| `bug-710` | User: options built in the coop menus don't save on Apply / don't persist across game restart. | OpenMOHAA exec order (common.c ~1794-1855): default.cfg -> saved config (omconfig) -> autoexec.cfg LAST. autoexec seta'd ~200 curated defaults, so it ran AFTER the user's saved config and OVERWROTE every menu-changed value every launch. 51  | (1) New coop_defaults.cfg holds the 51 menu-controlled option defaults (moved out of autoexec) + archive-seeds coop_xpKillPopup. (2) Engine hook: common.c execs 'coop_defaults.cfg' BEFORE the saved config, so the config loads AFTER and over |
| `bug-647` | e1l1: 'you dont see the turret when you man the panzer tank' - the whole top turret (panzer_cannon_drivable) invisible to the manning player; the tandem-linked MG42 kmodel stays visible | HZM world-gun visibility sync in VehicleTurretGun::UpdateOwner (bug-304 work) sets SVF_NOTSINGLECLIENT on ANY player-owned vehicle turret when the client renders FIRST person (m_bCoopView3p false). That is only correct for physically-mounte | Script-only mitigation (engine flagged, not edited per task rules): playerGetIn forces third person for the ride via the proven coop_mod/player.scr::set3rdPerson recipe (guarded level.gametype != 0, skipped if flags[coop_thirdperson_active] |
| `bug-598` | Hard crash (stack overflow) on m1l1 load - first run with the stufftext filter fix | cdb on the WER minidump: infinite MUTUAL RECURSION Com_Error <-> Cvar_Get. The cvar table hit MAX_CVARS=2048 (engine registrations + large archived config + challenges export ~360 + armory/obj bridges); the FIRST error of the session was 'T | MAX_CVARS 2048 -> 4096 (cvar.c) + re-entry guard around the com_errorCode Cvar_Set in Com_Error (static flag; nested error skips the write and proceeds to the normal error path). openmohaa.exe rebuilt; deploy needed rename-and-replace (stal |
| `bug-596` | Armory preview: guns upside down / tiny even with the shot0087-proven framing (kar98 = bottom-right sliver while enfield had rendered fine with identical values) | PER-GUN authoring variance: weapon world models have different origins and poses (and idle anims), so NO static modeloffset/modelscale works across 69 guns - the fixed standing-player bounds box amplifies the variance (enfield happened to f | New engine URC property `rendermodelfit 1` (UIFakkLabel): frames the model's REAL re.ModelBounds - the same auto-fit the inventory weapon-bar HUD uses to render every gun correctly. Preview labels now modeloffset 0, modelscale 1.0, rendermo |
| `bug-593` | Armory: only the rifles page clickable - other class pages' tiles draw but never receive clicks (ui_clickdebug trace: grid clicks resolve to 'bg') | Menu::ShowMenu (uimenu.cpp:131) only setShow(true)'s widgets whose enabledcvar is ON at MENU-OPEN. Display() deliberately SKIPS the m_visible check for cvar-gated widgets (uiwidget.cpp:1974) so late-enabled pages DRAW - but FindResponder ch | FindResponder now mirrors Display(): widgets with an enabledcvar hit-test purely by isEnabled() (clickable iff gate on, click-transparent iff off); m_visible only governs ungated widgets (vanilla behavior preserved). |
| `bug-590` | Armory 3D preview: some guns invisible/clipped; RE_RegisterModel NULL-name spam 220x/1.5s | UIFakkLabel rendermodel uses FIXED bounds (-16,-16,0)..(16,16,96) sized for a STANDING PLAYER: camera distance 179*modelscale, model dropped 48u below frame center - a weapon lying at z~0 draws half-clipped at the bottom at ~20% size (per-g | Engine: skip draw when the linked cvar is empty. Menu: preview labels reframed for guns - modeloffset z ~ +44..46 (recenter) + small modelscale (L 0.40 / P 0.25 / G 0.18 pulls the camera in). Also shipped: ui_clickdebug click tracer (uiwinm |
| `bug-587` | Armory menu: clicking a weapon tile does nothing (user report, first live test) | UIWidget::FindResponder hit-tests children in REVERSE file order and only checks m_visible, NOT isEnabled(); UIWindowManager::ViewEvent then DROPS events to disabled widgets. All 6 armory pages' tiles share the same grid rects, so an enable | Engine: FindResponder returns NULL for widgets with an enabledcvar whose gate is off (click-transparent, matching their draw behavior). Scoped to enabledcvar users so plain Disable()d widgets keep historical event-eating. openmohaa.exe rebu |
| `bug-554` | Random wrong sprint animation (old slow-walk) at run speed after stamina runs out mid-sprint | Legs statemap 'WALK_FORWARD : !RUN FORWARD' (player_legs.st:127) fires when CondRun is false. Shift (sprint key) arrives with BUTTON_RUN CLEAR; ClientMove re-asserts it (player.cpp:4759) but the statemap is sometimes evaluated BEFORE the re | Made Player::CondRun timing-independent: also returns true when coop_sprint on + moving + not Alt-walk (mirrors the re-assert), so RUN never flips to WALK while the sprint key is held. Rebuilt game.dll (backup game_pre_sprintcondrun_bak.dll |
| `bug-529` | Incorrect value in code | Had `CL_SendReport_f` | Changed to `#ifdef USE_CURL` |
| `bug-481` | Not seeing all the new guns / some rendered blank + one totally invisible (FG42) | TWO issues. (1) FG42 is an UNFINISHED/scrapped vanilla weapon (its tik literally says 'probably going to be scrapped'; all anims point at one placeholder fg42.skc) -> renders invisible in hand. (2) The S93 xw_weapons import had systematic E | (1) Removed FG42 from giveall.scr + developer.scr give_next_weapon. (2) Wrote scratchpad/gen_xwfix.py: indexed the pk3's actual texture files, rewrote each shader texture ref to the real file (matching basename, correcting ext/case) - 32 re |
| `bug-480` | HUD icons rendered as white squares (screenshot); log: 'WARNING: unknown general shader parameter P38parabellum_kun in stg2_kun' then 'Couldn't find image for shader hud_compass_hit' | The KUN weapon import generator (gen_kun.py) emitted a MALFORMED kun_weapons.shader: 2 blocks (stg2_kun, P38parabellum_kun) were missing their outer closing brace (45 open / 43 close). Cause: my shader parser's brace-matcher mishandled the  | gen_kun.py: normalize CRLF->LF + strip /* */ comments before brace-matching, and a safety net that appends any missing closing braces per emitted block. Regenerated (validated 45/45 balanced + running-depth clean), repacked, redeployed. Als |
| `bug-463` | Wall (standing back-to-wall) cover was buggy and could crash | Player::TickCoopCover's WALL pose did an entry view-snap and a peek step-out that setOrigin()'d the body toward a corner, which could shove the player into geometry. | Guarded the entire wall-detection block with if(false) so wallValid stays false and m_bCoopCoverWall can never engage. Crouch/LOW cover kept intact. Updated takecover.scr messages. Rebuilt+deployed game.dll. |
| `bug-462` | Service Record showed no progress bars and baked 'x50/x20' targets instead of live 0/50 progress | Bars relied solely on linkcvartoshader of coop_uiB<i>, blank until chal_ui_export runs in a live game -> nothing drew from the main menu. And the target was BAKED into the page texture (static 'x50'), so it could never show current/target o | Baked an always-visible empty TRACK into each page bg (bars visible even with no data); made the fill textures transparent-track+fill-only so the fill overlays the baked track. Added a live count Label per row (linkcvar coop_uiN<i>) and ext |
| `bug-461` | Service Record category tabs kicked the player out of the menu instead of switching pages | Tabs used stuffcommand 'popmenu 0; pushmenu coop_sr_<cat>'. MenuManager::PushMenu returns false immediately if m_lock is set (uimenu.cpp:519), and PopMenu SETS m_lock for the fade-out transition, releasing it only later via a deferred EV_Un | Rebuilt the 5 separate menus into ONE tabbed menu (ui/coop_sr.urc). Tabs switch category by flipping enabledcvar cvars (coop_srC/K/Q/T/M) - no pushmenu/popmenu at all - so the menu never closes. Bg + per-row fill/count labels are enabledcva |
| `bug-418` | Build mode crashes the engine when placing a posed dead body (models/posed/*.tik) - e.g. german_afrika_private, german_waffenss_shutze, german_elite_sentry. WER deduped multiple crashes into few dumps | models/posed/*.tik are CHARACTER models (path models/human/<x>, skelmodel .skb, idle .ska) - spawning one as a script_model in build mode loads/inits the human/actor model and crashes (needs Actor init it never gets). The models-agent had t | Removed ALL models/posed/*.tik (17) from the catalog + rubble_bigpile. Static body props (models/static\|animate/body_german01, body_us*) are SAFE and remain in BODIES + GEAR (user placed body_usvest06 fine). Diagnosed via WER minidumps in  |
| `bug-382` | Server crashed: Unknown state 'KILLED' referenced in state 'EMOTE_LOBBY_SELECT' - co_lobby1 crashes at CS_PRIMED->CS_ACTIVE (statemap load). | Added 'KILLED : KILLED' as a safety exit to the new EMOTE_LOBBY_SELECT legs state. The LEGS statemap (player_Legs.st) has NO 'KILLED' state - death is force-jumped to the kill state by the engine regardless of current state (per comments at | Removed the 'KILLED : KILLED' line; EMOTE_LOBBY_SELECT now has only 'EMOTE_LOBBY_SELECT : ANIMDONE_LEGS' (self-loop to re-pick the weighted coop_lobbyselect pool). Engine handles death transition automatically. |
| `bug-380` | Coop lobby: player model floats/flies around on WASD/space/ctrl (frozen anim but movable model); also spawned in plain STAND, not the at-ease pose. | Two issues in CoopLobbyPose. (1) FLOAT: the forced-respawn deploy leaves m_bSpectator set, so InitEdict (player.cpp:2380) puts the player in MOVETYPE_NOCLIP -> GetMovePlayerMoveType returns PM_NOCLIP (fly on WASD/space) AND EvaluateState ea | CoopLobbyPose now: (a) if IsSpectator() EndSpectator(), then setMoveType(MOVETYPE_WALK) - grounds the player so freezecontrols+FL_IMMOBILE lock him; (b) AttachToHolster(WEAPON_MAIN) FIRST (stable HAS_WEAPON), THEN EvaluateState(STAND torso) |
| `bug-379` | Coop lobby: rifle twitches back and forth in the player's hand; parade-rest pose never holds (persisted after the safeholster fix bug-378). | Statemap oscillation. The forced-respawn lobby deploy leaves the weapon in the 'banned -> re-allowed rifle' limbo (dmflags 532676608 bans all DM weapons; primarydmweapon rifle is rejected, engine re-allows late), so weapon-change conditions | New engine events coop_lobbypose/coop_lobbyunpose (Player::CoopLobbyPose, player.cpp). CoopLobbyPose: (1) EvaluateState(STAND torso, EMOTE_ATEASE legs) while the statemap is still live, (2) AttachToHolster(WEAPON_MAIN) to sling the rifle on |
| `bug-334` | Ctrl+V (paste) in the in-game console crashes the game | The [236] console-paste handler called uii.GetClipboardData(buf,len). That struct field is DECLARED in ui_public.h:146 but NEVER assigned in cl_ui.cpp (unlike its neighbors Cvar_Set/Sys_IsKeyDown), so the function pointer is NULL -> Ctrl+V  | [242] Rewrote the handler to use uii.Sys_GetClipboard() with a NULL guard, copying the proven working pattern in uimledit.cpp:803. Kept the stop-at-newline safety (multi-line paste can't auto-execute). Rebuilt openmohaa.exe (uilib links int |
| `bug-312` | New XP-bar HUD textures authored at non-power-of-two dims (896x72 etc) would upload HALVED: gl1 Upload32 rounds to POT and r_roundImagesDown defaults 1 = round DOWN (896->512). Caught pre-ship during  | tr_image.c Upload32 resamples every non-POT texture (roundImagesDown=1 rounds down); authoring HUD art at arbitrary 4x-virtual dims silently loses resolution. | Regenerated all 6 xpbar layers on POT canvases (1024x128/64x32/256x32/64x64/256x256/128x128) with geometry computed in virtual-space fractions; rule: ALL shipped textures must be POT (draw-rect aspect need not match texture aspect). |
| `bug-308` | HARD process crash the instant wall cover engaged (log froze mid-line at 'TAKECOVER: engaged WALL', no ERR_DROP text) | the cover entry-view orbit seed called cgi.get_camera_offset(NULL, NULL) - the client impl (cl_main.cpp:217-221) writes BOTH out-params unconditionally (*resetview=..., *lookactive=...) -> null write -> segfault on the first frame PMF_COOP_ | pass real locals (&bLookActive, &bResetView) like every other caller. LESSON: before calling an engine import with NULL args, read its implementation - MOHAA-era code rarely null-checks out-params |
| `bug-273` | Incorrect value in code | Had "sounds great" | Changed to "NAT traversal" |
| `bug-268` | Incorrect value in code | Had "XP/rank system PLANNED 07-06 (not built): full de | Changed to "XP/rank system PLANNED 07-06 (not built): full de |
| `bug-230` | Blood ground decals work now (red) but flash/flicker on and off as the camera moves | Z-fighting: the blood decal sits coplanar with the floor surface it lands on; without a depth bias the decal and floor trade the depth test each frame as the view moves, so the mark flickers. The base 'bloodsplat' shader (which our red text | Added scripts/coop_blood.shader defining 'coop_bloodsplat' with polygonOffset (depth bias so the mark always wins the depth test) + rgbGen identity (red baked into the texture). Repointed both blood decals - TryDropBloodTrail (trail) and Ad |
| `bug-159` | HUD score-widget dark box returned in gameplay (post-FX contaminating the HUD again) despite the earlier RB_SetGL2D fix; menu was clean but in-game HUD was not | The first HUD-split fix (bug-152) hooked RB_PostFxMaybeApply into RB_SetGL2D. But the in-game HUD is drawn by cgame entering 2D via the EXPORTED re.Set2DWindow (=Set2DWindow), which sets backEnd.in2D=true directly and never calls RB_SetGL2D | Moved the once-per-frame RB_PostFxMaybeApply call into Set2DWindow (tr_draw.c) itself - the universal 3D->2D chokepoint shared by both the engine UI and the cgame HUD - right after R_IssuePendingRenderCommands() (3D already flushed to backb |
| `bug-152` | gl1 post-FX contaminates the 2D HUD: black box / darkening behind the score widget (top-right) once SSAO was enabled (bloom was quietly doing it too) | RB_PostFxApply ran in RB_SwapBuffers, i.e. AFTER the 2D HUD was drawn. SSAO's depth-based multiply (and bloom's add) operate on the whole backbuffer; HUD 2D quads don't write depth, so the depth under them is the 3D scene, and the post-pass | Moved the post-pass to the 3D->2D transition. Added file-scope gates s_postfx_scene3D (set in RB_DrawSurfs) and s_postfx_applied; new RB_PostFxMaybeApply() runs once per frame from RB_SetGL2D when transitioning out of the 3D view (!backEnd. |
| `bug-143` | gl2 terrain/floor 'white sheen' that sweeps as the camera pans (view-dependent) | gl2 lightall_fp.glsl gives world surfaces with no specular map a default specular of vec4(1.0); the sun/primary-light block (CalcSpecular) applies it scaled by r_baseSpecular (0.04) -> a 4% white view-dependent highlight on every lit surfac | Changed r_baseSpecular default 0.04 -> 0 (and r_mergeLightmaps 1 -> 0 for the black-streak fix) in tr_init.c; matches gl1's no-specular look. Confirmed by user with seta r_baseSpecular 0 + full relaunch. |
| `bug-121` | t1l3 hard-crashes on load (openmohaa.exe faulting in ucrtbase.dll, exception 0xc0000409); 100% reproducible on t1l2->t1l3 transition | Minidump parsed in Python (no cdb): ExceptionInformation[0]=0x7 = FATAL_APP_EXIT = abort()/terminate from an UNCAUGHT C++ exception in server game.dll (faulting stack full of game.dll frames), NOT memory corruption. Trigger: t1l3 SP rowboat | Coop override gags/T1L3_BoatRide.Scr: per-player ($player[i]) not the array. P1 rides as SP; players 2+ glue to same boat seat (notsolid, freezecontrols 1) = ride at P1 vantage; AtDock ungluess all + playersWarpto to pier ($cappy_dest, setS |
| `bug-108` | When crouching and shooting in ADS the muzzle flash appears to the left instead of at the center of the gun barrel | The muzzle flash is a depth-hacked SPRITE. The surface render pass (RB_RenderDrawSurfList) switches to weaponProjectionMatrix (which carries the r_weaponshiftx/y ADS screen-shift) for RF_DEPTHHACK surfaces, but the sprite pass (RB_RenderSpr | In RB_RenderSpriteSurfList, when depthRange && weaponFovActive load weaponProjectionMatrix (and restore projectionMatrix on the non-depth branch and at cleanup), mirroring the surface pass. Renderer change -> openmohaa.exe rebuild (renderer |
| `bug-095` | M1 Garand front sight renders WHITE in first-person/ADS (also 'white at end of barrel') | The coop pk3's weapon .tik files (older, base-derived) declare fewer surfaces than the HRRTM HD .skd models actually contain. m1_garand.tik/m1_garand_lite.tik only mapped surfaces gun1-gun3 + material8, but HRRTM's garand.skd has gun1-gun5. | Added the missing surface->shader lines to the coop tiks: gun4/gun5 -> 'Garand' on m1_garand.tik and m1_garand_lite.tik; aim/aim1 -> 'bazooka' on bazooka.tik (matching HRRTM's own HD tik). Found via a scan comparing each coop weapon tik's d |
| `bug-092` | User (shot0008): white blade/box at the top of the M1 Garand muzzle (the muzzle flash) | The Garand muzzle flash is models/fx/muzflash.tik -> skelmodel muzzleflash.skd (surface 'material1') -> shader 'muzmodel' -> map models/fx/muzflash/flashnode1.tga, blendFunc GL_SRC_ALPHA GL_ONE + alphagen vertex. The whole asset chain exist | Shipped a 32-bit flashnode1.tga (alpha = brightest channel, uncompressed type-2) in the coop pk3 at the exact path - this both gives SRC_ALPHA a real alpha AND forces the texture to load from our highest-priority (homepath) pak. NEEDS in-ga |
| `bug-073` | ^~^~^ Script Error : Failed execution of command 'setfov' for class 'Player' Targetname 'player' (flooding); ADS zoom never applied | ads.scr called 'local.player setfov <fov>'. In this engine build EV_Player_SetFov ('setfov') is DECLARED (player.cpp:1573) but is NOT in the Player event response map (player.cpp:1805-1839 only map 'fov', 'safezoom', 'zoomoff'), so dispatch | Replaced 'setfov' with the mapped 'fov' command (EV_Player_Fov -> EventSetSelectedFov): aim = 'local.player fov <n>', release = 'local.player fov 0' (SetSelectedFov clamps <1 to the 80 default). This HZM build also has the MP fov clamp comm |
| `bug-059` | Server crashed: LoadTGA: Only type 2 (RGB), 3 (gray), and 10 (RGB) TGA images supported (m3l2, recurred after the 'fix') | The earlier LoadTGA soft-fail was applied to code/renderercommon/tr_image_tga.c, but THAT IS NOT THE ACTIVE LOADER. This build compiles the renderergl1 backend, which has its OWN TGA loader code/renderergl1/tr_image.c::LoadTGA (~line 1628)  | Applied the same soft-fail (ri.Printf PRINT_WARNING with filename + ri.FS_FreeFile(buffer) + return, leaving *pic=NULL -> default texture) to the 3 unsupported-format checks in renderergl1/tr_image.c:1670-1685. Rebuilt openmohaa.exe (cmake  |
| `bug-058` | Incorrect value in code | Had `ri.Error(ERR_DROP,...)` | Changed to `code/renderergl1/tr_image.c::LoadTGA` |
| `bug-052` | t2l2 bed passengers (glued to seat dummies) could not crouch | Plain 'glue' sets PMF_NO_MOVE with m_bGlueDuckable=false; Player::CondCrouch (player_conditionals.cpp:866) then suppresses ducking while glued. The engine has a separate 'duckableglue' script event (Entity::glue can_duck=true) intended exac | In coop_playerSpawned, changed 'local.player glue local.attach' -> 'local.player duckableglue local.attach' for bed seats 2-16 (seat 1 = cannon, unaffected). Mod-side only; engine already supports duckableglue. |
| `bug-043` | Incorrect value in code | Had `hzm-mohaa-coop-mod` | Changed to `hzm-coop-working` |
| `bug-029` | Incorrect value in code | Had "not spawning" | Changed to `level.coop_officer_waves_at_zone` |
| `bug-010` | Incorrect value in code | Had " g.y += t * speed;\n" | Changed to " g.y -= t * speed;\n" |
| `bug-gl2-01` | gl2 renders blood decals black + bullet-holes red + material stages black (user: 'messed up textures') | gl2 ParseShaderStage default-rgbGen block (CGEN_BAD) missing gl1's leading 'if(cntBundle\|\|stage->bundle[0].isLightmap)->CGEN_IDENTITY' branch; multi-bundle/lightmap stages with no explicit rgbGen fell to CGEN_IDENTITY_LIGHTING (darkened)  | Added the leading cntBundle\|\|isLightmap->CGEN_IDENTITY branch at tr_shader.c:1910 to match gl1 tr_shader.c:1751-1761 (Fix 1 of the gl2 re-port plan) |
| `bug-gl2-fog` | gl2 outdoor maps lose MOHAA atmospheric fog (snowy haze etc.); huge gl1-vs-gl2 divergence on outdoor maps | gl2 applies fog ONLY to Q3 fog-BRUSH volumes (ComputeFogValues returns early on `if(!tess.fogNum)`); MOHAA's GLOBAL farplane distance fog (viewParms.farplane_distance/bias/color, applies to all surfaces by eye-distance, no brush) is never r | PLANNED (Fix 9): add a screen-space depth-fog post pass in gl2 RB_PostProcess - linearize depth->eye distance, fogFrac=clamp((dist-bias)/(farplane_distance-bias),0,1), mix(scene, farplane_color, fogFrac). Isolated, tunable, mirrors gl1 GL_F |
| `bug-gl2-sweep-crash` | Session crashed during full 54-map gl2 A/B sweep; openmohaa processes accumulated to 24 (resource exhaustion) | Launched the sweep via `nohup powershell ... &` in Bash TWICE (relaunch after harness edit) without confirming the first was dead -> two concurrent sweeps booting openmohaa; the nohup child powershell is UNTRACKED (its $! is the bash subshe | 1) launch long game-boot sweeps via Bash run_in_background (tracked, killable via TaskStop) NOT nohup&; 2) NEVER run two concurrent boot-sweeps - verify openmohaa==0 + no sweep-ps before launching; 3) harness has taskkill Kill + pre-boot pi |
| `bug-gl2-foliage` | gl2: foliage/bush alpha renders OPAQUE BLACK (t2l1 snowy bush TAwintershrub = black rectangles with branch texture on top); gl1 correct | Bush shader TAwintershrub (mainta pak1 scripts/TAnatural.shader) has 3 stages gated by ifCvar/ifCvarnot r_blendtrees 0: (1) clampmap+depthwrite+alphafunc GE128+alphagen distfade+rgbgen lightinggrid, (2) depth-prime: depthwrite+nocolorwrite+ | FIXED 2026-07-26 (HZM gl2 re-port): (a) ParseStage end - apply shouldProcess (active=qfalse, rgbGen=CGEN_BAD, return qtrue) = gl1 parity; (b) ParseShader caller - stage returned with rgbGen==CGEN_BAD is NOT activated; slot wiped back to Ini |
| `bug-gl2-hudcolor` | gl2: HUD/menu tints dropped to white/opaque (health bar green-as-red class); rgbGen global / alphaGen globalalpha shaders untinted | gl2 ComputeShaderColors had no CGEN_GLOBAL_COLOR / AGEN_GLOBAL_ALPHA cases (values parsed but fell through switch -> baseColor stayed white/opaque). gl2 default 2D-GUI stage also used stock-ioq3 CGEN_VERTEX/AGEN_VERTEX instead of gl1's GLOB | VERIFIED 2026-07-26 (Fix 2): added CGEN_GLOBAL_COLOR (baseColor=backEnd.color2D/255) + AGEN_GLOBAL_ALPHA cases; switched BOTH LIGHTMAP_2D GUI default sites to CGEN_GLOBAL_COLOR/AGEN_GLOBAL_ALPHA matching gl1 tr_shader.c:3498. Boot: HUD chip |
| `bug-gl2-introhud` | USER (live watch): during scripted mission intro card under gl2, HUD elements leaked visible (gun highlight + blue bar WITHOUT its white squares); on gl1 nothing shows during the card | THEORY (fits code): intro hides HUD by drawing with global 2D alpha ~0; pre-Fix2 gl2 had no AGEN_GLOBAL_ALPHA case -> alpha fell through OPAQUE -> global-alpha-hidden elements leaked fully visible; widget-early-return-hidden elements (white | VERIFYING: intro-card A/B capture (settle 4s) gl1 vs gl2 on t2l1 with the Fix2 build. Normal-play white squares CONFIRMED rendering in user F12 shot post-Fix2. |
| `bug-gl2-texgen` | gl2: 8 materials render wrong texcoords - unknown texgen parm on lens/svt_lens/facewrap/facewrap_blood3/L_gloves/gestapo_ledermantel/sspanzer_ledermantel/Af_P_Glasses_reflection | MOHAA texgen environmentmodel (gl1 TCGEN_ENVIRONMENT_MAPPED2 model-space env map) not parsed by gl2 -> unknown-parm warning -> base texcoords. gl1 d<0 branch is exactly reflect(); d>0 passes viewer through. | VERIFIED 2026-07-26: TCGEN_ENVIRONMENT_MAPPED2 enum + parse + ATTR_NORMAL + permutation whitelist + GLSL define + GenTexCoords branch in generic_vp/lightall_vp. Boot: 0 warnings (was 8 distinct), 0 GLSL failures. sunreflection skipped (0 re |
| `bug-gl2-ztagmalloc` | gl2 main menu spams 'Z_TagMalloc, Negative or zero size 0 tag 12' repeatedly (gl1 menu boot clean) | UNKNOWN yet - gl2-only zero-size allocation each menu frame; NOT present under gl1 in the same A/B menu boot. Widget UI otherwise renders fine under gl2 (What's New board, text, buttons all draw - the .urc-invisible/FBO-ordering theory is D | OPEN - next: find tag 12 meaning + the gl2 alloc call site (grep Z_TagMalloc callers with size 0 guard missing) |
| `bug-gl2-modellight` | gl2 renderer: every animated TIKI model renders fullbright - rgbGen lightingSpherical (5048 uses) / lightingGrid (344) ignored, rgbGen static props ignore dlights (780) | tr_sphere_shade.cpp was transcribed into gl2 but never wired: RB_Sphere_SetupEntity/RB_Grid_SetupEntity/RB_Static_BuildDLights had zero callers in RB_RenderDrawSurfList; RB_SetupEntityGridLighting/RB_SetupStaticModelGridLighting in tr_light | Ported gl1 wiring: backend per-surface lighting-source selection block (grid vs sphere, sphere caching, numSpheresUsed reset), needsLGrid/needsLSpherical shader flags in FinishShader, R_GetLightingGridValueSingle (gl1 combined grid sampler) |
| `bug-gl2-dbnofx` | gl2: DBNO/low-health full-screen desaturate + red-vignette absent (gl2 builds no HZM post-FX module - ROOT C of gl2 re-port plan, Fix 5) | The effect is a gl1-only post pass (renderergl1/tr_postprocess_gl1.c:755-785 ramp + LOWHEALTH_FS shader); renderergl2 never had the module, so r_ppHealthFrac (published every frame by cg_view.c:1630-1662, forced 0.02 while DBNO) was consume | Renderer-agnostic cgame overlay per plan Fix 5: new CG_DrawGl2PostFxFallback() in cg_drawtools.cpp, called first in CG_Draw2D (under HUD, over scene). Draws a "*white" red wash (alpha hurt*0.30) + the existing radial-alpha vignette textures |
| `bug-gl2-introfade` | gl2: scripted mission-intro fade-from-black never renders (gl1 scene dark during intro card, gl2 fully visible; ab/intro_compare.jpg, user-confirmed) | Fade chain = script fadein (scriptthread.cpp:3278) -> level.m_fade_* -> Player::CalcBlend (fgame/player.cpp:6921-6967) -> ps.blend -> client View3D::DrawFades (cl_uiview3d.cpp:685-695) -> re.SetColor(=Draw_SetColor)+re.DrawBox. gl2's DrawBo | Rerouted gl2 DrawBox through the proven immediate stream/material pipeline (same recipe as Draw_StretchPic): R_FindShader("*white", LIGHTMAP_2D) (resolves via image hash to tr.whiteImage, no disk) -> RB_BeginSurface + RB_Color4bv(backEnd.co |
| `bug-gl2-suppressfx` | gl2: suppression tunnel-vignette (near-miss/under-fire FX) absent (ROOT C, plan Fix 6) | Same as bug-gl2-dbnofx: gl1-only post pass (tr_postprocess_gl1.c:381-405 SUPPRESSION_FS); r_ppSuppress (published by cg_view.c:1664-1696, decaying spike) consumed by nothing under gl2. | Second half of CG_DrawGl2PostFxFallback(): coop_ads_vignette tunnel at alpha s*0.72 (matches gl1 edge term 1-vig*s*0.72) + faint grey "*white" wash (alpha s*0.18) standing in for the desaturate term; s = r_ppSuppress * r_ppSuppressAmount cl |
| `bug-gl2-cullfront` | gl2 warns 'invalid cull parm front' on textures/fx/sandstorm_sky | Neither gl1 nor gl2 has an accepting branch for 'cull front' (both warn); behavior was already correct because CT_FRONT_SIDED is the default cullType - the warning is pure noise | Added front/frontside/frontsided -> CT_FRONT_SIDED branch to gl2 ParseShader cull parsing (HZM gl2 re-port comment documents that gl1 still warns; pixel behavior identical in both before and after) |
| `bug-gl2-gore` | gl2 missing the whole gore tier-4 UV-wound system (re.GoreImpact/GoreReset/GoreKillSplash NULL) - characters never show bullet wounds under gl2 (plan Fix 4 / ROOT D) | renderergl1/tr_gore.c was never ported; gl2 GetRefAPI left the three gore exports NULL (cgame call sites are NULL-guarded so it silently did nothing) | Full port of tr_gore.c to renderergl2 (wound logic byte-identical). gl2 adaptations: tess.texCoords is single-bundle vec2_t (dropped gl1's [0] bundle index); shader->stages instead of unfoggedStages; GL texture respec/updates through the tr |
| `bug-gl2-nextbundle2` | gl2 nextbundle residuals (audit HIGH #2): ~360 dual-texture non-lightmap stages lose the 2nd texture; blended tex+lightmap stages draw dark decal rectangles (split filter pass multiplies the whole qua | gl2 only rescued opaque tex+$lightmap nextbundle pairs via split(DST_COLOR*ZERO filter pass)+collapse; generic GLSL sampled only u_DiffuseMap so any non-lightmap bundle[1] was bound to TMU1 but never sampled; the split framebuffer-multiply  | Real single-pass second bundle in the generic program: stage->multitextureEnv stored at parse (GL_MODULATE/GL_ADD); CreateMultistageFromBundle now defaults tcGen for both bundles, ports the lightmap-first swap, keeps the split+collapse ONLY |
| `bug-gl2-texmods-null` | gl2 boot hangs deterministically during t2l1 map shader parsing; qconsole.log ends at the 'unknown tcMod parallax in textures/central_europe_winter/snowfield1' warning both boots; process alive until  | NOT an infinite loop: NULL-pointer write at exactly that parse position. The bundle-aware ParseTexMod (parity fix, first ever DEPLOYED in this build - the previous DLL routed all tcMods to bundle[0], which is why t2l1 booted ~60x) writes tm | Per-bundle texMod backing: static texMods[MAX_SHADER_STAGES][NUM_TEXTURE_BUNDLES][TR_MAX_TEXMODS]; InitShaderEx and the ifCvar stage-reset now wire stages[i].bundle[b].texMods = texMods[i][b] for every bundle. Deliberately BETTER than gl1,  |
| `bug-gl2-compass` | gl2 HUD compass (rose+bezel+ticks+N-marker+needle, ui/hud_compass.urc statbar compass -> cl_uistd.cpp DrawStatCompass -> re.DrawTrianglePic) completely absent under renderergl2; renders correctly unde | The HZM "Airborne" HUD shaders (scripts/hud_airborne_coop.shader: hud_compass_*, hud_health_*) are authored with 'alphaGen oneMinusVertex'. This pairs with renderergl1: gl1 Draw_StretchPic/Draw_TrianglePic/etc feed the 0..255 backEnd.color2 | renderergl2/tr_draw.c: changed the 5 immediate-2D stream helpers (Draw_StretchPic, Draw_StretchPic2, Draw_TilePic, Draw_TilePicOffset, Draw_TrianglePic) from RB_Color4bv(backEnd.color2D) to RB_Color4f(backEnd.color2D[0],[1],[2],[3]) - bit-i |
| `bug-gl2-hudsegments` | gl2 health bar shows ONE solid cyan bar instead of 4 discrete cyan segments; user: "blue health bar is there but the white rectangles are not". The white segment overlays (ui/hud_health.urc healthout  | SAME root cause as bug-gl2-compass. The solid cyan fill (hud_health_in, a raw tileshader texture -> default LIGHTMAP_2D global-color shader) rendered fine, but the segment overlay hud_health_out is an explicit shader using 'alphaGen oneMinu | renderergl2/tr_draw.c: changed the 5 immediate-2D stream helpers (Draw_StretchPic, Draw_StretchPic2, Draw_TilePic, Draw_TilePicOffset, Draw_TrianglePic) from RB_Color4bv(backEnd.color2D) to RB_Color4f(backEnd.color2D[0],[1],[2],[3]) - bit-i |
| `bug-gl2-foliage-white` | gl2-only: leafy tree/foliage BILLBOARDS (crossed alpha-cut quads) render as OPAQUE WHITE quads instead of transparent alpha-tested foliage. gl1 correct. Repro map e2l2 (maintt). Dark leaves visible ON | e2l2's white foliage are SS_OPAQUE alphaFunc-GE128 cutout surfaces: static models tree_commontree.tik/tree_oak.tik (shaders static_tree1_2..5 / static_tree4_*, rgbGen lightingSpherical + alphaGen distFade + deformVertexes flap), dynamic ani | Exclude alpha-tested (cutout) surfaces from the depth prepass so gl2 matches gl1 (which has no prepass). Added shader_t.hasAlphaTest (tr_local.h); set it in FinishShader (tr_shader.c) when any active finished stage carries GLS_ATEST_BITS; e |
| `bug-gl2-invisible-friendly-actor-root` | ROOT CAUSE of the gl2-only invisible friendly/scripted actor (e2l2 briefing squadmate sc_al_brit_cmd and the whole 58-bone sc_ scripted-character class: sc_al_brit_off/pilot, sc_al_us_radio, sc_ax_ita | gl2 back-face CULL FLIP for per-entity mirroring that gl1 does not have. In renderergl2 RB_StageIteratorGeneric (tr_shade.c) gl2 did, for non-two-sided shaders: when backEnd.currentEntity->mirrored is set, cullFront = !cullFront (stock ioq3 | Removed the per-entity mirrored cull flip from renderergl2 tr_shade.c RB_StageIteratorGeneric (the currentEntity->mirrored cullFront-toggle line), matching gl1 which has no such flip. Kept the isMirror flip (genuine mirror/portal views; gl1 |
| `bug-gl2-invisible-live-enemies-prepass-pose` | gl2-only: some LIVE animated skeletal enemies (Axis/German AI, and friendly/scripted actors) are INVISIBLE while alive but become VISIBLE the instant they are KILLED/frozen. Entity is fully present an | gl2 runs skeletal PRE-PASSES that gl1 does not have (the shadow view R_RenderView(shadowParms) VPF_SHADOWMAP\|VPF_DEPTHSHADOW at tr_main.c:2237/2284, and the sun-light view VPF_DEPTHSHADOW at ~2965/2997). Each pre-pass calls R_AddEntitySurf | In R_AddSkelSurfaces, for character (tiki->a->bIsCharacter / MOD_TIKI) models, call RE_ForceUpdatePose(&ent->e) at the MAIN-view add instead of R_UpdatePoseInternal, so the skeletor is always re-posed fresh from the current frameInfo regard |
| `bug-gl2-e2l2-briefing-npc-invisible` | gl2-only: the e2l2 mission-intro BRIEFING NPC ($lyndon, models/human/sc_al_brit_cmd composite w/ m1 garand) is invisible in gl2 during the intro cinematic. Present/targetable; SKELDRAW ent=213 dV=604  | NOT the model: the SAME models/human/sc_al_brit_cmd type renders FINE in gl2 on other maps (user-confirmed), so model/skinning/composition/58-bone are all refuted. NOT render-state: none of r_test_forcepose/twosided/maskrfx/noprepass/forcel | DOCUMENTED as a known LOW-IMPACT gl2 quirk (one intro-cinematic actor on one map; the model is otherwise fine in gl2). Not fixed - per coordinator, not worth a deep-dive. If revisited: investigate the e2l2 intro camera/cinematic view setup  |
| `bug-gl2-autosprite2-swim` | gl2 #71: far-LOD autosprite2 billboard trees lurch/swim as the camera moves (then swap to real 3D up close); r_uselod 0 had no effect (not model-LOD). | gl2's edgeVerts[6][2] table AND Autosprite2Deform() were the stock ioq3/Quake3 versions, never updated to MOHAA's. Q3's algorithm reprojects off the two SHORTEST edges with an index-walk to choose direction; MOHAA (gl1) rewrote it to key of | Ported gl1's OpenMOHAA edgeVerts table + Autosprite2Deform() verbatim into renderergl2/tr_shade_calc.c (adapting gl1 viewParms.ori -> gl2 viewParms.or), and restored the 'backEnd.currentStaticModel \|\|' condition. Autosprite2 only repositi |
| `bug-gl2-decal-red-dds` | gl2 #74: SOME bullet-hole/impact decals render RED (red mark + red ring) on walls; most render correct black. Residual of bug-gl2-01 after Fix 1 (rgbGen default). | NOT rgbGen: gl2's default-rgbGen resolution (tr_shader.c:1988-2001) and the CGEN_VERTEX/mark-color path are now byte-identical to gl1, and the two mark decal shaders (bulletset_1, bullet_plasterhit) carry EXPLICIT 'rgbgen vertex' so they ne | NOT FIXED this round - no safe blind change (a neutral texture through an rgbGen-identical shader cannot produce red, so the mechanism is unexplained; guessing risks regressing 2370 working world DDS or the mark system). DECISIVE BOOT TEST  |
| `bug-gl2-viewmodel-over-menu` | gl2 #73: first-person viewmodel draws OVER UI menus (rifle over the ESC panel; a black unlit gun over the main menu). gl1 suppresses it. | Not a shader/refent issue and not cgame-side gating: cgame is shared by both renderers and RB_StretchPic binds the 2D UI into tr.renderFbo AFTER the 3D DRAW_SURFS, so in a normal frame the menu (last 2D) correctly composites over the weapon | NOT FIXED this round - a blind render-order change risks regressing the working HUD-over-3D compositing. Needs a boot to trace the actual per-frame command order (which RE_RenderScene submits after the 2D menu). Recommended direction: gate  |
| `bug-gl2-forcepose-skips-composite-ally` | e2l2 allied combat squadmate (sc_al_brit_cmd) STILL invisible after the depth-prepass-exclusion fix, confirmed live up-close. Working-vs-broken SKELPIX diff: m3l2 Ranger (visible) pose=FORCED samplesP | The force-pose gate was `tiki->a && tiki->a->bIsCharacter`. bIsCharacter is set ONLY from the `ischaracter` keyword in a .tik (tiki_parse.cpp:1100 -> tiki_files.cpp:917 panim->bIsCharacter). The e2l2 ally is DISPATCHED as a COMPOSITE tiki ( | Dropped the bIsCharacter gate: force the fresh pose for EVERY skeletal model that reaches R_AddSkelSurfaces at the main-view add (`if (!r_test_forcepose \|\| r_test_forcepose->integer) { RE_ForceUpdatePose(&ent->e); }`). RE_ForceUpdatePose  |
| `bug-gl2-reddecals` | #74 gl2: some bullet-hole/impact decals render RED (bulletset_1, bullet_plasterhit, mark family); .dds HD-override theory refuted (r_ext_compressed_textures 0 forcing .tga stayed red) | gl2's R_GetLightingForDecal was an empty '// FIXME: unimplemented (GL2)' stub, so it left vLight (a vec3_t local in cg_marks.c::CG_ImpactMark) as UNINITIALIZED STACK GARBAGE. The renderer-agnostic cgame then builds the impact mark's per-ver | Ported gl1's R_GetLightingForDecal verbatim into the gl2 stub (tr_light.c:1116): R_GetLightingGridValueSingle(tr.world, vOrigin, vLight) [gl2's single-value light-grid lookup, NULL/gridless world -> tr.identityLightByte fallback], then the  |
| `bug-gl2-maxpolys` | gl2: 'Exceeded MAX POLYS' console spam + decal/effect thinning + lag spikes after sustained shooting | gl2's dynamic-poly ceiling was still the STOCK Q3 value (MAX_POLYS 600 / MAX_POLYVERTS 3000) while gl1 was long ago raised by HZM to 32768 / 131072 for effects-heavy scenes. The poly producers (bullet-hole MARKS + blood/decal polys, submitt | Raised gl2 MAX_POLYS 600->131072 and MAX_POLYVERTS 3000->524288 in tr_local.h (past gl1's 32768/131072 for comfort; user wants MORE blood). r_maxpolys/r_maxpolyverts default + R_Init floor-clamp key off these #defines, so the backEndData Hu |
| `bug-sfx-duck-filtered` | m3l1a SFX still way louder than music despite ducking s_volume + boosting s_musicvolume | The cgame server-command filter (CG_IsSetVariableAllowed) drops any server-stuffed set of an ARCHIVE cvar not on whiteListedVariables. s_volume/s_musicvolume/s_ambientvolume are all CVAR_ARCHIVE and were NOT whitelisted, so every coop_setCl | Added s_volume, s_musicvolume, s_ambientvolume to whiteListedVariables[] in cg_servercmds_filter.cpp; rebuilt cgame.dll (cmake --build .cmake --target cgame --config Release); deployed to GOG root AND overwrote the stale Jun-25 maintt + hom |
| `bug-lowhealth-scene-dependent` | low-health red vignette 'changes as I move around the world' (strong in dark hall, gone in bright room) at a CONSTANT 22 HP | LOWHEALTH_FS mixed toward a FIXED dark red vec3(0.45,0,0) weighted only by an edge vignette, so the additive red looked vivid on dark scenes and washed out on bright ones; plus a sin() heartbeat pulsed hurt by +/-20% over time. Combined = e | Made the red luminance-PROPORTIONAL (red = vec3(l)*vec3(1,0.30,0.26)) so a bright wall->bright-red, dark hall->dark-red and it reads the same regardless of scene brightness; made it a whole-frame wash (0.30) + edge boost (0.50*vig) instead  |
| `bug-menu-shader-label-invisible` | Coop Settings 'folder not there' (panel renders blank/invisible) | Folder/paper background were Label resources using shader textures/mohmenu/coop_folder\|coop_paper pointing at raw JPGs; raw image paths do not resolve as shaders in the URC UI material system, so the Labels drew nothing and the whole panel | Reverted folder/paper/foldertab to solid bgcolor fills (manila 0.58 0.47 0.29 / cream 0.90 0.86 0.74) with borderstyle 3D_BORDER - always render. Kept the multi-line toggles. |
| `bug-postfx-intensity-clobbered` | 'I dont feel like any of our effects are working now' (2nd occurrence) | Post-FX intensity cvars (r_ppBloomIntensity, r_ppSSAOIntensity, r_ppDoFIntensity...) are CVAR_ARCHIVE floats set by the in-game EFFECTS/POST-FX sliders. They got dragged down again (bloom 0.627, ssao 0.197, dof 0.384 vs good 1.3/1.0/0.6). E | Re-asserted known-good intensities in autoexec.cfg (execs after omconfig, so wins every launch): bloom 1.3/0.6, ssao 16/1.0/0.5, dof 0.6, exposure/contrast/saturation 1.0, lowhealth 0.5/1.0. |
| `bug-ps-home-var` | gl2 harness boots wrote homepath/logs to C:/Users/curry instead of G:/mohaa-gl2/home; verify greps found no log | Scripts assigned $home - PowerShell BUILT-IN automatic variable (user profile dir). Assignment does not override; fs_homepath silently became the profile dir. Boots stayed VALID (fs_basepath = isolated G: build carried all data/DLLs) but lo | Renamed to $ghome in single_boot/gl2_ab/gl2_probe; removed cfg litter from C:/Users/curry/maintt. RULE: never use $home or other PS automatic variable names. |
