<!-- GENERATED FILE - DO NOT EDIT.
     Produced by docs/tools/gen_public_pages.py from buglog.json, challenges.scr,
     the map scripts and the authored docs. Edits here are overwritten. -->

# Vehicles & rides

[<- back to all fixes](../BUGFIXES.md)

**45 fixes**, newest first.

| ID | Problem | Cause | Fix |
|---|---|---|---|
| `bug-2244` | user: 'When teh sherman gets blown up it kills the player and the model swap is extremely obvious when it gets destroyed... im also not sure if that sherman was actually shooting at the bunkers but it | coop_shermanShelled spawned retail models/fx/fx_tank_explosion.tik as a script_model. That TIKI's OWN init server block carries 'classname Explosion / radiusdamage 120 / meansofdeath explosion', and TIKI init server commands RUN even when t | New mod-owned models/fx/coop_tank_explosion.tik: byte-identical animations block, header rewritten without the Explosion classname, radiusdamage and meansofdeath. Retail's five uses of the original keep their damage. The swap now waits 0.25 |
| `bug-2183` | user (friend's screenshot, older build): m1l1 intro truck - the canvas walls and roof render as dense blue/green block noise. Everything else in the truck (benches, soldiers, lantern, crates) renders  | NOT DETERMINED from this end, but the obvious suspect is RULED OUT. Surface traced: models/vehicles/gmctruck_opening.tik surface `canopyin` -> shader `gmcinside` (Pak0 scripts/gmctruck.shader) -> textures/models/vehicles/gmc/gmcinside.tga.  | No code change. Current build verified clean, so the first move is to establish the reporter's version: gmcinside.dds only entered the mod on 2026-08-24, so <=v1.4.4 uses retail Pak2's 512 copy and >=v1.4.5 uses the mod's 1024 copy - differ |
| `bug-2073` | [user, live] 'the camera keeps going behind me instead of looking at me and the opening' - wall cover, third person. wallcover_plan_v1 §5.4/§5.5 designed the fix and NEITHER was ever implemented. | NOT A BUG IN THE CAMERA - THE FEATURE WAS NEVER BUILT. `grep coverShoulder cg_view.c` returned nothing: the third-person shoulder side came only from the archived preference cg_adsShoulderRight, so in cover the camera sat on whichever shoul | Implemented §5.4 as an OVERRIDE at the sign target only, never an assignment: cover proposes a shoulder, the player still disposes. Three constraints honoured, each with a bug behind it - (1) cg_adsShoulderRight is CVAR_ARCHIVE and is the p |
| `bug-1806` | User: the halftrack gunner on m3l3 needs to be way less accurate, it is too OP. | Two compounding defects. (1) The close-range branch sets `bulletdamage 5000` / `bulletspread 10 10` for any player within 1400 units, while the mount aims at group.head - a near-pinpoint headshot for roughly 7x a coop player's 750 max healt | Gated the 5000/10 close-range profile to `level.gametype == 0` so SP is byte-for-byte unchanged; coop gets bulletdamage 60 / bulletspread 65 at close range. Set coop's baseline spread to 75 (just inside SP-medium) instead of inheriting SP-h |
| `bug-1777` | text audit: two debug lines broadcast to every player on a loop, forever | thinktank.scr:307 sat inside a `while` with `wait 1` and printed 'thinkTank.scr - tank_stop_if_close' via iprintlnbold_noloc - a broadcast, once per second, the whole time the tank waited for a player within 450u. mg42_active.scr:299 did th | Both demoted to plain println. No extra gate needed: println is already developer-gated at three engine call sites (fgame/scriptthread.cpp:2858/:2869/:2883), so the information survives for debugging and no player sees it. |
| `bug-1584` | ^~^~^ Script Error : binary '/' applied to incompatible types 'float' and 'none' (gags/t2l1_tank.scr, 702) - 268 occurrences in one session, roughly 2 per second while the gag runs. | Wrong property name. The engine registers `max_health` (entity.cpp:1122/1131/1140); there is no `maxhealth` property, so `$player[1].maxhealth` reads an unset script var = 'none' and the division throws. The statement is skipped, so local.h | `$player[1].maxhealth` -> `$player[1].max_health`. NOT changed, deliberately: the three other `.maxhealth` sites in the same file (:338, :709, :716) reference level.friendly1/2 and self - same suspected defect, but they belong to a differen |
| `bug-1518` | User: want Grillo to have a bullet-hole wound in his chest and back when he dies, using the gore system - referenced the glider pilot (e2l1/gliderride.scr) as a working precedent | Grillos model (models/human/allied_oss_man_m1.tik) already declares gore-tier blood skins (blood1/blood2/blood3) on shirt/cullshirt (torso, front+back), pants, head and hand surfaces - added for the coop gore system but never triggered for  | m2l1.scr Grillo death handler now runs the same recipe as coop_bloodyGliderPilot: surface shirt/cullshirt +skin1, wait 0.5, then -skin1 +skin2 (heavy tier - soaked with bullet holes baked into the texture art), plus attachmodel coop_blooddr |
| `bug-1473` | 12690 script errors from global/vehicle_warning.scr lines 59/61/75/76/79/84/91: Cannot cast 'array' to listener, Cannot cast 'none' to vector, binary ops on 'none'. 48 percent of ALL script errors in  | ATTRIBUTION CORRECTED - originally logged against maps/t2l4.scr; it is not t2l4. Four call sites in gags/t3l1_enemyspawn.scr (TankLookForPlayer + TruckLookForPlayer) pass the bare $player token as local.target to the retail global CanSeeTar | FIXED 2026-08-06, deployed. gags/t3l1_enemyspawn.scr: both TankLookForPlayer and TruckLookForPlayer now resolve a single real player via coop_mod/replace.scr::player_closestTo self (the established project idiom, precedent gags/t2l4_captain |
| `bug-1437` | Ride audit: m1l3b +use turret takeover seats a still-glued passenger (dual position masters, leaked seat) | coop_playerForceToJeep never released the taker's passenger seat, leaving glue+turret both mastering the player and the seat marked occupied - 8-player seat exhaustion reachable. | waitthread coop_freePassengerSeat local.player added before seating - the same pre-seat cleanse coop_playerSpawned already performs. |
| `bug-1434` | Ride audit: t3l2 T-34 re-seat dead code twice over (wrong callback + reads self) | The 08-02 re-seat body sat only under coop_playerJustSpawned (deaths dispatch JustRespawned) and read the player from self, which in map callbacks is the dispatch trigger, not the player - the guard '!isAlive self' ended unconditionally. | Shared coop_t3l2_seatRider local.player routine wired to BOTH coop_playerJustSpawned and coop_playerJustRespawned with the positional-parameter signature the e3l1 verification proved correct. |
| `bug-1433` | Ride audit: e2l2 escape jeep - gunner disconnect mid-ride script-errors the progression thread at the blockade (blockade softlock; bug-1220 error family) | level.jeepAttachedPlayer is assigned once at mount and dereferenced unguarded at four sites; a disconnect NULLs it and the first deref kills the getout/jeepnewpath thread before the group glue, weapon handout and airfield objective run, wit | All four derefs guarded (NIL and NULL); ride cosmetics skip on a gone gunner but the thread now always reaches the airfield group glue. Full re-seat/promotion handler queued as Batch B. |
| `bug-1432` | Ride audit: e3l1 death during the first 1.5s of the medic ride still respawned frozen at start | level.playerInJeep=1 was set after the 'wait 1.5' in StartPlayerMedicRide, so the bug-1429 re-seat handler was gated off during the mount window. | Flag raised immediately after attachturretslot; the delayed assignment removed. |
| `bug-1431` | Ride audit: e3l1 medic ride ends with the ENTIRE server frozen at the courtyard (physics_off regime re-armed at ride end, never cleared) | StopSecondChaseTruck resets health scale via e3l1.scr::SetPlayerHealthScale 1.0, which unconditionally threads replace.scr::physics_off; player.scr re-freezes flagged players per frame and the map contains no release after the ride. bug-142 | WaitToGetOutOfJeep now runs exec replace.scr::physics_on after the dismount (t2l1/t2l3 release recipe); SetPlayerHealthScale only threads physics_off when scale != 1.0; dismount doUse guarded to a LIVING gunner; CleanupJeepRidePart3 repoint |
| `bug-1373` | 147x per level: "^~^~^ Script Error : IsTouching used with a NULL entity." at global/autoplane.scr:897 - 'while ( self && !(self istouching self.strafeofftrigger) )'. | stukafireend guards strafeofftrigger on ENTRY only (line 896), then spins in a while loop. Meanwhile the plane's run-completion path (lines 869-872) does 'self.strafeofftrigger remove' followed by '= NULL' from another thread. Once removed, | Shipped a mod override of the retail global/autoplane.scr whose ONLY change is re-testing the trigger inside the loop condition. Kept on one line - a leading && on a continuation line is a whole-file parse killer (bug-750). Note the overrid |
| `bug-1348` | user: 'they immediately spot me during the glider scene... i didnt do anything and they came up and shot me' | HandleSearchParty ran on its own clock from the crash while the player was still frozen hanging; its 4 lines + the GetPlayer hand-off had already fired by the time the player had control. Retail masked this by aborting the scene on eject (b | the scene now waits for level.coop_gliderPlayerLanded (30s cap) + 1.5s before starting its runto/dialogue, so it plays in front of a player who can see it |
| `bug-1347` | user: 'the drop puts you into DBNO' after the glider eject | the vanilla seat height (592) is ~192u above the wreck; physics_on dropped the player that far with no protection, and coop fall damage was enough to trigger DBNO | nodamage before physics_on + coop_ejectDamageRestore re-arms on groundentity (5s cap) - the same window coop_chuteDrop already used |
| `bug-1344` | user: 'isn't there a script where the italians walk around and have dialogue and dont notice you?' - there is, and nobody ever saw it | retail aborts the search-party walk-in + 4-line Italian exchange the INSTANT the player ejects (USE press) - since the eject prompt begs to be pressed, the scene died unseen. Additionally all four den_E2L1_ITALIAN mp3s are LOOSE-ONLY retail | eject no longer aborts; the scene ends via its own GetPlayer after line 4, or instantly when a player DAMAGES a search-party member (coop_searchPartyShotWatch - keeps disable_ai actors from being unreactive free kills). The 4 mp3s copied in |
| `bug-1341` | user (2nd report): windscreen blood still lasts only ~1s despite the bug-1338 base-anim fix | the SHOT anim replays its whole crack progression on every flak burst; its 2.1s step '-skin1 +skin2' forces cracks-only, wiping the script-applied blood regardless of when it lands. bug-1338 removed the base-anim wiper but not this one | coop_glassBloodKeeper threaded at blood application: re-asserts +skin1+skin2 every 0.5s until gliderCrashOver (+1 final assert). Any wipe is visible <=0.5s. Chose the keeper over editing the shot anim's 2.1s step - that step is load-bearing |
| `bug-1338` | user: bloodied windscreen appears for a moment then disappears | emitter_off (the glider's BASE anim) carried 'entry surface glider_body_glass +skin1 / -skin2' - every return to the base anim re-asserted cracks001 and cleared skin2, wiping the script-applied blood (skin1+skin2). User correctly guessed 'd | removed the base anim's two glass entry commands. Skin bits persist across anim changes; the shot anim owns the crack progression (208-210), the script owns the blood (gliderride:1561), bank anims' +skin2 is blood-compatible (preserves skin |
| `bug-1332` | user (3rd report): glider pilot's face is perfectly clean - no wound despite both-temple texture verified in the shipped pk3 | head1.skd contains TWO surfaces BOTH NAMED 'head' (LOD pair). Entity::SurfaceCommand resolves an exact name via gi.Surface_NameToNum which returns only the FIRST index, so 'surface head +skin2' flipped the non-rendered LOD and the visible f | 'surface "head*" ...' - the trailing * is retail SurfaceCommand prefix-match mode (entity.cpp:4237, mult=true) and applies to every matching surface. TRAP: any scripted surface command against a HEADS skelmodel must use the head* form |
| `bug-1328` | user: 'in coop you seem to spawn way close to the ground' after the glider crash | retail never moves the player: the body waits out the whole camera-flown ride at the map's only info_player_start (4494 6086 592) yaw 270 - seat height, ~192u above the static wreck origin (4462 5960 400) - then physics_on after USE settles | coop_handlePlayerGliderEject now sets the GLIDER player (only) to the vanilla origin/viewangles before the freeze, same recipe as coop_chuteDrop; chute players keep their user-scouted drop points |
| `bug-1328b` | user: 'I spawn close to the ground, then respawn again higher up shortly after' (glider player, post-crash) | the bug-1328 vanilla-origin set lived inside coop_handlePlayerGliderEject, threaded from the crash frame - camera-release ordering could show the ground-level body for a beat before the teleport | origin+viewangles now ALSO set inline in the crash-frame loop at glider-player selection, before any camera release; the handler's own set is idempotent |
| `bug-1328c` | glider spawn still popped ground->up despite the crash-frame origin set (bug-1328b) | any crash-time origin set races the camera release; vanilla avoids the class of bug entirely by having the body stand at info_player_start physics-off for the ENTIRE ride | coop_preplaceGliderPlayer threaded from ride init: pins the first active player at (4494 6086 592) until gliderCrashOver. USER-VERIFIED working. Immediate regression bug-1328d: the 0.5s re-pin also re-stuffed viewangles, locking the camera  |
| `bug-1322` | user: 'add a short period of dizziness after the glider crashes and you are hanging.. maybe 10 secs, as high as if damaged' | feature request - no dizziness existed on the hang | coop_crashDizzy threaded from BOTH hang paths (glider eject + every chute arrival incl. late joins): 0.3s lead-in (must land AFTER the same-frame coop_setSuppressionAll 0 release or it would be cleared), floor 0.9 for 10s (a damaging hit sp |
| `bug-1309` | FEATURE (user spec): e2l1 chute spawns - other players should spawn after the glider crash at scouted points and hit USE to cut their chute. | Post-crash arrival gap from OPEN.md: only players present at crash time got the eject beat, and nothing assigned a crash-site spawn - late joiners/respawns popped in at map defaults. User scouted 4 viewpos drop points above the crash site ( | coop_chuteSpawnInit (4 drop points + yaws, threaded from StartRide), coop_chuteDrop (teleport to next point round-robin, viewangles set via the lobby.scr:379 idiom - EV_SetViewangles verified as a wired EV_SETTER - physics_off freeze, parac |
| `bug-1308` | e2l1 totally broken: black screen on spawn, no glider sequence, spawned on the ground, r_globalFogForward 0 did nothing. Log: parse error, unexpected TOKEN_PLUS at gliderride.scr:1308 -> whole file fa | NEW PARSE KILLER for the checklist: a bare +token/-token as a command argument. 'self surface Sc_Al_US_Inf_Tunic +skin1' (bug-1305 blood code, written from the TIKI frame-command syntax where bare +skin1 is correct) - but the SCRIPT tokeniz | Quote the tokens, matching the mod's own helmet.scr and retail e2l2/truck_damage.scr: self surface X "+skin1" / "-skin1" "+skin2". All 6 lines in coop_bloodyGliderDude. Swept every other file touched today - clean. |
| `bug-1307` | e2l1: Pak/AA guns do not destroy vehicles (opeltruck, tanks) - user report. | FOUR separate causes, no engine defect. (C1 CONFIRMED, fully traced) FinalBattle.scr TankGo: 'self immune aagun' - retail protection against its own AI Flakvierlings; aagun1/3/5 fire P_aagun_cannon (MOD_AAGUN, blocked) while aagun2/4/6 fire | C1: immunity now gametype 0 only (coop players man the guns; guns 1-5 are destroyed before the tank battle so no AI MOD_AAGUN risk). C2: takedamage timeout failsafe thread - deliberately NO 'endon drive': Unregister drains the end-list FIRS |
| `bug-1303` | e2l1 glider: no smoke while the glider is being shot up. User: 'it's a black smoke I know vanilla had'. | NOT a gl2 regression - user A/B confirmed neither gl1 nor gl2 showed it. The glider's only stock emitters are cloudy / cloudy_side / cloudy_descent (CG4Aglider.tik:98-163), which are pale grey cloud wisps for the tow phase (color 0.4 grey,  | Added a damage_smoke emitter to the coop CG4Aglider.tik override reusing the cut kit's puffyStuff_dark.spr. REVERTED at user request after two passes and REMOVED from the shipped pk3. Pass 1 mistakenly took scalemin/scalemax 6/11 and spawnr |
| `bug-1302` | e2l1 glider: the pilot says 'We are clear, Two-Four' twice in a row. | Regression introduced by the bug-1297 repair. That fix replaced the nonexistent AA_pilot_talk4 with AA_pilot_talk3 to stop the thread aborting - but every AA_pilot_talk* animation carries its OWN voice line as an embedded TIKI frame command | Replaced the duplicate 'upperanim AA_pilot_talk3 / waittill upperanimdone' with a plain 'wait 3'. No repeated line, descent still starts on the original cadence. Re-enabling the GP1306 alias was considered and REJECTED - verified the mp3 do |
| `bug-1301` | e2l1 glider ride: cracked windscreen never appears and flak/explosion FX never seen. Log: "^~^~^ Event 'glider_body_glass' does not exist." x2 + "Failed execution of event for class 'ScriptModel'". | TWO independent retail-content defects, neither fog- nor renderer-related (an entire session was mis-spent on the renderer first). (1) CG4Aglider.tik lines 332 and 398, inside the server{} frame-command blocks of bank_left/bank_right, are b | (1) Coop override of CG4Aglider.tik: byte-exact copy with 'entry ' prefixed to lines 332/398 ONLY, CRLF preserved, validated by positive assertion. (2) gliderride.scr: replaced the DoGliderFlak call with new coop_gliderFlakVolley + coop_fla |
| `bug-1277` | m1l3c: the final Opel truck arrives after the lighthouse with no enemies in it | The bug-1030 guard added to truck_load on 2026-07-22 went too far. It was written to stop 'spawn NIL' crashes for addon vehicles that supply no crew model, and it did that by ending the thread outright: if(guy_type == NIL \|\| guy_type == ' | Default rather than skip, matching retail. Still crash-safe for bug-1030's case, because enemy_truck_think only calls truck_load when passengers > 0 - reaching truck_load at all means the caller genuinely wants a crew; a vehicle that wants  |
| `bug-1257` | T3l2 enemy tanks and flaks never react and are invulnerable | gags/T3L2_KingTiger.scr line 47 (retail, never extracted into the mod) does level.playertanktarget = $player. $player is an ARRAY in coop, and it clobbered the good single-entity marker built by global/vehicles_thinkers.scr players_tank 58  | Extracted T3L2_KingTiger.scr and T3L2_BattleTanks.Scr from retail pak1.pk3 into the mod and changed the array reads to $player[1], matching the already-fixed sibling gags t2l1_tank.scr:50 and t2l4_start.scr:1372. Also added the missing else |
| `bug-1173` | m1l1 truck-bed allies (guy01/guy2/guy3) render upside-down; entityNumber=1023 fix (bug-1167) did not resolve it on live retest | MatrixToEulerAngles (qcommon/q_math.c) has a gimbal-lock branch (cos(pitch) within ~0.06 degrees of 0) that discards roll entirely (forces it to 0) and uses a different yaw formula. The 3 passenger tags (guy01/guy2/guy3 origin, on the dedic | Added `local.a[2] = local.a[2] + 180` (roll correction) immediately after each of the 3 passenger gettagangles calls, before assignment to .angles - matching the correction pattern already used elsewhere in this exact file for other tags on |
| `bug-1129` | m3l2 farm courtyard dirt looks horrible: crunchy worm-like oversharpening artifacts + harsh dark parallel rut-bands (user screenshot shot0245.tga, gl1, 3440x1440) | Our Real-ESRGAN x4 upscale hallucinated crosshatch worm noise and crushed the dark wheel-rut bands on m3l3grass_bocroad_new. Under BT profile TAnatural.shader (mainta+maintt pak1) remaps shader m3l3grass_bocroad -> image m3l3grass_bocroad_n | Rebuilt m3l3grass_bocroad_new from stock 512 (maintt pak1) with 2x Lanczos + mild unsharp (no ESRGAN), DXT1 full mip chain via _hd_staging/_work/hd_dds_build.py write_dds; packed as _research/hdfix/zzzzzzzz_hd_groundfix.pk3 (8 z sorts after |
| `bug-1052` | [COMPREHENSIVE] none-cast storm LAYER (next after $player) across 7 files, ~17100 errors, surfaced by full 4-player sweep | Sequential-storm: fixing the $player array-casts let maps progress far enough to run weather/shutter loops whose timing globals (thundertime/windtime/shuttertime/rainvolume) are NIL in coop (vanilla inits bypassed - game.* persistent global | 5-agent workflow. shutter.scr retail override + windtime=5/shuttertime=0.3 NIL-guards; weather.scr thundertime=20/rainvolume=-1 NIL-guards; vehicles_thinkers playertanktarget ==0\|\|NIL\|\|NULL skip + enemytankspeed=200; itemhandler skip am |
| `bug-1051` | Cannot cast 'none' to <type> storms surfaced by 4-player sweep after $player fix: weather.scr:378 (8662x), shutter.scr:87/91/232 (8236x), + ~140 minor across vehicles_thinkers/itemhandler/moveto/entit | NEXT STORM LAYER (sequential-storm effect): once the $player array-casts were fixed, maps progressed far enough to run weather/shutter loops whose timing GLOBALS (level.thundertime, level.windtime, level.shuttertime, level.rainvolume) are N | 5-agent workflow (wf_nonecast_fix.js). shutter.scr: created retail override (main/Pak0.pk3) + NIL-guards restoring documented defaults windtime=5/shuttertime=0.3 at loop entry. weather.scr: NIL-guards thundertime=20, rainvolume=-1. vehicles |
| `bug-1050` | vehicle_warning.scr reported 'fixed:0' in $player batch despite 4270-hit attribution - suspected miss | MISATTRIBUTION, not a miss. The sweep attributes an array-cast to the line where .origin is dereferenced (vehicle_warning.scr:59/75 'local.target.origin'), but the bug is in the CALLER passing bare $player. Authoritative pak-wide scan: only | No change to vehicle_warning.scr. Confirmed both callers fixed. Lesson: array-cast error line != root cause line; trace to the caller. |
| `bug-941` | Paratroopers STILL killed by friendly tank run-overs after the crush-immunity deploy | Wrong damage type: engine vehicle run-overs deal MOD_VEHICLE (vehicle.cpp:5279/7188/7208, health*2 collision hits), NOT MOD_CRUSH. The immune-crush line never applied to the actual kill path. | Added `local.actor immune vehicle` (kept crush for movers/doors). MOD name string verified in g_utils.cpp table. Deployed with the mine-wall batch. LESSON: verify the actual MOD at the damage CALL SITE before immunizing - MOD names are gues |
| `bug-541` | m5l1b tank disappears with no wreck when destroyed early; allies wait forever | tankdeath (wreck spawn) only armed inside tankdrive, gated behind scripted tankstart; killing the tank early = no handler -> Vehicle vanishes, tankdead never set -> allies stuck. | Arm `$tank thread tankdeath` at level start with a self-guard against double-spawn. |
| `bug-257` | DBNO crawl overlay silently missing: dbno.scr testplayeranim 'walk_injured_forward' was a no-op (alias never loaded on player models) | The walk_injured aliases lived in anims_injured.txt whose include (via an include.txt override) was .bak-disabled at some point; player models never aliased walk_injured_*, and testplayeranim on a missing alias fails silently. Found by the  | New append-only override of models/player/base/anims_shared.txt (wins VFS over maintt/pak1; 435/435 vanilla lines preserved in order) adding the 5 walk_injured_* aliases. The old .bak include.txt route stays dead (it replaced servertype bra |
| `bug-240` | e3l3: plunger/detonator invisible on the hill; truck convoy never spawns/drives (user playtest) | Two more unimplemented-BT-class casualties: (1) $dasdetonator is a trigger_use with a pulse_detonator model - OpenMOHAA marks ALL triggers SVF_NOCLIENT (never sent to clients) so the model can never render; (2) the five trucks (dastruck_a/b | (1) companion script_model with the pulse_detonator model at the trigger's exact origin (swapped to detonator.tik on plant, matching the trigger's own model swap); (2) coop_spawnOpel helper respawns each truck as native script_vehicle (driv |
| `bug-071` | t2l2 vehicle ride: truck (and AI riders) 'steps forward in little jumps' while a player is glued aboard; only after death/respawn; noclip cures it; player's own view feels smooth | Walking view-bob. A glued rider has PMF_NO_MOVE and inherits the vehicle's velocity (~254 u/s), and groundEntityNum=vehicle sets ps.walking=true, so the stock view-bob (amplitude = VectorLength(velocity)) bobs the camera at vehicle speed. T | In cg_view.c view calc, gate the walking view-bob on !(cg.predicted_player_state.pm_flags & PMF_NO_MOVE) so glued riders (who can't self-move) get no footstep bob; the bob decays to zero via the existing else branch. Diagnosed via layered p |
| `bug-057` | t2l2 truck still won't take off + player+truck both blow up in a truck seat (opelcollision proxy fix failed) | Real cause: truck_INIT (vehicles_thinkers.scr:705) does `self takedamage` + `self solid`, re-solidifying the truck every init and overriding coop's notsolid. A SOLID glued bed passenger then MUTUALLY CRUSHES the solid truck (log: player 'Un | coopTruckKeepNonSolid thread forces `level.truck notsolid` every frame (wins vs truck_INIT), matching the halftrack. Truck becomes an invincible ride: no passenger crush, no block, no explode, drives fine. Trade-off: truck can no longer be  |
| `bug-033` | Missing error handling in unknown | Code path had no error handling ÃƒÂƒÃ‚Â¢ÃƒÂ¢Ã‚Â‚Ã‚Â¬ÃƒÂ¢Ã‚Â€Ã‚Â exceptions would propagate uncaught | Added try/catch block |
| `bug-032` | Missing error handling in unknown | Code path had no error handling ÃƒÂƒÃ‚Â¢ÃƒÂ¢Ã‚Â‚Ã‚Â¬ÃƒÂ¢Ã‚Â€Ã‚Â exceptions would propagate uncaught | Added try/catch block |
