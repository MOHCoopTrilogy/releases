# HANDOFF — Omaha (m3l1a) work in progress, 2026-09-05

Paste the block at the bottom into the new session. Everything above is context that block refers to.

---

## STATE: everything below is BUILT, DEPLOYED and SMOKE-TESTED

Deployed to `G:\mohaa-gl2\` (the live install) and the GOG root. Dedicated server boots m3l1a with
**0 script errors**. Binaries: `openmohaa.exe`, `omohaaded.exe`, `game.dll`, `cgame.dll`, both renderers.

| # | What | Where |
|---|---|---|
| 2449 | **Captain's bangalore dialogue was inaudible.** `S_FLAG_DO_CALLBACK` was `0x400` — bit 10 — and it is OR'd *into* an entity number then stripped unconditionally client-side. `GENTITYNUM_BITS` is 12, so every sound from entity 1024–2047 was bound to entity *n−1024*. Captain = 1694 → 670, a corpse across the map. **Protocol change**: flag → `0x1000`, wire field → `SOUND_ENTNUM_BITS`, two `#error` guards. exe + omohaaded + game.dll | `q_shared.h`, `msg.cpp` |
| 2450 | **`gun "none"` had been a no-op since 08-19.** Radioman kept a rifle while working the radio. Restored; cvar `coop_gunNoneStrips`. Trilogy-wide: 54 sites | `actor.cpp` |
| 2451 | **"Go go go" second voice could never fire** — gated on `.isbusy != 1` checked 2–3 s after `ai_add_todo` latches it. Speakers now picked at T+0 by distance | `coopified.scr` |
| 2452 | Water-impact cue: `auto` put it on the 3D path where **`set_3d()` discards the alias volume**. Back to `local`, emitted from the boat, hoisted onto the contact frame | `coop_audio.scr` |
| 2453 | **`git apply` + `core.autocrlf=true` silently rewrote 4 LF files to CRLF.** Recorded in TRAPS T2 | tooling |
| 2454/2432/2465 | **Every `playlocalsound <alias> 1` in the mod rendered at gain 0 since 2023.** `loopSoundMinDist` hardcoded 0; the mixer guards only `< 0`; volume law is `minDist² × v / d²` = 0. Fixed by making `PlayLocalSound` read the alias like `Entity::LoopSound`. **This also fixed the Omaha underwater heartbeat, which I had wrongly reported fixed earlier** | `player.cpp` |
| 2457 | Sound-pool gauge: `^~^~^ SNDPOOL 3d=n/96 peak= …`. Needed because the old "Couldn't play" line is `Com_DPrintf` and `developer 0` ships | `snd_openal_new.cpp` |
| 2458 | **Muzzle droop never fired** — it sat inside `if (s_fAdsPose > 0.001f)` while its angle is scaled by `(1 − AdsPoseFactor)`. Exact complements. Moved outside | `cg_modelanim.c` |
| 2459 | **Rotational weapon lag** — the spring was translation-only (drag); now also rotates at the grip (mass on a lever). `cg_weaponLagRot 0.6`, `cg_weaponLagRotMax 2.5` | `cg_view.c`, `cg_modelanim.c` |
| 2460 | **`+coopnade` was host-only since it shipped.** `+`/`-` commands never forward; the host only worked via the local-server shortcut. cgame relay added. **TRAPS T22 was wrong and is corrected** | `cg_consolecmds.c` |
| 2461 | Dry-fire clicks: 31 of 33 were *registered but uncached* off-list (the `snd_ping` defect). All 33 re-declared `always` + trilogy-wide maps, derived by sweep | `gen_coop_vo_override.py` |
| 2462/2463 | **Inspect shoulder clipping.** There is no torso in the fps model — 4 surfaces only; it was the right hand/forearm. Body roll capped ~12°, remainder moved to the barrel axis at the grip. Also found a **one-frame 38–52° whip at the end of every inspect** (`sgn` forced to +1 on expiry) | `cg_view.c`, `cg_modelanim.c` |
| 2464 | **Quick sidearm draw.** 0.250 s vs 0.767 s cheapest manual swap (3.07×) and 2.0 s Garand reload (8×). Primary parks in `WEAPON_OFFHAND`, pistol takes `WEAPON_MAIN`. Reload **interrupt** (user chose this over forced manual reloads). Foley on 4 beats. Bind **H**. 9 gate passes | many |
| 2466 | Engineers fired the wrong way — spawned at `((randomint 4) * 90)` then put in a **firing** pose. Now yaw 90 | `coopified.scr` |
| 2467 | Engineer with a pistol = **retail's `$left_engineer`**, whose `holster` doesn't stick. NOT a regression of 2450 (tested at both cvar settings). `coop_holsterFix` + probe `^~^~^ ENGHOLSTER n=` | `coopified.scr` |
| 2468/2469 | **Men standing at the wire.** `coop_vigSpawn` gives every vignette actor `nodamage` and restores it only for armed men on a coin flip, so the charge's kill was *refused* and survivors parked in `stand.scr`. Control group: `coop_beachCharge` men run the same code, spawned directly, and die. **My "42 standing" figure was wrong** — joined on `pos=`, which is the constructor default | `coopified.scr` |
| 2470 | **Higgins look moved underwater** and now **threads the sink itself** — the sink previously fired 9 s *after* the look (`RAMPLOOK` 12:48:50, `HIGGINSSINK` 12:48:59), so heel was 0.0° every time. Now 12.0° → 24.2°. The obstruction was never the seabed seam: it is `$ocean_wavy`, submodel `*84`, **opaque from below** | `coopified.scr` |
| 2471 | Men crouching underwater. Guard `feet_z + poseTop >= waterZ + 2`, both **measured** — water is piecewise, and `crouch_beach_idle` is **35 tall, not 48**. 11 call sites by funnel | `coopified.scr` |
| 2472 | **My own same-day regression**: the 2471 sweeper stood up retail actors mid-sequence (no `isinsequence`/`isbusy` exemption), breaking the captain's dialogue *and* the advance. Fixed | `coopified.scr` |

---

## SIX WORKFLOWS WERE IN FLIGHT WHEN THIS PAUSED

They write **fixer scripts + reports** into scratch dirs; none of them edit the repo. Several had
already written files. **Check each directory, read the `.md`, run the fixer with `--check`/dry-run
first, verify anchors against current bytes, then apply → build → deploy.**

Scratch root: `C:\Users\curry\AppData\Local\Temp\claude\C--mohaa-coop-dev\bc1e9658-c17a-4e35-93e3-e7cdf44d0f08\scratchpad\`

| dir | job | state at pause |
|---|---|---|
| `cinegaps\` | 5 items: wading men not visibly shot, no dead bodies on the seabed, **no bullet ricochets on the Higgins hull**, several underwater Higgins boats (intentional?), **water splashing against beach objects** | PATCH_* files written |
| `qdrawvis\` | **Parked primary is invisible** — must be visibly held in the LEFT hand, down and left of frame, while the pistol fires in the right. Plus **bazooka firing anim goes flat/underground** | patch + evidence written |
| `flankmg\` | **Flank MG42s never fire.** They reveal (`FLANKMG revealed left=2 right=2`) and get crewed (×4), which makes the script driver stand down — and the crews acquire no enemy in **523 of 558 samples** | fixer + diff written |
| `water\` | **Research only** — how to make ocean/waves/shore/underwater more realistic. Report the findings to the user; do not build without asking | lane A measured |
| `radioman2\` | Radioman goes **silent**, is shot in the head on approach; **the player** transmits `036h` from the set; wire `dfr_M3L1_300f` "Just get up the beach!" | just started |
| `sprintpose\` | Sprint viewmodel randomly picks between the current lowered carry and a **raised one-handed carry**. Licensed to say no if procedural won't read convincingly | not started |

---

## WAITING ON THE USER

- **Two wavs**: `coop_uw_airout`, `coop_uw_wake` (out-of-breath). Armed no-ops until they exist. 22050 mono 16-bit into `sound/coop_tinnitus/` — that path is duck-exempt, anywhere else is inaudible.
- **The trench grenade**: an ally says *"I'll toss some grenades in"*; nothing throws one. Build it or leave it?
- **A 10-second A/B**: does the concussion muffle do anything? Only headers are vendored, not the mixer.
- **`trench_ranger`** (ent 686) still stands at the wire — retail's, never animates in 47 samples. Left alone deliberately.

## DELIBERATELY NOT FIXED

- **Beach damage is sequential**, so time-to-death is ~52 s solo vs **~189 s with four players**. Dominant coop balance term; wants its own playtest.
- **Coop bazooka team can throw a live rocket** (`bazooka_shoot` has `server { first fire }`; enemyless actors get a target 2048 forward). Probe shipped, not fixed.
- **`docs/TRAPS.md` is 1 KB over its 70 KB ceiling.** Everything left is load-bearing — raise it or archive an older trap.
- **`docs/02-status-ledger.md:86`** still says gl2 is "PAUSED / sandbox-only". Stale; already caused one wrong fix.
- **Bodycam**: DoF focus *pull* instead of snap — recommended, not built.

## PROBES TO CHECK ON THE NEXT PLAYTEST

`^~^~^ STANDCOUNT atWire=` (want 0, was 4) · `^~^~^ ENGHOLSTER n=` (climbing ⇒ move to game.dll) ·
`^~^~^ SNDPOOL peak=` (vs 96) · `^~^~^ UWLOOK` · `^~^~^ WETPOSE` · `^~^~^ QDRAW`

---

# PASTE THIS INTO THE NEW SESSION

```
Continuing MOH Trilogy Coop mod work at C:\mohaa-coop-dev (Omaha Beach, m3l1a).

READ FIRST, in this order:
  docs/HANDOFF-2026-09-05.md   <- full state, in-flight work, and the traps below
  .wolf/OPENWOLF.md and CLAUDE.md
  docs/TRAPS.md
  .wolf/buglog.json entries bug-2449 .. bug-2472 (the last two days of work)

Everything through bug-2472 is built, deployed to G:\mohaa-gl2, and boots clean.

SIX pieces of work were in flight. Each wrote a fixer script and a report into a scratch dir but
NONE of them touched the repo. For each: read the .md, dry-run the fixer, RE-VERIFY EVERY ANCHOR
against current bytes (these files moved many times), then apply -> .\build.ps1 -> smoke test.
Scratch root:
C:\Users\curry\AppData\Local\Temp\claude\C--mohaa-coop-dev\bc1e9658-c17a-4e35-93e3-e7cdf44d0f08\scratchpad\

  cinegaps\    - no ricochets on the Higgins hull, no bodies on the seabed, wading men not visibly
                 shot, possible duplicate underwater Higgins boats, and water splashing against
                 beach objects (a user feature request)
  qdrawvis\    - the quick-draw parked primary is INVISIBLE. It must be visibly held in the LEFT
                 hand, down and to the left of frame, while the pistol fires in the right. Plus
                 bazooka firing animations going flat/underground
  flankmg\     - the flank MG42s never fire: they crew up, which makes the script driver stand
                 down, and the crews acquire no enemy in 523 of 558 samples
  water\       - RESEARCH ONLY on making ocean/waves/shore/underwater more realistic. Report to the
                 user, do not build without asking
  radioman2\   - radioman goes silent and is shot in the head on approach; the PLAYER transmits
                 "Shore party. Gap teams..." from the set; wire "Just get up the beach!" (300f)
  sprintpose\  - sprint viewmodel randomly alternates between the current lowered carry and a
                 raised one-handed carry. It is licensed to REFUSE if procedural will not convince

HOW THIS PROJECT EXPECTS YOU TO WORK — these are not style notes, each one cost a day:

1. VERIFY, DO NOT TRUST. Where a comment, a doc or a bug entry disagrees with the code, the code
   wins. Four separate times in the last two days a confident note in this tree was simply wrong -
   including ones I wrote hours earlier. Read the function, do not quote the comment above it.
2. "WIRED, PLAUSIBLE, AND DOING NOTHING" (TRAPS T23) is this project's most repeated failure. Before
   designing a beat, check the existing one actually RUNS -> PLACES -> IS IN FRAME -> IS AUDIBLE/
   VISIBLE, each with evidence. Several features shipped three times before anyone noticed they had
   never once executed.
3. MEASURE, THEN CLAIM. Three of my figures were wrong because I measured the wrong field: `pos=` is
   a constructor default, not a posture; a mono downmix under-reads narrow audio by ~3.5 LU; "not
   referenced by this map" is not "unused" when a sibling map speaks it.
4. LINE ENDINGS ARE PER FILE AND MIXED. Measure with a byte count every time. cg_view.c is LF,
   cg_modelanim.c is CRLF, cg_local.h is MIXED. `git apply` with core.autocrlf=true WILL rewrite a
   whole file silently - express .scr and C patches as Python FIND/REPLACE fixers opened with
   newline='', never as diffs.
5. .scr PARSE KILLERS take down the WHOLE file silently and the map then runs with no script: a
   non-ASCII byte, a BOM, an em-dash, a bare negative in an argument slot, an empty RHS, an
   unparenthesised println concat, a brace/label mismatch. Run docs/tools/depthscan2.py after
   every edit.
6. ADVERSARIAL VERIFICATION EARNS ITS COST HERE. Ultracode is on; use the Workflow tool. Gates
   caught, among others: a change that would have made every player's rifle vanish during Kar98
   reloads, a free-magazine exploit, and a stale-notetrack bug that silently refilled the sidearm.
7. TELL THE USER WHEN YOU WERE WRONG, unprompted and plainly. Several corrections above are mine.
   He would rather have the correction than the tidy story.

The user is playtesting and reporting as he goes. Ask him to close the game before deploying -
build.ps1 aborts while openmohaa.exe is running.
```
