# HZM Coop Conversion — Master Plan (consolidated)
Owner: this session drives BUILD + TEST; sub-agents deployed for FIXES. Created 2026-06-23 night.
Security: only edit under C:\mohaa-coop-dev. NEVER touch GOG launcher files; only overwrite G:\GOG\...\openmohaa.exe when the game is DOWN (standard deploy). NEVER remove campaign content (animations etc.) — raise limits / add coop-safe paths instead.

## 0. Where we are
- Phase-2 crash-test pass: maps 1–36 (all m-series + e1l1/e1l2/e1l3) toured CLEAN, zero crashes, after 4 engine fixes:
  1. TIKI order[]/temp_aliases[] sized MAX_TIKI_LOAD_ANIMS; 2. loadsurfaces[]+headmodels/headskins/AddChannel/fAnimWeights guards; 3. motionblend waitframe; 4. alias.c Alias_ListFindNode convalias[40] bounds guard (the dog-model m3l1a crash). MAX_POLYS 16384, MAX_MSGLEN 131072.
- FRONTIER: e1l4 onward = partially-converted maps that don't fully work in coop. e1l4 sits in a SERVER-RESPAWN loop -> MP team-select instead of coop auto-join (root-cause agent ae224b242 running -> e1l4_respawn_loop_rootcause.md).
- SCOPE CORRECTION (verified): ALL targets (e3l1-e3l4, t1l1-t3l2) already have coop_mod/main.scr::main. They are PARTIALLY converted; the work is per-element 16-player scaling + bug fixes, NOT initial hookup.

## 1. Research reports (the knowledge base) — all under C:\mohaa-coop-dev
- coop_vanilla_vs_hzm_diff.md — the canonical 10-step "coop-enable a vanilla map" recipe + replace.scr shim table + per-map callback matrix (Agent A).
- hzm_coop_framework_guide.md — coop_mod/*.scr reference, init call-order, MANDATORY vs OPTIONAL hook contract, the callback-spam fix (§5) (Agent B).
- coop_16player_scripted_events_plan.md — per-map scripted-event + objective inventory, 16-player risks, reusable helpers (Agent D).
- officer_coverage_audit.md — 3 buckets; 5 maps warrant an officer + candidate anchors (m3l3, m5l2a, m6l1b, m1l3a, e2l3).
- coop_menu_additions.md — menu = ui/coop_start.urc + ui/coop_start/*.cfg + ui/coop_maps.inc; t3 added; t-series need a Spearhead launch profile.
- e1l4_respawn_loop_rootcause.md — (pending) e1l4 server-respawn/team-select root cause + fix.
- pak_target_mapping.md (memory) — e-series=Breakthrough/maintt (current profile); t-series=Spearhead/mainta (NEEDS com_target_game=1 profile).

## 2. Canonical conversion recipe (Agent A) — minimal diff to coop-enable a map
1. Wrap body in `main:{ ... }end`. 2. First lines: `level.coop_aaMap=1` (m-series) / set `level.script="maps/<map>.scr"` (e/t), `level.coop_disableSpawnWarper=game.true` on fixed/ride intros, then `waitthread coop_mod/main.scr::main` (THE injection point — before any wait/entity access). 3. Spawn gate `level waittill spawn` -> `waitthread coop_mod/replace.scr::waitForPlayer` (keep `level waittill prespawn`). 4. Swap SP-only `$player`/client commands for coop_mod/replace.scr shims (39 catalogued). 5. Bare `$player` -> `for(...$player.size...)` loops. 6. Wrap player-activated BSP triggers with validateTriggerActivator / set level.coop_playerTank for vehicles. 7. Define only the per-player callbacks you need. 8. Coop spawnlocations update + ride coop_setup guarded by `if(level.gametype!=0)`. 9. AA precache adds `exec coop_mod/precache.scr` (e/t gap). 10. Don't touch missioncomplete at map level (centralized in global/missioncomplete.scr).
- Real callback names: coop_playerJust{Spawned,Respawned,Died,Left}.

## 3. Framework callback-registry fix (Agent B §5) — FOUNDATION, do first
Problem: the 4 optional per-map callbacks are dispatched unconditionally (main.scr::startThread -> spawn trigger_once setthread maps/<map>.scr::<cb>) -> "Could not find label" spam on ~57 maps lacking them.
Fix: add `main.scr::startMapCallback local.cb local.entity` that early-ends unless `level.coop_hasCallback[local.cb]==1`; change the 4 player.scr dispatch sites (≈819 Respawned, 822 Spawned, 1071 Died, 129 Left) to call it. Maps opt in with `level.coop_hasCallback["coop_playerJustSpawned"]=1` in main. Backward compatible; no edits to 57 silent maps. (Exact code in hzm_coop_framework_guide.md §5.) NOTE: edits player.scr — coordinate with e1l4 agent findings (same file).

## 4. Reusable helpers to build (Agent D) — additive, kill the biggest 16-player risks
1. coop_freezeAllPlayersForIntro / coop_releaseAllPlayers — replaces global freezeplayer/cuecamera (which freeze/hijack all 16) in 5+ map intros (t1l2,t1l3,t2l1,t2l3,t3l1; t3l2 endgame cam).
2. coop_applyToAllPlayers fan-out — collapses singular `$player fullheal/takeall/loadout` intro lines.
3. startRideCameras / stopRideCameras — generic Pattern-A per-player ride camera (m1l1/e1l1/e1l4/e2l1 dup'd; needed t1l1 plane, t1l3/t3l2 cams).
4. 16-seat escort-vehicle handler — extend m1l3b (caps at 8) for t2l2 halftrack; or walk-alongside geofence.
5. validateTriggerActivator list-variant — already at replace.scr:2507; add a list-accepting form. (vehiclehandler.scr is an empty stub — good home for new helpers.)
- NO objective-broadcast helper needed: engine addobjective auto-replicates to all 16 + seeds late joiners. Per-objective decision is just completion semantics.

## 5. Biggest 16-player risks (Agent D)
1. Global freezeplayer/cuecamera in intros (-> helper #1/#3). 2. t3l2 T-34 silent-no-board (players_tank scans King Tiger cannon model; T-34 differs — CRITICAL). 3. Vehicle seat caps at 8 (t2l2). 4. Squad-death mission-fail for all 16 (t2l1,t2l3). 5. parm.other/owner==$player trigger gates fire only for host (t2l2:853,t2l4:852-897,t3l2:976,t1l2,t1l3).

## 6. Conversion order (tractable -> hard)
Foundation: (A) framework callback-registry fix; (B) helpers #1,#2 (freeze/apply-all) — unblock most intros.
Then per map (Breakthrough first — current test profile): e3l3 (blocked, ~5), e3l2 (parse bugs FIXED, ~6), e3l4 (~7); e1l4 (apply root-cause fix).
Then Spearhead (needs com_target_game=1 profile): t1l1(plane,A), t3l1, t1l3, t2l1, t2l3, t2l4, t1l2; hardest last: t2l2 (16-seat halftrack), t3l2 (T-34 board bug).

## 7. Fixed already this phase
- e3l2/objectives.scr:8 + e3l2/prisoner_section_1.scr:456 — 0x92 (Win-1252 apostrophe) parse-killers -> ASCII. e3l2 objectives + prisoner section now load.

## 8. BUILD -> TEST -> FIX LOOP (the SOP this session runs)
TEST = the same Phase-2 harness we used for the rotation:
- cfg: hzm-mohaa-coop-mod/coop_mod/cfg/maptest_start.cfg (coop_maptest 2, coop_dbno 0, high-health/"god", waypoints/dwell, ui_dmmap = target). For t-series, a SEPARATE cfg + launch with com_target_game 1 (Spearhead profile; mounts mainta).
- Build pk3: powershell C:\mohaa-coop-dev\build.ps1 (packs+deploys pk3+cfg+cgame.dll; does NOT build the exe). Engine/exe changes: `cmake --build C:\mohaa-coop-dev\openmohaa-hzm\.cmake --config Release --target openmohaa` then copy .cmake/Release/openmohaa.exe -> G:\GOG\...\openmohaa.exe (game DOWN only).
- Launch/relaunch: write "RELAUNCH" to C:\mohaa-coop-dev\watchdog\command.txt; watchdog boots + auto-dismisses dialogs. On crash the watchdog WAITS for manual RELAUNCH. After each RELAUNCH: kill stale spawn_clicker (EXCLUDE current $PID in the match!) + start fresh spawn_clicker.ps1.
- Clicker: spawn_clicker.ps1 clicks SDL window center on MAPTEST_LOADED + heartbeat 10s (spawns player from spectator). 
- Watch: qconsole.log C:\Users\curry\AppData\Roaming\openmohaa\maintt\qconsole.log; background monitors notify on MAPTEST_LOADED / crashes / errors.
- PER-MAP SUCCESS CRITERIA (grep banners): MAPTEST_LOADED (loads, no crash) -> MAPTEST2_PATROL_SRC + MAPTEST2_WAYPOINT at distinct origins (teleport tour works; bsp_waypoints fallback for AI-only) -> player stays alive (high-health) -> MAPTEST2_OBSERVE (officer/ambient) -> MAPTEST2_DEATH_OK (clicker respawn). Officer: confirm coop_officer_init spawns a boss (anchor or dynamic fallback). 
- Crash diagnosis: Get-WinEvent Application Error -> faulting module + exception code (0xc0000409 = stack-buffer overrun via ucrtbase __report_gsfailure 0xa527e).
- FIX: deploy a focused agent per failing map/issue (root cause + patch, ASCII/no-BOM/no-em-dash/$player-array/global-cam gotchas), redeploy, re-test. Rinse repeat until the target maps pass the criteria.
- Known non-fatal noise (don't chase): DEATH_HANG (intermittent clicker miss — fix: bounded-poll verify + clicker window-focus hardening), asset fallback warnings (bazooka_fly etc.), DUPLICATE ALIASES.

## 9. Pending fixes carried from the rotation (apply during this phase)
- DEATH_HANG: maptest_phase2.scr death-verify (lines ~209-220) single wait15;check -> bounded poll (recheck health up to ~40s, re-emit RESPAWN_NEEDED) + harden spawn_clicker window find/focus.
- Missing-callback spam -> §3 registry fix.
- 5 officer-gap maps -> author anchors (officer_coverage_audit.md) once their maps verify.

## 10. NIGHT STATE (2026-06-24, post-conversion) - resume here
DECISION (user): run the ENTIRE trilogy through the ONE Breakthrough cvar (com_target_game 2), same as m/e today. t-series are Spearhead maps (BSPs in mainta); engine fs_basegame is exclusive (Breakthrough=maintt only). So we made an ENGINE FS-UNIFY change instead of a 2nd profile.
ENGINE CHANGES (need exe rebuild - bnhtnk3az building Release): 
 1. code/qcommon/files.cpp FS_Startup ~line 3506 - when fs_basegame=="maintt" also FS_AddGameDirectory "mainta" (added BEFORE maintt so maintt+mod win conflicts; only affects Breakthrough). => Breakthrough now mounts main+mainta+maintt+mod, so t-series + Spearhead assets load under the one profile.
 2. code/renderergl1/tr_bsp.c ParseMesh ~754-767 - bounds guard on patchWidth/Height (the e3l4 0xc0000409 /GS overrun, minidump-confirmed; points[MAX_PATCH_SIZE^2] was unguarded). 6th engine buffer fix.
CONVERSIONS: all 17 unconverted maps (e1l4,e2*,e3*,t*) coop-converted for 16p (reports convert_*.md). 
OBJECTIVE FIXES (M-series pattern, per user "do the cannon fix for all maps"): e3l2 cannons -> cannonThink-style CoopGunOrChargeThink (DONE); global/MountGunOrPlantCharge.scr coop-converted for parity (DONE); DistanceUse confirmed already-GREEN; PICKUP objectives (papers/docs/explosives) agent a1f3ddd RUNNING (fix_pickup_objectives.md). 
OFFICER: central policy in officer.scr (officer_spawn_policy.md + officer_9none_deepdive.md): stealth=NONE-until-goloud (e1l4/m1l2a/m1l3c/m2l2a/m4l0 DEFERRED w/ level.alarm wiring), combat anchors forced >=1000u from player start. PENDING officer-enable batch: m5l2b (warrants officer: auto-drive tank=players on foot + ai.scr infantry) + m2l2a/m4l0/m2l3/e3l4 need coop_feature_boss=1 + anchor to actually spawn.
SCRIPT FIXES: e3l1 Courtyard overflow (player_closestTo return discarded -> waitframe+assign, DONE+validated), e3l4 Bunker3.scr compile typo $piat guyexec->$piatguy exec (DONE), e1l4 papers Fix A + Fix B (restartMap no-op during maptest, DONE).
TESTING STATE: m-series 1-36 + e1l1-e3l2 validated clean (load/spawn/traverse/no-crash) under Breakthrough; e3l4 crashed (ParseMesh fix pending rebuild -> retest); t-series UNTESTED (need unified rebuild).
NEXT STEPS (resume): (1) wait exe build bnhtnk3az + pickup agent a1f3ddd. (2) deploy new exe to G:\GOG\...\openmohaa.exe (game DOWN). (3) rebuild pk3 (build.ps1) - captures pickup+bunker3+all. (4) watchdog/clicker are on BREAKTHROUGH profile (maptest_start.cfg, maintt log, com_target_game 2), cfg ui_dmmap=t1l1. RELAUNCH + fresh clicker. (5) VERIFY the startup pak-mount log now lists mainta (proves fs-unify) + t1l1 loads WITH coop+maptest. (6) tour t-series t1l1..t3l2; also retest e3l4 (ParseMesh) + e1l4 (officer DEFERRED=no spawn in stealth intro). Deploy fix-agents for failures, rinse repeat.

## 11. FINAL OVERNIGHT STATE (2026-06-24 ~02:15)
VALIDATED under the unified Breakthrough profile (one com_target_game 2 launch, mainta now mounted): full 52-map rotation reached MAPTEST_COMPLETE; 51/52 maps load+spawn+tour crash-free. e1l4 re-confirmed: officer DEFERRED (spawn count 0 in stealth intro) + no respawn loop. t-series all loaded (t1l1 confirmed touring). Engine: 2 new exe fixes deployed+running (files.cpp mainta-mount fs-unify; tr_bsp.c ParseMesh patch-bounds guard - PE ts 0x6a3b7617).
HOLDOUT = e3l4: a SECOND, distinct cold-load 0xc0000409 (/GS) in the CLIENT cgame-init / gamestate-parse / alias-cache replay path (CL_ParseGamestate/CL_InitCGame + cgame cg_commands.cpp AliasCache/CacheResource) - confirmed real + distinct from ParseMesh (which never fires for e3l4), but NOT pinnable by inspection and NO debugger (cdb/WinDbg) installed; Release exe has no PDB. RelWithDebInfo (matching symbols) WAS built this session for a future debugger pass. coop_para alias flood is mod-wide red herring (not e3l4-specific). Latent (non-trigger) defects to harden later: unbounded strcat into parmbuffer[2048] cg_commands.cpp:4291/4344 (per-call reset, bounded) + parameters[2048] scriptmaster.cpp:446/507. CURRENT e3l4 EFFORT: agent a7cbed2240 adding ^E3DBG Com_Printf breadcrumbs to the crash path -> rebuild exe+cgame.dll -> reproduce e3l4 -> grep qconsole tail for last ^E3DBG line -> pins the offending resource/buffer. e3l4 NEXT-STEP DECISION for user: (a) let breadcrumb cycle continue, or (b) install cdb/WinDbg + read the RelWithDebInfo dump for an exact stack (fastest/definitive).
BREADCRUMB RESULT (02:22, ^E3DBG in deployed exe+cgame.dll): crash LOCALIZED to inside CG_Init's cgame resource-cache replay. CL_ParseGamestate completes fine (1773 configstrings, dataCount=36284). Then CG_Init runs the cache-script alias replay; last breadcrumb = AliasCache name='power_generator2' real='sound/mechanics/SubPen_Generator_Run.wav' -> CacheResource logged, then crash. parmbuffer[2048] RULED OUT (parmlen stays ~41). So the /GS overrun is the NEXT cache op after power_generator2's AliasCache returns - an un-instrumented cgame command or the sound-load path (CacheResource->sound loader), NOT the alias param buffer. Remaining suspects: a fixed stack buffer in the cgame command-dispatch for the next cache entry, or the WAV/sound loader for a specific generator sound. NEXT NARROWING: instrument the cgame command-dispatch loop (log each command+arg) + CacheResource internals/sound-load -> the command after power_generator2 names the function. ALSO FIXED a real bug: build.ps1 was deploying a STALE cgame.dll from .cmake\code\cgame\Release (6/22) instead of the real output .cmake\code\client\cgame\Release - corrected build.ps1 cgameSrc.

## 12. e3l4 CRASH - PRECISELY LOCALIZED (2026-06-24 ~02:50, breadcrumb cycles)
ROOT TRIGGER PINNED: loading the sound file 'sound/mechanics/SubPen_Generator_Run.wav' crashes INSIDE cgi.S_RegisterSound -> S_memoryLoad -> WAV codec. Proof (^E3DBG trail): CacheResource BEFORE S_RegisterSound(wav) 'subpen_generator_run.wav' prints with NO matching AFTER, while the prior sound mec_generatorrun_01.wav printed BEFORE+AFTER (loaded fine). The /GS canary (0xc0000409) trips loading THIS specific wav.
BUILD-CONFIG GOTCHA (cost me a wasted fix): CMakeCache has NO_MODERN_DMA:BOOL=ON -> the OLD SDL sound path is compiled (code/client/snd_dma.c, snd_codec_wav.c, sdl_snd.c; built objs: snd_dma.obj/snd_codec_wav.obj, NOT snd_dma_new.obj). The snd_*_new.cpp files are DEAD CODE here. My S_RegisterSound off-by-one fix in code/client/snd_dma_new.cpp is therefore in DEAD CODE (harmless, correct-if-ever-enabled, but NOT the active path). The ACTIVE chain is clean where inspected: snd_dma.c S_Base_RegisterSound -> S_FindName (HAS the strlen>=MAX_QPATH guard) -> S_memoryLoad -> WAV codec. So the overrun is in S_memoryLoad (snd_dma.c:407) or the WAV codec (snd_codec_wav.c S_ReadRIFFHeader/S_FindRIFFChunk/S_ReadChunkInfo). Obvious codec stack buffers look bounded on inspection (dump[16] reads 12; name[5] holds 4+null) - so the faulting frame is subtle (candidate: a malformed/unusual header in SubPen_Generator_Run.wav driving a bad read/seek, or a stack buffer deeper in S_memoryLoad/adpcm/wavelet decode).
WAV IS VALID (hexdumped): SubPen_Generator_Run.wav = standard RIFF/WAVE, fmt PCM(1)/1ch/44100Hz/16-bit, proper data chunk, 635736 bytes (LARGE). NOT malformed -> the loader/codec crashes on a VALID but large WAV (sibling mec_generatorrun_01.wav loads fine). So it's a genuine engine bug in S_memoryLoad/sample-load, likely size/sample-count related, NOT bad content (a plain WAV-override workaround may only help if the trigger is size, untested).
EXACT REPRO now = load e3l4 (or just S_RegisterSound that one wav).
RESOLVED (07:58) crash #1 = the sound. Confirmed root cause via the dbghelp StackWalk/scan tool (tools/minidump_scan.exe, built against RelWithDebInfo PDB): S_Base_RegisterSound->S_memoryLoad->S_LoadSound->ResampleSfx loading the VALID 635KB SubPen_Generator_Run.wav faults (engine resampler bug on a large valid WAV). WORKAROUND applied: overrode sound/mechanics/SubPen_Generator_Run.wav in the mod with the smaller valid Mec_GeneratorRun_01.wav (229KB) -> SubPen now loads (CacheResource BEFORE+AFTER), e3l4 LOADS (43/52) + tours.
CRASH #2 on e3l4 (still open): a DIFFERENT 0xc0000409 ~waypoint 4 of the teleport tour, in the TERRAIN-COLLISION trace (cm_terrain.c CM_TracePointThroughTerrainCollide / CM_TestTerrainCollideSquare <- CM_TraceThroughTree, via SV_ClipMoveToEntities) - the terraincollide_trace_t / a deeper buffer. LIKELY a maptest TELEPORT artifact: 51 other maps (incl. m-series terrain maps) toured via teleport with no such crash, so it is e3l4-terrain + that-waypoint specific; teleporting the bot to a raw pathnode (-4720 3760 1056, high Z) then tracing terrain hits the edge case. UNKNOWN if it affects normal walk-through play -> needs a human playtest of e3l4. The minidump_scan tool + RelWithDebInfo build are ready to pin it (dump openmohaa.exe.27488.dmp) if playtest shows it matters.
CLEANUP PENDING: deployed exe+cgame.dll carry ^E3DBG breadcrumbs (in cl_parse.cpp, cl_cgame.cpp, cg_commands.cpp, snd_mem_new.cpp[dead], snd_dma_new.cpp[dead off-by-one fix]) - strip them + rebuild CLEAN Release for the final deliverable. Keep: files.cpp fs-unify, tr_bsp.c ParseMesh guard, alias.c convalias guard, the WAV override. NEXT STEP (clean finish): (a) DEBUGGER - now trivial: RelWithDebInfo build exists + 1-asset repro; cdb/WinDbg on the dump gives the exact frame instantly (needs user OK to install). OR (b) hexdump SubPen_Generator_Run.wav header (extract from mainta pak) to spot the malformation, then guard the codec. OR (c) WORKAROUND: override SubPen_Generator_Run.wav with a valid WAV in the mod pk3 to unblock e3l4 (doesn't fix the codec vuln). The ^E3DBG breadcrumbs are still in the deployed exe(2:18)+cgame.dll(2:32) - rebuild CLEAN once fixed. NOTE: deployed exe+cgame.dll currently carry ^E3DBG breadcrumb spam - rebuild CLEAN (remove breadcrumbs) for the final deliverable once e3l4 is fixed.
MORNING ITEMS (non-blocking): interactive mechanics need USER PLAYTEST (vehicles t2l2/t3l2 board+drive, t1l1 plane / t1l3 boat base-pak gag cinematics - non-fatal script errors, flagged); officer-ENABLE batch (m5l2b warrants officer [auto-drive tank=on-foot+infantry] + m2l2a/m4l0/m2l3/e3l4 need coop_feature_boss=1+anchor to actually spawn the boss - policy buckets correct, spawn gated off); DEATH_HANG intermittent (spawn_clicker mid-map respawn-click miss - bounded-poll verify + clicker focus hardening, non-fatal).
