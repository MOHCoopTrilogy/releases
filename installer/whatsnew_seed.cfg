// one-shot post-update announcement: pushes the What's New card (ui/coop_whatsnew.urc,
// changelog baked into the code pk3) shortly after the main menu appears. The updater
// blanks this file on every up-to-date launch, so the card shows exactly once per update.
// This SEED ships via the release manifest so even installs updated by an OLDER updater
// (which doesn't know how to arm the card) show it for the release that introduces it.
// CONTENT MUST STAY CONSTANT ACROSS RELEASES - a stable hash means the updater downloads
// it exactly once ever; from then on the new updater owns arming/blanking it.
wait 300
pushmenu coop_whatsnew
